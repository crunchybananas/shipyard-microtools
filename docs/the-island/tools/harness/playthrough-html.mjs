const esc = (s='') => String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const TITLES={
 '01-arrival':'Ashore','02-east-room':'A room kept ready','03-the-working-model':'The working model',
 '04-the-first-crossing':'The first crossing','05-listening':'Learning to listen','05b-the-drain':'Under the standing stones',
 '06a-the-tower':'Eighty-three treads','06b-the-signal':'The light on the cliff','06c-beneath-the-bluff':'The rooms under the bluff','06d-the-line':'The hanging line',
 '06-the-signal':'The signal and the line','07-the-other-hands':'Other hands','08-the-unfinished-boat':'The unfinished boat','09-homecoming':'Homecoming',
 'ending-tend':'Take the watch','ending-carry':'Bring the water home','ending-open':'Join the rooms','ending-close':'Enough for tonight',
};
function state(s){return `<details class="state"><summary>Action and saved state</summary><p>${esc(s.action)}</p><p>Level ${s.level}. Water ${s.tide.toFixed(3)}. ${s.notes.length} earned observations.</p><p>${Object.keys(s.flags).filter(k=>s.flags[k]).map(esc).join(', ')}</p><p>Position ${s.position.map(v=>v.toFixed(2)).join(', ')}</p></details>`;}
function frame(s){return `<a class="frame" href="${s.image}"><img loading="lazy" src="${s.image}" width="1440" height="900" alt="${esc(s.title)} — captured in the running game"></a>`;}
function words(s){return `${s.reading?`<blockquote>${esc(s.reading.text)}</blockquote>`:s.whisper?`<blockquote>${esc(s.whisper)}</blockquote>`:''}${(s.additionalPages||[]).map(p=>`<blockquote>${esc(p.text)}<a class="page-link" href="${p.image}">Page ${p.number} capture</a></blockquote>`).join('')}${s.finaleText?`<p class="afterword">${esc(s.finaleText)}</p>`:''}`;}
export function renderPlaythrough({stages,method,manuscript,review}){
 const chapters=[...new Set(stages.map(s=>s.chapter))];
 const moments=stages.filter(s=>!s.presentation||s.presentation==='moment');
 const hero=stages.find(s=>s.title==='The western lighthouse')||stages.find(s=>s.title==='Above the whole island')||stages[0];
 const nav=chapters.map(c=>`<a href="#${c}">${TITLES[c]||c}</a>`).join('');
 const sections=chapters.map(c=>{
   const items=stages.filter(s=>s.chapter===c);
   return `<section id="${c}"><h2>${TITLES[c]||c}</h2>${items.map(s=>s.presentation&&s.presentation!=='moment'
    ? `<details class="extra" id="stage-${s.number}"><summary>${esc(s.title)} <span>${s.presentation==='page'?'page included above':'recorded action'}</span></summary>${frame(s)}${words(s)}${state(s)}</details>`
    : `<article class="moment" id="stage-${s.number}"><header><span class="step">Stage ${s.number}</span><h3>${esc(s.title)}</h3></header>${frame(s)}<div class="caption"><p>${esc(s.action)}</p>${words(s)}${state(s)}</div></article>`).join('')}</section>`;
 }).join('');
 const places=[['06a-the-tower','Above the whole island','Up the tower'],['05b-the-drain','The drain chamber','Under the stones'],['06c-beneath-the-bluff','The inverted lighthouse','Under the bluff'],['06c-beneath-the-bluff','Through the western doorway','The western study']].map(([id,title,label])=>{
  const s=stages.find(s=>s.title===title);return s?`<a href="#stage-${s.number}"><img src="${s.image}" alt="${label}"><span>${label}</span></a>`:'';
 }).join('');
 const lore=Object.entries(manuscript.lore).map(([id,l])=>`<details id="artifact-${esc(id)}"><summary>${esc(l.title)} <small>${esc(l.hand)}</small></summary>${l.pages.map(p=>`<p>${esc(p)}</p>`).join('')}${l.deep?.length?'<h4>Pages revealed below</h4>'+l.deep.map(p=>`<p>${esc(p)}</p>`).join(''):''}</details>`).join('');
 return `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>The Island — places and playthrough</title><style>
 :root{color-scheme:light;--ink:#243e48;--sea:#3b6876;--line:#ccd7d8;--paper:#f6f9f8}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--paper);color:var(--ink);font:18px/1.65 Georgia,serif}a{color:var(--sea);text-underline-offset:.22em}button,summary{cursor:pointer}a:focus-visible,button:focus-visible,summary:focus-visible{outline:3px solid #346c80;outline-offset:5px}.cover{min-height:72vh;background:#264953 url('${hero.image}') center top/cover;display:flex;align-items:flex-end;color:white;padding:70px 7vw;position:relative}.cover:before{content:'';position:absolute;inset:0;background:linear-gradient(0deg,#112c36ed,transparent 90%)}.cover>div{position:relative;max-width:720px}.cover h1{font-weight:normal;font-size:clamp(45px,6.5vw,88px);line-height:1.04;margin:0 0 25px}.cover p{max-width:53ch;font-size:19px}.cover .edition{font:14px/1.5 system-ui,sans-serif}.places{display:grid;grid-template-columns:repeat(4,1fr);gap:18px;padding:36px 5vw 0;max-width:1560px;margin:auto}.places a{text-decoration:none;display:block}.places img{width:100%;aspect-ratio:1.6;object-fit:cover;display:block}.places span{display:block;padding-top:10px;font:15px/1.5 system-ui,sans-serif}.layout{max-width:1560px;margin:auto;display:grid;grid-template-columns:200px minmax(0,1fr);gap:64px;padding:55px 5vw}nav{position:sticky;top:25px;align-self:start;font:14px/1.35 system-ui,sans-serif;max-height:92vh;overflow:auto}nav a{display:block;padding:9px 0;text-decoration:none}nav a:hover{text-decoration:underline}nav p{font-weight:600;margin:0 0 14px}main{min-width:0}.intro{max-width:72ch;margin:0 0 40px}.intro p{margin-top:0}.controls{display:flex;flex-wrap:wrap;gap:10px;margin:24px 0}.controls a,.controls button{font:14px/1.5 system-ui,sans-serif;padding:9px 15px;background:#e2ebeb;border:1px solid #b9cccf;border-radius:5px;color:var(--ink);text-decoration:none}.intro details{font:14px/1.6 system-ui,sans-serif}.intro details p{margin-top:16px}section{scroll-margin-top:25px}h2{font-weight:normal;font-size:38px;line-height:1.2;margin:75px 0 32px;max-width:20ch}h3{font-size:26px;font-weight:normal;line-height:1.3;margin:8px 0 20px}.step{font:12px/1.4 system-ui,sans-serif;color:#5d7b85}.moment{margin-bottom:52px;scroll-margin-top:24px}.frame{display:block;text-decoration:none}.frame img{width:100%;height:auto;display:block;background:#d2dfe0;border-radius:3px}.caption{max-width:73ch;padding-top:20px}.caption>p{font-size:16px;margin:0 0 20px}blockquote{margin:24px 0;padding-left:22px;border-left:2px solid #aac0c5;white-space:pre-wrap;line-height:1.75}.page-link{display:block;font:12px/1.5 system-ui,sans-serif;margin-top:8px}.state{font:12px/1.6 system-ui,sans-serif;color:#58707b;overflow-wrap:anywhere}.afterword{white-space:pre-wrap}.extra{border-top:1px solid var(--line);padding:14px 0;font:14px/1.6 system-ui,sans-serif;scroll-margin-top:25px}.extra summary{display:list-item}.extra summary span{color:#778c91;font-size:12px;margin-left:12px}.extra>.frame{margin:20px 0}.extra blockquote{font:17px/1.7 Georgia,serif;max-width:65ch}.extra[open]{margin-bottom:30px}.manuscript details{padding:22px 0;border-top:1px solid var(--line)}.manuscript summary{font-size:22px}.manuscript small{font:13px system-ui,sans-serif;color:#6a7f88;margin-left:14px}.manuscript p{max-width:63ch;white-space:pre-wrap}footer{padding:42px 5vw;background:#e4eded;font:13px/1.6 system-ui,sans-serif}.layout a,.extra{overflow-wrap:anywhere}@media(max-width:1050px){.layout{gap:32px;grid-template-columns:165px minmax(0,1fr)}}@media(max-width:740px){.cover{min-height:500px;padding:40px 24px}.places{grid-template-columns:1fr 1fr;padding:24px 22px 0;gap:22px 14px}.layout{display:block;padding:30px 22px}nav{position:static;max-height:none;display:grid;grid-template-columns:1fr 1fr;gap:0 22px;margin-bottom:42px}nav p{grid-column:1/-1}h2{font-size:32px;margin-top:52px}h3{font-size:23px}.caption{padding-top:16px}blockquote{padding-left:16px;font-size:17px}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}@media print{nav,.controls,.extra{display:none}.cover{min-height:250px}.layout{display:block;padding:20px}.moment{break-inside:avoid}}
 </style><header class="cover"><div><p class="edition">The Island · Landfall revision</p><h1>Above the light.<br>Below the stone.</h1><p>The tower, the buried rooms, and the long way home. A complete journey through the revised island.</p></div></header><div class="places">${places}</div><div class="layout"><nav aria-label="Journey chapters"><p>The route</p>${nav}<a href="#manuscript">Every readable page</a></nav><main><div class="intro"><p>${moments.length} illustrated moments lead through the game. Extra pages sit with the artifact they belong to. Similar views are folded into the action record; every one of the ${stages.length} original captures remains available.</p><div class="controls"><button id="captures" aria-pressed="false">Expand all stage captures</button><a href="manuscript.md">Complete manuscript</a><a href="stages.json">Capture record</a><a href="../index.html">Revision overview</a></div><details><summary>How the route was captured</summary><p>${esc(method)}</p><p>All four endings branch from the same earned source checkpoint. Each returns through the full stack and commits at the refuge lamp.</p></details></div>${sections}<section class="manuscript" id="manuscript"><h2>Every readable page</h2><p>All ${Object.keys(manuscript.lore).length} artifacts, including optional discoveries and the pages revealed below.</p>${lore}</section></main></div><footer>Captured from the running game at 1440 × 900. This folder contains fictional game text.</footer><script>const button=document.getElementById('captures');button.addEventListener('click',()=>{const on=button.getAttribute('aria-pressed')!=='true';document.querySelectorAll('.extra').forEach(d=>d.open=on);button.setAttribute('aria-pressed',String(on));button.textContent=on?'Fold repeated stage captures':'Expand all stage captures';});function reveal(){const e=document.getElementById(location.hash.slice(1));if(e?.classList.contains('extra'))e.open=true;}addEventListener('hashchange',reveal);reveal();</script></html>`;
}
