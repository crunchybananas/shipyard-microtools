// Refresh review navigation and capture provenance without changing any game state.
import { readFileSync, writeFileSync, readdirSync } from 'node:fs';
import { createHash } from 'node:crypto';
import { resolve, relative, join } from 'node:path';
import { fileURLToPath } from 'node:url';
import { renderPlaythrough } from './playthrough-html.mjs';

const island = fileURLToPath(new URL('../../', import.meta.url));
const dir = resolve(process.argv[2] || join(island, 'loop/playthrough/2026-09-05/landfall'));
const root = resolve(dir, '..');
const edition = relative(root, dir);
const data = JSON.parse(readFileSync(join(dir, 'stages.json')));
const manuscript = JSON.parse(readFileSync(join(dir, 'manuscript.json')));
writeFileSync(join(dir, 'index.html'), renderPlaythrough({ ...data, manuscript }));

const files = ['index.html', 'style.css', ...readdirSync(join(island, 'js'), { recursive: true }).filter(f => f.endsWith('.js')).map(f => 'js/' + f), 'assets/harbor-rooms.glb', 'assets/landfall.glb'];
const hashes = Object.fromEntries(files.sort().map(f => [f, createHash('sha256').update(readFileSync(join(island, f))).digest('hex')]));
const parts = JSON.parse(readFileSync(join(island, 'tools/blender/landfall-geometry.json')));
const pages = Object.values(manuscript.lore).reduce((n, l) => n + l.pages.length + (l.deep?.length || 0), 0);
writeFileSync(join(dir, 'build.json'), JSON.stringify({ source: 'local Landfall revision', runtimeSha256: hashes, landfall: parts, captures: data.stages.length, review: data.review, artifacts: Object.keys(manuscript.lore).length, pages, endings: 4 }, null, 2) + '\n');

const find = title => data.stages.find(s => s.title === title)?.image;
const scene = (title, caption) => `<figure><a href="${edition}/index.html#stage-${data.stages.find(s => s.title === title)?.number}"><img src="${edition}/${find(title)}" alt="${title}"></a><figcaption>${caption}</figcaption></figure>`;
writeFileSync(join(root, 'index.html'), `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>The Island — Landfall</title><style>
body{margin:0;background:#f6f9f8;color:#233f49;font:19px/1.65 Georgia,serif}main{max-width:1250px;margin:auto;padding:65px 32px}h1{font-size:clamp(46px,7vw,88px);font-weight:normal;line-height:1.08;max-width:14ch}h2{font-weight:normal}p{max-width:64ch}a{color:#286578;text-underline-offset:.2em}a:focus-visible{outline:3px solid #286578;outline-offset:5px}.links{display:flex;gap:15px;flex-wrap:wrap;margin:32px 0 50px}.links a{padding:13px 22px;background:#deeaeb;border-radius:5px;text-decoration:none}.places{display:grid;grid-template-columns:1fr 1fr;gap:28px 24px;margin:40px 0}figure{margin:0}img{width:100%;height:auto;border-radius:4px;display:block}figcaption,.small{font:14px/1.65 system-ui,sans-serif}figcaption{margin-top:12px}details{border-top:1px solid #beced1;margin-top:40px;padding-top:24px}summary{cursor:pointer}.old{display:flex;flex-wrap:wrap;gap:22px;margin-top:25px}@media(max-width:680px){main{padding:35px 22px}.places{grid-template-columns:1fr}.links{display:grid}}
</style><main><p>The Island · Landfall revision</p><h1>There is more room here.</h1><p>Climb the lighthouse past three windows. Follow the hatch stair beneath the bluff. Cross into the western study and lift the lid of a bread tin left among the drying papers.</p><div class="links"><a href="${edition}/index.html">Open the complete journey</a><a href="${edition}/manuscript.md">Read the manuscript</a><a href="../../../index.html">Play The Island</a></div><div class="places">${scene('Above the whole island','Eighty-three physical treads lead to the lantern gallery.')}${scene('The drain chamber','A stone ramp leads under the standing stones.')}${scene('The inverted lighthouse','The cellar opens onto the hanging lighthouse.')}${scene('Through the western doorway','Two steps lead from the cellar into the western study.')}</div><h2>A clearer playthrough</h2><p>${data.review.moments} illustrated moments cover the journey and all four endings. Related pages are grouped; repeated views fold into the action record. All ${data.stages.length} source captures, exact actions, saved states and ${pages} manuscript pages remain in the folder.</p><p class="small">The tower stair and underground connectors use actual player collision in both directions. Long travel is accelerated and encounters use disclosed placement fixtures. This is a scripted route audit with screenshots.</p><details><summary>Source and verification</summary><p class="small">Original Blender lighthouse, stairs, vaults, rock and wind pines are in tools/blender/landfall.blend. The room and boat kit remains in harbor-rooms.blend. Both have reproducible Python generators.</p><p class="small"><a href="../../../tools/blender/landfall.blend">Landfall Blender source</a> · <a href="../../../tools/blender/landfall.py">Generator</a> · <a href="../../../tools/blender/landfall.png">Tower source render</a></p><p class="small"><a href="${edition}/validation.txt">Verification record</a> · <a href="${edition}/build.json">Build fingerprints</a> · <a href="${edition}/stages.json">Every captured stage</a></p></details><div class="old small"><a href="revised/index.html">Previous room revision</a><a href="baseline/index.html">Original diagnostic views</a></div></main></html>`);
writeFileSync(join(root, 'README.md'), `# The Island — review folder\n\nOpen index.html for the latest Landfall revision. ${data.review.moments} illustrated moments organise ${data.stages.length} source captures, all four endings and ${pages} pages across ${Object.keys(manuscript.lore).length} artifacts. The tower, drain, cellar and western study are physically traversed. Repeated views remain in the expandable action record.\n\nlandfall/ contains the latest manuscript, states, source checkpoint, homecoming save, verification and build fingerprints. revised/ preserves the prior room revision; baseline/ preserves the original diagnostic views. The root build.json and validation.txt belong to the prior room revision.\n\nBlender sources and generators live in ../../../tools/blender. No private narrative source or identifying biography is included.\n`);
console.log(`REVIEW BUILT: ${data.review.moments} moments / ${data.stages.length} captures / ${pages} pages`);
