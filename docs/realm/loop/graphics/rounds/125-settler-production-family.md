# Round 125 — Settler Production Family

Date: 2026-08-01
Runtime revision: 184

## Outcome

The default citizen is no longer a mixed derivative family. All sixteen
settler action/direction rows now compile from one hash-locked A14 source set
and are promoted under `a14-modular-settler-actions` provenance.

## Source set

- `wayfarer`: a distinct lean young woman identity with warm medium-brown
  skin, dark wavy hair, and a low braid.
- `hearth-indigo`: faded-indigo hood and jerkin, oat linen, rust sash, dark
  trousers, and wrapped travel boots.
- `settler-spade`: four independently authored views with a D grip, ash shaft,
  and pointed forged blade.
- Shared directional cargo crate and approved A2 eight-beat joint authority.

The garment needed two review corrections before compilation. The first
separated its sash, belts, and trouser assemblies. The second converted every
hollow garment cavity from black fill to chroma-key transparency. The latter
was caught only by viewing the first compiled contact sheet; it now has a
dedicated source regression check.

## Production result

- `16` rows / `128` frames, all `512x84` with `64x84` cells.
- `76px` median body height on ground row `79`; rows `80–83` clear.
- Exact `27x35`, `35x46`, `54x70`, and `64x84` runtime tiers.
- Binary runtime alpha, one deterministic 48-color family palette, no
  warnings, no waivers, and byte-identical clean rebuilds.
- Atomic sixteen-row promotion; `224` production rows and `0` candidates.
- A6 now covers nine baked-container owners: `324` role/resource/direction
  combinations at identical actor and cargo rectangles.

## Verification

- A14 compiler and strict independent clean rebuild.
- Source provenance, reference hashes, crop margins, and hollow-hood test.
- Sprite workbench family staging and atomic promotion.
- Full source, frame, gait, direction-phase, coverage, and animation audits.
- Sprite Lab production/candidate isolation.
- Live A14 preview plus ordinary-world and in-world settler carry checks.
- Nine-family cargo browser proof with smoothing disabled at integer scale.

## Next

Build A15 rancher as the next distinct modular identity, clothing, tool, and
workstation family. The remaining older families after rancher are trader,
innkeeper, scholar, and forager.
