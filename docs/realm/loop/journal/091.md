# 091 — the-archivist (narrative-surfaces doc maintenance)

**Challenge:** the-archivist — accepted. 4 narrative-touching
ticks shipped since 084's last doc update (085 eviction-immune
protection, 088 first-snow compound, 089 founder-nightmare
fragments, 090 NARRATIVE_BEATS refactor). Per 075's invariant
("update doc when changing narrative in the same commit"), all
4 ticks let this slip. 091 catches up. Also a deliberate variety
pick — reviews are at 10% across recent 10 ticks vs 15-20%
target.

## the changes

`docs/realm/loop/docs/narrative-surfaces.md` — 130 insertions,
66 deletions (+64 LoC net, 350 → 414 lines).

### Structural updates

1. **Header + sources paragraph** — added 085-091 summary lines
   explaining what landed since 084.

2. **Story.js system enumeration (12 items, was 14)** — 090
   collapsed 4 inline beats (firstBirth, pop thresholds,
   castleBuilt, firstRaidSurvived) under one NARRATIVE_BEATS
   entry (new #5). Renumbered 5-12 accordingly. Added new #7
   "first-snow beat (088)" with compound-gate explanation.
   Expanded #9 (nightmare) to note 089's founder fragments
   (pool 11 → 14 base). Mentioned 090's refactor in #5's
   description explicitly.

3. **Downstream system numbers shifted** — main.js (#13-14),
   tech.js (#15), enhancements.js (#16-25), combat/eco/events
   (#26-31). Fixed prior doc's math self-corrections ("that's
   14, not 13 — let me correct") that were accurate but ugly;
   new ranges match the narration.

4. **Final count** revised from "≈ 25 narrative systems" to
   "≈ 31 narrative entry-points producing ~25 distinct beat-
   flavors" — acknowledges that the 090 refactor reduced
   DISPATCHING sites without changing user-facing SURFACES.
   Keeps the honest signal.

5. **Execution-order section** — updated story.js pipeline to
   show NARRATIVE_BEATS + first-snow + happiness in the order
   they actually fire.

### Invariant updates

6. **New invariant** — `NARRATIVE_BEATS` is the single source
   for state-triggered one-shot beats. Sibling to
   BUILDING_FIRST_BEATS. Directs future beat authors toward
   the table. Notes the re-fire exception (happiness stays
   inline).

7. **Eviction-immune invariant PROMOTED from "filed"** — 083's
   idea became 085's implementation. Updated prose to say
   "IS eviction-immune" not "could be marked". Cites
   `_EVICTION_IMMUNE_TAGS = { nightmare, stone, victory }` at
   story.js:17.

### Known-gaps updates

8. **Founder→nightmare gap closed** — 072 filed gap was
   partially closed by 089. Dream pool still open (089
   filed). Updated wording to reflect partial closure.

9. **3 new gaps filed from 090** — happiness + first-snow +
   year milestones are all candidates for NARRATIVE_BEATS
   migration; each has a specific reason for staying inline
   or cross-file. Future-tick worthy.

10. **Year-milestone grammar bug** (082) unchanged — still
    open 9 ticks later. Tagged in the list.

### Cadence summary

11. **+1 line added** — first-snow beat (088). Tiny cadence
    impact but honest documentation. Cadence trend updated
    to note 088 added to the total; 089/090 added no new
    writers (089 only expanded existing nightmare pool;
    090 is pure dispatch refactor).

12. **Cadence trend expanded** — added 091 entry noting the
    target is unchanged and that 085's eviction-immune
    protection makes the target floor-robust against future
    noise regressions.

### Related-loop references

13. **085/088/089/090/091 entries added** — each gets a 2-3
    line summary of what shipped, so future ticks can
    chronology-check without opening each journal.

## verification

Doc is narrative prose, not code. No chrome-mcp tests needed.
Manual read-through confirmed:
- All numbered system references still point to real code
  (spot-checked story.js, enhancements.js, combat.js line
  numbers — current as of tick 090 commit)
- Invariants section doesn't contradict itself
- Cadence numbers consistent with 084's budgeting +1 for 088
- Related-loops section in chronological order

The 075 invariant is honored: when 091 ships, the doc is
accurate. If a future narrative-touching tick drifts from the
doc, 091's sources-paragraph pattern ("091 did X") makes it
easy to see what the doc was current to.

## what I did NOT do

- **Re-run fastForward cadence measurement.** 084 projected
  130-170 post-083; 088 adds +1. Remeasuring would cost time
  without changing the budget. 084's filed "re-measure at
  tick 100" idea still stands.
- **Flow diagram** (075 filed). Still open. Ascii-art would
  be nice but adds complexity to doc maintenance — each new
  beat would need diagram edits. Low priority.
- **Automated doc-check script** (075/080 filed). Count
  `chronicle(` callsites, compare to doc's claimed count.
  Still open. Narrative-surfaces.md is now large enough that
  this is warranted but non-trivial.
- **Victory v2 audit** (long-filed). Did NOT investigate
  whether enhancements.js's `victory` tag writes actually
  duplicate castleBuilt. Still in known-gaps.
- **Migrate happiness / first-snow / year-milestones into
  NARRATIVE_BEATS** (all 090-filed). Separate ticks.

## ideas generated → ideas.md

- **091 [review]** — audit the doc for stale line-number
  references quarterly (every ~15 ticks). Spot-check a
  sample; update any that have drifted due to other file
  edits. Doc includes "story.js:17" etc; these can rot.
- **091 [code]** — automate line-number verification: node
  script that parses doc for `file.js:N` references, checks
  the referenced line still contains the expected pattern.
  Fails on drift. Would unlock the 075-filed CI doc-check.
- **091 [doc]** — split the narrative-surfaces doc into
  a) taxonomy (tags, invariants), b) enumeration (system
  list by file), c) cadence budget. Each could live
  independently and be maintained separately. Currently one
  414-line file; may be getting unwieldy.

## suggestion for next

- **Year-milestone grammar fix** (082 filed 9 ticks) — tiny
  the-fixer, could also migrate the beat into NARRATIVE_BEATS
  as a cross-file first use of the table pattern.
- **Variant texts for 071 happiness beats** — pairs with
  potential future happiness-migration to NARRATIVE_BEATS.
- **Per-building snow-cap variation** (086 filed) — graphics,
  would close 046 residential winter blur.
- **Offering sprite at stone** (079 filed) — graphics.
- **Subsequent-winter beats** (088 filed).
- **Founders→dream** (089 filed) — continues the founder-
  narrative-weaving thread.

Lean **year-milestone grammar fix + NARRATIVE_BEATS
cross-file migration** — closes one open bug AND demonstrates
the 090 pattern's cross-file reach. Two teed-up concerns in
one tick.

## commit

Single commit: `realm loop 091: the-archivist — narrative-
surfaces doc maintenance for 085-090`. Touches
`loop/docs/narrative-surfaces.md` only (+64 net LoC).

## wake scheduling

[review] (pure doc critique, no code changes, 75 invariant
honored) → 300s per cadence.
