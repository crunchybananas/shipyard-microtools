# 169 — refactor-only (type-scale phase 4)

**Challenge:** refactor-only — accepted. Phase 4 of
the type-scale migration arc (140 tokens defined →
143 chronicle panel → 150 phase 2 → 160 phase 3 →
169 phase 4). 9 ticks since 160; comfortable
spacing.

Variety from graphics-heavy run (8 of last 10
ticks were graphics axis). Non-graphics non-audio
non-narrative tick — pure CSS hygiene.

## the build

8 exact-match `font-size` migrations across 6 high-
visibility surfaces in `index.html`:

| Selector              | Before    | After          | Surface       |
|-----------------------|-----------|----------------|---------------|
| `#hud`                | 0.82rem   | `var(--fs-base)` | Top status bar (most visible UI) |
| `.feed-item`          | 0.65rem   | `var(--fs-xs)`  | Activity feed item |
| `.feed-item .feed-icon`| 0.75rem  | `var(--fs-sm)`  | Activity feed icon |
| `#tooltip .tt-lbl`    | 0.65rem   | `var(--fs-xs)`  | Tooltip label |
| `.tt-shortcut kbd`    | 0.65rem   | `var(--fs-xs)`  | Keyboard shortcut display |
| `.tut-action`         | 0.75rem   | `var(--fs-sm)`  | Tutorial action label |
| `#event-banner`       | 0.75rem   | `var(--fs-sm)`  | Event banner |
| `.eb-days`            | 0.65rem   | `var(--fs-xs)`  | Event days counter |

All zero perceptual shift (source values match
token targets pixel-for-pixel: 0.65=fs-xs,
0.75=fs-sm, 0.82=fs-base).

Net LoC 0 (8+/8-).

## why this shape

- **Highest-visibility surfaces.** #hud is the
  most-seen UI in the game (always-visible top
  bar showing day, resources, season). Activity
  feed is the dominant secondary information
  stream. Tooltip + tutorial are core
  interaction surfaces. Event banner shows on
  every random event.
- **Same panel-by-panel + exact-only template
  as 143/150/160.** Conservative: ship only
  zero-shift migrations; near-exacts (0.72→0.75,
  0.8→0.82, 0.7→0.75, 0.85→0.82, 0.78→0.82)
  reserved for a deliberate design-shift tick.
- **No bundled-line edits this tick.** Line 520
  has `.tech-name` 0.82 + `.tech-cost` 0.65 +
  `.tech-desc` 0.7 (near-exact) all on one line;
  precise string-match would risk breaking
  other selectors in that line. Filed for a
  phase 5 tick that's surgical about bundled
  lines.
- **Skipped #hud rediscovery.** Earlier 150
  journal claimed #hud was migrated; current
  state has 0.82rem still inline (whether 150
  reverted, was overlooked, or my journal was
  off). Re-shipped now. Documents that the
  loop's claims should be verified against
  state, not memory.
- **Clean break from graphics axes.** 8 of last
  10 ticks were graphics; 169 is the first
  non-graphics in 4 ticks (since 166 frog).
  CSS hygiene rebalances tick mix.

## verification

No js touched; no `node --check` relevant.
Static verification:
- All 8 replaced values match token definition
  exactly (`:root` at lines 21-27: fs-xs=0.65,
  fs-sm=0.75, fs-base=0.82).
- Each edit confined to the `font-size:` value
  assignment; no other CSS property touched.
- Git diff: 8 insertions + 8 deletions only,
  confined to `index.html`.

Live-verify deferred — should read as identical
to pre-169 since all are zero-shift.

## what this closes

- **130 HIGH (phase 4)** — ongoing migration.
  Pre-169: ~18 of ~155 declarations migrated
  (140+143+150+160). Post-169: ~26 of ~155
  migrated (~17%). Phase 5 remaining: ~14
  exact-match candidates plus the bundled-line
  cluster on line 520.
- **160 [code]** — "phase 4 exact-match font-
  size migrations: `.tt-shortcut kbd`
  0.65→fs-xs, `.tech-name` 0.82→fs-base
  (bundled-line precise match), `.tech-cost`
  0.65→fs-xs (same bundled line). ~3-5 more
  exact-match candidates." Partial close;
  shipped `.tt-shortcut kbd` and 7 others.
  Bundled-line `.tech-name`/`.tech-cost`
  deferred for surgical phase 5 tick.

## what I did NOT do

- **Bundled-line edits.** `.tech-name`,
  `.tech-cost`, `.tech-desc`, `.tech-icon` all
  live on line 520 in one tag. Need precise
  string-match strategy for phase 5.
- **Near-exact migrations.** `.mission-progress`
  0.72→0.75 (+4.2%), `.scen-name` 0.8→0.82
  (+2.5%), `#missions h3` 0.8→0.82, `#tooltip`
  0.72→0.75 — all deferred for design-choice
  tick.
- **Display-tier consolidation.** 1.8rem+
  display sizes left inline per 140 invariant.
- **Touch js, 3D, or SVG.** Pure CSS hygiene
  tick.

## ideas generated → ideas.md

- **169 [code]** — phase 5 exact-match
  migrations: `.diff-btn` 0.82→fs-base,
  `.rate` 0.65→fs-xs, `.at-title` 0.65→fs-xs,
  `.ach-sum-row` 0.75→fs-sm, `.ach-sum-note`
  0.65→fs-xs, `.ach-item` 0.75→fs-sm,
  `.tut-skip` 0.65→fs-xs, `.trade-empty`
  0.82→fs-base, `.rp-sublabel` 0.65→fs-xs,
  `.tc-desc` 0.65→fs-xs, `.rp-pct` 0.65→fs-xs.
  ~11 more exact-match candidates. [index.html]
- **169 [code]** — phase 6 bundled-line
  surgery: line 520 `.tech-name` 0.82,
  `.tech-cost` 0.65 (line has 5 selectors;
  use anchored regex or full-line replace
  with care). [index.html]
- **169 [doc]** — once phase 5+6 close,
  document the migration arc in
  `loop/docs/render-layers.md` (or a sibling
  `type-scale-arc.md`) — total LoC migrated,
  invariants, deferred design-shift list.
  [loop/docs/]

## suggestion for next

Recent mix (159-169):
- Surprise: 2 (159/166)
- Refactor: 2 (160/169)
- Meta (axis open): 1 (161)
- Code (sprite/mesh): 5 (162/163/164/167/168)
- Archivist: 1 (165)

Refactor at 2/11; surprise at 2/11; code/sprite
5/11. Healthy rebalance.

170 options:

- **5th SVG sprite** — tower or tavern. Tower
  pairs with castle (162); tavern is simpler.
- **3D engine 3rd touch** — animate windmill
  blades (167 filed) or `pushFrustum` primitive
  or sun rotation.
- **Strategic-decision meta tick** (165 filed)
  — should SVG replace canvas, or stay sandbox?
- **Phase 5 font-size** — 11 more exact-matches
  filed.
- **Surprise** — Evolution mandate; 3 ticks
  since 166.
- **Live-play verification** — chrome needed.
- **Cover-art recipes doc** (104 filed) — 098
  found frame-2 winter-dusk recipe; doc-style.

Lean **5th SVG sprite (tower)** — continues axis
cadence at established rhythm; tower is the
natural next building (matches 162 castle's
spire language). OR **strategic-decision tick**
if the user wants to know whether to commit to
SVG integration with 4 sprites + 2 cross-axis
triangles in hand.

## commit

Single commit: `realm loop 169: refactor-only —
type-scale phase 4 (8 exact-match migrations
across HUD + feed + tooltip + tutorial + event
banner; zero visual shift)`. Touches
`index.html` only. Net LoC 0.

## wake scheduling

[code] (refactor-only; small; partial close of
130 HIGH) → 420s per cadence.
