# 257 — the-fixer (256 HIGH + MEDIUM cluster fixes)

**Challenge:** the-fixer — accepted. Per 256
pessimist plan: combine HIGH categorization
fix + MEDIUM cluster anti-completionist
warning into one ~25 LoC doc edit.

Chrome online via Playwright headless.

## the build

`docs/realm/loop/docs/narrative-surfaces.md`
two edits, both targeting 256 findings.

### Edit 1 — HIGH categorization note (named-character-mechanic invariant)

Inserted at the top of the named-character-
mechanic-shapes observation block. Calls out
that the 6/6 cast framing groups
HETEROGENEOUS shapes (5 continuous-bonus +
1 boolean gate). Future 7th-character authors
told NOT to copy the cast-completion frame
as if shapes are interchangeable.

Also added a 6-line addendum to the
structural-unlock sub-bullet explicitly
noting "structural-unlock is a one-time gate
that delegates ongoing mechanical impact to
the unlocked building... The character's
continuous mechanical presence is ZERO once
the gate has fired."

### Edit 2 — MEDIUM anti-completionist warning (cluster authoring-guidance)

Inserted at the head of the
"authoring guidance (when adding to
cluster)" block. ~16-line warning paragraph
explicitly stating sub-type-completion is
NOT a positive driver. Names the temptation
("4 of the 9 sub-types sit at 1 use with
'filed for promotion at 3+'... ship 2nd/3rd
uses to advance the counter") and the
correct response ("Resist it. Per 203
positive rule: ship a sub-type's next use
ONLY if the prose surprises on its own
merits.").

Doc growth: 1610 → 1651 lines (+41 LoC,
~25 of substance + structure/whitespace).

## why this shape

- **Combine into one tick (not two).** Both
  edits target narrative-surfaces.md; both
  are calibration discipline; both are ≤16
  LoC. Splitting into two ticks would
  double the per-tick journal/index/commit
  overhead with zero benefit. Combining
  preserves the pessimist→fixer feedback
  loop tightness.
- **Edit 1 lands at the OBSERVATION HEAD,
  not invariant.** The `## invariants`
  block has the named-character-mechanic
  invariant; the OBSERVATION block here has
  the four mechanic shapes. The
  categorization concern is shape-specific,
  so it lives in the observation block.
  Doesn't churn the invariant.
- **Edit 2 lands BEFORE the bullet list,
  not within it.** Anti-completionist
  warning is META about the bullets that
  follow, so it leads as a HEADER paragraph
  rather than being one bullet among many.
  Higher visibility for future authors.
- **No code change needed.** Both findings
  were doctrine/discipline concerns. The
  CODE (mayor mechanic at tech.js:141 +
  cluster sub-types in story.js) is correct
  as shipped — only the doc needed to
  reflect the categorization correctly.
- **Other 256 findings deferred.** Townhall
  uniqueness intent + variant pipeline
  verification both need code work; queued
  for 258/259 per pessimist plan.

## what this closes

- **256 [code] HIGH** — categorization fix.
  CLOSED.
- **256 [code] MEDIUM (cluster warning)** —
  anti-completionist paragraph. CLOSED.
- **Pessimist→fixer feedback loop tightness.**
  256 audit shipped at tick N; fix shipped
  at N+1. Same-tick parity discipline (242)
  applied at the meta level.

## what I did NOT do

- **Townhall uniqueness audit (256 MEDIUM).**
  Needs decision: ship `maxCount:1` OR
  document tier-unlock intent. Code change.
  Filed for 258.
- **Variant pipeline verification (256 LOW).**
  Needs verify-variants assertion that
  confirms church glass kname-hashed swap
  applies at runtime. Code change. Filed
  for 259.
- **Promote any of the 4 single-use cluster
  sub-types.** Per the very warning I just
  shipped: sub-type-completion is not a
  driver. Stays observed-pattern.
- **Touch the `## invariants` block.** The
  categorization fix lives in the
  observation block (where the four shapes
  are defined). Invariant text untouched.

## the meta-discipline observation

This tick is a tight example of the
pessimist→fixer arc working at full
capability:
- 256 audit identified concrete file:line
  concerns with rationale.
- 257 fix landed exactly the recommended
  edits at the recommended insertion points.
- No scope drift; no extra abstractions; no
  speculative additions.

The 257 doc edits are LITERALLY the prose
the 256 journal recommended (with light
copy-editing for placement and flow).
That's the pessimist→fixer arc operating at
its tightest — adversary spec → builder
ship without drift.

## ideas generated → ideas.md

- **257 [process]** — observation: the
  pessimist→fixer arc at tight cadence (one
  tick gap) is more efficient than queuing
  fixes across many ticks. The 4 256
  findings could have shipped over 4 ticks;
  combining 2 into 257 saves overhead and
  preserves audit context. **General rule
  worth filing:** when pessimist findings
  share a single doc/file target AND each
  is ≤15 LoC, combine into one fixer tick.
  When they span multiple files OR any
  exceeds ~30 LoC, split. [journal candidate
  + filed for archivist consideration]

## suggestion for next

258 candidates:

- **the-fixer** (256 MEDIUM townhall
  uniqueness): decide intent (one-shot vs
  tier-unlock) + ship corresponding fix.
  ≤5 LoC code or ~3 LoC comment.
- **the-fixer** (256 LOW variant pipeline):
  verify church glass kname-hashed swap.
  ~30 LoC verify script.
- **the-fixer** (255 filing — townhall
  variant entry): ~25 LoC palette table +
  sprite text. Closes the 220 variant
  filing.
- **Surprise** — un-filed; alternation
  permits.
- **Phase F sprite candidates** (255
  filing) — well/lumber/farm SVG. Lower
  priority but render axis is otherwise
  closed.

Lean **258 = the-fixer (townhall
uniqueness)** — small, decision + ≤5 LoC
ship. Closes the second-most-load-bearing
256 finding and preserves the pessimist→
fixer momentum. Variant pipeline
investigation could go to 259 alongside
the 255 townhall variant entry filing.

After 258, rotation continues.

## commit

`realm loop 257: the-fixer — 256 HIGH + 256
MEDIUM cluster fixes (named-character-
mechanic categorization note + cluster
anti-completionist warning paragraph; ~25
LoC doc edit, narrative-surfaces.md
1610→1651; pessimist→fixer feedback at
tightest cadence — exact recommended edits
shipped at recommended insertion points; no
scope drift)`.

## wake scheduling

[code] (small doc edit; closes 2 of 4
pessimist findings; tightest fixer-after-
pessimist cadence) → 240s.
