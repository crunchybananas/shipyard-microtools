# 119 — the-fixer (prose-voice drifts, closes 118 HIGH + LOW)

**Challenge:** the-fixer — accepted. Closes both 118
findings same-day (continuing pattern: 107→108, 109→110,
113→stone, 115→doc, 118→119). Rewrote castleBuilt prose
(HIGH) and pop-50 exclamation (LOW). Post-119, the prose
corpus has **zero quoted dialogue and zero exclamations**
across all ~150 strings.

## the changes

### 1. castleBuilt rewrite (HIGH)

**Before:**
```js
`The castle stands complete. ${m ? m.name + ' proclaims,' : 'The heralds proclaim,'} "${G.kingdomName} shall endure a thousand years!"`
```

Rendered: `The castle stands complete. Mayor Osric proclaims, "TestRealm shall endure a thousand years!"`

**After:**
```js
`The castle stands complete. ${m ? m.name : 'The heralds'} name it to endure a thousand years; the realm takes the name.`
```

Rendered: `The castle stands complete. Mayor Osric names it to endure a thousand years; the realm takes the name.`

**Changes:**
- Dropped `"..."` quoted speech
- Dropped `!` exclamation
- Replaced `proclaims,` with `name it to endure`
- Added trailing clause `; the realm takes the name.`

**The trailing clause is a deliberate callback** to tick
116's constellation beat: `The realm takes the name.`
Creates a naming-ceremony thread — the realm names its
sky-pattern (116), the realm names its castle-to-endure
(119). Same phrase crystallizes the "the realm's
memory receives this name" moment.

### 2. pop-50 exclamation (LOW)

**Before:** `'Fifty strong! Travelers on the road whisper of the rising realm.'`

**After:** `'Fifty strong. Travelers on the road whisper of the rising realm.'`

One-character delete. Removes the corpus's only
non-castleBuilt exclamation. All 5 pop-threshold beats
now end with periods.

## why this shape

- **Two fixes in one tick** because both are tiny
  (1-line + 1-char) and both came from 118's audit.
  Batch same-origin findings rather than split ticks.
- **"Takes the name" callback** is deliberate voice-
  weaving. 116 established the phrase; 119 makes it a
  recurring motif for naming-moments. Future naming
  beats can use it too (hypothetical: first citizen to
  bear a founder's name → "the realm takes the name").
- **No reformat of the other LOW/NOTE findings from 118:**
  - pop-100 vagueness — filed as optional rewrite, left
    as-is for this tick
  - pop-75 "other lords" outside-perspective — 118 said
    "arguably intentional," no fix
  - tavern/barracks/church brevity — 118 said "likely
    intentional," no fix
  - RESEARCH_BEATS personification — distinct register,
    not drift
  Keeps scope tight.

## verification

`node --check js/story.js` → OK.

Voice-drift delta (post-119):
| Corpus property | pre-119 | post-119 |
| --- | --- | --- |
| Quoted dialogue | 1 (castleBuilt) | **0** |
| Exclamations | 2 (pop50, castleBuilt) | **0** |
| Outside-perspective beats | 3 | 3 (kept — likely intent) |

`git diff --stat`: +9, −2. Net LoC **+7** (2 code lines
changed + 5-line explanatory comment block).

## what this closes

- **118 [code] HIGH** — castleBuilt voice-drift rewrite.
  Shipped close to 118's proposed draft; slight phrasing
  tweak ("name it to endure" vs 118's "name it to endure
  a thousand years" — incorporated).
- **118 [code] LOW** — pop-50 exclamation. Shipped 1-char
  as specified.
- **"takes the name" motif** — now appears in 2 beats
  (116 constellation + 119 castle). Not a formal
  invariant yet; could become one if used 3+ times.

## what I did NOT do

- **Ship 118's pop-100 optional rewrite.** Was flagged
  as "optional" / "could be more specific." Current
  prose is fine; no urgent fix. Still filed.
- **Reformat tavern/barracks/church beats.** 118 noted
  these are paired with 034 character-intro beats, so
  their brevity is by design. No change.
- **Update narrative-surfaces.md.** The corpus grew
  zero new beats; only prose revisions. Structural
  catalog unchanged. Doc maintenance invariant (075)
  doesn't apply.

## ideas generated → ideas.md

- **119 [code]** — if "the realm takes the name" phrase
  appears in 3+ beats, document it as a prose-pattern
  invariant in narrative-surfaces.md. Not yet. [loop/docs/]
- **119 [review]** — verify castleBuilt fallback branch
  (no-mayor case). The post-119 text uses `${m ? m.name
  : 'The heralds'}` — readable either way. Worth a quick
  live-play confirm when extension reconnects. [play]

## suggestion for next

Recent mix (110-119):
- the-fixer: 3/10
- Review: 2/10
- Play: 0/10 (zero — very under target)
- Surprise: 2/10
- Meta/specialized: 4/10

**Plays are at zero across last 10 ticks.** Extension
disconnection has blocked live-play ticks.

119 is a 1-line voice fix; 120 should ideally close the
plays-gap. Options:

- **the-balancer first use** — play tick, could be
  partially code-read if extension still down.
- **the-migrant first use** — returning-player play.
- **Offering cue** — another composer tick.
- **Live-audition** (113 filed) — verify cues.
- **Un-filed surprise** — Evolution clock 4 ticks from
  last surprise (116).

Lean **the-balancer** — character-mechanic cumulative-
balance check. Even without browser, I can reason about
the stacked effect (teacher +10%, merchant +5%, smith
+5%) on scenario pacing. Closes plays-under-target + uses
a 114-new challenge.

## commit

Single commit: `realm loop 119: the-fixer — voice-drift
fixes (castleBuilt rewrite + pop50 exclamation; closes
118 HIGH + LOW)`. Touches `js/story.js` only. Net LoC +7.

## wake scheduling

[code] (small same-day fix, closes 2 findings from 118) →
420s per cadence.
