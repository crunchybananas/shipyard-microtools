# 269 — the-fixer (closes all 3 268 findings in one tick)

**Challenge:** the-fixer combined —
accepted. Per 257/259 [process]
combination rule + 268 plan: HIGH +
MEDIUM (same target main.js) + LOW
(story.js) ≤15 LoC total.

verify-logic 32/32 passing.

## the build (3 changes, 1 tick)

### Change A — HIGH + MEDIUM: newGame() resets

`docs/realm/js/main.js:324-330` —
added 4 reset lines to the existing
window.newGame() block:

```js
G.realmEnded = false;
G.lastRaidDay = undefined;
G.lastDeathDay = undefined;
G.lastUnderpopDay = undefined;
```

Plus a 5-line comment citing 268 HIGH
+ MEDIUM rationale.

### Change B — LOW: 267 particle cap guard

`docs/realm/js/story.js` summer_falling_
star after-callback — added cap guard:

```js
if (G.particles.length >= 400) return;
```

Plus a 4-line comment matching the
neighboring spawn-site convention in
particles.js.

### Change C — verify test #32

`docs/realm/scripts/verify-logic.mjs`
asserts that after window.newGame():
- realmEnded === false (was previously
  true)
- lastRaidDay === undefined (cleared
  from stale 30)
- lastDeathDay === undefined (cleared
  from stale 50)
- lastUnderpopDay === undefined
  (cleared from stale 40)
- filter === '' (was grayscale)

Single combined assertion. ~22 LoC test.

## post-fix re-verification

Re-ran `/tmp/repro-268.mjs`:
```
FRESH:    { realmEnded: undefined, chron: 1, filter: '' }
FALL:     { realmEnded: true,      filter: 'grayscale(0.85)…' }
newGame:  { realmEnded: false, chron: 0, filter: '', day: 1, pop: 3 }
```

Compare to pre-269 state in 268
journal:
```
newGame (pre-fix):  { realmEnded: true, chron: 0, filter: 'grayscale(0.85)…' }
newGame (post-fix): { realmEnded: false, chron: 0, filter: '' }
```

Filter cleared. realmEnded cleared.
The new realm is no longer
born-already-dead.

(chron stays 0 immediately after
newGame because newGame() doesn't
call beginGame() to write the
founding text — that's the existing
expected flow; founding text is
written when the game begins, not
when state resets.)

## why this shape

- **Combine via 257/259 [process] rule.**
  3 findings, 2 same-target (main.js
  newGame), 1 small-LoC sibling
  (story.js after-callback). All ≤15
  LoC each, total ~12 LoC code + ~22
  LoC test. Splitting into 269/270/271
  would triple overhead for no value.
- **Reset to `undefined`, not `0` or
  `null`.** Existing trigger gates
  (`G.lastRaidDay !== undefined &&
  ...`) already check for undefined
  as the "no event yet" sentinel.
  Resetting to undefined matches
  pre-event semantics rather than
  treating "raid happened on day 0".
- **Keep the existing newGame() block
  unchanged otherwise.** No
  refactoring scope creep. 4 reset
  lines added, 0 lines removed.
- **Don't reset namedCharacters.**
  Considered. Outside 268 scope; could
  be a separate finding (newGame DOES
  inherit previous realm's mayor/
  bard/etc — almost certainly wrong
  but not what 268 audited). Filed
  as a future audit candidate.
- **Don't add reset to startNewGame.**
  Per 268 reasoning: startNewGame is
  for first-time-launch flow where
  G defaults haven't been mutated
  yet. The save/load path at
  save.js:193 has its own restoration
  logic. Only newGame() (in-game
  restart) was the exposed vector.
- **Verify single combined test
  rather than 4 separate.** All 4
  resets must happen together for
  the bug to be fixed; testing them
  individually would split the
  signal. One failing assertion
  reveals which field regressed.

## what this closes

- **268 HIGH** — newGame realmEnded
  reset. CLOSED.
- **268 MEDIUM** — newGame sustained-
  state-tracker reset. CLOSED.
- **268 LOW** — 267 particle cap
  guard. CLOSED.

All 3 268 findings now closed in 1
tick. **Cleanest pessimist→fixer arc
since 256-259** (which closed 4
findings in 3 ticks).

## what I did NOT do

- **Reset namedCharacters in newGame.**
  Out of scope. Filed for separate
  audit.
- **Reset realmEnded in startNewGame.**
  Defensive but unnecessary; first-
  time launch path doesn't expose
  the bug.
- **Add reset for the 267 particle
  type 'shootingstar' to the
  newGame G.particles = [] reset.**
  Already covered — newGame at
  main.js:307 sets `G.particles = []`
  which clears all particles
  including any in-flight
  shootingstar.
- **Promote anything to invariant.**
  Per 231/251/257 conservative-
  promotion. The newGame reset
  pattern stays observed practice.
- **Apply 267 cap guard to
  hypothetical future synchronized-
  surprise patterns.** Pattern is
  templated in the 267 [process]
  filing; future authors will
  follow the convention.

## meta-observation

The 268→269 arc demonstrates the
combination rule (257/259 [process])
is now robust:
- 257 closed 2 of 4 256 findings
  (HIGH + MEDIUM cluster) in one
  tick, both same doc target.
- 259 closed 256 LOW + 255 filing
  in one tick, both touching
  _VARIANT_PALETTES.
- 269 closes 3 of 3 268 findings
  in one tick, two same target
  (main.js newGame) + one sibling
  (story.js after-callback).

**Combination rule cadence**: pessimist
discipline plus combination rule
makes each pessimist→fixer arc 1-2
ticks tight. The 268 audit cost 1
tick; the 269 fix cost 1 tick; net
2 ticks to clear 3 findings. Without
combination, would have been 4 ticks
(audit + 3 separate fixers).

## ideas generated → ideas.md

- **269 [code]** — newGame() also
  doesn't reset G.namedCharacters.
  Out of 268 scope but discovered
  during this fixer. Player whose
  realm has a named mayor/bard/etc
  who clicks "New Game" sees the
  same characters in the new realm.
  Almost certainly wrong; the new
  realm's first tavern build
  should re-call ensureMayor().
  ~1 LoC + verify test. [the-fixer
  small filed thread]
- **269 [process]** — observation:
  pessimist→fixer arc with
  combination rule has now run 3
  times (256-259 / 268-269 /
  earlier 192→260+261). Each closed
  N findings in N or N-1 ticks.
  The combination rule's
  consistent net-savings is
  worth promoting from observed-
  pattern to invariant after one
  more confirming arc. 3 uses,
  threshold 3+ met; per 231 still
  observed-pattern but ripe for
  next archivist review. [doctrine
  candidate]

## suggestion for next

270 candidates:

- **the-fixer** (269 [code] follow-
  on): newGame G.namedCharacters
  reset. ≤2 LoC + verify test.
- **the-archivist** — filing-cluster
  pessimist→fixer combination rule
  for promotion review (3rd use met).
- **Surprise** — un-filed; 269 was
  the last tick before alternation
  becomes due (last surprise was
  266; 267-269 = 3 fixer-class).
- **Phase F sprite** (255/259
  filings).
- **the-fixer** (220 filing):
  variant entries for remaining
  sprite types.

Lean **270 = surprise** —
alternation per 254 precedent (3-
tick fixer streak). 267-268-269
were 2 fixer + 1 review; that's
fixer-class for alternation
purposes. Possible: a fresh
observation, a small mechanic
improvement, or a [play] tick
verifying the 269 fix doesn't
regress under longer playthroughs.

After 270, rotation continues.

## commit

`realm loop 269: the-fixer — closes
all 3 268 findings (HIGH G.realmEnded
reset + MEDIUM sustained-state
tracker reset in newGame at
main.js:324; LOW 267 particle-cap
guard at story.js after-callback;
~12 LoC code + 22 LoC verify test;
post-fix repro confirms newGame()
no longer inherits realmEnded;
verify-logic 32/32; combination
rule applied per 257/259 [process])`.

## wake scheduling

[code] (small surgical fix; closes
all 3 pessimist findings in 1 tick;
demonstrates combination rule's
3rd application) → 240s.
