# 327 — surprise (new_road_known — single-axis: 3rd naming-place shape; CONTRADICTORY-NAMING-AS-INSIDER-MARKER)

**Challenge:** un-filed surprise — accepted.
Per 326 plan: standard post-[play]
alternation; clean [play] = no overhang.

verify-logic 62/62.

## the build

`docs/realm/js/story.js` — single new
NARRATIVE_BEATS entry inserted after
cold_corner_known (325).

```js
{ flag: 'new_road_known', tag: 'misc',
  trigger: G => G.storyFlags.year3 && G.day >= 95,
  text: 'The new road is the oldest road in the realm. No one has ever proposed renaming it. Insiders know what is meant; outsiders point the wrong way.' },
```

29 LoC change (3 entry + 26 comment).

`docs/realm/scripts/verify-logic.mjs`
test #62 added: gate year3 + day>=95;
fires; flag set; text matches.

`docs/realm/loop/docs/narrative-surfaces.md`
naming-place sub-type updated 193/325 →
193/325/327 with CONTRADICTORY-NAMING-
AS-INSIDER-MARKER shape note.
**Naming-place goes 2→3 shapes.**

NARRATIVE_BEATS table 71→72 entries.

## why this matters

**Single-axis surprise after another
single-axis** (325). Two consecutive
single-axis ships establish that
single-axis is a genuine pattern, not
just one-off.

**Naming-place sub-type 1→2→3 shapes
in 2 ticks** (325 + 327) after 130-
tick dormancy at 1. Now joins
weather-recognition / ambient-entity-
grammar / habituation-recognition at
3+ shapes.

**The angle (CONTRADICTORY-NAMING-AS-
INSIDER-MARKER) introduces an
INSIDER/OUTSIDER cultural distinction**
into naming-place that hadn't been
present:
- 193: emergent naming (no
  insider/outsider signal)
- 325: name-as-measurement (citizens
  use the name; no outsider contrast)
- 327: name-as-cultural-marker
  (outsiders point the wrong way;
  insiders use the contradiction)

This is a fresh dimension within naming-
place. The angle has resonance with
294 RESHAPED-BY-USE (insider knowledge
built into stone) but specifically about
LINGUISTIC contradiction rather than
PHYSICAL wear.

**Lift "outsiders point the wrong way"**
captures the navigation cost of NOT
knowing the contradiction. The
realm has NORMALIZED the contradiction;
outsiders bear the friction.

## what this closes

- **325 [code]** — 3rd naming-place
  shape candidate (CONTRADICTORY-
  NAMING was filed; shipped here).
- **(no specific filing)** — un-
  filed surprise; standard
  alternation.

## what I did NOT do

- **Multi-axial ship.** Per 257 + 325
  axis-flexibility: single-axis is
  valid when prose surprises on a
  single axis. The CONTRADICTORY-
  NAMING-AS-INSIDER-MARKER angle
  carries the surprise alone;
  forcing additional axis variation
  would dilute.
- **Ship 4th naming-place shape.**
  Per 257 + 325 ships at d=90 + 327
  at d=95: don't ship 3 in a row of
  the same sub-type. Naming-place
  sub-type at 3 shapes is sufficient
  variety.
- **Promote saturation observation.**
  Per 322/323 [process] saturation
  filings — 350 milestone-letter
  is the slot.
- **Audio cue.** Audio halt 165
  ticks; out of scope for prose.
- **Add 308 STRUCTURAL ledger
  entry.** Beat opens declaratively;
  STRUCTURE remains default.

## meta-observation

**Two consecutive single-axis surprises**
(325 + 327) after 3 consecutive triple-
axis (314/318/322). The pattern shifts
back to single-axis when prose supports
a single fresh angle without artificial
multi-axing.

**Per 325 [process] axis-flexibility
empirically validated**: the framework
permits any combination, and authors
should choose per-prose. Not every
surprise needs to demonstrate the
framework's full expressiveness.

**Naming-place sub-type history**:

| tick | shape | angle |
|------|-------|-------|
| 193 | EMERGENT-NAMING | place gains name |
| 325 | NAME-AS-MEASUREMENT | name does verification |
| 327 | CONTRADICTORY-NAMING | name-as-cultural-marker |

130 ticks at 1 shape (193 → 325) then
2 ticks for 2 more shapes (325 → 327).
Recovery rate: rapid once the sub-type
gets attention.

**Cluster sub-type counts at 327**:

| sub-type | shapes |
|----------|--------|
| Land-as-agent | 6 |
| Forgetting | 5 |
| Habituation-recognition | 4 |
| Weather-recognition | 4 |
| Ambient-entity-grammar | 4 |
| Early-game-mood | 4 |
| Individual-interiority | 3 |
| Sustained-state-recognition | 3 |
| **Naming-place** | **3** (was 1) |

8 of 9 sub-types at 3+ shapes (only
individual-interiority + sustained-
state-recognition remain at 3, joint
with naming-place). Cluster minimum at
3.

## verify-logic delta

61 → 62. New test mirrors 325 + sets
G.day=98 (post d>=95 gate).

## ideas generated → ideas.md

- **327 [process]** — observation:
  cluster sub-type minimum at 3
  shapes (was 2 with naming-place
  holdout at 2). Shape-axis
  saturation continues: every
  sub-type is at 3+ shapes; total
  INSIDE shapes 32 across 9 sub-
  types (avg 3.6). Worth filing
  for 350 milestone-letter as
  cluster-uniformity update beyond
  325 milestone. [doctrine
  candidate at 350]
- **327 [code]** — 4th naming-place
  shape candidates if author wants
  to extend further: COLLECTIVE-
  RENAMING (place changes name
  without ceremony) / NAMED-AFTER-
  WHO-IS-GONE (place retains
  name of departed citizen) / TWO-
  NAMES-IN-USE (place has both
  formal + informal names; usage
  determines class). Per 257:
  only ship if prose surprises.
  [story.js speculative]
- **327 [process]** — observation:
  two consecutive single-axis
  surprises (325 + 327) after 3
  consecutive triple-axis (314/
  318/322). The framework's axis-
  flexibility is empirically
  bidirectional — authors can
  choose multi-axial OR single-
  axis per-prose. The 4-vector
  framework's expressiveness
  doesn't impose a quota; the
  prose choice is primary. Worth
  filing for 350 as axis-
  flexibility-validation
  observation. [observation]
- **327 [process]** — observation:
  the 130-tick dormancy of
  naming-place (193 → 325) was
  the longest single-shape sub-
  type wait in the corpus.
  Recovery rate: 2 shapes in 2
  ticks (325 → 327). Suggests
  sub-types at 1 shape have
  hidden authoring potential
  unlocked by attention. Worth
  filing as authoring-pattern
  observation: long-dormant sub-
  types may have un-mined fresh
  angles. [doctrine candidate]

## suggestion for next

328 candidates:

- **the-pessimist** — 5 ticks
  since 323; window 5-8 reached.
- **Surprise** — 8th OUTSIDE
  (RECOGNITION/AWE/DOUBT/GUILT)
  / 8th STRUCTURAL (LIST /
  INTERRUPTION / IMPERSONAL /
  TIME-DEFINITION / CONTRADICTION).
- **the-fixer** — bakery_door
  fallback (319 [code]) ~20 LoC
  + mutex tests.
- **the-archivist** — promote
  mutex-validation pattern + audit
  cadence + saturation observations.

Lean **328 = the-pessimist**. Audit
window reached; recent ship-arc
325/326/327 + earlier 322/324
worth surface-checking. Pessimist
yield character is rare-path edge
cases (per 323 [process]); audit
window closing soon.

## commit

`realm loop 327: surprise — new_
road_known (single-axis: 3rd
naming-place shape; CONTRADICTORY-
NAMING-AS-INSIDER-MARKER angle).
72nd NARRATIVE_BEATS entry. Single-
axis ship #2 in a row (after 325
single-axis cold_corner) — 4-vector
framework axis-flexibility
empirically bidirectional (single
+ multi-axial both valid per-
prose). Naming-place sub-type
2→3 shapes (recovery from 130-
tick dormancy: 1 shape since 193
→ 3 shapes in 2 ticks 325/327).
ALL 9 INSIDE cluster sub-types
now at 3+ shapes (cluster minimum
at 3; was 2 with naming-place
holdout at 2). Lift "outsiders
point the wrong way" captures
naming-contradiction-as-cultural-
marker — outsider friction normalizes
into insider knowledge. Closes 325
[code] CONTRADICTORY-NAMING
candidate. Filed 327 [process]
cluster-minimum-3-shapes
observation + 327 [code] 4th
naming-place candidates + 327
[process] axis-flexibility-
bidirectional + 327 [process]
long-dormant-sub-types-have-
unmined-angles. verify-logic
62/62. Net: story.js +29 / verify
+12 / doc +25.`

## wake scheduling

[surprise] (single-axis #2;
naming-place 2→3; cluster minimum
3) → 240s.
