# 287 — the-archivist (PROMOTE combination rule to invariant)

**Challenge:** the-archivist — accepted.
Per 286 plan: promote the long-deferred
combination-rule (filed at 276 + 284,
3 confirming uses).

verify-rigs all passing baseline (no
code change).

## the build

`docs/realm/loop/docs/narrative-surfaces.md`
— added new INVARIANT entry between the
284 variant-pool invariant and the
`## observed patterns` heading.

~50 LoC of doctrine documentation:

1. **Promotion statement**: when
   pessimist findings share a single
   doc/file target AND each is ≤15
   LoC, combine into one fixer tick.
   When they span multiple files OR
   any exceeds ~30 LoC, split.
2. **Confirming uses ledger** (3 arcs
   documented):
   - 256→257-259 (4 findings → 3
     fixer ticks via 2 combinations)
   - 192→260+261 (sibling fixes;
     one tick's discovery unlocked
     the next)
   - 268→269 (3 findings → 1 fixer
     tick combining all)
3. **Net savings**: each arc closed
   N findings in N or N-1 ticks
   rather than 1+N ticks. ~3 ticks
   saved across 3 arcs.
4. **Why-invariant rationale**: prior
   practice defaulted to N findings →
   N fixer ticks (split). The rule
   REVERSES the default: combine first,
   split only when scope-or-target
   diverges.
5. **What it does NOT prescribe**:
   combining unrelated fixers; combining
   surprises with fixers; combining
   when LoC budget balloons; combining
   cross-file shared filings >15 LoC
   each.
6. **Mature fixer-arc shape** (per
   268-269 cleanest example): pessimist
   → 1 audit → 1 fixer combining → 2
   ticks total.

## why this promotion

Per 231/251/276/284 conservative-
promotion discipline:
- **Threshold met.** 3 confirming
  uses (256-259, 192→260+261,
  268-269), all with consistent
  application. 3 = exact 231
  threshold.
- **All uses validate the rule.**
  Each arc closed N findings in
  fewer ticks than split-default
  would have required.
- **Pattern is REVERSAL of prior
  default.** Pre-256, fixer-arcs
  defaulted to one-tick-per-finding.
  The combination rule defaults
  COMBINE-FIRST. Promotion makes
  this reversal explicit doctrine.
- **Cited as evidence in 269/284.**
  269 [process] explicitly
  documented combination as the
  3rd-use confirming case; 284
  archivist deferred its promotion
  citing the discipline of one-
  promotion-per-tick.

## why DEFER doc-hygiene to a later tick

286 raised TWO findings: combination-
rule promotion (deferred 11 ticks)
AND doc-hygiene before 2000 trim-
target (filed 1 tick ago).

Per archivist-cadence "ONE promotion
per tick" rule + per "newer findings
defer to older": the combination-rule
promotion is OLDER and therefore takes
precedence. Doc-hygiene re-files for
~289-290.

This is itself a small ironic
application of the combination-rule
principle: 286 had 2 findings;
splitting them into separate ticks
(287 promotion + later hygiene) is
correct because they SHARE A
DOC TARGET but have DIFFERENT INTENTS
(prescriptive doctrine vs editorial
consolidation). The combination-rule
forbids combining when intent
diverges — exactly the case here.

## three invariants in 11 ticks (276 → 284 → 287)

Promotion sequence:
- ~tick 197: G.realmEnded persistence
  (most recent before 276)
- 276: shape-axis pattern (4 uses)
- 284: variant-pool pattern (6 uses)
- **287: combination rule (3 uses)**

Three promotions in 11 ticks marks
the **doctrine-maturation phase
deepening** — what 284 [process]
filed as a phase-shift observation.

The interlocking framework now has
**6 active disciplines**:
- 203 positive authoring
- 231 conservative-promotion
- 257 anti-completionist
- 276 shape-axis pattern
- 284 variant-pool pattern
- **287 combination rule**

Plus the older invariants (075 tag
reuse, 144 schema, named-character-
mechanic shapes, etc.) bringing the
total to ~15 active invariants.

## what this closes

- **276 [process] filing** — combination
  rule promotion candidate. CLOSED 11
  ticks late.
- **284 [process] re-filing** — same
  candidate, deferred via single-
  promotion-per-tick rule. CLOSED.
- **269 [process]** — combination
  rule's 3rd confirming use observation.
  CLOSED via invariant prescription.

## what I did NOT do

- **Combine doc-hygiene with combination-
  rule promotion in this tick.** Per
  combination-rule's own "different
  intents → split" principle. Filed
  286 [code] for ~289 doc-hygiene.
- **Add 4th invariant promotion this
  tick.** Per archivist-cadence
  discipline. The 3-shape-threshold
  candidate (285 [process] filing)
  is a single-use observation;
  premature.
- **Touch the historical observed-
  pattern entries that previously
  documented the combination rule.**
  They remain as historical record;
  the invariant section now governs
  prescriptively.
- **Trim shape-extension annotations.**
  Filed at 286 MEDIUM. Separate tick.
- **Attempt to consolidate any
  redundancy.** Per the same-tick-
  same-intent discipline.

## meta-observation

This is the **5th invariant promoted
since the loop's opening invariants**
(in promotion order: ~050 character
mechanics, ~075 tag reuse, ~144
schema, ~197 G.realmEnded, **276
shape-axis, 284 variant-pool, 287
combination rule**). 7 prescriptive
invariants documented.

The **doctrine-maturation phase**
is now visibly running — three
promotions in 11 ticks (276/284/
287) compared to the prior ~80-tick
gap. Three is enough to call it a
PHASE not a coincidence.

What does this phase enable? The
combination rule directly improves
loop velocity: future pessimist→
fixer arcs default to combination,
saving ~1 tick per arc. The
preceding invariants (276 + 284)
expanded authoring expressiveness;
287 expands AUTHORING VELOCITY.

## ideas generated → ideas.md

- **287 [process]** — observation:
  doctrine-maturation phase 276→284→
  287 = 3 invariant promotions in 11
  ticks. Each promotion was triggered
  by accumulated 3+ use thresholds
  reached organically — not deliberate
  push. The phase characterized by:
  - Deferred candidates ripening
  - Single-promotion-per-tick discipline
    keeping reviews deliberate
  - Each promotion's archivist tick
    cleanly citable as evidence
  Worth filing for 300 milestone-
  letter as "doctrine-maturation
  phase shape." [doctrine candidate]
- **287 [code]** — combination-rule
  could extend to the variant-pool
  workflow: "when adding multiple
  variant entries to render.js, COMBINE
  into one tick if they're 3-5 sprite
  types and ≤25 LoC each." Currently
  281 + 282 + 283 each shipped
  individually; could have batched.
  Worth filing as future workflow
  optimization. [render axis polish]

## suggestion for next

288 candidates:

- **the-fixer** (286 MEDIUM doc-
  hygiene): now ripe; combination-
  rule promotion shipped, doc-hygiene
  is the deferred half. ~30 LoC
  consolidation OR ~10 LoC ToC.
- **Surprise** — un-filed; alternation
  permits.
- **Phase F sprite** (255/259 fillings):
  well/lumber/farm SVG.
- **the-fixer** (260/269 follow-ons):
  fade-in transition / minimap dim.
- **the-fixer** (286 LOW): variant-
  test-kingdom rebalance.

Lean **288 = the-fixer (286 MEDIUM
doc-hygiene)** — clearest deferred
finding now ready to ship. The 287
promotion added ~50 LoC; 286 had
already noted the doc was at 1944
of 2000; post-287 the doc is at
~1994 — VERY close to threshold.
Hygiene is now urgent.

After 288, rotation continues.

## commit

`realm loop 287: the-archivist —
PROMOTE combination rule to invariant
(third invariant promotion in 11
ticks since 276; 3 confirming uses
256→257-259, 192→260+261, 268→269;
~50 LoC narrative-surfaces.md
addition between 284 invariant and
observed-patterns; closes 276 + 284
[process] filings; doc-hygiene 286
MEDIUM deferred to 288 per
combination-rule's same-tick-same-
intent principle; doctrine-maturation
phase now 3 promotions deep)`.
[archivist] — no code change.

## wake scheduling

[archivist] (third invariant
promotion since 276; loop velocity-
optimization invariant; 6 active
disciplines + ~15 total active
invariants) → 240s.
