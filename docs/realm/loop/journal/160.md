# 160 — refactor-only (type-scale phase 3)

**Challenge:** refactor-only — accepted. Type-scale
migration phase 3. 143 was refactor-only (12 ticks
ago); 150 was re-shipper (10 ticks ago, same thread).
Comfortable spacing; 130 HIGH still progressing.

Rotation value: after 158 (fixer, graphics) and 159
(surprise, graphics), a non-graphics non-audio tick
balances mix.

## the build

8 exact-match `font-size` migrations across 7
panel surfaces in `index.html`:

| Selector            | Before     | After           | Panel       |
|---------------------|------------|-----------------|-------------|
| `.upgrade-btn`      | `0.75rem`  | `var(--fs-sm)`  | Info panel  |
| `.log-filter-btn`   | `0.65rem`  | `var(--fs-xs)`  | Activity log|
| `.log-entry`        | `0.75rem`  | `var(--fs-sm)`  | Activity log|
| `.help-close`       | `0.82rem`  | `var(--fs-base)`| Help card   |
| `.hp-score-label`   | `0.82rem`  | `var(--fs-base)`| Happiness   |
| `#stats-content`    | `0.82rem`  | `var(--fs-base)`| Stats panel |
| `.trade-btn`        | `0.75rem`  | `var(--fs-sm)`  | Trade panel |
| `.rp-label`         | `0.75rem`  | `var(--fs-sm)`  | Research    |

All zero perceptual shift (source values match
token targets pixel-for-pixel: 0.65=fs-xs,
0.75=fs-sm, 0.82=fs-base).

Net LoC 0 (8+/8-).

## why this shape

- **Same invariants as 143/150.** Exact-only,
  no near-exact; zero-shift guarantee. Reserves
  design-shift migrations (0.72→0.75 +4.2%,
  0.8→0.82 +2.5%, etc) for a deliberate
  design-choice tick.
- **Higher-visibility panels prioritized.**
  Activity log (log-entry, log-filter-btn) is
  the dominant secondary reading surface (after
  chronicle which 143 covered). Stats/happiness
  /help panels are the summary screens. Trade
  panel button is clicked often. Research
  panel label is visible in every research
  interaction.
- **No bundled-line edits.** `.tech-name` and
  `.tech-cost` exact-matches (line 520) are
  bundled with 3 other selectors on one line
  — editing them requires a more surgical
  string match. Deferred to phase 4 with
  `.tt-shortcut kbd`.
- **Single-file change.** Only `index.html`
  touched; no js changes; no test re-runs
  needed beyond visual confirmation.
- **Scope = ~8 migrations.** 150 did 8; 160
  does 8. Consistent per-tick workload makes
  the phase arc predictable: phase 2 (8) +
  phase 3 (8) + phase 4 (~5 remaining exact +
  1 design-shift tick for near-exacts). Total
  ~22 of ~155 declarations migrated post-160
  (~14%).

## verification

No js touched; no `node --check` relevant.
Static verification:
- Each replaced value matches token definition
  (`:root` at line 21-27: fs-xs=0.65, fs-sm=0.75,
  fs-base=0.82).
- No other CSS property touched in replacements
  (each edit confined to the `font-size:` value
  assignment).
- All 8 selectors still parse identically; no
  accidental text in adjacent properties.
- Git diff: 8 insertions + 8 deletions,
  confined to `index.html`.

Chrome extension disconnected; filed live-verify
(cold-start visual diff should read as identical
to pre-160 across activity log, stats, happiness,
help, trade, research, info panels).

## what this closes

- **130 HIGH (phase 3)** — ongoing migration.
  Pre-160: ~10 of ~155 declarations migrated
  (143+150). Post-160: ~18 of ~155 migrated
  (~12%). Phase 4 target: tech-panel cluster
  (`.tech-name`, `.tech-cost`) + tooltip
  (`.tt-shortcut kbd`) + any remaining
  single-exact-match declarations.

## what I did NOT do

- **Near-exact migrations.** `.mission-progress`
  0.72→0.75 (+4.2%), `.scen-name` 0.8→0.82
  (+2.5%), `#missions h3` 0.8→0.82, `#tooltip`
  0.72→0.75, etc — all deferred to a
  deliberate design-choice tick.
- **Bundled-line edits.** Line 520 has
  `.tech-icon`/`.tech-info`/`.tech-name`/
  `.tech-desc`/`.tech-cost` on one line. Next
  phase will use a precise string match.
- **Display-tier.** 140 left 1.8rem+ inline
  deliberately. Still appropriate.
- **Removal of `_WINTER_CAPS.granary`** (158
  did this). 160 only touches index.html.
- **Live-verify.** Extension disconnected.

## ideas generated → ideas.md

- **160 [code]** — phase 4 exact-match
  migrations: `.tt-shortcut kbd` 0.65→fs-xs,
  `.tech-name` 0.82→fs-base (bundled line, use
  precise match), `.tech-cost` 0.65→fs-xs
  (same line). ~3-5 more exact-match
  candidates. [index.html]
- **160 [code]** — design-shift tick
  (deferred again): group near-exacts into
  one deliberate migration pass.
  [index.html]
- **160 [doc]** — once phases 4-5 complete,
  close 130 HIGH entirely in ideas.md and
  capture the migration arc in a brief
  loop/docs/ entry. [loop/docs/]

## suggestion for next

Recent mix (150-160):
- Re-shipper: 1/11 (150)
- Fixer: 3/11 (151/153/158)
- Surprise: 2/11 (152/159)
- Skeptic: 1/11 (154)
- Archivist: 1/11 (155)
- Refactor-only: 2/11 (156/160)
- Meta: 1/11 (157)

Fixers 3/11, refactor/surprise/review 2/11 each —
solid spread. Graphics axis recently busy (151,
158, 159) — 3 of 11 = 27%, matching MANIFEST 1-in-3.

161 options:

- **Windmill sail-tip caps** (158 filed) —
  graphics fixer; continues conforming-cap
  thread. 3-tick gap from 159 is comfortable.
- **Live-play verification** — filed at 157,
  158, 159. PRIORITY pile building.
- **148 wanderer citizen-tether** — fixer
  (unclear scope).
- **Surprise** — 2/11 is on target; not urgent
  but welcome.
- **120 bard/mayor/rival** — 154 budget caveat
  still in play.
- **Phase 4 font-size migration** — re-shipper;
  ~5 more exact-matches to close 130 HIGH. 10
  ticks from 150 / 1 tick from 160 — too soon.

Lean **windmill sail-tip caps** (158 filed) —
concrete, bounded, continues 095 closure work,
fixer. OR **live-play verification** if chrome
available.

## commit

Single commit: `realm loop 160: refactor-only —
type-scale phase 3 (8 exact-match migrations
across log/info/help/stats/happiness/trade/
research panels; zero visual shift)`. Touches
`index.html` only. Net LoC 0.

## wake scheduling

[code] (refactor-only; small; partial close of
130 HIGH) → 420s per cadence.
