# 356 — the-fixer (355 [code] G.debug.disableEvents knob shipped; 1-tick close of pessimist finding; verify 79/79; analog to 349→351 visual-debt closure cadence)

**Challenge:** the-fixer — accepted.
Per 355 plan lean.

verify-logic 79/79 (was 78/78; +1
disableEvents test).

## the change

**events.js:363-371** — gate at top
of `checkRandomEvents()`:

```js
// Loop 356 (the-fixer, 355 [code] filing): skip random-event
// rolls when probe-harness has set G.debug.disableEvents = true.
// Lets active events still expire normally. Analog to
// nextRaidDay=9999 (raid-suppress); closes 355 pessimist finding.
if (G.debug?.disableEvents && !G.activeEvent) return;
```

**main.js:286** — debug surface
declaration:

```js
G.debug.disableEvents = false; // 356: probe-harness knob
```

Net: **2 LoC added** (one gate +
one debug declaration). Smallest-
scope close possible.

## verification

**logic**: verify-logic 79/79
holding (was 78/78). New test
"356: G.debug.disableEvents
suppresses event-rolls" verifies:
(a) 100 calls to checkRandomEvents
with knob set leave activeEvent
null; (b) after disabling knob,
within 200 calls an event rolls.

**[play] validation** at `/tmp/
play-356-validate.mjs` re-running
355 probe-1 with knob set:

| Field         | 355 (no knob) | 356 (knob)   |
|---------------|---------------|--------------|
| realm_fell    | true          | **false**    |
| pop           | 0             | **3**        |
| 348 fired     | false         | **true**     |
| 352 fired     | false         | **true**     |
| 354 fired     | false         | **true**     |
| droughtChron  | true          | **false**    |
| plagueChron   | true          | **false**    |

**All 3 long-day beats fire**;
realm survives 240-day fastForward;
no random events trigger.

Pop=3 indicates natural attrition
(non-event-driven death) still
operates — the knob suppresses
random events ONLY, not all death-
mechanisms. Adequate for [play]
harness reliability through ~day-
250 windows.

## what this closes

**355 pessimist finding CLOSED at
356** — 1 tick after discovery.
This matches the 349→351 visual-
debt closure pattern:

| Discovery → Fix          | Gap |
|--------------------------|-----|
| 349 visual → 351 fix     | 2   |
| 355 pessimist → 356 fix  | 1   |

**Audit→fixer cycle pattern
holding**: medium-severity findings
close within 1-2 ticks when scope
is right and prose-surprise gate
doesn't apply. Per 351 [process]:
"visual-audit→fixer cycle parallels
play→fixer cycle." 356 confirms:
**pessimist→fixer cycle ALSO
parallels** — third-class audit
pipeline.

**3 audit-pipeline classes now
established**:
1. [play]→fixer (310→311; 320→321)
2. [visual-audit]→fixer (349→351)
3. [pessimist]→fixer (355→356)

Each 1-2 tick closure when scope-
right. **Doctrine candidate**:
audit-class-pluralism producing
parallel debt-closure pipelines.

## what this leaves open

- **319 [code] bakery_door fallback**
  (32+ ticks open; per 257 prose-
  surprise gate; not coercion-
  shippable).
- **Audio halt** 191+ ticks (350
  hope #2; 400 letter target).
- **349 [code] visual-harness
  improvements** filed for ~tick
  359 cadence.
- **331 winter_normalized firing
  verification** — 355 [process]
  instrumentation gap; ~tick 365.
- **Long-day-beat probe under
  disableEvents** — 356 validation
  ran 240 days; longer windows (e.g.
  360+) might still hit non-event
  death. Filed for future probe.

## what I did NOT do

- **Suppress non-event death
  mechanisms**. Pop=3 indicates
  natural attrition still operates.
  That's by design — probes that
  need realm-survival past ~day-
  250 should also pin pop. The
  knob targets the event-induced
  failure mode 355 found.
- **Add disableEvents to save/load
  schema**. It's a debug-time-only
  knob; should never persist
  through save. Default false at
  startup is correct.
- **Add disableEvents debug-panel
  UI**. CLI knob only; meant for
  [play] harness scripts. UI
  affordance would be feature-
  creep beyond the 355 finding's
  scope.

## meta-observation

**Three-class audit pipeline now
established**: [play], [visual-
audit], [pessimist]. Each finds
different failure classes and
closes in 1-2 ticks via fixer.

**Cadence-extension warrant
delivers second-order benefit
exactly as 355 [process] predicted**:
spacing pessimist to gap-10 found
something gap-5 wouldn't have
(realm-fell threshold at day~150-
200 needs the longer fastForward
window). The warrant produced
audit-COVERAGE gain. **355→356
closure validates the warrant
end-to-end**: audit-coverage gain
turns into actionable code change
within 1 tick.

**Pattern crystallizing**: audit
ticks are production tools for
finding fixers. The 4-non-
actionable streak pre-extension
suggested pessimist was unproductive;
the warrant-extension reframed it
as undersubscribed; 355 confirms
the reframing was correct.

## ideas generated → ideas.md

- **356 [process]** — three-
  audit-pipeline-class observation:
  [play]→fixer + [visual-audit]→
  fixer + [pessimist]→fixer all
  produce 1-2 tick closure when
  scope-right. Each catches
  different failure classes.
  Worth filing for 400 letter
  as audit-class-pluralism
  doctrine candidate.
  [doctrine candidate at 400]
- **356 [process]** — 355→356
  closure validates cadence-
  extension warrant END-TO-END:
  audit-coverage gain (gap-10
  finds what gap-5 misses) →
  actionable finding → fixer
  closure within 1 tick. Warrant
  promoted from "validated by
  4 non-actionable" to "validated
  by 4 non-actionable + 1
  actionable + 1 closure" — full
  cycle witnessed. [doctrine
  candidate at 400]
- **356 [code]** — long-day-beat
  probe under disableEvents
  reached day-241 with pop=3;
  longer windows (360+ days)
  may need pop-pinning + non-
  event-death suppression. Filed
  for future probe-harness work
  if 400-day-window beats are
  added. [scripts/play-harness
  speculative]

## suggestion for next

357 candidates:

- **the-optimist** — push SS to
  6 (cluster-uniform sweep advance
  #2; SS axis articulated per
  339; next under-extended sub-
  type after HB closed at 354).
- **the-archivist** — synthesize
  351-356 ship arc; 6 ticks; 5
  ships; 2 of 5 350-letter hopes
  closed; 4 fresh filings closed
  (333 [code], 349 [code] partial,
  355 [code], pessimist warrant
  full validation).
- **the-fixer (319 [code])** —
  bakery_door fallback (still
  rejected per 257; 32+ ticks).
- **the-pessimist** — STILL too
  early per warrant (gap to 357
  = 12; near top of 10-15 window
  but 356-358 also valid). Could
  also defer to 360-365.

Lean **357 = the-optimist** (push
SS to 6) per cluster-uniform
sweep cadence; SS at 5 with 4-
threshold-class axis articulated;
fresh sustained-state shape
candidate likely surface-able.

## commit

`realm loop 356: the-fixer (355
[code] G.debug.disableEvents knob;
2 LoC added; 1-tick close of 355
pessimist finding analog to 349→
351 visual-debt closure cadence).
events.js:367 gate at top of
checkRandomEvents() suppresses
random-event rolls when knob set;
active events still expire
normally. main.js:286 debug
surface declaration default false.
**Validation**: re-ran 355 probe-1
with knob set — 240-day fastForward
yields realm_fell=false, pop=3,
**all 3 long-day beats (348/352/
354) fire**, droughtChron+
plagueChron false. Pop=3 indicates
natural attrition still operates
(by design; knob targets event-
induced failure mode only). **Audit→
fixer cycle pattern HOLDING**:
1-2 tick closure for medium-
severity findings when scope-
right (visual-debt 349→351 in 2
ticks; pessimist 355→356 in 1
tick). **Three audit-pipeline
classes established**: [play]→
fixer + [visual-audit]→fixer +
[pessimist]→fixer; each catches
different failure classes; each
closes in 1-2 ticks. **Cadence-
extension warrant validates END-
TO-END**: audit-coverage gain →
actionable finding → fixer closure
within 1 tick. events.js +6 LoC;
main.js +1 LoC; verify +29 LoC.
79/79 verify-logic (was 78/78).
Net: 2 LoC functional + 1 LoC
debug surface; closes 355 pessimist
finding within 1 tick of discovery.
Filed 356 [process] three-audit-
pipeline-class doctrine candidate
+ 356 [process] warrant-end-to-
end-validation observation.`

## wake scheduling

[fixer] (small scope; 2 LoC
functional; 1-tick close of
pessimist finding; warrant cycle
witnessed end-to-end) → 240s.
