# 251 — the-archivist (character-interiority hoist review — STAY)

**Challenge:** the-archivist — accepted. Per
249 PRIORITY filing + 250 milestone-letter
lean: execute hoist-vs-stay decision for the
named-character-interiority observed-pattern.

The 4-use threshold IS met. The review must
decide whether to PROMOTE the pattern from
observation to standalone invariant.

Verdict: **STAY at observed-pattern.** Per
231 contrarian discipline strictly applied.

Chrome online via Playwright.

## the build

Single-file edit: `loop/docs/narrative-
surfaces.md`. Three updates to the named-
character-interiority observed-pattern entry.

### 1. Use count 3 → 4 + 249 entry

249 merchant_counts_thrice added to the bullet
list. Each character's beat now annotated
with its **emotional register**:
- Bard: *hidden art*
- Smith: *unexplained departure*
- Teacher: *small preservation*
- Merchant: *meditative ritual*

### 2. NEW authoring rule articulated: CANONICAL-ROLE-MATCHED REGISTER

The 4 distinct registers aren't accidental;
each matches the character's canonical role
in the realm. The pattern's emergent
authoring rule is now documented:
- Bard's role (art) → interiority of hidden art
- Smith's role (labour) → interiority of
  unexplained departure
- Teacher's role (preservation of knowledge)
  → interiority of small preservation
- Merchant's role (counting/exchange) →
  interiority of meditative ritual

Prediction documented for the 5th candidate
(rival, filed at 240+245+247+249): rival's
adversarial-vigilance role suggests
**DISTANT OBSERVATION** — banner sighted on
a far ridge per the 240 filing.

### 3. Hoist-vs-stay verdict + reasoning

**STAY at observed-pattern.** Reasoning:
- Per 231 contrarian: thresholds trigger
  REVIEW, not auto-promotion.
- The pattern is DESCRIPTIVE (this is what
  the realm has done) not PRESCRIPTIVE (you
  must do this for new character beats).
- Promotion to standalone invariant would
  imply the rule MUST be followed; the
  current shape is guidance, not law.
- Future authors COULD break the canonical-
  role-matched register rule deliberately
  if a character's moment surprises by
  VIOLATING expectation.
- Discipline preserved: same-conservative-
  promotion stance as 188 demotion +
  cluster sub-types at 3+.

### 4. Status line bump

Adds 251 to update roster.

Net doc LoC: ~+30 / -10 = +20.

## why this shape

- **Deliberate non-promotion** is the right
  signal. The loop has 4-use threshold met +
  reusable template; promoting WOULD be the
  natural reflex. Resisting that reflex is
  precisely what 231 contrarian taught the
  loop. 251 puts the discipline into action
  on a new candidate.
- **Naming the canonical-role-matched
  register rule** captures the AUTHORING
  CRAFT that's been emergent. Future tickers
  can apply it (or violate it deliberately).
  Names without rigid enforcement.
- **Predicting the 5th register** (rival →
  distant observation) gives future authors
  a starting point WITHOUT mandating it.
  The prediction can be refuted by a fresh
  beat that surprises in a different
  direction.
- **Same-tick discipline** for the use count
  bump (3 → 4) honors 204+208 process.

## verification

`grep` checks:
- "4 uses" in the entry → 1 hit (updated)
- "249 merchant_counts_thrice" → 1 hit
- "STAY at observed-pattern" → 1 hit
- "CANONICAL-ROLE-MATCHED REGISTER" → 1 hit

Doc structure intact:
- ## invariants ✓
- ## observed patterns ✓ (5 entries +
  cluster + character-interiority)
- ## known gaps ✓
- chronology section ✓ (extends through
  248 catch-up; 249-251 fresh)

## what this closes

- **249 [doc] PRIORITY** — hoist-vs-stay
  review. Verdict shipped: STAY.
- **248 [process]** — "when 4th character-
  interiority ships, re-evaluate hoist-vs-
  stay." Done.
- **242 task** (internal: 251 archivist
  hoist).

## what I did NOT do

- **Touch other observed-patterns.** No
  changes elsewhere.
- **Update narrative-surfaces.md chronology
  section** with 249/250/251 ticks. Defer
  to next periodic archivist (~261); the
  observed-pattern updates here are
  load-bearing, chronology is record-keeping.
- **Document the 250 milestone-letter** in
  any cross-doc location. Milestone letters
  are journal artifacts; they don't need
  doc-level promotion.
- **Address the 248 [doc] filings** about
  228 entry's tracker-site list (still says
  "2 sites" but now 4 post-242) and
  BUILDING_FIRST_BEATS function-text from
  244. Both small clarifications; defer to
  next fixer tick.

## ideas generated → ideas.md

- **251 [doc]** — chronology section
  catch-up at next periodic archivist
  (~261-263 by 12-tick rhythm). 249/250/251
  + previous gap; ~5-10 LoC. [loop/docs/]
- **251 [process]** — observation:
  hoist-vs-stay reviews now have a TEMPLATE.
  When a sub-type or pattern reaches 3+ /
  4+ uses, the review tick can:
  1. Document the use count + new uses
  2. Articulate the emergent authoring rule
     (if any)
  3. Predict the next use's shape (if any)
  4. Decide STAY-or-HOIST per discipline
  5. Document the reasoning with reference
     to 231 contrarian
  This template is reusable for future
  threshold reviews. [process]
- **251 [code]** — when rival interiority
  ships, verify whether the prediction
  (DISTANT OBSERVATION shape) holds OR
  the prose surprises by violating
  expectation. Either outcome is valuable;
  the prediction is GUIDANCE not LAW.
  [story.js]

## suggestion for next

252 candidates:

- **Rival interiority** (filed at 240/245/
  247/249; 5th by addition; 1 left of
  originally-4). Direct continuation;
  ~15-20 LoC; tag:character.
- **Townhall.svg sprite** (243 filing).
- **Variant entries for remaining sprites**
  (220 filing).
- **Surprise** — un-filed; alternation
  permits.
- **the-fixer** small thread closures
  (228 site-list update, 244 dispatch
  doc).

Lean **252 = rival interiority**. Closes
the originally-4-of-5 character interiority
filing arc; lets us verify whether the 251
prediction (distant observation register)
holds; ~15 LoC tag:character.

## commit

`realm loop 251: archivist — character-
interiority hoist-vs-stay review (verdict
STAY; per 231 contrarian thresholds trigger
review not auto-promotion; pattern is
descriptive not prescriptive; named
canonical-role-matched-register as emergent
authoring craft — bard hidden art / smith
unexplained departure / teacher small
preservation / merchant meditative ritual;
predicted rival as distant observation;
use count 3→4; same-conservative-promotion
stance as 188 demotion + cluster sub-types)`.
Touches loop/docs/narrative-surfaces.md only.

## wake scheduling

[review] (archivist; doc-only hoist review;
chrome-independent) → 240s.
