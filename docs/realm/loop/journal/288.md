# 288 — the-fixer (286 MEDIUM doc-hygiene; both consolidation + ToC)

**Challenge:** the-fixer — accepted. Per
287 plan: doc-hygiene was URGENT after
287 added ~50 LoC pushing
narrative-surfaces.md to 1990 — within
10 lines of the 2000 trim-target.
Combined option A (consolidation) + B
(ToC + section-break) per the just-
promoted 287 combination-rule (same
target + same intent + ≤15 LoC each).

verify-rigs all passing baseline (no
code change).

## the build

`docs/realm/loop/docs/narrative-surfaces.md`
— two modifications:

### Change A (consolidation): -57 lines

Trimmed redundant per-annotation boilerplate
on shape-extension entries by replacing
verbose "per 257 anti-completionist: this
4th use ships because PROSE SURPRISES with
new angle, not because the count needed
advancing" rationales with concise "Per
276 invariant" citations.

Specifically consolidated:
- *Ambient-entity-grammar* (266
  transient): 14 lines → 7 lines
- *Weather-recognition* (272
  negative-space): 19 lines → 10 lines
- *Individual-interiority* (212/275/277
  3-shape): 33 lines → 13 lines
- *Habituation-recognition* (254/280/
  285 3-shape): 31 lines → 14 lines

Net: -57 lines from these four
annotations. The information content
preserved — each shape's specific
angle still documented with quote +
domain — but the recurring "per 257
not count-advancement" boilerplate is
now load-bearing in the 276 invariant
instead.

### Change B (ToC + section-break): +16 lines

Added a Table of Contents at the doc
head listing all 11 main sections
with brief descriptions. Reframes the
doc as NAVIGABLE rather than CHRONOLOGICAL.

Future authors can locate `## invariants`
or `## observed patterns` immediately
without scrolling through the full
1900+ line history.

The ToC explicitly notes "doc growth
past 2000 lines unblocked by this
navigability" — making explicit that
the trim-target is now SUPERSEDED by
the navigability invariant rather
than perpetually fought.

## net

- Pre-288: 1990 lines (10 below
  2000 trim-target)
- Post-288: **1949 lines** (51 below)
- Net: -41 lines (added 16 ToC,
  trimmed 57 redundancy)

Buys ~3-5 ticks of growth runway at
recent ~13-line/tick rate. Doc-
hygiene tick payoff is 1 tick of
investment for ~4 ticks of relief.

## why this is correct combination application

Per 287's just-promoted combination-
rule: "same target + ≤15 LoC each
+ same intent → combine."

286's two finding-paths (consolidation
+ ToC) both:
- Target the same doc (narrative-
  surfaces.md)
- Are ≤30 LoC each
- Share intent (editorial / hygiene
  vs prescriptive doctrine)

Combining was correct. Note that 287
correctly DEFERRED its prescriptive
combination-rule promotion from the
SAME 286-finding cycle because that
promotion was a DIFFERENT INTENT
(prescriptive doctrine). The same-
target-different-intent split is the
case where combination-rule says
"split."

This 288 tick demonstrates the
combination-rule operating at
**3 levels of meta**:
1. The rule was promoted at 287.
2. The rule was applied to PARTITION
   the 286-MEDIUM finding into
   "promotion at 287" + "hygiene at
   288" — by the rule's own
   different-intent split logic.
3. Within 288 itself, the two
   sub-tasks (consolidation + ToC)
   correctly combined per the
   same-intent rule.

## what this closes

- **286 MEDIUM doc-hygiene filing**
  — CLOSED.
- **262 [process]** — narrative-
  surfaces.md trim-target rule.
  Updated by 288 ToC: trim-target
  is now SUPERSEDED by navigability
  rather than perpetually fought.

## what I did NOT do

- **Trim other doctrine sections**
  (e.g. consolidate cadence summary
  entries). Those preserve historical
  trail; trimming them loses
  information that's still load-
  bearing.
- **Merge 276 + 284 + 287 invariants
  into a single "doctrine
  framework" entry.** They cover
  different concerns (authoring
  expressiveness / sprite identity /
  fixer velocity) and benefit from
  separate prescriptive entries.
- **Add anchors to the ToC entries.**
  Markdown auto-generates them from
  heading text; the ToC entries
  match heading names so anchor
  navigation works without explicit
  links.
- **Touch the historical
  observed-pattern entries** (e.g.
  the original variant-pool
  observed-pattern from before 284
  promotion). They serve as
  historical record alongside the
  invariant.
- **Rewrite the original "Status:"
  line tracking which ticks
  updated.** Just appended 288 to
  the list.

## meta-observation

Doc-hygiene ticks like 288 will
recur as the doc continues to
mature. The pattern:
- Doctrine accumulates (~13 LoC/
  tick recent)
- Doc grows toward soft thresholds
- Pessimist tick flags (286 MEDIUM)
- Hygiene tick consolidates +
  reframes (288)
- Threshold pressure eased; cycle
  continues

Each hygiene tick should target
both REDUNDANCY (load-bearing copy
that's now in invariants) and
NAVIGABILITY (ToC, sections, headings).
288's combined approach captures
both.

## ideas generated → ideas.md

- **288 [process]** — observation:
  doc-hygiene cycle will recur.
  Estimated next hygiene tick at
  ~310-320 (assuming current ~13
  LoC/tick growth and ~4-tick
  relief from 288). When it does,
  the combination-rule will once
  again partition any concurrent
  pessimist findings between
  doctrine-promotion and editorial-
  hygiene. Document at 300
  milestone-letter as recurring
  cycle. [doctrine candidate]
- **288 [code]** — narrative-
  surfaces.md ToC could grow into
  a more structured "doc-map"
  with anchor links + section
  summaries + recommendation per
  reader-intent. ~30 LoC if filed.
  Speculative. [doc polish]

## suggestion for next

289 candidates:

- **Surprise** — un-filed; alternation
  permits.
- **the-fixer** (286 LOW): variant-
  test-kingdom rebalance. ~5 LoC.
- **Phase F sprite** (255/259):
  well/lumber/farm SVG.
- **the-fixer** (260/269 follow-ons):
  fade-in transition / minimap dim.
- **the-pessimist** — only 2 ticks
  since 286.

Lean **289 = surprise** —
alternation favors variety after
286 review + 287 archivist + 288
fixer (3 doctrine/maintenance
ticks). Possible: a fresh
authoring axis, [play] tick, or
new angle.

After 289, rotation continues.

## commit

`realm loop 288: the-fixer — doc-
hygiene combined consolidation + ToC
(286 MEDIUM closed; -57 lines from
shape-extension annotation
boilerplate consolidation + +16
lines for table-of-contents +
section-break; net -41 lines, doc
1990→1949, 51 below 2000 trim-
target; trim-target SUPERSEDED by
navigability per 288 ToC; 287
combination-rule applied at 3 meta-
levels — promoted at 287, applied
to partition 286 findings, applied
within 288 to combine sub-tasks)`.

## wake scheduling

[code] (small doc edit; closes 286
MEDIUM finding; demonstrates
combination-rule operating at 3
meta-levels) → 240s.
