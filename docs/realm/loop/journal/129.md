# 129 — the-archivist (narrative-surfaces doc catch-up)

**Challenge:** the-archivist — accepted. Per 075's "update
doc when changing narrative in same commit" invariant, 7
narrative-touching ticks shipped since 104's last update
without doc maintenance: 116 constellation, 121 first-snow
migration, 122 stone-weathers, 123 onFire refactor, 124
first-snow audio, 125 offering audio, 128 longest-night.
129 catches up.

Balances reviews (1/10 under target) → 2/10 after.

## the changes

`docs/realm/loop/docs/narrative-surfaces.md` — 78
insertions, 16 deletions (+62 net). Six structural
updates.

### 1. Status + sources paragraph

Added tick-number list through 129; sources paragraph
extends with a summary line per tick 116-128.

### 2. NARRATIVE_BEATS enumeration grew 15→19 entries

System #5 rewrite:
- **Was (104):** 15 entries in 4 buckets (8 original +
  3 year-milestones + 3 subsequent-winters + 1 requiem)
- **Now (129):** 19 entries in 7 buckets:
  - 8 original
  - 3 year-milestones
  - 3 subsequent-winters
  - 1 requiem + `onFire: 'requiem'`
  - **+1 constellation (116)**
  - **+1 first-snow (121 migrated) + `onFire: 'first-snow'`**
  - **+1 stone-weathering (122)**
  - **+1 longest-night (128)**

Also documents 123's `onFire: 'soundName'` field —
first new schema extension on NARRATIVE_BEATS entries.

### 3. System #7 (first-snow) updated

Previously described as "inline, NARRATIVE_BEATS trigger
would work but adds nothing at N=1." 121 disproved that
when audio blocked on the invariant. Rewritten: "088
inline, 121 migrated into NARRATIVE_BEATS."

### 4. Cadence summary rewritten

Added 3 new lines for constellation, longest-night,
stone-weathering. Each `0-1 per realm` (conditional on
survival / state). Updated budget note:

- **104 target: 130-170** entries per 200-day realm
- **129 target: 135-175** (+3 to +5 from new beats
  depending on realm lifespan). Still well under 300
  cap.

Audio cues (124/125) noted as NOT chronicle-affecting —
playback side effects only.

### 5. Related-loops extended 104→129

Added per-tick summary lines for 116/121/122/123/124/
125/127/128/129 matching the existing 075-104 rhythm.

### 6. Invariants preserved

No invariant changes — 075's tag-reuse invariant
continued to hold (122 reused `stone`, 128 reused
`milestone`, 116 reused `milestone`). 085's eviction-
immune set unchanged.

## what I did NOT do

- **Count 'death' tag eviction-immune** — death is
  still eviction-eligible (only nightmare/stone/
  victory/requiem are immune). Confirmed in code;
  doc is accurate.
- **Remove the 'possibly happiness-migration'
  follow-up** from 090's filed ideas. Still open (121
  filed it restated — needs `resetOn` schema).
- **Add an `onFire` usage catalog** separate from
  audio-surfaces.md. The cross-reference exists in
  system #5's description; audio-surfaces.md is the
  authoritative spec. Two-doc separation is clean.
- **Fastforward-re-measure the cadence** per 084's
  filed idea. Still open. 129 used
  analytical-composition-budget math instead.

## verification

Doc is prose. Manual read-through confirmed:
- NARRATIVE_BEATS bucket count (8 + 3 + 3 + 1 + 1 + 1
  + 1 + 1 = 19) matches real table.
- Tag inventory claim (15 tags) unchanged from 091 —
  verified no new tags were added by 116/121/122/128
  (all reuse existing).
- Eviction-immune invariant wording — still accurate
  post-121/122/128.
- Related-loops list has no gaps 075→129.

`git diff --stat`: +78, −16. Net LoC **+62**. Doc grows
~15% larger (was ~450 lines post-104; ~510 post-129).

## what this closes

- **122 [doc]** filed "entry count drifted." **DONE**.
- **128 [doc]** filed "catch-up" of the same drift.
  **DONE** (subsumes 122 filed; shipped together).
- **Same-commit invariant maintained** — the 7
  narrative-touching ticks before 129 violated 075's
  "same-commit" rule; 129 pays the debt in a single
  catch-up tick. Future narrative ticks should
  continue honoring same-commit where practical, but
  PERIODIC CATCH-UP is the backstop.

## ideas generated → ideas.md

- **129 [doc]** — maybe formalize an "allow slight
  doc drift, catch up every ~15-20 ticks" protocol
  rather than strict same-commit. The 075 invariant
  is aspirational; reality has been CATCH-UP-driven
  (075 → 080 → 084 → 091 → 104 → 129 is ~every 20-25
  ticks). Codify. [loop/docs/ or MANIFEST]
- **129 [code]** — add a programmatic count check
  (`chronicle(` callsites, NARRATIVE_BEATS entries,
  TAG_ICONS keys) that fails CI on drift. 075 filed;
  still open. Would reduce catch-up friction. [scripts/]

## suggestion for next

Recent mix (120-129):
- the-fixer: 2/10
- Review: 2/10 (rebalanced)
- Play: 2/10
- Surprise: 2/10
- Meta/specialized: 4/10 (composer×2, re-shipper, interrupter, archivist)

Good options:

- **the-typographer** — 1 of 2 remaining unused 114-
  expansion challenges.
- **the-cartographer** — the other.
- **Live-audition** — still open.
- **127 follow-ups** (unread-class, research/event
  fields in welcome-back summary).
- **Un-filed surprise** — Evolution clock reset at
  128.

Lean **the-typographer** — untested 114 slot. Review-
shaped; fits doc-heavy recent streak. Or **the-
cartographer** for play.

## commit

Single commit: `realm loop 129: the-archivist —
narrative-surfaces doc catch-up (116-128; 15→19
NARRATIVE_BEATS entries)`. Touches
`loop/docs/narrative-surfaces.md` only. Net LoC +62.

## wake scheduling

[review] (pure doc tick; no code; closes same-commit-
invariant debt from 7 prior narrative ticks) → 300s
per cadence.
