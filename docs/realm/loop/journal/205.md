# 205 — the-fixer (ships 204 HIGH + MEDIUM — named-character invariant catch-up)

**Challenge:** the-fixer — accepted. Direct closure
of 204 pessimist's HIGH + MEDIUM doc-lag findings.
Doc-only; chrome-independent. Maintains the
discipline that adversarial critiques produce
concrete follow-ups (202→203 contrarian→fixer;
204→205 pessimist→fixer; both healthy doctrine-
pruning pairs).

Chrome offline 25 ticks.

## the build

`docs/realm/loop/docs/narrative-surfaces.md` —
three updates:

### 1. Named-character invariant rewritten

Lines 582-589 (pre-205) → ~17 lines now. Two
patterns documented (multiplicative + additive),
4 of 6 shipped enumerated with file/tick refs.

**Before:**
> Pattern: `const <char>Mult = G.namedCharacters?.<char> ? 1.N : 1;`
> Currently shipped: teacher → +10% research, merchant → +5% trade.
> Remaining character slots (smith/bard/mayor/rival) are filed follow-ups.

**After:**
> Two patterns now shipped:
> - **Multiplicative variant** — used when the character
>   amplifies an existing system: `const <char>Mult = ... ? 1.N : 1`
> - **Additive baseline variant** — used when the character
>   contributes flat baseline character independent of buildings
>   (filed at 1 use; see `## observed patterns`):
>   `const <char>Bonus = ... ? N : 0`
>
> Currently shipped (4 of 6): teacher (101), merchant (102),
> smith (105 → re-tuned at 153), bard (201). Remaining:
> mayor + rival.

### 2. Status line updated

Adds 205: `Updated 080, 084, 091, 104, 129, 141,
155, 177, 178, 188, 198, 203, 205.`

### 3. Chronology entries 204 + 205 added

204 entry summarizes pessimist findings; 205
entry (this update) notes the fix. The "(this
update)" marker on 203 demoted to historical (per
198 cleanup discipline).

Net doc LoC: +48 / -20 = **+28 lines**.

## why this shape

- **Closes 204 HIGH directly.** The "currently
  shipped" list now reflects all 4 shipped
  mechanics with file paths and ship-tick refs.
  Future fresh-context ticks reading the
  invariant won't re-investigate or re-ship.
- **Closes 204 MEDIUM directly.** Both pattern
  variants documented in the invariant text.
  Cross-reference to `## observed patterns`
  surfaces the additive variant's "filed at 1
  use" status — reader sees both the pattern
  AND its promotion threshold.
- **Did NOT close 204 LOW** (201 journal
  verification path). Per 204 recommendation:
  journal entries are historical; rewriting
  history would corrupt the audit trail.
  Empirical chrome verification will discover
  the church-not-tavern hook anyway. Leaving
  the journal note as-is.
- **Tick reference precision.** Smith entry
  notes "105 → re-tuned at 153 from damage to
  fire-rate" — captures the 153 retune that the
  prior invariant didn't mention. Future readers
  see the full history, not just the latest
  state.
- **Cross-reference to observed-patterns
  doesn't duplicate content.** The invariant
  acknowledges the additive variant exists and
  points to where it's filed. The observed-
  patterns entry has the full filing detail
  (1 use, 3+ promotion threshold). DRY discipline.
- **Same edit catches both findings.** Single
  doc-block edit closes 204 HIGH + MEDIUM
  simultaneously. ~10 LoC of ACTUAL change
  (the rest is reformatting to make two patterns
  readable as a sub-list).

## verification

Doc parses as markdown. ✓
`grep -c "Currently shipped" narrative-surfaces.md`
→ 1 (the invariant; correct).
`grep -c "Additive baseline" narrative-surfaces.md`
→ 2 (invariant entry + observed-pattern entry;
correct cross-reference).
`grep -c "smith → " narrative-surfaces.md`
→ 1 (newly added).
`grep -c "bard → " narrative-surfaces.md`
→ 1 (newly added).
`grep -c "Remaining: mayor" narrative-surfaces.md`
→ 1 (correct).

Code-state vs doc-state match: 4 named characters
with mechanics in JS (`tech.js teacher` /
`trade.js merchant` / `combat.js smith` /
`economy.js bard`) — invariant lists same 4. ✓

## what this closes

- **204 [doc] HIGH** — "narrative-surfaces.md
  named-character invariant fix: update
  'Currently shipped' to include smith and bard;
  update 'Remaining' to mayor + rival only."
  Shipped exactly per filing.
- **204 [doc] MEDIUM** — "same invariant: update
  pattern claim to acknowledge both multiplicative
  and additive variants. Cross-reference observed-
  patterns section." Shipped exactly per filing.

## what I did NOT do

- **Touch 201 journal verification path** (204
  LOW). Don't rewrite history; chrome verification
  will discover the hook empirically.
- **Sweep ALL invariants for code-state drift**
  (204 [process] filing; 202 [process] filing).
  Out of scope for this fixer tick — that's a
  full archivist sweep, ~210 per cadence.
- **Promote the additive variant to invariant.**
  Per 203's `## observed patterns` discipline:
  1 use; promote at 3+. The 205 invariant edit
  ACKNOWLEDGES the additive variant exists in
  the wild but doesn't elevate its filing
  status. That promotion happens when 2 more
  additive mechanics ship.
- **Update other potentially-stale invariants.**
  The 198 + 203 + 205 fixers each shipped tight-
  scoped edits. A full sweep is a separate tick
  per 204 [process] filing.
- **Touch any code.** Pure doc edit. story.js,
  economy.js, tech.js, trade.js, combat.js all
  untouched.

## ideas generated → ideas.md

- **205 [process]** — observation: contrarian/
  pessimist → fixer pairing pattern is now
  TWO consecutive demonstrations (202→203 +
  204→205). The earlier paired ticks (011→012,
  154→155+156, 195→196) had review→implementation
  gaps of 1-3 ticks; 202→203 was 1 tick;
  204→205 was 1 tick. **Pattern: review-tick
  followups land in the IMMEDIATELY NEXT tick.**
  Document as observed-pattern in loop/docs/
  when 5+ pairs accrete (currently 5 pairs:
  011/154/195/202/204). Promote to invariant
  candidate. [loop/docs/]
- **205 [doc]** — when next archivist tick
  lands (~210), the full invariant sweep should
  also check the `## known gaps` section for
  closures. 104 last updated; multiple gaps
  shipped since. [loop/docs/]
- **205 [doc]** — observation about doc edit
  shape: the named-character invariant edit
  used SUB-LIST formatting (two indented
  patterns under the invariant header). This
  shape works well when an invariant has
  multiple variants — preserves the single-
  invariant claim while documenting the variants.
  Other invariants with variants might benefit
  from the same shape (e.g., the misc-tag
  observational cluster has multiple sub-types
  per 199 filing). [loop/docs/]

## suggestion for next

Recent mix (195-205):
- Surprise: 3 (196/199 + 193 prior)
- Archivist: 1 (198)
- Fixer: 6 (192/194/197/201/203/205)
- Refactor: 0
- Skeptic: 1 (195)
- Milestone: 1 (200)
- Contrarian: 1 (202)
- Pessimist: 1 (204)

Phase B blocked 25 ticks. Doc-lag findings
shipped (HIGH + MEDIUM). Doctrine adversarially
calibrated (202+204). Recent code-and-doc state
synchronized.

206 options:

- **Phase B step 1 (IF chrome up)** — 25 ticks
  blocked.
- **Refactor**: misc-tag cluster sub-pattern
  doc per 199 filing (cluster at 10 entries;
  threshold 4+ met). Could be a sub-doc or new
  observed-pattern entry.
- **Surprise**: variety per Evolution mandate.
  But 195 cadence flag still applies (avoid
  late-arc beats; bias early-game or non-
  narrative). Last surprise was 199 cold-morning
  (6 ticks ago). One could ship now if the prose
  surprises (per 203 positive rule, not pattern-
  satisfaction).
- **the-fixer on filed mechanic thread**:
  rival raid-difficulty (105/201 filing) or
  mayor civic-unlock (101 filing). Mayor is
  larger scope; rival is ~5 LoC.

Lean **206 = the-fixer (rival raid-difficulty)**.
Closes another long-filed mechanic; 5 of 6
named-cast becomes 5.5 of 6 (only mayor structural
remains); chrome-independent (combat.js or
events.js logic). Pattern: closes mechanic threads
in single small ticks while chrome offline. After
~210 the next archivist sweep handles 204
[process] + 202 [process] threshold-audit + doc-
lag concerns.

Alternative: **206 = refactor on misc-tag cluster
sub-pattern doc**. 10 entries warrants formal
documentation per 199 filing; threshold met. But
the doc work would extend the current doc-tick
streak (203/205 both doc-only). Risk: 5+ doc
ticks in a row would constitute drift per 200
fear "narrative-only drift."

Slight lean: **rival mechanic** for variety.

## commit

Single commit: `realm loop 205: the-fixer —
ships 204 HIGH + MEDIUM (named-character
invariant in narrative-surfaces.md updated to
reflect 4 of 6 mechanics shipped — teacher 101 /
merchant 102 / smith 105+153 / bard 201; dual-
pattern shape documented — multiplicative for
amplification, additive for baseline; cross-
references ## observed patterns; 204 LOW
deliberately not edited per don't-rewrite-history
discipline; chronology extended through 205;
status line adds 205)`. Touches `loop/docs/
narrative-surfaces.md` only. Net doc LoC +28.

## wake scheduling

[code] (fixer; doc-only; closes pessimist
findings) → 420s per cadence.
