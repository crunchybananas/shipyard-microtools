# 351 — the-fixer (house _SPRITE_SIZES bump 42×48 → 50×54; closes 349 HIGH visual finding)

**Challenge:** the-fixer per 350 plan
verbatim — house sprite size bump,
smallest-scope close of 349 HIGH
finding "houses render at ~65% the
pixel-width of townhalls."

verify-logic 76/76 baseline holding.

## the change

`render.js:216` — single LoC:

```diff
-house: { w: 42, h: 48 }, // +17% / +20%
+house: { w: 50, h: 54 }, // 351: bumped from 42×48 per 349 visual audit
```

Net: +19% width / +12.5% height
on `_SPRITE_SIZES.house`. The 1.1×
parent envelope still multiplies these.

## verification

**logic**: verify-logic 76/76 holding.
No tests reference `_SPRITE_SIZES`
directly so the bump is data-only.

**visual A/B**: ran play harness at
`/tmp/play-351-visual.mjs` mirroring
349's setup (10 buildings: 4 houses
+ 6 civic/military, deterministic
seed 20260430, mid-day phase, defense
+ raid suppression).

Captured at z=1.0 / 1.6 / 2.4 / 3.0
to `/tmp/realm-351-{label}-z{N}.png`.

Compared against 349 baseline at
identical zoom (`/tmp/realm-349-*`):

- **z=2.4 inspect**: houses visibly
  larger; gap to townhall/church
  narrowed but houses still distinctly
  smaller (preserving civic > house
  hierarchy as intended).
- **z=3.0 pixel**: houses clearly
  more substantial; thatched-roof
  silhouette reads at default play
  zoom rather than getting lost.
- No regression in other building
  types (none changed).

No console/page errors.

## what this closes / leaves open

**CLOSES** (349 HIGH visual finding,
candidate A — smallest-scope fix):
- houses no longer read as "super
  small" relative to civic buildings.

**STILL OPEN** (349 candidates B/C):
- (B) revise house.svg art to fill
  more of 128×128 viewBox — heavier
  ~30 LoC SVG rework. **Defer per 349
  recommendation**: (A) sufficient.
- (C) both — not needed if (A)
  empirically sufficient.

**STILL OPEN** (other 349 findings):
- MEDIUM: visual-audit harness needs
  dialog-dismiss + pop-below-threshold
  + dayPhase-override defaults.
  Filed for ~tick 359 visual cadence.
- LOW: default-night-render forces
  manual dayPhase override. Not
  blocking; documented in harness.

**STILL OPEN** (older debt):
- Audio halt 188+ ticks (350 hope #2).
- 319 [code] bakery_door fallback
  (29+ ticks open).
- 333 [code] last-updated annotation
  (18+ ticks open).

## visual debt status update

Pre-351:
- HIGH: house sizing
- MEDIUM: scenario-dialog blocks audit
- LOW: default-night-render

Post-351:
- ~~HIGH: house sizing~~ **CLOSED**
- MEDIUM: scenario-dialog (still open)
- LOW: default-night-render (still open)

**One visual finding closed within 2
ticks of discovery** (349 audit → 351
fix). This validates the cadence: deep
visual audit identifies debt, fixer
slot closes it shortly after.

## what I did NOT do

- **Revise house.svg geometry** (349
  candidate B). Defer per (A)
  sufficient.
- **Implement harness improvements**
  (349 [code]). Filed for next visual
  audit ~tick 359.
- **Run interactive headed session**.
  Headless A/B sufficient for size
  verification.
- **Touch any other building's
  `_SPRITE_SIZES`**. Single-house
  scope; preserves rest of envelope
  table verbatim.

## meta-observation

**350 letter hope #1 ("visual-debt
momentum: 349's HIGH house-size
finding closed by ~360") closes at
tick 351 — 9 ticks ahead of pace.**

Pattern: visual-audit-tick (349) →
milestone-letter-tick (350) →
fixer-tick (351). The audit
identifies; the letter records the
finding into doctrine; the fixer
closes within 2 ticks.

This is the **[visual-audit] → [code]
cycle** parallel to the established
**[play] → [code] cycle**:
- [play]+critic ticks discover logic
  bugs / cluster gaps → fixer ticks
  close (310→311, 320→321).
- [visual-audit] ticks discover
  rendering issues → fixer ticks
  close (349→351).

Both validate the user's 348 mandate:
visual+play cadence on its own track,
not subordinate to logic verification.

## ideas generated → ideas.md

- **351 [process]** — observation:
  visual-audit→fixer cycle parallels
  play→fixer cycle. Both close
  findings in 1-2 ticks when scope
  is right. Worth filing for 400
  letter as evidence that
  audit-types-not-interchangeable
  (per 349 [process]) generates
  parallel debt-closure pipelines.
  [doctrine candidate at 400]
- **351 [code]** — visual-audit
  harness improvements still pending
  (349 [code]); ~20 LoC; deferred
  to ~tick 359 visual cadence so
  the harness ships with the next
  audit not before it.
  [scripts/play-harness.mjs]

## suggestion for next

352 candidates:

- **the-pessimist** — 4 consecutive
  non-actionable per 350 letter
  (328+333+338+345); cadence-
  extension warrant validates
  pessimist window 5-8 → 10-15.
  Per the warrant, 352 may yield
  non-actionable; that's expected
  signal not failure.
- **the-optimist** — narrative beat
  tick; 8th INSIDE sub-type at 5+
  (Sustained-state-recognition
  currently at 5; could push to 6
  with another beat). Or grow one
  of the 9 existing sub-types.
- **the-archivist** — 350-letter
  consolidation; index updates;
  surface staging.

Lean **352 = the-optimist** (narrative
beat). Per 350 letter cadence-
extension, pessimist window can
extend; optimist beats are the
default fall-through.

## commit

`realm loop 351: the-fixer (house
_SPRITE_SIZES bump 42×48 → 50×54
per 349 HIGH visual finding). 1
LoC change in render.js:216;
+19% width / +12.5% height. Visual
A/B at zoom 1.0/1.6/2.4/3.0 vs
349 baseline confirms houses now
render proportionate to civic
buildings; civic > house hierarchy
preserved (church/townhall still
visibly larger). Fix candidate (A)
from 349 — smallest scope; defers
heavier (B) house.svg rework as
unnecessary if (A) sufficient.
**Closes 350 letter hope #1
(visual-debt-momentum: HIGH closed
by ~360) at tick 351 — 9 ticks
ahead of pace**. Visual debt status:
HIGH→CLOSED; MEDIUM (scenario-
dialog) + LOW (default-night-render)
remain open for ~tick 359 cadence.
**Visual-audit→fixer cycle
established as parallel to play→
fixer cycle**: both close findings
within 1-2 ticks when scope is
right. 76/76 verify-logic holding.
Net: 1 LoC code change; 4
screenshots at /tmp/realm-351-*.png;
play harness at /tmp/play-351-
visual.mjs.`

## wake scheduling

[fixer] (small scope; 1 LoC; visual
A/B confirmed; closes 349 HIGH +
350 hope #1) → 240s.
