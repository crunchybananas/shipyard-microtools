# 029 — the-fixer (023's prescription, finally)

**Challenge:** the-fixer — accepted. Close 023's
minimalist prescription that's been pending five ticks
(023 → 024 → 025 → 026 → 027 → 028 → now). 019's
triage principle ("trivial and long-waiting > trivial
and recent") applied: picked 023 over 027's citizen
hit-box for this tick. 10/29 the-fixer (34%), still
under 020's monoculture threshold.

## the fix

One line in `js/scenarios.js:14`:

```diff
- { text: 'Research stonework, then build a Church', check: () => G.buildings.some(b => b.type === 'church') },
+ { text: 'Build a Church', check: () => G.buildings.some(b => b.type === 'church') },
```

15 characters deleted. Check function unchanged.

## verification

Chrome-mcp, fresh New Game. Peaceful scenario's 3
objectives now read atomically:

```
[
  "Reach 10 population",
  "Build a Church",
  "Reach 80% happiness"
]
```

Mission panel DOM confirms the new text renders:
*"Build a Church"* in the objectives list. No stale
references to "stonework" anywhere in the game UI
(grep'd scenarios.js — it's the only occurrence).

`node --check js/scenarios.js` → OK.

## the long-waiting premise

023 (the-minimalist, first use) prescribed this
15-char fix in detail. 024 (pessimist), 025 (fixer on
undo+chronicle), 026 (refactor-only), 027 (the-kid),
028 (archivist) all noted 023's prescription as
"pending" in their suggestion lists — and all five
chose something else. That is normal loop behavior
(other findings were also legitimate) but the 5-tick
gap was on the wrong side of 019's "long-waiting >
recent" rule.

019 ships the first "long-waiting" fix (tutorial
progress label, 12 ticks after 007 filed it) with the
observation: *"The loop should preference 'trivial and
long-waiting' over 'trivial and recent' when
triaging."* 029 applies that discipline. It took
effort — 028's suggestion list put citizen hit-box
first — but the prescribed order was correct.

## why this change matters (recap, from 023)

Before: *"Research stonework, then build a Church"* —
the only chained objective in the game, the only one
naming a tech by name, and the name it used
("stonework") doesn't match the engine's ("masonry").
The check function only verified half its own text.

After: *"Build a Church"* — atomic. Matches the shape
of all 14 other objectives across the 5 scenarios. No
lying check. No terminology mismatch.

Research discovery (the research half of the old text)
was already handled by tutorial step 6 and the
Research button's top-bar presence. The objective text
doesn't need to do that work; it was doing it badly.

## what I did NOT do

- **Mission-objective tone pass** (021 idea). 15 other
  objectives across 5 scenarios could also be
  rewritten in a more evocative voice. Valid follow-up
  but a separate tick — one challenge per iteration.
- **"Build a Windmill + Bakery"** in Industrial (023
  noted this as a conjunction variant). Similar
  structural issue. Also a separate tick.
- **Widen citizen hit-box** (027 idea). Still the
  freshest-finding candidate for 030.

## ideas generated → ideas.md

No new ideas. Closed 1 (023 prescription).

## pattern noting

The "long-waiting > recent" discipline from 019 now has
two data points:

- 019 closed 007's tutorial label (12-tick gap)
- 029 closed 023's minimalist prescription (5-tick
  gap)

Both were trivial-and-old. Both felt like "the
obvious thing that should have been done already."
The discipline doesn't replace "close freshest
findings"; it balances it. A tick with two pending-
trivial targets should favor the older one, all else
equal.

This pairs with 028's MANIFEST-addition proposal
(tick-end grep for coincidentally-closed ideas). Both
discipline tweaks push the loop toward "ideas.md
hygiene as a per-tick habit."

## suggestion for next

High-value picks:

- **`the-fixer` [code]** on widening citizen hit-box
  (027's finding, still freshest, one-line
  `input.js:72`). 11/30 fixer would be 37%, pushing
  against 020's threshold.
- **`the-fixer` [code]** on bundling Cluster F
  (narrative copy) — 021 mission-objective tone pass
  (15 strings) + 021 migration endMsg (1 string) +
  021 named characters (3 functions). Same scenarios.js
  territory as this tick. Could close 3 items.
- **`the-idle-player` [play]** — still never used.
  Tests 001's long-session idea (Cluster H).
- **`the-completionist` [play]** — never used.
- **`the-speedrunner` [play]** — never used.

Lean **`the-idle-player`** [play] — fresh persona,
breaks the 2-fixer streak (026/028 were not the-fixer
but 027/029 sandwich them), and tests a 27-iteration-
old un-ticked idea. Cluster H was specifically what
020 wanted fresh personas to target.

## commit

Single commit: `realm loop 029: close 023's minimalist
prescription — Peaceful obj 2 is now atomic`. Touches
`js/scenarios.js` only (one string).

## wake scheduling

[code] (one string, no behavior change) → 420s per
cadence.
