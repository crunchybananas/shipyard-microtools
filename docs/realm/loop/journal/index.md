# journal index

One line per iteration. Newest at bottom. Format:

```
- [NNN](NNN.md) — <summary; file hint(s)> [challenge: <name> <accepted|mutated|rejected>]
```

- [001](001.md) — genesis: loop/ scaffolding, manifest, challenges pool (34 challenges), seed ideas from LOOP_STATE known issues  [challenge: (none — scaffolding tick)]
- [002](002.md) — the-30-seconds baseline: strong first-action loop; onboarding gaps around pop-growth, placement-preview, HUD tooltip density, welcome-banner overlap  [challenge: the-30-seconds accepted]
- [003](003.md) — the-photographer: day-lighting curve tonally compressed (midday ≈ dusk); winter under-exposed; composition bones are solid; photo-mode would help next hunts  [challenge: the-photographer accepted]
- [004](004.md) — the-profiler (lighting curve): `G.debug.lightCurve()` live in state.js+main.js; curve is SEASON-INVARIANT (winter darkness is tint, not luminance); 42% of day is flat-1.0 plateau; night-floor 0.55 still the raised fix  [challenge: the-profiler accepted]
- [005](005.md) — the-art-director (lightCurve critique, mutated): 3 failure modes identified (frame lies about time, plateau shaped wrong, sky/ground uncoupled); proposes peaks at dawn/dusk t=0.22/0.78 aligned with sky gradient; acknowledges load-bearing 0.55 floor constraint  [challenge: the-art-director mutated]
- [006](006.md) — the-fixer: minimap-click idea DROPPED (bug was stale); repositioned `#toast` from viewport-center to top-5rem in index.html so welcome no longer covers starting citizens; incidentally confirmed placement-preview grid exists  [challenge: the-fixer accepted]
- [007](007.md) — the-novice: 006 fix verified in the wild (no modal over citizens); 6 new onboarding findings (scenario desc missing, tutorial progress label, persistent build selection across tutorial steps, unnamed tile dots, decorative balloon reads clickable, placement-grid low contrast); named-character hover ("Ada Pike") is a delightful beat  [challenge: the-novice accepted]
- [008](008.md) — the-fixer: surfaced scenario descriptions on landing page from existing `SCENARIOS[id].desc` via `setScenario` + `#scenario-desc`; closes 007 #1 finding; one source of truth preserved  [challenge: the-fixer accepted]
- [009](009.md) — the-fixer (mutated): placement-grid contrast; pulse alpha 0.06–0.30 → 0.14–0.42, fill #22c55e→#4ade80, added pale-green stroke at 2× pulse for edge definition; closes 007 #6  [challenge: the-fixer mutated]
- [010](010.md) — the-gamer: Hard+Military; 006 + 009 pass in the wild (danger toasts at top-center, grid visible on grass); 5 new findings including "danger toast names buildings player can't build" and "survive-5-raids increments without defenses"  [challenge: the-gamer accepted]
- [011](011.md) — the-contrarian (against 005): don't re-shape the lightCurve; 005's fix violates its own constraints, mis-diagnoses the perceptual failure, and 010 is evidence that winter-dark lives outside the curve; recommends hue-varying the multiply tint + photo-mode instead  [challenge: the-contrarian accepted]
- [012](012.md) — the-fixer (tint hue-variation): closes 011's recommendation; three-band profile in render.js (dawn rose → dusk-night lerp amber→indigo); magnitude unchanged; dusk reads warm, night reads cool, mid-scene lands near-neutral; verified numerically + visually  [challenge: the-fixer accepted]
- [013](013.md) — the-screenshot-critic: 6 frames across seasons/time-of-day; 012 holds up — winter-dusk (5b) clearly distinct from winter-midday (5a), 003 complaint resolved; weakest frame is winter-midday (no tint, bare cold tileShift); recommends tileShift tune next  [challenge: the-screenshot-critic accepted]
- [014](014.md) — the-fixer (survive-raids counter): moved unconditional increment from main.js day-tick into economy.js checkRaids, gated on existing hasDefense variable; verified in chrome-mcp — defenseless raid no longer counts (stays 0), defended raid increments (0→1); closes 010 #3 correctness bug  [challenge: the-fixer accepted]
- [015](015.md) — the-fixer (smart danger-toast): rephrased "No defenders!" hint at economy.js:383 via `isBuildingUnlocked`; three tech-state branches verified (no-techs → "Research Masonry to unlock walls", +masonry → "Build walls. Research Military for barracks/towers", full → original text); closes 010 #1 UX cliff  [challenge: the-fixer accepted]
- [016](016.md) — the-gamer (replay): all three recent fixes (012/014/015) verified in live Hard+Military play; 3 new findings (raid-flash accumulates across rapid raids, tutorial sits frozen during mass death, pop counter reads weird with skull stack)  [challenge: the-gamer accepted]
- [017](017.md) — the-fixer (mutated): halved SEASONS.winter.tileShift from [-10,-5,+15] to [-5,-3,+8] per 013 rec; winter midday now reads "dormant cool" not drained; winter dusk still warm (012 amber + new cooler tiles); retires the 6-tick lighting thread  [challenge: the-fixer mutated]
- [018](018.md) — the-screenshot-critic (capstone): 6-frame re-shoot of 013's gallery with 012+017 in place; winter-midday moved from "drained" (weakest) to "dormant cool, alive"; summer-dawn is the new weakest but arguably correctly subtle; lighting thread explicitly retired  [challenge: the-screenshot-critic accepted]
- [019](019.md) — the-fixer: originally aimed at 016 #1 raid-flash cap, re-diagnosed and dropped (code already uses `=1.0` not `+=`; the "stuck at 0.35" was a chrome-mcp hidden-tab render-pause artifact, not a real-player bug). Pivoted to 007 #2 tutorial progress label: `"2 / 10"` → `"Step 2 of 10"` at ui.js:937  [challenge: the-fixer accepted]
- [020](020.md) — the-future-you (milestone letter to iter 100): audit of persona usage (the-fixer = 7/19, 23 of 34 challenges never taken), hopes (persona variety, more reviews, photo mode, research-gate onboarding), fears (the-fixer monoculture, lighting reopened speculatively, journals get skimpy); asks iter 100 to book-end this letter  [challenge: the-future-you accepted]
- [021](021.md) — the-lore-hunter (first use): narrative surface audit. Good: all 24 buildings have desc+lore quotes, tutorial copy strong. Gaps: 12 of 24 buildings have no first-build chronicle beat (blacksmith/school/windmill/bakery/archery most felt), mission objectives read like JIRA tickets vs scenario-desc mood, migration event has empty endMsg, 5+ named characters never wired  [challenge: the-lore-hunter accepted]
- [022](022.md) — the-fixer: added 5 first-build chronicle beats (blacksmith/school/windmill/bakery/archery) in story.js:checkStoryBeats per 021's drafts; verified in chrome-mcp — all 5 fire in order on placement; closes 021's top audit finding  [challenge: the-fixer accepted]
- [023](023.md) — the-minimalist (first use): argued to remove "Research stonework, then " prefix from Peaceful scenario's obj 2 — only chained objective in the game, references a tech the engine calls "masonry" not "stonework", check function ignores half its own text; prescribes a 15-char cut that makes all 5 scenarios atomic  [challenge: the-minimalist accepted]
- [024](024.md) — the-pessimist (first use): 4 adversarial hunts against recent changes. 3 pass (flag permanence, zero-resource gate, retroactive-beats-are-just-quirks). **1 real bug**: undo after first-build chronicle fire leaves the entry + flag intact, so rebuild never re-narrates. Fix approach (a): undo-aware story-flag snapshots. Also filed future-pessimist target list  [challenge: the-pessimist accepted]
- [025](025.md) — the-fixer: closes 024's real bug. `_undoStack` entries now wrap `{b, flagsSnapshot, chronicleLen}`; `undoLastBuild` restores flags + truncates chronicle to pre-place length. Verified end-to-end with 024's exact repro — rebuild after undo now re-narrates correctly  [challenge: the-fixer accepted]
- [026](026.md) — refactor-only (first use): data-drove the 13 simple first-build chronicle beats in story.js into `BUILDING_FIRST_BEATS` table + loop. 3 side-effect beats (tavern/barracks/church with ensureMayor/Rival/Bard) stay inline. Net −33 LoC. No behavior change verified — all 20 beats fire in order, cross-dependencies preserved. Closes 020's "one refactor-only tick" hope  [challenge: refactor-only accepted]
- [027](027.md) — the-kid (first use): chaos-click pass. Findings: citizen click-radius at input.js:72 is 15px (tight vs sprite size → near-miss feel), tile decoration dots still not clickable (021 confirmed), HUD icons actually DO have `title` tooltips (corrects 007 #4 partially), dead-clicks on tiles/mountains/ambient creatures feel unresponsive. 5 new ideas incl. widen citizen hit-box, dead-click flavor beats, ambient-tap-flavor  [challenge: the-kid accepted]
- [028](028.md) — the-archivist (first use): full ideas.md audit. 63 filed, 15 closed (13 DONE + 2 DROPPED), 47 open (3.1:1 ratio, healthier than 020's projected 6:1). Closed 002 HUD audit → 027 (incidentally resolved, hadn't been marked). Sorted 47 opens into 8 themed clusters for future-tick prioritization. Proposes a MANIFEST addition: tick-end grep for coincidentally-closed ideas  [challenge: the-archivist accepted]
- [029](029.md) — the-fixer: closes 023's minimalist prescription (5 ticks pending). 15-char deletion at scenarios.js:14 — "Research stonework, then build a Church" → "Build a Church". All 3 Peaceful objectives now atomic. Applies 019's "long-waiting > recent" triage principle  [challenge: the-fixer accepted]
- [030](030.md) — the-idle-player (first use): speed=4 + 2×60s waits. Finding: chronicle IS alive during idle (10 entries in 10 game-days incl. house fire + summer transition + farmer flavor quotes). Also: chrome-mcp hidden-tab throttles heavily — game stalled between 60s checks. Environment-level, not a realm bug. 3 new ideas: welcome-back summary, extended raid countdown, persistent disaster markers  [challenge: the-idle-player accepted]
- [031](031.md) — the-fixer: closes 027's citizen hit-box finding (3 ticks pending). input.js:72 radius 15→22px. Verified via synthesized mousedown at offset 0/10/15/18/22/25 — old code hit 2 of 6, new hits 5 of 6. The "I clicked exactly on them, nothing happened" gap is closed in the 15–21px band  [challenge: the-fixer accepted]
- [032](032.md) — the-speedrunner (first use): Peaceful Valley minimum path measured. 6 buildings (3 houses + church + well + tavern) + 4 techs (masonry/commerce/engineering/brewing) = ~1300 research-ticks. At speed 4× that's ~5:25 of research wait alone; real speedrun ~7-10 min. Happiness progression: 65→70 (well) →80 (tavern). 4 friction findings incl. no "how to get happiness" signposting, no speedrun timer, research panel not shown as dep-graph  [challenge: the-speedrunner accepted]
- [033](033.md) — the-fixer: closes 021's migration endMsg (12 ticks pending). 1-line string add at events.js:64. Discovered while verifying: 021's framing undercounted — ~10 duration:0 events have empty endMsg, not just migration. Shipped migration per 021 spec; filed the broader pattern + a [review] question as follow-up  [challenge: the-fixer accepted]
- [034](034.md) — the-fixer + MANIFEST cadence shift: user steered toward builder ticks over persona plays. MANIFEST adds tick-type-mix section (60%+ code, 15-20% play, 15-20% review); supersedes 020's fixer-monoculture fear (now reframed as within-code variety). Also shipped 021 named-NPCs: ensureSmith/Merchant/Teacher + extended BUILDING_FIRST_BEATS with optional `after` field; verified 3 buildings each fire milestone + character-intro beats  [challenge: the-fixer accepted]
- [035](035.md) — the-fixer (photo mode): closes 003's 32-tick-pending photo-mode idea. H key toggles `G.photoMode` via `window.togglePhotoMode`. CSS `body.photo-mode` hides ~15 HUD elements (hud/build-bar/missions/minimap/controls/toast/feed/panels). `renderPauseOverlay` early-returns on photoMode so paused screenshots are clean. Small badge at bottom gives the exit hint. 5 files, ~30 LoC. Verified cover-art frame at 018 recipe  [challenge: the-fixer accepted]
- [036](036.md) — the-fixer (sticky raid banner + end-toast): closes 010's "raids off-screen" finding. New `#raid-banner` shows "⚔ Raid in progress — N raiders" while enemies active; extended updateRaidChronicle to fire a summary toast alongside the chronicle entry. Honors photo-mode hide. Verified end-to-end: banner during raid, toast at end ("Raid on day 37 repelled — 2 foes slain, none of ours lost."), chronicle entry  [challenge: the-fixer accepted]
- [037](037.md) — silent-module (first use): built `G.debug.tintCurve()` sampler in state.js. Pure function mirroring render.js:2515-2530 post-012 math. Returns per-sample `{t, hour, daylight, kR/G/B, tintR/G/B}`. Plumbing pays rent later — gives future lighting ticks a numeric diff capability. Also noted that `lightCurve` (004) has stale `overlayR/G/B` fields using pre-012 static coefficients; filed as follow-up  [challenge: silent-module accepted]
- [038](038.md) — the-fixer: closes 036's fresh finding. One-line gate removal in updateRaidChronicle so defenseless-raid outcomes now fire a summary ("2 fell, no foe answered. The survivors bury their own."). Existing message-picker already had the right branch; only the outer `raidsSurvived > 0` check was blocking. All 5 raid outcomes now covered  [challenge: the-fixer accepted]
- [039](039.md) — the-dream (first surprise challenge) + MANIFEST Evolution mandate: every 14 days at dawn the realm wakes to a deterministic shared dream composed from kingdom name + day + present named characters + present buildings. 8 dream threads (founding/forge/harvest/market/warning/hearth/water/learning); per-slot hashes pick threads + images; dedup by image text. Realm's first procedurally-generated narrative surface that reacts to player state. Three test kingdoms verified distinct dreams; determinism preserved  [challenge: the-dream accepted]
- [040](040.md) — the-screenshot-critic (user-flagged UX audit): research panel clips 5 of 13 techs below fold with NO scroll indicator (879px content in 496px viewport); sparse tiers (MASTERY, TIER 3) leave 300px horizontal whitespace; "TIER 3" label inconsistent with thematic neighbors. HUD at 1280 under stress (long kingdom + all counters + active research) overflows by 123px — `justify-content:center` clips Wood/Stone on left and Trade/Chronicle/3D on right SILENTLY. 6 concrete [code] follow-ups filed for next ticks  [challenge: the-screenshot-critic accepted]
- [041](041.md) — the-fixer: closes 040's HIGH HUD overflow. Three CSS changes in index.html — `#hud` gap 1.5rem→1rem, ellipsis+min-width:0 on `#kingdom-display`, min-width:0+ellipsis on `#day-display`. Verified 040's exact stress state: scrollWidth 1403→1280 (no overflow), kingdom 180→120px, date 195→125px; all 5 resources + 3 counters + research + 10 buttons fit  [challenge: the-fixer accepted]
- [042](042.md) — the-fixer: closes 040's other HIGH — research panel "more below" scroll cue. Added `_wireScrollCue` in ui.js toggling `.has-more-below` class on panel via scroll + open events; CSS gradient fade + "more below ▾" text appear when content scrollable AND not at bottom. Verified 3 states (top/bottom/back-to-top). Both 040 HIGH findings now closed  [challenge: the-fixer accepted]
- [043](043.md) — a-scene-that-happens-once (NIGHTMARE): once per realm lifetime, on a kingdom-seeded day in [50,250] at dawn, `checkNightmareBeat` replaces the ordinary dream with something darker — 11 base images + 4 character-conditional fragments (mayor/smith/bard/teacher), 3 picked via per-slot hashes and deduped, chronicled with `nightmare` tag + 🌑 danger toast. `nightmare_fired` flag prevents recurrence. Verified: seed → day 56 for TestRealm_043; 6 kingdoms → 6 unique days across full range; dayPhase>120 gate holds; flag blocks repeat  [challenge: a-scene-that-happens-once accepted]
- [044](044.md) — time-aware (dream lens): `_realWorldDreamLens()` consults real calendar before dream fires. Seasonal thread boost (winter→hearth+warning, spring→harvest+water, summer→water+market, autumn→warning+forge) only applied to threads already available in realm. 5 special-date intros override "At dawn the realm wakes" (Halloween "thinnest night", solstices, equinox, midnight witching-hour). Verified: 12-month sweep → correct buckets; 6/6 special cases fire; 40-realm Monte Carlo shows boosted threads dominate seasons (Apr: water 33/40, harvest 27/40; Dec: hearth 29/40; Oct: forge 27/40). `G.debug.dreamLens` exposed  [challenge: time-aware accepted]
- [045](045.md) — the-color-eye (first use, graphics axis): canvas pixel-sampled 5 time slices + 4 seasons at midday. HIGH finding: midday sky is season-blind — spring/summer/autumn/winter top-sky all [77-78, 97-98, 124-125], distance ≈1.7/255 = 0.7%. Ground `tileShift` (state.js:167-178) carries the only seasonal signal at midday (daylight plateau disables night-tint at render.js:2516). Root: render.js:118-121 hardcodes dayTop/dayBot without a `skyShift` lookup. MEDIUM: dusk's warm bottom-band is clipped by typical camera angle → upper 60% reads cool, dusk loses its warmth signal unless horizon visible. GOOD: 012's dusk→night hue-lerp still holds. Fix shape filed for 046  [challenge: the-color-eye accepted]
- [046](046.md) — the-silhouette-test (first use, mutated to source-read): tried live pixel-sampling; grass/non-grass classifier failed at guessed tile positions. Pivoted to reading each `draw*` function's shape primitives. HIGH: house ≈ tavern ≈ bakery share peak-roof + chimney silhouette (render.js:3080/4144/5178) — chimney width deltas of 2-3 px don't distinguish. MEDIUM: barracks ≈ blacksmith (render.js:3763/5510), both ~23-25-wide stone blocks with 2-pixel width delta. Good silhouettes: castle twin-spire (4532), church cross (4901), windmill rotating blades (5122), tradingpost pier (4352), well flat-profile (4012), granary dome (4777). Tweak filed: project tavern sign above roof line (~6 lines) — breaks the 3-way residential blur for smallest effort. `G.debug.renderBuildingIsolated` helper filed to make future silhouette audits automatable  [challenge: the-silhouette-test mutated]
- [047](047.md) — the-fixer (045 skyShift): added `SEASONS.<season>.skyShift` triples (state.js:166-180) and applied via shiftHex to `dayTop`/`dayBot` at render.js:118-135. Only the `day` blend coefficient uses shifted values; dawn/dusk keep 012's neutral hue framing. Verified fresh on port 8890 (Chrome module cache held old render.js across hard-reload): midday sky spring↔autumn distance went 1.7→12.4 (7× improvement), autumn↔winter 19.1. Dusk unchanged across all 4 seasons (confirmed 012 thread intact). Closes 045's HIGH finding. Filed cache-busting infra idea  [challenge: the-fixer accepted]
- [048](048.md) — unnamed-thing (first use in 47 ticks): added `_t048` block in enhancements.js — a small dark silhouette that traverses the map from one edge to the opposite, once per season (skipping winter) at 55% spawn chance, deterministic from kingdom+season. Module-local state (no G reference); no chronicle beat; no toast; no named character; no string literal identifying what it is. Future ticks / attentive players decide what to call it. Verified: served code has registration hooks, zero G-leaks, zero chronicle-tag leaks, 40+ tick season cycle ran error-free. Filed the reusable pattern + potential follow-ups for whoever names it  [challenge: unnamed-thing accepted]
- [049](049.md) — silent-module (dev cache-buster): added `sw.js` (13 lines) + localhost-only registration in index.html. SW forces `cache: 'no-store'` on `/js/*.js` fetches, fixing Chrome's aggressive ES-module cache that cost ~40min across 044/045/047/048. Verified end-to-end on :8891 (the stale port) — sentinel added to main.js picked up after plain reload; sentinel removed, next reload saw it gone. Hostname-gated (localhost|127.0.0.1) so production deploys see normal browser caching. No more port-bumping between graphics ticks  [challenge: silent-module accepted]
- [050](050.md) — the-future-you (halfway letter, milestone): second take of the-future-you after 020's letter-to-iter-100. Scored 020's 6 fears (4 didn't materialize, 1 partial, 1 prescient dodged-once near-miss) and 6 hopes (4 realized ✓, 1 partial, 1 open). Delta audit: 11→24 distinct challenges picked, the-fixer share 37%→40% but variety exploded, reviews 10%→14% (under target), ideas ratio 2.7:1→3:1 (no collapse), journals stayed dense, user steering produced MANIFEST updates twice (Tick-type mix + Evolution mandate). Identified 3 best post-020 ticks (026 refactor, 039 dreams, 040 UX audit, 049 SW). New hopes/fears for 100 + 3 fresh ideas filed. Asks 100 to bookend + report if the work still feels honest  [challenge: the-future-you accepted]
- [051](051.md) — the-fixer (046 tavern-sign): closes 046's HIGH silhouette blur. Added 9-line projecting roof-peak flag to `drawTavern` (render.js:4197, after signColors) — pole + 5×2.5 flag + accent band + dark outline, reusing per-tavern signColors (27's palette). Flag projects RIGHT of centerline so tavern silhouette becomes topologically asymmetric at the peak, distinct from house/bakery's centered-chimney verticals. First graphics fix to use 049's SW cache-bust: plain `location.reload()` on :8891 fetched fresh bytes, no port-bump. Couldn't pixel-verify the flag on canvas — same tooling gap 046 hit. Filed `G.debug.renderBuildingIsolated` again as HIGH  [challenge: the-fixer accepted]
- [052](052.md) — silent-module (renderBuildingIsolated): new debug helper `G.debug.renderBuildingIsolated(type, opts)` in render.js returns a small offscreen canvas with one building drawn on a known background. Closes the tooling gap 046 and 051 both hit. Verified via full 20-building profile: peakY/height/pixelCount per type. CONFIRMED 051's fix — tavern peakY 64→55 (9px delta vs house). Surprise CORRECTION to 046: bakery wasn't in the house/tavern blur cluster; its chunky chimney already put it at peakY=43, 21px above house. Filed grid/animated variants for later  [challenge: silent-module accepted]
- [053](053.md) — the-completionist (first use in 52 ticks): programmatic 155-day playthrough of a fully-built realm (all 23 buildings, all 14 techs, all 6 named chars). HIGH: 4 buildings still have no first-build beat (tower, well, chickencoop, cowpen) — 021 found 12, 022 fixed 5, these 4 remain. HIGH: mid-game chronicle thins to 1 beat every 14 days (just the dream); 039 thread pool starts repeating by 5th fire. COMPOUND WIN: 043 nightmare + 034 named chars + 044 time-aware composed cleanly at day 150 for default-kingdom "Realm" — the best single narrative moment in the run. Default kingdom quirk: every default game hits the same nightmare-day 150 with same image slots. 6 ideas filed (4 missing beats, season transitions, pool expansion, _t048 getter, end-of-realm, state-reactive dreams)  [challenge: the-completionist accepted]
- [054](054.md) — the-fixer (053 HIGH dream-pool expansion): added 2 new `_DREAM_THREADS` entries in story.js — `weather` (always-available: rain-indoors, wrong-season snow, omniscient fog) and `children` (gate: pop >= 10: footprints-up-walls, lullaby-chain, unison-first-word). 10 threads total now (was 8). Verified on :8891: 11-dream run shows repeat ratio 48%→35%, unique-image count 17→20, both new threads surface in 2/11 dreams each. Children-gate respected. Skipped the 3rd planned thread (`dead`) to avoid tonal overlap with 043's nightmare  [challenge: the-fixer accepted]
- [055](055.md) — constant-shift (first use in 54 ticks): dream cadence 14 → 10 days in `checkDreamBeat` (story.js:411-412). Honest trade-off: 36% more dream fires per 155-day realm (11→15), unique-image count 20→26, first dream arrives day 10 not 14 (new realms feel the system sooner). BUT repeat ratio went 35%→46%, clawing back most of 054's pool-expansion gains. Late-game repetition concentrated post-day-100; early/mid-game feels fresh. Filed cadence=12 compromise + staggered-cadence idea as follow-ups if feedback finds 10 too frequent  [challenge: constant-shift accepted]
- [056](056.md) — a-scene-that-happens-once (second take): added `checkStoneBeat` in story.js — once-per-realm, seeded day [30,200], a standing stone found at dawn inscribed with the kingdom's own name. Sibling to 043's NIGHTMARE but structurally distinct (dir+phrase variants, no toast, discovery tone vs disturbance, `stone` chronicle tag). 4 directions × 4 phrases = 16 prose variants. Verified 8 kingdoms: 7/8 unique days, all 4 directions + all 4 phrases surfaced, determinism + dawn-gate + no-refire all confirmed. Pure narrative — rendering filed for next graphics tick  [challenge: a-scene-that-happens-once accepted]
- [057](057.md) — the-fixer (053 HIGH): added 4 missing first-build beats to BUILDING_FIRST_BEATS at story.js:172 — tower ("eyes reach the horizon"), well ("cold sweet water meets the light"), chickencoop ("new kind of dawn chorus"), cowpen ("milk and hide from patient beasts"). Closes 021→022→053→057 audit chain. 021 found 12 missing, 022 fixed 5, 057 fixes the final 4 that genuinely deserve a beat (excluding road/wall by policy). Verified on :8891 — all 4 beats fire, all 4 flags set, tag=milestone, exact drafted text  [challenge: the-fixer accepted]
- [058](058.md) — the-fixer (render 056's stone): compound with 056's narrative beat. story.js:checkStoneBeat now stores stone_x/stone_y in storyFlags (seeded tile based on direction + offset hash, range [22,58] for map interior). enhancements.js gains _stoneDraw block via registerWorldRenderer — trapezoid menhir with dark-grey gradient, ground shadow, left-edge highlight, hidden at zoom < 0.6. Verified on :8891: 9 stone-palette pixels in tight 8×6 bbox at expected location after Avalon seed fires at day 70 on the western edge. Stone is now the second ambient anonymous surface alongside 048's _t048 (one moves, one persists). Filed snap-to-grass + winter-snow-cap + minimap-marker as polish follow-ups  [challenge: the-fixer accepted]
- [059](059.md) — chronicle-echo (surprise, new system): added `checkEchoBeat` in story.js — at dawn, ~2% chance (deterministic per kingdom+day) to resurface a memorable prior beat wrapped in one of 4 memory frames ("An elder tells a child", "Travelers at the market repeat", "The bards still sing", "Old folk remember"). Source tags: milestone/victory/character/birth/dream/nightmare/stone/raid — never misc or echo-of-echo. 14-day age gate + daily flag prevent re-fire or near-repeat. Verified: 3 echoes in 170-day run (expected 3.4), determinism confirmed, filters hold. Realm now has its first narrative-memory surface — earlier beats return wearing someone else's voice  [challenge: surprise accepted]
- [060](060.md) — the-archivist (narrative-surface audit, round checkpoint): grep'd all 16 chronicle() call sites across 5 files, mapped 13 distinct tags to 9 narrative systems. 200-day ArchivistRealm programmatic run showed tag distribution (milestone 21 / dream 20 / echo 6 / character 6 / nightmare 1 / stone 1 / victory 1). Findings: composition is clean, no tag contention, 10-day cadence produces 9-day silent stretches (by design). HIGH correctness finding: 059's _ECHO_SOURCE_TAGS misses `event` and `season` — 1-line fix. Caught 053's mis-identified "missing" season-transition idea — it DOES exist at main.js:310. Flagged real gaps: research-completion, happiness-threshold, sustained-state beats. 6 new ideas filed  [challenge: the-archivist accepted]
- [061](061.md) — the-name-giver (first use in 60 ticks): renamed `_t048` → `_wanderer` in enhancements.js (and `_t048_tick/_t048_draw/_t048_season` accordingly). Completes 048's explicit invitation "A later iteration can choose what this is." Internal-identifier-only rename — player-facing element stays silent per 048's philosophy (no chronicle beat, no toast, no tooltip, no G reference). Seed key retained "048" token so existing saves keep identical spawn decisions. Browser verification deferred (chrome-extension disconnect) but change is mechanically safe — pure rename, node --check passes, grep confirms zero dangling refs. 3 follow-ups filed (spawn chronicle beat, click-tooltip, pilgrim sibling variant)  [challenge: the-name-giver accepted]
- [062](062.md) — cut-as-you-add (first use in 61 ticks): consolidated the 3 inline first-build blocks (tavern/barracks/church at story.js:~204) into BUILDING_FIRST_BEATS via the existing `after:` hook pattern. 060 audit explicitly teed this up. Added 3 entries + 4-line comment, removed 3 × 6-line if-blocks. Net LoC −13 (requires ≤ 0 ✓). BUILDING_FIRST_BEATS is now the single source of truth for every building-triggered beat (16 entries; zero inline copies). Verified on :8891: all 3 flags set, all 6 beats fired (3 milestone + 3 character) with exact pre-refactor text, ensure* hooks via `after:` work. Filed NARRATIVE_BEATS generalization + `text:` as function + tone-audit of newly-table entries  [challenge: cut-as-you-add accepted]
- [063](063.md) — the-fixer (060 HIGH echo-tag fix, 3 ticks sitting): added `event` + `season` to `_ECHO_SOURCE_TAGS` at story.js:645. 059's origin-class-tag set missed these two legitimate memorable-beat tags (events.js cloaked-stranger/plague/rival-emissaries, main.js:310 season transitions) so they were invisible to the memory system. Verified: seeded a realm with only event+season entries, echo fired at day 109 citing a tag:event beat. Filed `death`-tag inclusion + weighted echo-source selection + long-run echo audit as follow-ups  [challenge: the-fixer accepted]
- [064](064.md) — the approach (surprise, un-filed): in the 10-day window before a once-per-realm beat fires, `_approachingEventBoost()` biases the dream pool thematically. Nightmare → warning imagery. Stone → founding+hearth imagery. Mirrors 044's boost shape. Window = 1 dream-cadence cycle so exactly one pre-event dream gets boosted. Verified: 16-kingdom sample shows nightmare-approach warn +6pp, stone-approach hearth +12pp vs control (day 60 far from events). Subtle-by-design — boost is ×2, enough for attentive players to feel the lean without telegraphing the event. Flag-kill confirmed (post-fire window returns no boost). Wires together 039/043/044/056 for the first time; the realm's narrative layers now know about each other temporally  [challenge: surprise accepted]
- [065](065.md) — the-fixer (060 HIGH, widest narrative gap): research-completion chronicle beats. Added RESEARCH_BEATS table in tech.js (11 entries for researchable techs) + chronicle(text, 'research') call at completion in updateResearch. Each tech gets an imagistic one-liner tone-matched to BUILDING_FIRST_BEATS ("The realm masters masonry. Stone now speaks in lines and courses." etc). Fallback for future techs. `research` added to `_ECHO_SOURCE_TAGS` so beats are echo-eligible. Verified: all 11 researchable techs fire correct beat with correct tag. Progression axis is now narratively surfaced; fills the silent mid-game on research-active realms  [challenge: the-fixer accepted]
- [066](066.md) — the-color-eye (second take, via 052 helper): per-building palette audit at 4 daylight levels × 20 buildings = 80 canvases sampled via `renderBuildingIsolated`. HIGH finding: the helper does NOT apply 012/017 night-tint multiply overlay. Proof: farm's palette is IDENTICAL at dawn/midday/dusk/night (same bit-for-bit counts). Castle shows no indigo shift at night. The tint lives in render()'s top-level code (render.js:2515) outside drawBuilding. Means all future tint-related audits via the helper will be silently wrong. Fix shape filed: extract tint to shared applyNightTint() function. MEDIUM: confirmed 046's tower≈barracks silhouette+palette family overlap. Good signals: tavern/castle/church/bakery distinctive at midday, per-tavern signColors variants work. 5 follow-up ideas filed  [challenge: the-color-eye accepted]
- [067](067.md) — the-fixer (066 HIGH): extracted 012/017 night-tint multiply from render()'s inline block into exported `applyNightTint(ctx, daylight, dayPhase, dayLength)` at render.js. Both render() and renderBuildingIsolated now call it; helper gets a new `opts.dayPhase` param (default midday). Math preserved bit-for-bit — main render unchanged. Verified: castle midday stays untinted (#808060/#606040/#606060), dawn/dusk/night now show distinct rose/amber/indigo shifts through the helper. 066's color-eye axis is now fully testable. Filed DAY_PHASES presets, 066 re-run, skyShift extraction, tone-audit follow-ups  [challenge: the-fixer accepted]
- [068](068.md) — the-color-eye (third take, post-tint re-run): re-ran 066's 80-canvas audit with 067's tinted helper. HIGH finding the helper was blind to is now visible: at NIGHT, 7 residential buildings (house/tavern/lumber/bakery/market/windmill/granary) produce near-identical pixel signatures `#406020:~600-800 | #204020:~350-400` — the residential cluster blurs HARDER at night than midday. Castle/church/bakery retain identity via warm native colors (`#806040` surfacing under tint). Farm is near-tint-invariant (dark earth palette barely moves across phases). Reclassifies 066's tower≈barracks finding as LOW-MEDIUM (shape wins), elevates residential-night-blur as MEDIUM. Audit-method note: pixel palettes at night measure tint dominance, not building identity — silhouette shape + window-glow (daylight<0.75 branch, not sampled here) are the real night differentiators. 4 follow-ups filed  [challenge: the-color-eye accepted]
- [069](069.md) — the-idle-player (first real-time play in 34 ticks): loaded :8891, placed house+farm, speed=4, observed 90s real time. chrome-mcp throttles hidden tabs severely: ~60s real = ~2 game days. Programmatic G.day-advance is the only workable mid-game method, confirmed. HIGH finding that NO programmatic audit could have caught: **triple chronicle fire at season change** — `event` (toast-routed) + `season` (direct main.js:310) + `misc` (elder-saying) all write on the same day for one conceptual event. 060's archivist audit missed this because its programmatic run never drove real season transitions. Also: hidden season-keyed elder-saying system (misc tag, needs 060 surface-map update). Dream-image-out-of-season is by-design. Resource economy idle-positive. 4 follow-ups filed including a 3-line HIGH fix for the season-triplicate  [challenge: the-idle-player accepted]
- [070](070.md) — the-fixer (069 HIGH season-triplicate): fixed the duplicate chronicle write at season change by passing `{ chronicle: false }` to main.js:302's `notify()` call. The opt-out mechanism already existed in notifications.js:62-64 (loops 15's unrelated "don't pollute story history" fix); 070 just uses it. Simpler than the skip-list 069 proposed. Verified on :8891: season change now fires exactly 1 chronicle row (tag:season, "Long days and warm winds...") not 2. Toast + activity feed still update. The separate `misc` elder-saying is legitimate seasonal content; 070 doesn't touch it  [challenge: the-fixer accepted]
- [071](071.md) — the-fixer (060 widest narrative gap): happiness-threshold beats in `checkStoryBeats`. Fires tag:milestone on happiness ≥ 80 (peak: "The realm is glad. Doors stand open; songs carry further than they should.") or ≤ 20 (crisis: "A grey mood settles over the realm. Doors close earlier; the bards hold their tongues."). Hysteresis via 35-65 mid-range reset band — same realm can cycle through both beats as conditions change. Population gate ≥ 5 prevents early-game volatility. Verified: 8-step trajectory (50→85→85→60→15→15→50→85) produces exactly 3 beats (peak, crisis, peak again) with correct flag resets. Auto-echo-eligible via milestone tag. Closes 060's 11-tick-old happiness gap  [challenge: the-fixer accepted]
- [072](072.md) — surprise (un-filed, founders named): new `checkFounderBeat` in story.js. On a kingdom-seeded day [3,6] at dawn, the three founders receive names from a 20-name pool (3 picks without replacement → ~7k combinatorial variants). Fires once per realm, tag:character. Names stored as storyFlags (founder1/founder2/founder3). Prose echoes the day-1 founding + 039 founding-thread images ("the fire does not go out"). Verified: 8 kingdoms produce distinct name triples on distinct seeded days; determinism + dawn-gate + no-refire all hold. Compounds with echo (059/063), could feed into nightmare (043) and founding dream thread (039) via future cross-reference work. Last surprise before was 064 (7 ticks back, at Evolution-mandate threshold)  [challenge: surprise accepted]
- [073](073.md) — the-archivist (enhancements.js chronicle audit): traced 069/070's filed advisor.js audit — not there. enhancements.js has **25 undocumented chronicle() callsites across ~12 narrative subsystems** that 060's surface map entirely missed: year milestones (year 2/3/5), rival messenger, bard compositions, mayor declarations, building-count milestones, disaster events (plague/drought/fire/earthquake), merchant arrival, victory beat v2, refugee arrival, seasonal proverbs (the "elder" 069 saw), 100-building milestone. HIGH: **5 first-build beats are DUPLICATED** between BUILDING_FIRST_BEATS (story.js, 022/057) and enhancements.js (loops 187+) — school/granary/blacksmith/windmill/archery. Different prose. BUILDING_FIRST_BEATS wins execution order → ~30 lines of dead prose in enhancements.js. Ideas.md updated with corrected 20-system map; surface-map doc filed  [challenge: the-archivist accepted]
- [074](074.md) — cut-as-you-add (073 HIGH dead-code): removed 5 duplicate first-build blocks in enhancements.js (loops 187/191/193/195/197 — firstSchool/firstGranary/firstBlacksmith/firstWindmill/firstArchery). Preserved section headers as audit-trail stubs matching the file's existing "(Stub)" convention. tradingpost block (loop 189) kept — it's NOT a duplicate. Net LoC −24 (45 removed, 21 added for stubs). Verified on :8891: all 5 flags still set + all 5 beats fire via BUILDING_FIRST_BEATS with the table's prose. No regression. Compatible with future NARRATIVE_BEATS refactor  [challenge: cut-as-you-add accepted]
- [075](075.md) — silent-module (narrative-surfaces doc): wrote `loop/docs/narrative-surfaces.md` (new file, ~230 lines) cataloging all ~25 chronicle-producing systems across story.js / main.js / tech.js / enhancements.js / combat.js / economy.js / events.js / notifications.js. Sections: why-this-exists (audit chain), 14-tag inventory, 25-system map grouped by file, execution order, 8 invariants (BUILDING_FIRST_BEATS = single source, _ECHO_SOURCE_TAGS governs echo eligibility, dawn-only gating convention, once-per-realm + hysteresis flag patterns, {chronicle: false} convention, chronicle cap = 300, entry shape), known gaps, cadence summary (~80-120 entries per 200-day realm). Prevents 053-style re-discovery and forces future ticks to update the doc when changing narrative systems. 4 follow-ups filed (CI doc-check, re-audit at 100/125, flow diagram, G.debug.chronicleSources)  [challenge: silent-module accepted]
- [076](076.md) — the-archivist (duplicate-toast audit, 070 follow-up): surveyed 19 notify() callers across js/. Found 3 HIGH duplicate-chronicle patterns (combat.js:133 raider-kill writes tag:raid AND tag:death; economy.js:337 starvation death same; economy.js:162 pop milestone writes tag:event AND story.js popCheck writes tag:milestone — duplicate at 10/25/50/75). 5 MEDIUM wrong-tag patterns (ui.js:13 food-low / economy.js:344 settler-leaves / economy.js:577 upgrade-fail / economy.js:693 building-burned / main.js:251 trade-fail all write tag:raid for non-raid events due to tagMap's danger→raid alias). 2 LOW design-choice patterns (trade-success / upgrade-success chronicle-every-action). Fix pattern is 070's `{chronicle: false}` applied per-callsite. All findings filed with concrete fix shapes  [challenge: the-archivist accepted]
- [077](077.md) — the-fixer (076 HIGH+MEDIUM cleanups): applied `{chronicle: false}` to all 8 notify() sites 076 flagged. combat.js:133 raider-kill, economy.js:337 starvation, economy.js:162 pop milestone (HIGH duplicates) + ui.js:13 food-low, economy.js:344 settler-leaves, economy.js:577 upgrade-fail, economy.js:693 building-burned, main.js:251 trade-fail (MEDIUM wrong-tag). All 1-line edits + explanatory comments. Verified 4 representative cases on :8891: raider-kill and starvation now write 1 entry (tag:death) not 2, pop-milestone writes 1 (tag:milestone) not 2, food-warning writes 0 (UI-only). Toast + activity feed unchanged. Saves ~10-20 duplicate/miscategorized chronicle entries per 200-day realm. The 070 pattern + 075 invariant fully applied across codebase  [challenge: the-fixer accepted]
- [078](078.md) — the-fixer (060 chronicle tag-filter UI, 10 ticks queued): compact chip row above chronicle entries lets player filter by tag. `TAG_ICONS` expanded to all 14 documented tags. Chips only shown for tags actually present in THIS realm's chronicle + show icon+count. Click same chip twice (or All chip) to clear filter. `_chronicleFilter` is module-local (view state, not save). First UX tick since 042's HUD overflow. Verified: 7 seeded entries across 6 tags produce 7 chips (All + 6); clicking dream filters to 1 row, clicking All resets to 7. 4 follow-up ideas filed (localStorage persist, emoji-tone review, keyboard shortcuts, tag-group meta-chips)  [challenge: the-fixer accepted]
- [079](079.md) — surprise (un-filed, offering at the stone): cross-system compound beat. `checkOfferingBeat` fires once per realm at ~15% probability on a dawn where 056 stone + 071 happiness-peak are BOTH active (post day 30). 6 offering items picked by hash. Tag: stone (reuse, per 075 anti-proliferation). First chronicle beat requiring the intersection of 3 systems to fire — emergent composition, not parallel. Verified 7 gates + 8-kingdom rate check (mean 22/170 = ~13%, expected 15%). Sample fire: "At dawn, a small blue stone the rivers have not seen was found at the standing stone. No one admits to placing it there." Auto-echo-eligible via stone tag  [challenge: surprise accepted]
- [080](080.md) — the-archivist (narrative-surfaces doc maintenance): 075 established "update doc when changing narrative in the same commit." 076/077/078/079 all changed narrative without touching doc. 080 catches up in 11 updates to loop/docs/narrative-surfaces.md — adds system #14 checkOfferingBeat, execution-order update, 2 NEW invariants (tag-reuse over proliferation + cross-system-AND gates), cadence summary updated post-077 cleanup, known-gaps relabeled as-of-080, related-loops extended to 076-080. 075 invariant honored. Demonstrates "076 audit was guided directly by 075 invariant" — the doc is paying rent as designed  [challenge: the-archivist accepted]
- [081](081.md) — the-fixer (069 filed 12 ticks: fastForward helper): new `G.debug.fastForward(days)` in main.js — synchronously advances the sim N game-days by calling simTick() in a tight loop with temporarily-boosted G.speed=60. Bypasses rAF throttling. Verified: 10-day advance in 526ms, 200-day advance in 3860ms. For default "Realm" kingdom, 200-day run fires all 3 once-per-realm beats (nightmare day 150 + stone day 176 + founders [3,6]) and produces 11 distinct chronicle tags. Chronicle hits 300-entry cap at day 211. Input validation + safety cap prevent runaway. Unblocks mid-game audits that 069's real-time play couldn't reach  [challenge: the-fixer accepted]
- [082](082.md) — the-completionist (re-take via 081 fastForward): first real use of 081's helper. 200-day advance in 2155ms, drove the REAL main-loop. HIGH: **raid-tag chronicle pollution** — 197 of 299 chronicle entries are `raid` tag but only ~8 are real raid memories. The other ~190 are UI-toast passthroughs (scouts-report/raiders-approach/RAID-spawn/no-defenders × ~40 raids). 076 classified these LOW; 082's volume data promotes to HIGH — tag:raid filter is useless. HIGH: fastForward may systematically MISS dawn-gated beats (dreams/nightmare/stone/echo/offering all 0 in a 200-day run that SHOULD have fired stone at day 131). Caught systems 053 missed (season 28, misc 29 seasonal proverbs, year milestones). Grammar bug: "enters second year with 1 souls" (singular). 4 follow-up ideas filed. 081's helper is validated end-to-end  [challenge: the-completionist accepted]
- [083](083.md) — the-fixer (082 HIGH × 2 closed in one fix): applied `{chronicle: false}` to 3 raid-warning notify callsites in economy.js (scouts-report, raiders-approach, the for-loop report). Preserved enhancements.js:4632-4640 resolution beats. Re-ran 082's exact methodology: raid went 197→35 (all 35 are real memories), chronicle went 299→156 (48% reduction). **SURPRISE: 082 HIGH #2 was closed by this fix too** — dream/stone/echo went 0→20/1/2. 082's hypothesis "fastForward skips dawn-gates" was wrong. The real mechanism: 197 noise entries were CAP-EVICTING the dawn-gated beats out of the 300-entry window. One 3-line fix, two HIGHs closed. fastForward is trustworthy after all  [challenge: the-fixer accepted]
- [084](084.md) — the-archivist (narrative-surfaces doc update for 081-083): applied 083's filed doc-update. New invariant: **noise evicts signal at the cap** — if any system writes >1 chronicle entry per game-day in steady state, it can push rare one-shot beats out of the 300-entry window. Rule of thumb filed for future chronicle-writer additions. Cadence summary revised: 75's 80-120 target updated to **130-170** (075 undercounted season+proverb recurring). New "retired hypotheses" section captures 082 HIGH #2 ("fastForward skips dawn-gates") as CLOSED-BY-EVIDENCE, preventing future re-investigation. Related-loops extended 081-084. Known-gaps relabeled as-of-084 with 2 new items. 075 invariant honored  [challenge: the-archivist accepted]
- [085](085.md) — the-fixer (083 filed defensive protection): enforces 084's noise-evicts-signal invariant at the code level. `_EVICTION_IMMUNE_TAGS` Set in story.js:17 preserves nightmare/stone/victory entries when the 300-cap kicks in — non-immune entries drop oldest-first, immune entries survive even if buffer soft-overflows. 4 tests verified: basic cap still works, single-nightmare survives 400-entry flood, all-3-immune-types survive together, soft-overflow correctly engages when all entries are immune (305 victory-only entries stayed at 305). Future noise regressions now leave visible bloat instead of silently deleting once-per-realm memory. Design constraint 084 wrote is now defended by code  [challenge: the-fixer accepted]
- [086](086.md) — the-silhouette-test (per-season via helper): 20 buildings × 4 seasons = 80 canvases. Spring=summer=autumn bit-for-bit (drawBuilding only branches on winter snow-cap). Winter adds ~112px uniform snow-cap to 14 buildings; well+granary grow peakY (79→70, 77→70) via caps drawn above their short sprites; tower gets half-size cap (61px); church cap is smaller but has cross above; farm+quarry excluded entirely (winter farm = summer farm). 046's residential-family blur PERSISTS in winter because uniform cap doesn't disambiguate. Market doubles-up (awning + cap). Filed farm-snow-dust + per-building cap variation as follow-up graphics work  [challenge: the-silhouette-test accepted]
- [087](087.md) — the-fixer (086 filed farm/quarry winter): added rhombus-shaped semi-transparent snow dusting for farm + quarry at `render.js:2974-2988`. Matches tile footprint, 0.35 alpha so sprite shows through, plus 2 frost highlights. Runs after the roof-cap block; mutually exclusive by type. Verified on :8891: farm +70 non-bg pixels in winter, quarry shows 884 pixels shifted to a lightened palette. Every building/tile that can have winter visual now does (road + wall deliberately excluded — plowed / stone). 086's "farm is season-invariant" claim is now stale  [challenge: the-fixer accepted]
- [088](088.md) — surprise (un-filed, first-snow beat): ~10-line block in story.js fires once the first time the realm enters winter past day 10. References founder1 by name if 072 has named them ("The first snow falls on the realm. Lira stands in the fields a long time before going indoors."); otherwise generic ("The settlers stand..."). Tag:milestone, auto-echo-eligible. Cross-system compound — needs 072 to have fired for the personalized variant AND the realm to have reached winter. Pairs with 087's winter visuals (farm/quarry dusting). 5 gate tests pass (day<10 blocks, non-winter blocks, generic/named variants both fire, no-refire holds). Evolution mandate reset  [challenge: surprise accepted]
- [089](089.md) — the-fixer (founder→nightmare compound, 072 filed 17 ticks): added 3 founder-conditional fragments to `_nightmareImagesForState` gated on `founders_named` flag. Pool 11→14 (founders only) or 11→18 (with named chars). Images: `${founder1}'s footprints in fresh soil, leading only away`, `${founder2} at the gate after sunset, not looking up`, `${founder3}'s face on a stone that no hand in the realm carved`. Sample fire (TestRealm_089_A): "a bell that tolls itself; and behind it, Maia's face on a stone…; and behind it, Torrin at the gate after sunset…" — 2 of 3 images founder-referencing. 5 gate tests pass (no-flag/flag-set/8-kingdom-sample/char+founder-combo/refire-block). Second consecutive founder-compound tick (088 used founder1 in narration; 089 uses all three in nightmare pool). Proves 072's founder triple is load-bearing  [challenge: the-fixer accepted]
- [090](090.md) — refactor-only (NARRATIVE_BEATS generalization, closes 062 filed 28 ticks): consolidated 8 previously-inline one-shot beats (firstBirth + 5 pop-thresholds + castleBuilt + firstRaidSurvived) into a shared `NARRATIVE_BEATS` table with `{flag, tag, trigger(G), text(string|function)}` entries. Mirrors BUILDING_FIRST_BEATS pattern. Dynamic text supports castleBuilt's mayor-or-heralds branch. Happiness peak/crisis stay inline (hysteresis reset doesn't fit one-shot model); first-snow stays inline (story-specific). Net LoC −2 (28 added, 30 removed). 9 chrome-mcp tests verify bit-for-bit behavior preservation (all 8 beats fire correctly, refire blocks, both castle fallback branches work). Variety pick: breaks the-fixer streak (5 of last 10) with a pure refactor. Closes longest-open idea in the backlog  [challenge: refactor-only accepted]
- [091](091.md) — the-archivist (narrative-surfaces.md maintenance for 085/088/089/090): 4 narrative-touching ticks shipped since 084's last update let the doc slip per 075's invariant. 091 catches up: header+sources paragraph updated through 091, story.js system list collapsed to 12 (post-090 NARRATIVE_BEATS refactor) with renumbered downstream systems, new "NARRATIVE_BEATS is the single source for state-triggered one-shot beats" invariant added, 085's eviction-immune protection promoted from "filed" to implemented (`_EVICTION_IMMUNE_TAGS` at story.js:17), 088's first-snow added to cadence summary (+1 beat), 3 new known-gaps filed (happiness/first-snow/year-milestones as NARRATIVE_BEATS migration candidates), 089's partial closure of founder→nightmare gap noted. Doc 350→414 lines (+64 net). Balances tick-type mix toward [review] (was 10%, now 11%)  [challenge: the-archivist accepted]
- [092](092.md) — the-fixer (year-milestones → NARRATIVE_BEATS + 082 grammar, 9 ticks open): migrated year2/year3/year5 beats from enhancements.js:5082 updater into story.js NARRATIVE_BEATS table. Fixed 082's "1 souls" grammar bug via text-as-function with conditional pluralization `${n === 1 ? '' : 's'}`. Trigger changed from year-math to direct `G.day >= 29/57/113`. 6 chrome-mcp tests verify: grammar singular ("1 soul") + plural ("7 souls"), year3 with kingdom name, year5 static, day=28 doesn't fire, refire-block. First cross-file use of NARRATIVE_BEATS — proves the 090 pattern's architectural reach ("story.js owns the registry, other files own the triggers expressed as predicates"). Net LoC −1 (+12 story.js, −7 enhancements.js + 6 breadcrumb comment). Closes 082 + 073 + 090#3 filed ideas in one tick  [challenge: the-fixer accepted]
- [093](093.md) — surprise (subsequent-winter beats via NARRATIVE_BEATS, founder-aging arc): 3 new entries to NARRATIVE_BEATS for the 2nd, 3rd, and 5th winters the realm experiences. 088 filed the idea; 093 adds the un-filed creative layer — founder1 ages across the four-beat arc (088's first-snow + 093's three). Arc: experience → observation → receding memory. "Lira knows what to store…" / "Lira counts them like elders, pressing each one into memory…" / "Lira remembers the first, and speaks of it less often now." Graceful fallback for unnamed-founder realms ("The realm knows…" / "The realm counts its own…" / "The oldest in the realm remember…"). Year 4 skipped by design (narrative time-compression). 7 chrome-mcp tests pass (named/unnamed/3-winter arc/summer-gate/refire-block/fresh-realm-at-day-51). Evolution mandate satisfied. Net LoC +33. Third generation of NARRATIVE_BEATS use (090 landed, 092 cross-file, 093 native). Founder1 now referenced by 088 first-snow + 089 nightmare + 093×3 = 5 surfaces  [challenge: surprise accepted]
- [094](094.md) — the-fixer (per-building winter cap variation, 086 filed 8 ticks): new `_WINTER_CAPS` table in render.js assigns distinct cap geometry (w × h × yOffset) per building-type. Residentials that 086 found bit-for-bit identical in winter (house/tavern/bakery/lumber/market/windmill/granary) now produce distinct pixel signatures: widths span 18-34px, minY span 66-75. Pixel-diff via 052 helper confirms all 11 building-types produce unique `(w, h, minY)` triples. Tower/church retain pre-094 geometry (already distinct). Farm/quarry unchanged (087 ground dusting). First graphics tick since 087 (6 narrative ticks in between) — rebalances mix toward MANIFEST's "1 in 3 should be graphics" target. Partial closure of 046's residential blur — breaks it in WINTER; non-winter silhouette blur remains. Net LoC +31  [challenge: the-fixer accepted]
- [095](095.md) — the-screenshot-critic (094's filed perceptual check): placed all 9 residentials in a winter row on :8891 via programmatic placement + photo-mode-H + 2.5x zoom. Pixel-diff from 094 confirmed dimensionally distinct, but VISUAL inspection finds 3 building-types with floating-cap issues: **windmill** (worst — ~4-6px gap between sail-cross and cap due to y=-32), **granary** (wide ellipse sits above the dome creating "two stacked domes" look, y=-26), **lumber mill** (minor gap above chimney, y=-29). House/tavern/market/bakery/blacksmith/barracks read naturally. Root cause: 094 prioritized dimensional pixel-diff over perceptual roof-conformance. Filed 095-HIGH fix: revert windmill/granary/lumber y-offsets closer to -28 baseline while keeping width variation (preserves audit distinctness, gains visual coherence). Plays were at 10% across last 10 ticks; 095 rebalances toward 15-20% target. Last screenshot-critic was 040 (55 ticks ago)  [challenge: the-screenshot-critic accepted]
- [096](096.md) — the-fixer (095 HIGH cap y-offset alignment, 1 tick feedback): 3-line `_WINTER_CAPS` fix in render.js. windmill y:-32→-28 (was floating above sails), granary y:-26→-22 + h:4→3 (was stacked-dome), lumber y:-29→-28 (was floating above chimney). Width axis preserved (18/22/26/30 for windmill/lumber/house/granary) — pixel-diff silhouette audits still produce unique widths. Chrome-mcp screenshot re-verified on same 9-residential scene as 095: all 3 problem caps now perceptually sit on their roofs; 9/9 read naturally (was 6/9 in 095). Net LoC +7 (includes 094→095→096 learning comment). Case-study in pixel-diff vs perceptual-diff: audit tools agreeing dimensionally can still miss visual defects. 5th the-fixer in 8 code ticks; committing to non-fixer for 097  [challenge: the-fixer accepted]
- [097](097.md) — surprise (founders→dream, 089 filed): added founder-conditional image variants to 3 dream threads in `_DREAM_THREADS`. **founding** gets founder1 ("${f} keeping the first fire alive, without effort or speech"), **hearth** gets founder2 ("${f} standing at a door that opens on a room they do not recognize"), **harvest** gets founder3 ("${f} counting grain by the handful, unable to remember how many handfuls"). Converted 3 array-form images to function-form (pattern already established by forge/market/learning). Graceful degradation when founders_named isn't set. 3 chrome-mcp tests pass: 100-dream multi-realm survey (founder names surface 7-13% of dreams), fallback test (0 founder-names when flag absent), sanity test (all 3 variants reach surface within 50 fires). Founder1 now canonical across 5 narrative surfaces (088 first-snow + 089 nightmare + 093 winter-3 + 097 fire-keeper). Each founder owns a thematic thread: fire/hearth/harvest. Non-fixer variety pick per 096's commitment. Net LoC +30  [challenge: surprise accepted]
- [098](098.md) — the-photographer (cover-art gallery, first-pick since 003 = 95 ticks): programmatic 10-building settlement on :8891, photo-mode, 5 frames across winter/summer × dawn/midday/dusk/night. **Frame 2 winter-dusk is the cover-art winner** — warm-cool contrast, glowing castle windows against plum-rose dusk sky, 094/096 winter caps read cleanly, moon visible. **Surprise finding: green-crop farms in winter** — 087's 0.35-alpha rhombus dusting lets the farm sprite's summer-green crops show through, breaking seasonal consistency. Filed 098-MEDIUM fix (raise alpha to 0.55-0.65) + larger winter-sprite idea. **Secondary finding: "0/1" worker labels visible in photo-mode** — 035's CSS hide-list doesn't cover canvas-drawn per-building UI labels. Filed 2-line fix. 4 follow-up ideas filed. Frame-2 recipe (winter + day 22-28 + dayPhase=0.78) documented for future cover-art regressions  [challenge: the-photographer accepted]
- [099](099.md) — the-fixer (closes both 098 findings in one tick): render.js:2981 — farm/quarry winter dust alpha 0.35 → 0.55 (lower end of 098 range; enough to make "snowy" dominant while keeping sprite readable). render.js:3120 — worker-count labels gated on `!G.photoMode` so cover-art frames don't show "0/1👤" floating above buildings. Both verified on :8891 with same 098 cover-art setup — screenshot A (photo-mode on): no labels, farms read snowy; screenshot B (photo-mode off): labels visible, normal play unchanged. Net LoC +10 (13 added, 3 removed; 2 actual code-line changes + explanatory comment blocks). Same-day-after-feedback fix pattern mirrors 095→096. Small tick on purpose — keeps tick 100 clean for milestone  [challenge: the-fixer accepted]
- [100](100.md) — the-future-you (milestone letter to 020+050+future). 3rd in the series (020 wrote forward; 050 bookmarked halfway; 100 closes the loop). **020's 6 hopes: 5/6 realized** (persona variety 11→32 distinct, reviews 10%→17%, photo-mode shipped, 3 refactor-only ticks, no FINAL.md; only "research-gate onboarding" still open). **020's 6 fears: 4 didn't materialize, 1 partial (the-fixer 40%→40% — flat, not growing), 1 prescient but dodged (fresh-context re-do twice caught at journal time).** **050's hopes: 4/5 realized** (long-session play via 053/082 + fastForward; `_t048` named to `_wanderer` in 061; dream-stack audited 060+075+080+084+091; graphics revisit 086+095+098; 1 partial = no full chrome art-director review). **050's fears: 5/6 didn't, 1 partial (named-character still decoration, not mechanic).** 5 surprises 050 couldn't predict: NARRATIVE_BEATS as portable pattern (090→092); founder-weaving as emergent arc (Lira canonical across 5 surfaces 088/089/093×3/097); pixel-diff vs perceptual-diff audit method (094/095/096); eviction-immune protection (085); fastForward industrialized mid-game audits (081). Realm at 100: 31 narrative entry-points, 14 tags, cover-art-achievable. Answering 050's "still enjoying this?" — yes; tone has range. Suggests next: named-character mechanic (50+ ticks filed). "The loop has worked."  [challenge: the-future-you accepted]
- [101](101.md) — the-fixer (teacher research-bonus mechanic, 050 filed 50+ ticks): closes the single most-called-out structural gap from 020/050/100's letters. `G.namedCharacters.teacher` adds a flat +10% to research speed, additive alongside the existing school bonus. 4-condition chrome-mcp matrix verified: 1.0x baseline / 1.1x teacher-only / 1.5x 1-school-no-teacher / 1.6x both. **First named character with a game-mechanic effect** — graduates the 034 decorative layer (mayor/smith/bard/teacher/merchant/rival) into gameplay. No chronicle beat (034 already narrates teacher arrival), no UI indicator (matches 048/058 silent-ambient philosophy). Net LoC +6 (2 code, 4 comment). Post-milestone tick, deliberately non-graphics. 5 follow-up ideas filed for remaining characters (merchant price bonus, smith combat bonus, bard happiness, mayor civic unlock, per-name variation)  [challenge: the-fixer accepted]
- [102](102.md) — the-fixer (merchant price-bonus mechanic, sibling to 101): `G.namedCharacters.merchant` multiplies trade return by 1.05 in `executeTrade`. 6-case chrome-mcp matrix across 3 partners: Albion +2 gold (40→42), Thalos +2 gold (30→32), Orisk 0 (rate-0.3 × 20 × 1.05 rounds to same integer). Math verified; bonus applies uniformly across all 4 partners. Second named character with mechanic effect — proves the 101 pattern is portable. Pattern extraction documented: `const <char>Mult = G.namedCharacters?.<char> ? 1.N : 1;`. Net LoC +5 (2 code, 3 comment). 4 follow-ups filed (smith combat, bard happiness, per-partner merchant variation, cumulative-balance review)  [challenge: the-fixer accepted]
- [103](103.md) — surprise (end-of-realm beat, 053 filed 50 ticks): closes the structural narrative gap 020/050/100 all flagged (realm origin-heavy, death-silent). New NARRATIVE_BEATS entry: flag `realm_fell`, trigger `G.day > 1 && G.population === 0`, tag `requiem` (new 15th tag, 🕯️). Text: "The last fire in the realm goes out. ${founder1}'s name is the last spoken, and then there is no one to speak it." (or generic fallback). `requiem` added to `_EVICTION_IMMUNE_TAGS` — critical defensive protection because final starvation/combat death floods would otherwise cap-evict the realm-fell beat. Completes the Lira arc at 7 narrative surfaces: 088 (fields), 089 (nightmare pool), 093×3 (winters), 097 (fire-keeper), 103 (last-spoken). "The arc closes." 6 tests pass: named/generic variants, day-1 gate, pop>0 gate, refire-block, 400-entry eviction flood. Net LoC +17  [challenge: surprise accepted]
- [104](104.md) — the-archivist (narrative-surfaces doc maintenance for 092-103): per 075 invariant, 6 narrative-touching ticks shipped since 091's update let doc slip (092/093/097/101/102/103). 104 catches up in 8 structural updates: tag inventory 14→15 (+requiem); NARRATIVE_BEATS expansion to 15 entries across 3 generations (090+092+093+103); dream thread updates for 097 founder variants; 2 new invariants captured (named-character mechanic pattern + end-of-realm beats first-class); known-gaps rewrite with 2 closures struck through (year-milestone grammar, end-of-realm beats); cadence summary adding requiem + subsequent-winter lines; related-loops section extended per-tick 092-104. Doc +76 net LoC (104 insertions, 28 deletions). Reviews rebalanced to 2/10  [challenge: the-archivist accepted]
- [105](105.md) — the-fixer (smith combat-bonus mechanic, third in 101/102 arc): `G.namedCharacters.smith` multiplies tower/barracks projectile damage by 1.05. Applied in `combat.js:updateTowers` at projectile push site. 2-case matrix verified: noSmith=10, withSmith=10.5. Half the 6-slot named-character cast (teacher/merchant/smith) now has mechanic effects; remaining (bard/mayor/rival) all filed. Pattern boilerplate by tick 3: `const <char>Mult = G.namedCharacters?.<char> ? 1.N : 1`. Net LoC +5 (2 code, 3 comment). Closes 101's filed smith idea  [challenge: the-fixer accepted]
- [106](106.md) — surprise (first audio tick in new loop protocol): nightmare sound cue. New `nightmare` case in `audio.js:playSound` plays a dissonant low chord when 043's once-per-realm nightmare beat fires. Sound design: 64 Hz + 68 Hz triangle waves (4 Hz beating/tremolo, minor-second interval), 384 Hz sine ghost-bell overtone, long attack (0.5s) + long decay (2.2s), quiet gain (0.10). Plays exactly once per realm. Added late-bound `_PLAY_SOUND` import in story.js mirroring existing `_NIGHTMARE_NOTIFY` pattern. 3 tests pass: direct playSound() no crash, checkNightmareBeat still fires chronicle+toast correctly, audio context initialization works. Un-filed (064 filed APPROACH cue, 106 is FIRE-moment counterpart). First audio tick since loop-001 began — the old 3-agent loop touched audio heavily, new protocol had been silent for 105 ticks. 043 nightmare now has sight + light-pollution + sound across 3 sensory channels. 4 follow-ups filed (other once-per-realm audio cues, live-play verify, audio-surfaces doc). Net LoC +29  [challenge: surprise accepted]
- [107](107.md) — the-accessibility-reviewer (keyboard-only audit, one of 2 un-picked challenges remaining): mapped 18 keyboard shortcuts across input.js + ui.js. **HIGH finding**: `input.js:319` `if (e.key === 'Home' || e.key === 'h')` + early `return` shadows the photo-mode handler at `input.js:388`. Photo-mode H hotkey has been **dead since 035** (~72 ticks) — badge says "Photo Mode — press H to exit" but H recenters camera instead, trapping users in photo mode. Loop's console-only toggling meant this never surfaced. 2-token fix: drop `|| e.key === 'h'`. MEDIUM findings: no keyboard building-placement path (mouse-required for core gameplay); 4/39 buttons have aria-label; B-cycle lacks visual selection indicator. 3 LOW findings (M conflict, undocumented L/Tab, inconsistent shortcut hinting). Honest conclusion: realm is mostly keyboard-accessible except placement. 6 specific fixes filed, HIGH is a 2-token 108 candidate  [challenge: the-accessibility-reviewer accepted]
- [108](108.md) — the-fixer (closes 107 HIGH same-day): dropped `|| e.key === 'h'` from `input.js:319`. H now falls through to the photo-mode handler at line 388 as originally intended. Home still recenters (standard OS convention). The "Photo Mode — press H to exit" badge is now a TRUE instruction for the first time in 73 ticks. Static code-read verification only (Chrome extension disconnected during tick); node --check passes, no other `e.key === 'h'` matches in input.js. Filed 108 [review] follow-up for live-keystroke verify when extension reconnects. Closes a 73-tick-old latent user-facing bug the loop didn't catch because 035/098/099 all toggled photo-mode via console. Net LoC +4 (2 code + 4 comment)  [challenge: the-fixer accepted]
- [109](109.md) — the-mobile-reviewer (last un-picked challenge; **34/34 pool complete**): code-read audit of CSS + touch handlers for 390×844 iPhone portrait. Touch basics work (tap place, drag pan, pinch zoom). Viewport has 2 responsive breakpoints (1400px, 600px) shrinking HUD/minimap/missions. **HIGH finding**: `user-scalable=no` in viewport meta blocks accessibility pinch-zoom — WCAG violation; 1-line fix to swap for `maximum-scale=5`. MEDIUM findings: minimap+missions panels dominate right half of 390px viewport; no scroll-cue on build-bar x-scroll; no long-press tooltip for touch users (14/39 button titles invisible without hover). LOW findings: title-screen max-width 480px may overflow; no haptic; no swipe gestures. Honest: realm is "works on mobile" not "designed for mobile." Pool coverage trajectory: 020=11, 050=24, 100=32, **109=34 (100%)**. 7 fixes filed + live-device verify follow-up  [challenge: the-mobile-reviewer accepted]
- [110](110.md) — the-fixer (a11y viewport fix, closes 109 HIGH same-day): swapped `user-scalable=no` for `maximum-scale=5` in the viewport meta at index.html:5. Restores native pinch-zoom accessibility for vision-impaired users (was a WCAG violation); caps zoom at 5× to prevent runaway layout stretch. 1-line mechanical edit (no `.js` touched, no `node --check` needed). Same-day-after-audit pattern (109→110 mirrors 095→096 and 107→108). Net LoC 0 (1 insertion, 1 deletion)  [challenge: the-fixer accepted]
- [111](111.md) — surprise (requiem bell-toll, 106-filed; Evolution mandate satisfied): new `requiem` case in `audio.js:playSound` — single sine-wave bell toll (196 Hz fundamental + 392 + 588 Hz harmonic overtones, 5s decay, pure consonance). **Inverse of 106's nightmare dissonance**: nightmare = triangle dissonant 2-tone with 4 Hz beating (unstable); requiem = sine 3-tone harmonic bell (final). Wired into NARRATIVE_BEATS dispatch loop via `if (beat.tag === 'requiem')` inline branch; reuses 106's `_PLAY_SOUND` late-bound import. Lira arc now has its ONLY audio moment at the ending — the 6 other surfaces are prose-only. 103 realm_fell beat now has prose + eviction-immunity + sound. 2nd audio cue in new loop protocol (after 106). 3 follow-ups filed (per-kingdom bell pitch, audio-surfaces.md, stone chime next). Net LoC +22  [challenge: surprise accepted]
- [112](112.md) — the-archivist (new audio-surfaces.md): sibling to narrative-surfaces.md (075). Captures 106+111's audio pattern before 3rd cue lands. ~140 lines, 8 sections: design philosophy (5 principles: silent-by-default / miss-able / once-per-realm / distinct fingerprints / no UI), 2-cue catalog with trigger + sound design + affect + code location, acoustic-contrast table (8 axes — nightmare vs requiem are deliberately opposite on every one), 2 integration patterns (dedicated check-function vs dispatch-loop inline branch; when to refactor to `onFire` callback), 7 invariants (once-per-realm only, gain ≤0.10, <6s total, no UI, late-bound imports, try/catch around calls, no inline-beat audio until migrated), 7 open-idea cues + 2 filed reviews. Balances review mix 1→3/10. Rule documented: "2 tag-specific branches is coincidence; 3 is a pattern" — prevents premature `onFire` generalization. Doc shape mirrors narrative-surfaces.md so readers of one know the other  [challenge: the-archivist accepted]
- [113](113.md) — the-fixer (stone chime, 3rd audio cue; tests 112 spec): new `stone` case in `audio.js:playSound` — ascending perfect-fifth chime (659 Hz E5 + 988 Hz B5 at +80ms + 1976 Hz B6 shimmer at +120ms). Short attack 0.01s, mid decay 1.3s, gain 0.07. Bright, consonant, discovery-coded. Contrasts 106 (dissonant low triangle) and 111 (mournful low bell) — contrast table now 3 columns spanning C2→G3→E5 (4 octaves). Pattern 1 fire at end of `checkStoneBeat` (dedicated function). Reuses 106's `_PLAY_SOUND` late-bound import. **Same-commit doc maintenance per 112 invariant** — audio-surfaces.md catalog extended to 3 cues, contrast table, open-idea struck through. Closes 106+111 filed stone idea + 112 3rd-cue-invariant-review. 3 cues now cover the emotional triangle (tension/finality/discovery). Net LoC +46 (code +20, doc +26)  [challenge: the-fixer accepted]
- [114](114.md) — **meta (challenge-pool expansion, 109 filed): 34 → 42 challenges**. Third MANIFEST-level structural tick ever (after Tick-type-mix post-033 and Evolution mandate post-038). Added 8 new challenges motivated by loop-native patterns the original pool didn't anticipate: **the-composer** (audio cue design), **the-balancer** (cumulative-mechanic play), **the-cartographer** (map/biome axis), **the-migrant** (returning-player), **the-typographer** (UI text audit), **the-interrupter** (re-engagement), **the-critic** (prose-voice drift), **the-re-shipper** (explicit anti-drift for long-open ideas, counterweight to fixer). Placed in a dedicated `### expansion (added tick 114; loop-native patterns)` section so original 34 stay visibly pool-original. Loop-native rationale per challenge documented in the journal. No code change; deliverable is `prompts/challenges.md`. Suggests `the-composer` as 115 to validate the expansion shape  [challenge: meta accepted]
- [115](115.md) — **the-composer (first use of 114 expanded pool): founders audio cue.** 4th entry in audio catalog. Three-note ascending C-minor triad (523/622/784 Hz = C5/E♭5/G5), 0.25s spacing, soft 0.05s attack each, ~0.9-1.1s decay, gain 0.06. Ceremonial register: one note per founder slot mirrors 072's "three settlers name each other" beat. Pattern 1 fire at end of `checkFounderBeat` (dedicated function). **First multi-note cue in catalog** — acoustic-contrast table gets new `Structure` row. Emotional coverage grows from triangle (tension/finality/discovery) to quadrilateral (+ceremony). Same-commit doc maintenance per 112 invariant (audio-surfaces.md catalog 3→4, contrast table, open-idea closed, related-loops). Pattern 1 count now 3 — doesn't trigger `onFire` refactor since Pattern 1 already IS the general case. Closes 106-filed founders cue + validates 114 challenge-pool expansion in practice. Net LoC +50  [challenge: the-composer accepted]
- [116](116.md) — surprise (un-filed: constellation named; Evolution mandate satisfied): new NARRATIVE_BEATS entry — realm names its own star-pattern on first autumn past day 15. Deterministic per kingdom-name hash from 20-shape pool (`the Hare`, `the Ash-Tree`, `the Plough`, etc — medieval-rural iconography, not classical-Greek). **First beat where founder2 is SOLE focal** — 100's letter flagged founder1-oversaturation (7 surfaces before 116); 116 begins rebalancing the founder arc. founder2 role: "the one who looks up." Cast now: founder1=7 surfaces, founder2=4, founder3=3. Tag: milestone (reuse per 075). Echo-eligible. Text fallback to "elders" if founders_named hasn't fired. Shape stored implicitly via hash — recoverable by any future visualizer. 4 follow-ups filed (visual render, audio cue, echo frame, play-review). Net LoC +28  [challenge: surprise accepted]
- [117](117.md) — **the-re-shipper (second use of 114 expanded pool): stone-phrase pool 4→8, closes 056 filed 61 ticks ago**. Model the-re-shipper pick: concrete spec from the filed journal, real finding (3-of-8 kingdoms shared the "barely-legible" phrase), tiny scope. 4 new phrases keep 056's invariants (kingdom name is always the text; no witness-of-carving framing) while adding new descriptive angles: moss-filled, unnaturally deep, rain-lightened, stone-remembers-being-named. Birthday-paradox math: collision rate expected to halve from ~3/8 to ~1.7/8. Tests the tick-114 new challenge: the-re-shipper vs the-fixer distinction documented — re-shipper explicitly picks 20+ tick-old ideas as quality pass, not backlog drain. Net LoC +10  [challenge: the-re-shipper accepted]
- [118](118.md) — **the-critic (third use of 114 expanded pool): prose-voice audit across ~150 chronicle strings.** Read every chronicle-producing surface (BUILDING_FIRST_BEATS, NARRATIVE_BEATS, dreams, nightmare pool, RESEARCH_BEATS, stone phrases, character intros, season transitions, etc). **HIGH drift: castleBuilt is the ONLY beat with quoted direct speech AND exclamation** ("Mayor X proclaims, 'Kingdom shall endure a thousand years!'") — shifts from narrator-observer voice to theatrical-proclamation voice. Fix draft proposed: drop quote marks + exclamation, replace with "${mayor} names it to endure a thousand years; the realm takes the name" (callback to 116 constellation's "the realm takes the name"). **LOW drifts**: pop-50 "Fifty strong!" is only non-castleBuilt exclamation; pop-75 "Other lords watch" is outside-perspective (arguably intentional for milestone visibility). **NOTES**: tavern/barracks/church beats are 9-word vs 18-word corpus average (by design: paired with 034 after-intro beats); RESEARCH_BEATS leans stronger personification than BUILDING_FIRST_BEATS (distinct register, not drift). **Both HIGH + LOW drift items isolated to pre-090 code** — post-090 prose all in voice. Most-earned: 088+093×3 Lira winter arc, 103 requiem, 071 crisis ("the bards hold their tongues"). 4 fixes filed. Voice overall strong given 150-string corpus across 100+ ticks  [challenge: the-critic accepted]
- [119](119.md) — the-fixer (closes 118 HIGH + LOW same-day): rewrote castleBuilt prose (drops `"..."` quoted dialogue + `!` exclamation) and pop-50 exclamation (1-char delete). Castle text now: `The castle stands complete. ${m?.name ?? 'The heralds'} name it to endure a thousand years; the realm takes the name.` **Post-119 corpus has zero quoted dialogue and zero exclamations across ~150 strings.** Trailing `the realm takes the name` is a deliberate callback to 116 constellation — creates a naming-ceremony motif across 2 beats (if it appears in a 3rd, promote to prose-invariant). Same-day-after-audit pattern continues. Net LoC +7 (2 code changes + 5-line explanatory comment)  [challenge: the-fixer accepted]
- [120](120.md) — **the-balancer (fourth use of 114 expanded pool): cumulative mechanic analysis**. 4/8 new challenges now used. Math-analysis of graduated-named-character-mechanic impact across teacher (+10% research speed), merchant (+5% trade), smith (+5% projectile damage). **Cumulative verdict: ~75 seconds saved on 5-min realm (~25% speedup) — forgiving but not game-breaking.** Findings: (MEDIUM) smith bonus has marginal impact due to integer HP rounding (10→10.5 damage doesn't change kill-count at most raider HPs) — consider +10% or fire-rate axis; (LOW) teacher bonus diminishes with schools scale (+10% absolute → +4% relative at 3 schools); trade bonus invisible on small trades (rate 0.3 × 20 × 1.05 still rounds to same int). Per-system breakdown: research saves ~47s on full tech tree, trade +54 resources over ~30 realm trades, combat ~28s over ~40 raids. Effects are INDEPENDENT (no emergent combo — teacher research doesn't unlock better weapons for smith). Recommends documenting mechanic-stacking rules BEFORE bard/mayor/rival land. 5 fixes/reviews filed. Live-play verification filed for extension-reconnect. Code-read + math analysis (extension still disconnected)  [challenge: the-balancer accepted]
- [121](121.md) — the-re-shipper (3rd use): migrated 088's inline first-snow beat into NARRATIVE_BEATS table. Closes 090 filed 31 ticks ago. Honors 112 audio-surfaces.md invariant ("migrate inline beats to table BEFORE adding audio"). Same trigger/text/tag/flag as inline version — pure migration, zero semantic change. Dispatch location shifts from post-loop to in-loop (imperceptible). Table grew 16→17 entries. Post-121 founder-aging arc is fully table-based: first-snow + 3 winters + requiem + constellation all dispatch through NARRATIVE_BEATS. **Discovery during migration:** Pattern 2 audio-dispatch uses `beat.tag === '<tag>'` but first-snow shares tag 'milestone' with many other beats — filed follow-up to switch Pattern 2 to flag-specific branches OR promote to `onFire` callback. Net LoC +3 (migration balances inline removal)  [challenge: the-re-shipper accepted]
- [122](122.md) — surprise (un-filed: the stone weathers; Evolution mandate satisfied): 3rd beat on 056's standing-stone arc (discovery 056 → offering 079 → weathering 122). Triggers when `stone_found` flag set AND day ≥ 150 — only long-lived realms see their stone age. 4 kingdom-hashed variants: moss fills the name, stone leans, weather-writes lines beside the name, bird nests in lee. Kingdom name still appears in all 4 (preserves 056's inscription invariant). Tag: stone (reuse per 075; eviction-immune per 085). Creates realm's longest-running object-centered narrative: 3 beats across ~100-150+ days about the same physical thing. Realm's narrative arcs now: one character (Lira, 7 beats) / one object (stone, 3 beats) / one place (sky, 1 beat). NARRATIVE_BEATS table grew 17→18. Audio cue deferred (Pattern-2 flag-vs-tag issue from 121 blocks it). Net LoC +19  [challenge: surprise accepted]
- [123](123.md) — refactor-only (Pattern-2 `onFire` field; closes 121 flag-vs-tag; 112-anticipated): added optional `onFire: 'soundName'` string field to NARRATIVE_BEATS entries. Replaces 111's inline `beat.tag === 'requiem'` audio branch. Requiem entry gains `onFire: 'requiem'` — same behavior, zero user-visible change. **Unblocks 3 filed audio cues** (first-snow/constellation/stone-weathers) that share tags with other beats. Dispatch loop now data-driven — new audio is a 1-line entry change, not dispatch-code change. String-form `onFire` covers current cases; callback-form can extend later if needed. Same-commit audio-surfaces.md update per 112 invariant — Pattern 2 spec rewritten, 121+123 entries in related-loops. 112's "2-is-coincidence, 3-is-a-pattern" rule triggered exactly at the predicted threshold. Net LoC +58 (doc-heavy; code changes minimal)  [challenge: refactor-only accepted]
- [124](124.md) — the-composer (2nd use): first-snow audio cue — **first new cue shipped under 123's `onFire` refactor**. Adds `first-snow` case in audio.js: two overlapping high-freq noise bursts (8 kHz + 6 kHz cut) + 1568 Hz sine overtone. Gain 0.04 (quietest cue). **First non-tonal cue** in catalog — breaks the pure-sine pattern of 111/113/115 because snow isn't pitched. Wired via `onFire: 'first-snow'` field on the 121-migrated NARRATIVE_BEATS entry — smallest possible audio addition post-123 (one field). Validates the refactor pays off in practice. 5 cues now; acoustic-contrast table gets 5th column with new "noise + sine" wave, non-tonal texture, whispering character. Same-commit doc maintenance per 112 invariant. Only `offering` remains from the original 106 cue list  [challenge: the-composer accepted]
- [125](125.md) — the-composer (3rd use): **offering audio cue; CLOSES original 106-filed audio list**. Minor→major resolution chord: D4 + F4 (minor 3rd) at t, F4 decays as F#4 + A4 (major triad completion) arrive at t+0.3s. 1.8s total; gain 0.05. Sibling to 106 nightmare — same minor-interval opening but RESOLVES instead of beating into unease. Pattern 1 via `checkOfferingBeat` (cross-system probabilistic gate doesn't fit NARRATIVE_BEATS). **Full original 106 list complete: nightmare / requiem / stone / founders / first-snow / offering.** 6 affects covered: tension / finality / discovery / ceremony / quiet-wonder / resolution. Keys claim distinct centers (C-minor / G-minor / E-implied / C-minor / non-tonal / D-major). Same-commit audio-surfaces.md update per 112 invariant — catalog 5→6, contrast table 6th column, open-ideas struck, milestone recorded. Net LoC +61  [challenge: the-composer accepted]
- [126](126.md) — the-interrupter (6th use of 114 expansion): code-read audit of re-engagement UX after simulated 2-min absence. **HIGH finding: no "welcome back" summary** — grep confirmed zero `visibilitychange` / `document.hidden` handlers in js/. Toasts fade 2.8s, activity-feed items gone at 9.2s, feed caps at 5 items, log badge hides 5s — 10s+ of absence loses all "just happened" signals. Player has TOOLS to catch up (chronicle L-key, activity log, missions panel) but no NUDGE — returning player often doesn't know they need to check. Confirms 030-filed welcome-back-summary idea at 96 ticks age. 5 fixes filed including 030 ship, "unread" class on feed items during absence, pause-on-blur setting. Accept-the-loss is defensible for the genre but the current shape drops too much information without cue. Plays rebalanced  [challenge: the-interrupter accepted]
- [127](127.md) — **the-fixer: welcome-back summary shipped. Closes 030 filed 97 ticks ago (possibly the oldest open idea in the backlog) + 126 HIGH same-day.** `visibilitychange` handler in main.js: on hidden, snapshots `{day, season, population, food/wood/gold, citizensBorn/Died, raidsSurvived, wallTime}`. On visible, computes deltas and composes brief summary via existing `notify()` as type:info with `chronicle:false`. Skips trivial absences (< 1 day AND < 30 wall-seconds). Example output: "While you were away: 3 days, → winter, +1 born, 1 raid survived." Accept-the-loss design preserved per 126's conclusion — game doesn't pause, summary is passive information. 50 lines in main.js. 4 follow-ups filed (unread class, pause-on-blur setting, research/event fields, live-play verify)  [challenge: the-fixer accepted]
- [128](128.md) — surprise (un-filed: the longest night; Evolution mandate satisfied): new NARRATIVE_BEATS entry firing once-per-realm deep in first winter night. Trigger: `G.season === 'winter' && G.day >= 22 && G.dayPhase > 0.85 × dayLength`. Text references founder3 as "night-watcher": `The night is longer than any the oldest will admit to remembering. ${f3} keeps watch at the fires.` **Continues 116's founder-arc rebalancing** — cast now founder1=7, founder2=4, founder3=4 (was 7/3/3 pre-116). Distinct from 088 first-snow (which fires at dawn); 128 is a DEEP-NIGHT beat — new time-of-day slot in the catalog. Tag: milestone (reuse per 075). Table grew 18→19 entries. Audio cue deferred. 4 follow-ups filed  [challenge: surprise accepted]
- [129](129.md) — the-archivist (narrative-surfaces doc catch-up 116-128): 7 narrative-touching ticks since 104's doc update had drifted past 075's same-commit invariant. 129 pays the debt. Status updated, sources paragraph extended with per-tick summary (116/121/122/123/124/125/127/128). **NARRATIVE_BEATS enumeration grew 15→19 entries** (4 new: constellation/migrated-first-snow/stone-weathers/longest-night; documents 123's new `onFire: 'soundName'` schema field). System #7 (first-snow) rewritten post-121 migration. Cadence summary extended with 3 new beat lines (constellation, longest-night, stone-weathers); budget revised 130-170 → 135-175 (+3-5 new beats per long-lived realm). Related-loops list extended 104→129 matching 075-104 rhythm. Filed idea: formalize "catch-up every ~15-20 ticks" as acceptable protocol (075 aspirational, reality is catch-up-driven). Doc +62 net LoC  [challenge: the-archivist accepted]
- [130](130.md) — the-typographer (7th use of 114 expansion): index.html font-size inventory. **26 unique font-size values across ~150 CSS rules** vs typical 6-8 tier scale. 4 obvious near-duplicate clusters: 0.60/0.62/0.65/0.67/0.68 (5 values within 13%); 0.70/0.72/0.75; 0.78/0.80/0.82/0.85; 0.88/0.90/0.92/0.95. Symptom of "author eyeballs it" after 130 additive ticks. HIGH fix filed: define `--fs-xs` through `--fs-2xl` in `:root` + migrate (~100 CSS lines, refactor-only or re-shipper). MEDIUM: low-opacity body text rgba(0.4-0.5) may fail WCAG AA ~3.2:1 contrast. NOTE: **chronicle prose typography is the BEST in the game** — 0.8rem Georgia serif, 1.45 line-height, 0.8 opacity, ~8.5:1 contrast. Most important surface got the most deliberate treatment. Title-screen scale is isolated (5rem display) and doesn't leak. No drift blocks shipping. 114 expansion now at 7/8 used (only the-cartographer remains)  [challenge: the-typographer accepted]
- [131](131.md) — the-cartographer (code-read audit of world.js map generation). **HIGH finding: map seed is TIME-based (`Date.now() % 100000`), not kingdom-seeded** — narrative is reproducible per kingdom (founder names, nightmare day, constellation) but TOPOLOGY is not. Two realms named "Avalon" a month apart share all narrative but get different islands. Fix = 1 line in startNewGame calling `setSeed(hashOfKingdomName)`. MEDIUM findings: no biome diversity (single-island radial falloff, 7 fixed tile types — every realm has "fields / western edge / eastern ridge" matching prose); river always flows east from the same offset (no variation). LOW: 20 fixed resource clusters; starting clearing always 11×11 grass (gameplay affordance). 131 corrected 130's miscount — 114 expansion is **7/8** used post-131; the-migrant still un-picked. 5 fixes filed. Single-island-always-same-shape design is cognitively-friendly but topologically-monotonous after many realms  [challenge: the-cartographer accepted]
- [132](132.md) — the-fixer (closes 131 HIGH same-day): map RNG now seeded from kingdom name. `startNewGame` reads `G.kingdomName` first, hashes via inline char-fold (same algorithm as `story.js:_dreamHash`), calls `setSeed(hash)`, then `generateWorld()`. Before 132: `_seed = Date.now() % 100000` at state.js module load — two "Avalon" realms at different times got different islands. After 132: same kingdom name → identical island shape, river path, resource clusters. **Narrative and topology now fully kingdom-coherent.** loadAndStart unaffected (save.js:111 already restores saved seed). Imported `setSeed` added to main.js's state.js import line. Inline hash rather than importing story.js's `_dreamHash` (avoids cross-module coupling for 4 lines). `|| 1` fallback handles the zero-hash edge case. Net LoC +10. 3 follow-ups filed (live-verify identity, extract `hashString` helper if 3+ consumers, kingdom gallery)  [challenge: the-fixer accepted]
- [133](133.md) — **the-migrant (8th and final use of 114 expansion — completes 8/8 coverage)**. Returning-player audit: what player sees/loses on Continue. **HIGH finding: no cross-session welcome-back** — 127's summary fires on same-session `visibilitychange` but NOT on loadAndStart; a player returning after a month gets no acknowledgment of time passed or re-orientation to the realm's last chronicle entry. Fix filed: 10-line `notify("Where we left off (day X): ...")` on loadAndStart. **MEDIUM: save doesn't record savedAt timestamp** — can't compute "your realm has been waiting 3 days." LOW: chronicle filter state (078) doesn't persist across reloads. **The tutorial is effectively invisible to migrants** (auto-dismiss via `G.gameTick > 30`) which is correct design but means zero re-orientation. 114 expansion usage audit: composer 3× (audio axis 6 cues), re-shipper 2×, others 1× each (11 total uses). Pool now 42/42 challenges ever-used across loop history  [challenge: the-migrant accepted]
- [134](134.md) — surprise (un-filed: a namesake; Evolution mandate satisfied): new NARRATIVE_BEATS entry. Once-per-realm, fires when `founders_named && citizensBorn >= 5`. A child is born bearing one of the three founders' names — which founder is **deterministic per kingdom via `_dreamHash(\`${kname}_namesake_who\`) % 3`**. Text: "A child is born in the realm and takes the name ${f}. The news carries from house to house — no one gathers, but no one stays still." Tag: character (reuse per 075). Compound extends 072 founders + firstBirth into intergenerational-continuity. Across many realms, each founder gets the namesake role ~33% of the time (roughly balances founder arc without worsening founder1's lead). Audio cue deferred. Table grew 19→20 entries. Net LoC +19  [challenge: surprise accepted]
- [135](135.md) — the-fixer (closes 133 HIGH same-day): cross-session welcome-back on `loadAndStart`. When a saved realm is continued and chronicle has entries, notify fires with the last entry's text + day — "Where we left off (day 84): The second winter comes as no surprise. Lira knows what to store..." `chronicle: false` (reading, not writing). Complements 127's within-session `visibilitychange` delta summary — 135 is ECHO (resume from save), 127 is DELTA (tab-blur within session). **Absence-coverage trilogy now complete**: 5-30s silent / 30s-10min delta (127) / cross-session echo (135). Net LoC +13. Same-day pattern continues  [challenge: the-fixer accepted]
- [136](136.md) — the-fixer (river direction per-kingdom, closes 131 MEDIUM): river now flows 1 of 4 directions (E/S/W/N), picked via 132-seeded `rng()` → deterministic per kingdom. Start offset scales `sx + 4*dxDir, sy + 4*dyDir`; meander swaps axis perpendicular to flow (horizontal flow → vertical meander). **Before 136: every realm's river flowed east** (cartographer audit finding). After: Avalon might flow south, Kestrel west, Eldrin north — cross-realm topological fingerprinting. Fisherman's hut placement now varies per-kingdom (river banks = sand in different directions). Single-river-per-realm unchanged; no new rivers, just rotated. Net LoC +9  [challenge: the-fixer accepted]
- [137](137.md) — the-composer (4th use): namesake audio cue for 134's beat. 7th entry in audio catalog. Two-note F4 → A4 ascending major-3rd intonation mimicking the rhythm of a spoken name. F4 at t (0.4s sustain), A4 at t+0.28s (1.2s sustain), brief overlap. Gain 0.045/0.050 — intimate, not broadcast. **Pattern 2 via `onFire: 'namesake'`** — 1-token addition to 134 NARRATIVE_BEATS entry validates 123's refactor (3rd Pattern-2 cue). 7-cue emotional map now complete: tension / finality / discovery / ceremony / quiet-wonder / resolution / **intergenerational-continuity**. Same-commit doc maintenance per 112 invariant (catalog 6→7, contrast table 7th column, related-loops 137 entry). Net LoC +48  [challenge: the-composer accepted]
- [138](138.md) — surprise (un-filed: the shepherd's song; Evolution mandate satisfied): new NARRATIVE_BEATS entry. Fires once-per-realm when `founders_named && firstCowpen && day ≥ 30 && (autumn || winter)`. Text: "${founder2} is heard singing at the cowpen one cold evening. The cattle stop moving, briefly, to listen." **Gives founder2 a 5th canonical surface** (shepherd-singer, adds to nightmare-pool / hearth-door / sky-watcher / founders-audio). **Founder arc is now fully balanced at 7/5/4** (was 7/3/3 pre-116, 7/4/4 post-128). No founder at double another's count. Compound extends 072 founders + 057 cowpen + seasonal state. Tag: character (reuse per 075). Table grew 20→21 entries. Net LoC +19  [challenge: surprise accepted]
- [139](139.md) — the-fixer (savedAt + real-world wait, closes 133 MEDIUM): save.js records `state.savedAt = Date.now()`; loadGame restores to `G._savedAt`; loadAndStart computes delta and adds a wait-time prefix to 135's welcome-back notify. 3 tiers: **< 1h silent / 1-24h "a few hours" / ≥ 1 day "N day(s)" with singular-plural grammar per 092 pattern.** `delete G._savedAt` after use = one-shot. Legacy saves (no savedAt field) degrade gracefully to 135's bare echo. Re-engagement trilogy (127/135) → **quintet across 5 absence-magnitudes**: silent / within-session delta / new-session-fresh / new-session-hours / new-session-days. Net LoC +24. Same-day pattern (133 → 135 → 139 closes 3/3 filed 133 items)  [challenge: the-fixer accepted]
- [140](140.md) — silent-module (type-scale tokens, phase 1 of 130 HIGH): defined 7 CSS variables in `:root` — `--fs-xs` (0.65rem), `--fs-sm` (0.75), `--fs-base` (0.82), `--fs-md` (0.92), `--fs-lg` (1.1), `--fs-xl` (1.35), `--fs-2xl` (1.8). **No migrations yet** — zero user-visible change. Recommended piecemeal migration rather than one-shot refactor (avoids "close enough" boundary breakage; lets future typography-touching ticks migrate in context). Token mapping from observed 26-unique values documented in CSS comment. Display tier (2.4rem+) left inline. Reviews infra unchanged but 141+ can now write `font-size: var(--fs-sm)` instead of `0.75rem`. Classic silent-module payoff. Net LoC +20  [challenge: silent-module accepted]
- [141](141.md) — the-archivist (narrative-surfaces catch-up 134-140): 13 ticks since 129's last update; 2 new NARRATIVE_BEATS entries needed documenting (134 namesake, 138 shepherd's song). Status line extended through 141. Sources paragraph extended with per-tick summary 134/137/138/139/140/141. **NARRATIVE_BEATS entry list 19→21.** Cadence summary adds 2 new lines (namesake, shepherd's-song — both 0-1 per realm conditional on multiple gates). Budget revised 135-175 → **137-178**. Related-loops extended 129→141 matching 20-tick rhythm. Reviews rebalanced 1/10 → 2/10. Honors 075 invariant; 141 is slightly early relative to 20-tick rhythm but catches debt cleanly. Net LoC +36  [challenge: the-archivist accepted]
- [142](142.md) — surprise (un-filed: a letter from the other realm; Evolution mandate satisfied): **first beat to reference the world OUTSIDE the island**. Gates: `firstMarket && day ≥ 60`. Text: "A traveler brings word from ${distantKingdom}: ${news}." — 10 medieval-folkloric distant-kingdom names (Norrith/Velar/Ashen Fields/etc) × 6 news fragments (grain red and bells at dusk / rivers run backward / a child who speaks only in rhymes / etc) = **60 unique realm/news pairs**. Both picked deterministically via separate hash keys (`_distant_name`, `_distant_news`) — every Avalon hears from the same distant place with the same news. Tag: `event` (reuse per 075; echo-eligible). Opens a new narrative axis: prior beats all internal-to-realm; 142 implies a wider world. Table grew 21→22 entries. Net LoC +29  [challenge: surprise accepted]
- [143](143.md) — refactor-only (first type-scale token migration proves 140): migrated 2 chronicle-panel font-sizes. `.chron-day-h` 0.75rem → `var(--fs-sm)` (exact match, zero size change); `.chron-row` 0.8rem → `var(--fs-base)` = 0.82rem (+0.02rem = +2.5%, well below ~2-4% perceptual threshold). Chose chronicle panel per 130/118 flags as highest-value reading surface. **Refactor-only** envelope held: deferred `.chron-chip` 0.7rem → 0.75rem as it's +7% (visible; outside refactor-only constraint — filed as design-choice tick). Template established: each panel migration picks exact/near-exact mappings, leaves design-shift ones for deliberate ticks. Net LoC 0 (2 inserts, 2 deletes). ~2 of ~150 font-size declarations migrated  [challenge: refactor-only accepted]
- [144](144.md) — the-fixer (namesake citizen-name override, closes 134 filed): extends NARRATIVE_BEATS schema with `after: (G) => void` callback for side effects (mirrors BUILDING_FIRST_BEATS `after:` used since 034). 134 namesake entry gets after hook: renames the newest citizen to the kingdom-hashed founder's name. Recomputes the same hash as `text()` to stay consistent. Citizen names persist across save/load (confirmed save.js:29). Before 144: 134 was theatrical-only (chronicle referenced a name not reified in game). After: a real citizen carries the founder's name, visible in tooltips. **Schema evolution snapshot**: pre-123 plain, 123 added `onFire`, 144 added `after` — narrow well-defined capabilities, not a mega-schema. Rule held: generalize when 3+ need it. `onFire` had 3 usages by 123; `after` has 1 so far. Filed: `G.storyFlags.namesake_citizen_idx` for robust future death-tracking. Net LoC +22  [challenge: the-fixer accepted]
- [145](145.md) — the-composer (5th use): shepherd's-song audio cue for 138. **8th cue in catalog.** Detuned-pair sine (392 Hz G4 + 395 Hz) produces ~3 Hz beating that reads as a sustained voice with natural wobble — same trick as 106 dissonance but much quieter/slower. Low G3 drone (196 Hz) at +0.2s adds pastoral warmth. Slow attack (0.15-0.20s) — voice fades IN rather than announces. Gain 0.045 (quieter than namesake 0.050). Pattern 2 via `onFire: 'shepherds-song'`. **First voice-emulation cue in catalog** — prior cues were bells/chimes/chords/noise/spoken-name/resolution. Detune-beat is the low-cost trick that reads as singing when quiet. 8-cue emotional topology adds **pastoral presence**. Same-commit doc maintenance per 112 invariant (catalog 7→8, contrast table 8th column with new "detune beat" / "sine + detuned" values). Net LoC +42  [challenge: the-composer accepted]
- [146](146.md) — **the-contrarian (first use since tick 011 — 135 ticks ago, longest-dormant original challenge). Argues founder-arc has become the loop's default crutch: 16 narrative surfaces across Lira/f2/f3 (7/5/4) — 75% of recent un-filed surprises lean founder-focal (116/128/134/138 did, 122/142 didn't)**. Proposes a **10-tick founder-moratorium (ticks 147-157)**: new narrative surfaces should pick OTHER focal points (characters, locations, objects, weather) unless the tick is structural or closes a pre-arc filed idea. "Rebalancing" across founders was a crutch disguising the crutch. The realm has ~20+ citizens, 6 named-characters (034), 1 wanderer, many unnamed entities — all under-weighted. 011 contrarian against 005 curve-reshape was right; 146 is 135 ticks overdue as a pattern check. **Metrics for tick-157 check-in**: ≥ 3 non-founder surprises, ≥ 2 new focal points, founder-surface increase ≤ 1. Track record: 1-for-1 on major pattern corrections  [challenge: the-contrarian accepted]
- [147](147.md) — **surprise (moratorium tick 1; non-founder: a great storm remembered)**. Un-filed; first tick of 146's founder-moratorium window. NARRATIVE_BEATS entry fires in autumn of year 2 or later. Text: "Some of the realm remember a great storm: ${storm}. The children born after do not believe it." — 5 kingdom-hashed storm-memory variants (wind through kingdom / rain overflows wells / east-storm bent trees / sky bronze-colored / lightning struck nothing). **Weather/memory as new focal** — no character references, no `G.storyFlags.founder*`, no named-character mentions. Intergenerational nod via "children born after" phrasing, not founder bookkeeping. Tag: event (reuse per 075; echo-eligible via 063). **Moratorium scorecard**: 1 of ≥ 3 non-founder surprises needed; 1 of ≥ 2 new focal points (WEATHER/MEMORY); founder-surface increase 0. Table 22→23 entries. Net LoC +22  [challenge: surprise accepted]
- [148](148.md) — **surprise (moratorium tick 2: wanderer acknowledgment)**. Un-filed. Gives 048's silent walker its first narrative surface after 87 ticks (048 introduced; 061 renamed internal; 148 acknowledges publicly without naming). Text: "There is a figure that crosses the realm from one edge to the other, some seasons. No one knows where they go. No one calls them anything." — **single static string, no variants** (048's design asserts one wanderer per realm; variants would imply plurality). Tag: misc (reuse per 075; elder-saying register). Day ≥ 50 gate — ~80%+ of realms have spotted the wanderer by then (55%-per-non-winter-season spawn × 3 seasons). **Moratorium scorecard**: 2 of ≥ 3 non-founder surprises; **≥ 2 new focal-point target ALREADY HIT** (147 weather-memory + 148 silent-walker). Completes 048's unnamed-thing arc (introduced → named internal → acknowledged without naming) across 100 ticks. Patient writing. Table 23→24  [challenge: surprise accepted]
- [149](149.md) — **the-composer (6th use): great-storm audio cue for 147's beat. 9th entry in catalog.** Low-band noise burst (400 Hz cut, 0.03 gain) + D2 sine rumble (72 Hz, 0.4s attack, 0.05 gain) + D3 octave overtone (144 Hz) at +0.3s. Slow rising attack — a storm REMEMBERED rather than present. First cue to occupy sub-bass sine register (lowest root in catalog at 72 Hz); first cue to deliberately exclude content above ~500 Hz (the absence signals temporal distance — sound-waves-over-time lose highs first). First audio for an event-tag beat — validates onFire dispatch as truly tag-agnostic. Pattern 2 via `onFire: 'great-storm'` (5th Pattern-2 cue). 9-cue emotional topology adds **distant past, remembered**. Matches 147's "children born after do not believe it" — the sound needs to read as memory, not presence. Same-commit doc maintenance per 112 invariant (catalog 8→9, contrast table 9th column, related-loops). **Moratorium tick 3** — composer doesn't add a new founder surface; scorecard unchanged (2/≥3 non-founder surprises, 2/≥2 focal points hit). Net LoC ~+43  [challenge: the-composer accepted]
- [150](150.md) — **the-re-shipper (4th use): type-scale phase 2; 130 HIGH partial close.** 140 defined tokens (phase 1); 143 migrated chronicle panel (first migration); 150 migrates title screen (3) + missions panel (2) + population panel (3) = **8 exact-match migrations** across 3 coherent panel clusters. Zero perceptual shift per migration (all source values match token targets pixel-for-pixel: 0.65 → fs-xs, 0.75 → fs-sm). Refactor-only envelope held: tightened from 143's "exact/near-exact" to EXACT-only because 3 clusters in one tick makes boundary-creep dangerous. Post-150: ~10 of ~155 declarations migrated (~6.5%); ~15 more exact-match candidates across help/trade/tech/stats/happiness panels for phase 3. **Moratorium tick 4** — refactor-only doesn't touch narrative; scorecard unchanged. Net LoC 0 (8+/8-)  [challenge: the-re-shipper accepted]
- [151](151.md) — **the-fixer: 116 constellation visual render, 35-tick-old filed idea.** First enhancements.js rendering touch since 099 (~52 ticks — graphics axis overdue per MANIFEST 1-in-3). Reactivates 016's disabled `renderConstellations` path. Replaces hand-crafted 3-constellation array with **procedural kingdom-hashed asterism**: 5-7 star points seeded from `${kname}_constellation_pts`, cached per kingdom, placed in upper 17% vertical / middle 70% horizontal sky band. **Gated on `G.storyFlags.constellation_named`** — un-named realms see blank sky; post-116 realms see their OWN asterism in the night sky. The realm has to NAME the constellation before it appears — gives 116's beat a quiet visual payoff 15+ days later. Faint connecting lines (alpha 0.15) + brighter vertex stars (alpha 0.9, radius 1.7px) over the default 180-star field. Removed 016's per-vertex cross-sparkle (unnecessary against existing twinkle field). Why procedural vs curated: 116's 20 named shapes ("the Plough", "the Hare", etc) would need 200+ lines of per-shape point data; real asterisms don't look like their names either — the pattern-recognition projection IS the name. **Moratorium tick 5** — renders existing surface, no new founder reference; scorecard unchanged. Net LoC +18 (54+/36-)  [challenge: the-fixer accepted]
- [152](152.md) — **surprise (moratorium tick 6: night-shape acknowledgment — 3rd non-founder surprise HITS moratorium target)**. Un-filed. Gives 031's ghost entity (wispy wraith spawning on grass/forest late at night, ~30% per 500 ticks, zoom ≥ 0.7 only — 121 ticks with visual presence and zero narrative) its first narrative surface. Mirrors 148's wanderer-acknowledgment pattern for a DIFFERENT ambient focal: wanderer is a DAY figure crossing edge-to-edge; 152's night-shape is a NIGHT thing seen by children. No "ghost" word in prose (142 uses "winter-ghost" but only for a DISTANT realm's news — using it for our own realm would collapse the ambiguity 031's entity deserves). Text: "There are nights when the youngest in the realm speak of a bright shape in the fields. No one asks them to describe it." **Single static string**, no variants (148 restraint carried over; ambient entity is singular by nature). Tag: misc (reuse per 075; 148 precedent). Gate: `day ≥ 60 && firstBirth` (children must exist to have seen it). **Moratorium scorecard: 3/≥3 non-founder surprises HIT; ≥2 focal points hit; 0 founder-surface increase.** 148+152 now form a **paired grammar** for acknowledging ambient entities without incorporating them into founder arcs. Table grew 24→25. Net LoC +15  [challenge: surprise accepted]
- [153](153.md) — **the-fixer (moratorium tick 7): smith mechanic damage → fire-rate, closes 120 MEDIUM filed 33 ticks ago.** 120 balance audit found that smith's +5% projectile damage hit integer-HP rounding at common raider HPs (10→10.5 damage rarely crossed kill thresholds — narrow benefit band around HP 21, 31-33, etc). 153 swaps axis: `b.fireTimer = 60 / smithBonus` instead of `damage * smithMult`. Same ~5% DPS magnitude (10/57.14 = 10.5/60) but continuous — affects every shot at every HP. Cumulative 3-char speedup budget (120: ~25%) preserved. Sibling pattern to 101/102/105 retained (`const <char>Bonus = G.namedCharacters?.<char> ? 1.05 : 1`). No UI / silent effect (101/102/105 precedent). Baseline (no smith) unchanged — `smithBonus = 1` → `fireTimer = 60` identical to pre-105. Minimal code change (1 line moved, 1 line modified, comment expanded); moratorium-clean (no narrative touch). Net LoC +7 (10+/3-)  [challenge: the-fixer accepted]
- [154](154.md) — **the-skeptic [review] (moratorium tick 8: audio axis growth without verification).** Reviews underweighted at 1/10 recent (only 146 contrarian). The audio axis shipped **9 cues across 18 audio-axis ticks (106-149) with ZERO live-auditions** — 7 open audio ideas waiting to ship, 0 completed auditions. Case: every cue is "silent-by-default, miss-able, no UI, ≤0.10 gain, ≤6s, once-per-realm" — structurally adversarial to player attention; Chrome throttles hidden tabs; laptop speakers can't reproduce 72 Hz (great-storm root). Audio-surfaces.md acoustic-contrast table proves CODE-level fingerprint distinction but NOT PERCEPTUAL distinction. 137 filed "re-audit silent-by-default philosophy" 17 ticks ago — unanswered. **Dead weight: the PROCESS of shipping new cues before auditing old ones**, not any single cue. Recommendation: halt new audio cues (151/152/128/116/149-variants stay filed) until 149 live-audition ships with real speakers + blind labels. If 3+ cues land as distinct: resume; if users conflate: reductive composer tick to merge. Steelmanned 4 counterarguments (audio-surfaces IS verification / headphone users hear more / cues are cheap / cues are insurance) — none refute. Flagged sibling over-investment axes for future the-skeptic: NARRATIVE_BEATS table (25 entries), named-character chain (3/6 graduated), type-scale series. No code change. Net LoC 0  [challenge: the-skeptic accepted]
- [155](155.md) — **the-archivist (moratorium tick 9: narrative-surfaces catch-up 142-152).** 14 ticks since 141; practical cadence has been 12-15 ticks (not 20 aspirational). Doc debt: 4 new NARRATIVE_BEATS entries (distant-letter 142 / great-storm 147 / wanderer 148 / night-shape 152) + schema evolution (144 added `after: (G)=>void` callback field) + 146 founder-moratorium status. Eight structural doc updates: status line through 155; sources paragraph extended per-tick 142-155; system #5 NARRATIVE_BEATS enumeration 21→25 with schema signature updated; two new invariants captured (**ambient-entity acknowledgment paired grammar 148+152** — misc-tag, static string, no variants, elder-silence register, no "name" word, future candidates owls/frogs/rams/trade-ships/hawks; **144 `after` schema field** — 3-or-more-need rule for schema growth justified); two known-gaps closures (named-character 3/6 graduated via 101/102/105+153, founder-arc documented at 16 surfaces 7/5/4 with moratorium scorecard HIT); cadence summary revised 137-178 → **141-182** (+4 conditional beats with gate+probability per beat); related-loops extended 141-155. Doc +195/-19 (net +176 LoC, 588→764 lines). No code change. Paid the doc debt; moratorium unchanged  [challenge: the-archivist accepted]
- [156](156.md) — **refactor-only (FINAL moratorium tick 10/10): ambient-entity grammar header comment.** Closes 155-filed pattern-grouping idea. 29-line template comment block placed above the 148 wanderer entry in `js/story.js`: restates the 6 grammar rules (misc tag / static string / no "name" word collapsing anonymity / timing gate / no founder bookkeeping / different pattern if entity has kingdom name or player interaction), references `loop/docs/narrative-surfaces.md` as the canonical spec (comment is inline summary + pointer, not duplicate), lists future candidate entities (owls/frogs/rams/trade-ships/hawks) with the 155 queue-drain warning preserved. Zero behavior change — no trigger/text/flag/onFire/after touched. **Moratorium WINDOW CLOSES with all 3 targets met** (3/≥3 non-founder surprises, ≥2 focal points, 0 founder-surface increase). 10-tick mix: 1 review + 3 surprise + 1 composer + 1 re-shipper + 2 fixers + 1 skeptic + 1 archivist + 1 refactor — healthy variety. 157 check-in tick is pre-specified per 146's proposal (milestone-style review comparing predictions to outcomes). Net LoC +29 (pure comment)  [challenge: refactor-only accepted]
- [157](157.md) — **meta (moratorium check-in: 10 ticks later).** Pre-scheduled by 146's proposal; 157 is the synthesis review. **Scorecard 3-for-3 HIT** (3/≥3 non-founder surprises, 2/≥2 new focal points, 0 founder-surface increase — under the ≤1 limit). Headline undersells: the constraint FORCED the ambient-entity grammar into existence (148+152 became a reusable pattern, not one-offs). 146 implicitly predicted the realm had non-founder narrative capacity; 147-156 delivered 4 new non-founder beats (142 distant-letter, 147 great-storm, 148 wanderer, 152 night-shape) + grammar formalization (155 invariant, 156 template comment) + schedule room for meta-analysis (154 audio-skeptic) + schema docs (144 `after` captured by 155). Counterfactual: without moratorium, ~2-3 more founder surfaces would have landed at 116/128/134/138's pace, crossing 100's saturation concern. **Founder arc unchanged at 7/5/4 across the window** — narrative fabric grew, founder threads didn't. Moratorium formally CLOSES from 158; soft-cap policy replaces hard rule: new founder surfaces must close a long-open idea OR resolve a structural gap, not just "add flavor." Milestone-check-in protocol established (propose multi-tick constraint + pre-schedule check-in tick → makes proposals falsifiable). **What's still open:** live-play verification of both wanderer + night-shape beats, and player-verified test of founder-arc saturation (compositional hypothesis remains un-proven without chrome). No code change; synthesis review only. 146+157 as a complete arc: the contrarian proposal was right  [challenge: meta accepted]
