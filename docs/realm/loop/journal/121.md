# 121 — the-re-shipper (first-snow → NARRATIVE_BEATS)

**Challenge:** the-re-shipper — accepted. **Third use of
tick-114 expansion** (after 117 stone-phrases, 120 balance).
Closes 090 filed 31 ticks ago — migrates 088's inline
first-snow beat into the NARRATIVE_BEATS table.

Honors 112's audio-surfaces.md invariant:
> No audio cue in checkStoryBeats outside the dispatch
> loop. Inline beats (happiness, first-snow) deliberately
> have no audio yet; if one gets audio, add it to the
> same tag-dispatch pattern once it can be migrated to
> NARRATIVE_BEATS.

121 does the migration now; 122+ can add the audio via
Pattern 2 tag-dispatch.

## the build

`docs/realm/js/story.js` — two changes.

### 1. New NARRATIVE_BEATS entry (above constellation, below requiem)

```js
{ flag: 'first_snow_seen', tag: 'milestone',
  trigger: G => G.season === 'winter' && G.day >= 10,
  text: G => {
    const f = G.storyFlags.founder1;
    return f
      ? `The first snow falls on the realm. ${f} stands in the fields a long time before going indoors.`
      : 'The first snow falls on the realm. The settlers stand in the fields a long time before going indoors.';
  } },
```

Same trigger predicate (`G.season === 'winter' && G.day
>= 10`), same text (including founder1 conditional),
same tag (`milestone`). Pure migration, zero semantic
change.

### 2. Removed inline block (was lines 409-420)

The 8-line inline `if` block in `checkStoryBeats` is
replaced with a 3-line breadcrumb comment pointing at the
table entry. Reads:

```js
// Loop 088 (surprise): first-snow beat migrated to NARRATIVE_BEATS
// in 121. Kept here as a sourcing breadcrumb — the beat now dispatches
// through the table loop above with flag first_snow_seen.
```

## why the breadcrumb

The inline comment's job: make `git log -p` searches for
"first-snow" find the 088 origin AND the 121 migration
point. Without the breadcrumb, anyone grepping for 088's
work in checkStoryBeats would find nothing; they'd need
to know the flag name to find it in the table.

5-line breadcrumbs are the 074 cut-as-you-add convention.

## why this migration

1. **Enables audio.** 112's invariant blocks audio from
   inline beats. Migrating first-snow makes it eligible
   for a Pattern 2 audio cue (tag-dispatch branch),
   matching requiem's existing shape.
2. **Completes NARRATIVE_BEATS coverage of founder-
   touching beats.** Post-121, the founder-aging arc is
   fully table-based:
   - first-snow (088) → now in NARRATIVE_BEATS ✓
   - second_winter_seen (093) — already in table
   - third_winter_seen (093) — already in table
   - fifth_winter_seen (093) — already in table
   - realm_fell/requiem (103) — already in table
   - constellation_named (116) — already in table
   All founder-referencing beats share one dispatch pattern.
3. **Removes one of two remaining inline dispatched
   beats** in checkStoryBeats. Happiness-peak/crisis is
   still inline due to re-fire semantics (its own
   hysteresis). First-snow never needed to stay inline;
   090 filed that at the time.
4. **the-re-shipper validation.** 090-filed was 31 ticks
   old (threshold is 20+). Concrete, low-scope, low-risk.
   Model re-shipper pick.

## verification

`node --check js/story.js` → OK.
`git diff --stat`: +15, −12. Net LoC **+3** (table entry
grew vs inline because of the explanatory comment block,
but the body is functionally equivalent).

Static behavior-equivalence check:
- Trigger condition identical: `G.season === 'winter' &&
  G.day >= 10`
- Text branches identical (named + generic)
- Flag identical: `first_snow_seen`
- Tag identical: `milestone`
- Dispatch location: previously fires after NARRATIVE_BEATS
  loop → now fires DURING NARRATIVE_BEATS loop. Ordering
  shift is ~1 tick worth of dispatch latency within
  `checkStoryBeats`; no observable player impact.

Chrome extension still disconnected; filed 121 [review]
for live-verify when it reconnects.

## what this closes

- **090 [code]** (filed 31 ticks) — "migrate 088's
  first-snow into NARRATIVE_BEATS. Trivial once pattern
  exists. ~3 lines." Shipped at +3 net LoC including
  comment (vs 090's "~3 lines" projection — aligned).
- **112 audio-surfaces invariant note** — first-snow
  unblocked for future audio addition.
- **NARRATIVE_BEATS table grew to 16 entries** (was 15
  post-103). Constellation 116 was 16th; 121's first-
  snow migration is... wait, 116 was added as 16th,
  121 brings it to 17. Let me recount: 090 landed 8
  (firstBirth + 5 pop + castle + raid) + 092 added 3
  year-milestones = 11, + 093 added 3 subsequent-
  winters = 14, + 103 added realm_fell = 15, + 116
  added constellation = 16, + 121 adds first-snow = 17.

## what I did NOT do

- **Add the first-snow audio cue.** Separate tick (122+
  the-composer). 121 purely refactor-shape; audio is
  a new feature on top.
- **Migrate happiness-peak/crisis.** That's 090's other
  filed idea with an `onFire` or `resetOn` callback.
  Hysteresis semantics don't fit one-shot NARRATIVE_BEATS
  without schema extension. Filed; deferred.
- **Update audio-surfaces.md.** No new cue shipped; no
  catalog entry changes. But the invariant text could
  be updated to remove "(happiness, first-snow)"
  examples since first-snow is no longer inline. Minor
  doc drift; filed as follow-up.
- **Update narrative-surfaces.md.** 104 already lists
  first-snow as NARRATIVE_BEATS system #5 (anticipating
  this migration). Doc was correct.

## ideas generated → ideas.md

- **121 [doc]** — update audio-surfaces.md invariant
  that said "(happiness, first-snow) deliberately have
  no audio yet." Post-121, only happiness remains
  inline; first-snow is now NARRATIVE_BEATS-dispatched.
  [loop/docs/audio-surfaces.md]
- **121 [code]** — migrate happiness peak/crisis beats
  into NARRATIVE_BEATS with a `resetOn: (G) => bool`
  field that clears the flag. Would complete the table-
  based dispatch of all one-shot + hysteresis beats.
  ~15 lines. [story.js]
- **121 [code]** — first-snow audio cue (115 filed,
  still open). Pattern 2 dispatch-loop branch for tag
  ='milestone'... WAIT — first-snow shares the tag
  'milestone' with many beats. Need to distinguish by
  FLAG not tag for Pattern 2. This breaks Pattern 2's
  tag-dispatch assumption. Will need a new sub-pattern
  (flag-specific branch) or promotion to the filed
  `onFire` callback. Flagging for audio-surfaces.md. [audio.js + story.js]

## suggestion for next

Recent mix (112-121):
- the-fixer: 2/10
- Review: 3/10
- Play: 1/10 (still under)
- Surprise: 1/10
- Meta/specialized: 5/10

Good options:

- **First-snow audio cue** — now technically unblocked
  BUT discovered flag-vs-tag problem above. Needs
  Pattern 3 (flag-specific branch) or `onFire` callback.
- **Offering cue** — composer; uses Pattern 1 via
  checkOfferingBeat. Closes last of 106 audio list.
- **Un-filed surprise** — Evolution mandate due (last
  surprise 116, 5 ticks back at 121).
- **the-interrupter** / **the-typographer** / **the-
  cartographer** — untested 114-expansion slots.

Lean **un-filed surprise** (Evolution mandate hit) OR
**offering cue** (composer again, avoid the first-snow
flag-vs-tag complication). Both work.

## commit

Single commit: `realm loop 121: the-re-shipper — first-
snow migrated to NARRATIVE_BEATS (closes 090 filed 31
ticks; unblocks audio for 122+)`. Touches `js/story.js`
only. Net LoC +3.

## wake scheduling

[code] (tiny migration, closes 31-tick-old filed idea,
unblocks audio-surfaces invariant for future ticks) →
420s per cadence.
