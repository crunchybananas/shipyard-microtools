# 218 — Phase B step 3 (winter cap composition verified)

**Challenge:** the-fixer (Phase B continuation).
Per 217 hypothesis: step 3 (winter cap
composition on SVG path) should be FREE because
217's structural change moved the SVG path
INSIDE the scale/translate envelope, putting
all downstream composition (damage cracks,
winter caps, hover halo) AFTER the dispatch
restore.

**Verified.** Step 3 ships as a verify-only
tick — no code change to render.js needed.

Chrome online via Playwright headless.

## the build

Single file change: `docs/realm/scripts/
verify-phaseb.mjs` — added a 4-screenshot
winter mode block:

1. After the existing flag-on/off/back tests,
   set `G.season = 'winter'` (canvas path).
2. Screenshot: `phaseb-winter-canvas.png`.
3. Toggle SVG on (winter still active).
4. Screenshot: `phaseb-winter-svg.png`.

The caps should render on BOTH paths if step 217's
structural change worked. They do.

## verification

Both screenshots show buildings with white snow
caps on top:
- **winter-canvas.png**: canvas-drawn buildings
  with `_WINTER_CAPS` overlay caps (per 094
  per-type geometry table; granary uses 158's
  dome-conforming arc).
- **winter-svg.png**: SVG sprites (simpler
  silhouettes) with the SAME `_WINTER_CAPS`
  overlay composing on top. Caps are MORE
  visible on SVG because the SVG silhouettes
  are simpler/cleaner.

Toggle clean both directions; zero page errors.
Sprite cache shows all 11 ready.

The composition order in `drawBuilding` is:
1. Foundation diamond + AO ring + shadow
2. ctx.save() + 1.1× scale + +3y translate
3. Dispatch — SVG sprite OR canvas drawX
4. ctx.restore()
5. Damage cracks (composes on either)
6. Winter cap (composes on either) ✓ verified

## what this closes

- **216 [code] step 3** — winter cap
  composition. Free win from 217's structural
  change.
- **209 task** (internal) — Phase B step 3
  verification.

## what I did NOT do

- **Touch render.js.** Step 3 was a hypothesis
  test, not a code ship. Result: hypothesis
  confirmed; no code needed.
- **Per-cap geometry tuning for SVG.**
  `_WINTER_CAPS` was tuned for canvas drawX
  building heights/widths; on SVG sprites
  (which have slightly different proportions
  per `_SPRITE_SIZES` table) the caps may sit
  ~1-2 pixels off-position. Acceptable —
  visually it reads as snow on roofs in both
  paths. If a per-path cap-geometry tuning is
  needed later, file then.
- **Granary dome-conforming arc on SVG.** The
  158 dome-conforming arc fix targeted canvas
  drawGranary's specific dome geometry. On
  the SVG granary the dome arc may not align
  perfectly with the rendered dome top.
  Visually acceptable; filed for future tune.
- **Push past step 3 in this tick.** One step
  per tick keeps the verify-batch tight.

## ideas generated → ideas.md

- **218 [code]** — `_WINTER_CAPS` per-path
  tuning: cap geometry (w/h/y) was tuned for
  canvas drawX. SVG sprite proportions differ
  per `_SPRITE_SIZES` table. Per-path cap
  table could be added. ~10 LoC. Defer until
  visual review flags it. [render.js]
- **218 [code]** — granary dome-conforming
  arc (158) targets canvas-specific geometry.
  On SVG granary the arc may not align
  perfectly. ~5 LoC tune to match SVG dome
  position. [render.js]

## suggestion for next

219 = **Phase B step 4 (hover halo + fog
modulation on SVG path)**. Same hypothesis as
217→218: 217's structural change should make
this free. Verify by hovering a test building
in the script-driven mock.

Hover halo lives in `render.js` near the
selected-building pulse logic (loop 75 + 188);
fog-of-war is its own pass. Both should
compose on SVG by virtue of running AFTER the
ctx.save/restore envelope.

If clean: ship 219 as another verify-only
tick + screenshot; move to step 5
(per-instance variation).

## commit

`realm loop 218: Phase B step 3 — winter cap
composition verified on SVG path (no code
change needed; 217's structural fix moved SVG
path INSIDE the scale/translate envelope so
caps + damage compose after restore on either
path; verify-phaseb extended with 2 winter
screenshots; both paths show caps correctly)`.
Touches scripts/verify-phaseb.mjs only.

## wake scheduling

[review] (Phase B step 3 verify; no code) → 300s.
