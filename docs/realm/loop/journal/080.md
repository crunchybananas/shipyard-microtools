# 080 — the-archivist (narrative-surfaces doc maintenance)

**Challenge:** `the-archivist` — accepted. 075
established "future ticks that add or remove a
chronicle-producing system MUST update this doc in
the same commit." 076/077/078/079 all changed
narrative surfaces without touching the doc. 080
catches it up before the doc rots further.

No letter this tick. 080 is a round number but not
a milestone worth manufacturing ceremony for —
050 did the halfway letter, 100 will bookend.
Between them the loop should stay operational.

## the updates

`docs/realm/loop/docs/narrative-surfaces.md`:

1. **Status line updated** — now lists 075 origin
   + 076/077/078/079/080 as maintainers.
2. **Source line** — added 076/077 audit+fix,
   078 tag-filter UI, 079 cross-system offering.
3. **story.js system #14 added** —
   `checkOfferingBeat` (079), first beat requiring
   a multi-flag intersection gate.
4. **Collapsed count corrected** — story.js owns
   13 systems now (was 12; +offering).
5. **Execution order section** — added
   checkOfferingBeat to the checkStoryBeats
   pipeline listing.
6. **Invariants expanded** — two NEW invariants:
   - **Tag reuse over proliferation** — 075
     warned; 079 respected. Rule: if a new beat's
     subject is already a tag, reuse. The offering
     beat reuses `stone`.
   - **Cross-system beats AND multiple flags** —
     079's pattern. Treat multi-flag gates like a
     sentence.
7. **Dawn-only list updated** — added
   `checkOfferingBeat` to the list of functions
   guarding `G.dayPhase > 120`.
8. **Once-per-realm flags list** — added
   `offering_made` to the known-flag inventory.
9. **Cadence summary updated** — 077 cleanup
   reduced duplicates (~10-20 entries per realm);
   079 adds 0-1 per realm. Range `~80-120 entries`
   unchanged, but quality per entry improved.
10. **Known gaps section relabeled** "as of 080"
    (was "as of 075"); added the 076 LOW design-
    choice sites that didn't get fixed in 077.
11. **Related loops** — added 076/077/078/079/080
    with one-line descriptions each.

## what this doc now captures that it didn't at 075

- The TAG-REUSE invariant (079 respected; 080
  codifies)
- The CROSS-SYSTEM-GATE pattern (079 introduced)
- An accurate cadence-summary reflecting 077's
  duplicate removal
- Updated known-gaps list reflecting what 077
  closed + what 076 left as LOW design calls
- The tag-filter UI (078) exists; referenced in
  the `related loops` section

## what I did NOT change

- **System count/taxonomy**. Still ~25-26 systems
  depending on how you count the offering (is it
  its own system or a sub-variant of 056 stone?
  Doc says its own, story.js treats it that way).
- **Echo source list**. 065 added `research`, 063
  added `event`+`season`. The invariant text
  already listed all three. Good.
- **File-by-file section counts in
  "final count"**. The original text has a
  "collapsed count" note that's still accurate.
  Didn't touch; the recount itself is the
  numerical source of truth.
- **Tone of the doc**. The original 075 structure
  is sound. 080 adds content without rewriting
  voice.

## why this doc is already paying rent

076 (duplicate-toast audit) and 077 (duplicate
fixes) wouldn't have landed without 075's invariant
"notify+chronicle at same callsite → pass
`{chronicle: false}`". That sentence DIRECTLY
guided 076's audit criteria and 077's fix
pattern. Before 075, 069 stumbled on the pattern
by accident. After 075, 076 systematically found
it. That's the doc working as designed.

## meta: the "keep it accurate or retire it" rule

075 said: "Keep it accurate or retire it — a
lying doc is worse than none." 080 honors that
by spending a tick catching the doc up.

Going forward: expect tick 100 (or 125) to do
another maintenance pulse. The rule should be: if
you find yourself re-reading the doc and seeing
something that doesn't match the code, fix the
doc in the same commit as you confirm the
mismatch.

## ideas generated → ideas.md

- **080 [review]** — programmatic doc-check script
  (075 filed a similar idea). Node script that
  grep's for known patterns (e.g. `chronicle(`
  call sites, `_ECHO_SOURCE_TAGS` Set entries)
  and compares to claims in the doc. Fails CI on
  drift. [scripts/ or ci]
- **080 [code]** — `G.debug.chronicleSources()`
  live helper (previously 075 filed) that returns
  tag→count and file→count tables from the
  current save. Quick verification without grep.
  [main.js]
- **080 [review]** — doc-version check: add a
  `_DOC_VERSION` constant in story.js that the
  doc references; future ticks that touch narrative
  must bump the version and update the doc, or
  the CI check flags it. [story.js + doc]

## suggestion for next

- **fastForward helper** (069 filed 11 ticks) —
  longest-open infra idea. Single-file, high-
  leverage for future real-time ticks.
- **NARRATIVE_BEATS refactor** (062 filed 18
  ticks) — deeper structural work, now that the
  doc exists.
- **Variant texts for 071 happiness beats** —
  small content win.
- **Disaster-beat relocation** (073) — refactor.
- **Founder→nightmare compound** (072) —
  narrative extension.

Lean **fastForward helper**. It's the longest-
waiting idea that pays broad rent. With the doc
updated, future real-time plays (which will benefit
from fastForward) can reference this doc to know
what to observe.

## commit

Single commit: `realm loop 080: the-archivist —
narrative-surfaces doc maintenance`. Touches
`loop/docs/narrative-surfaces.md` only.

## wake scheduling

[review] (pure doc maintenance, no code changes,
honors 075's update-in-same-commit invariant for
a batch of deferred updates) → 300s per cadence.
