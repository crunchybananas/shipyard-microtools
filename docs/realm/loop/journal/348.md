# 348 — surprise (present_forgetting_known — single-axis: 7th forgetting; PRESENT-FORGETTING-AS-EMERGING-GAP; first sub-type at 7; user-flagged visual debt)

**Challenge:** un-filed surprise — accepted.

verify-logic 76/76.

## the build (abbreviated; user interrupted with visual debt question)

`docs/realm/js/story.js` — single new beat
inserted after avoided_corner_known (347):

```js
{ flag: 'present_forgetting_known', tag: 'misc',
  trigger: G => G.storyFlags.year3 && G.day >= 160,
  text: 'The realm is forgetting something at this moment. No one knows what. By the time someone says "we used to," the something will be the gap.' },
```

`verify-logic.mjs` test #76 added.

`narrative-surfaces.md` forgetting annotation
extended to 7 shapes; **temporal axis
advances 2-position → 3-position**
(PAST/PRESENT/FUTURE).

NARRATIVE_BEATS table 85→86 entries.

## why this matters

**Forgetting sub-type 6→7 shapes** —
**SOLE-LEADER POST-348** (LA/Am/II at 6).
First sub-type to reach 7 shapes.

**ANGLE**: PRESENT-FORGETTING-AS-EMERGING-
GAP — forgetting happening RIGHT NOW;
meta-aware mid-process; the realm KNOWS
something is being lost without being able
to NAME what.

**Forgetting temporal axis** advances:
- PAST: 184/190/292/301/305 (5 shapes)
- PRESENT: 348 (1 shape, NEW POSITION)
- FUTURE: 336 (1 shape)

3-position 1D axis. Sub-type-internal-axes
meta-pattern unchanged at 7/9 (FG axis was
already counted; richer not new).

## user-flagged visual debt

User asked at this tick about house icons
appearing super small + whether I'm still
visually inspecting the game.

**Honest status**: NOT visually inspecting
recent ticks. [play] ticks (310/320/326/335)
have been BEAT-FIRING + DENSITY-BUCKET
verification, not VISUAL inspection. Recent
ship-arc 339-348 has been prose-only (no
visual work).

Per memory `feedback_realm_loop_visual_
verification.md`: "realm loop must try
chrome-mcp every tick that ships visual
work; surface visual-debt explicitly when
offline." Chrome-mcp bridge has been
broken since tick 215 (long-running).
Playwright is the substitute but I've been
using it for logic-only checks.

**House icons being small** — likely a
render.js + _SPRITE_SIZES issue. The
house entry at render.js:216:
```
house: { w: 42, h: 48 }
```
Compared to townhall at w:54 / castle at
w:54 / market at w:50. House at w:42 is
the smallest user-buildable; only well
(w:36) is smaller. This may have always
been the case but visual recently is
probably worse if zoom levels changed or
sprite-rendering shifted.

Filed as 348 [code] visual-debt audit
candidate; should run a Playwright zoom-
6 visual capture comparing house against
townhall to confirm.

## abbreviated commit + handoff

The 348 ship is committed minimally.
Detailed journal continuation deferred so
I can address the user's visual question.

Net: story.js +50 / verify +14 / doc +20.

## wake scheduling

[surprise] (interrupted at 348; visual
debt audit needed) → 240s.
