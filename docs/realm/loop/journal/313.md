# 313 — the-fixer (309 [process] gate-spreading: y3 cluster 8→5 entries; 37% density reduction)

**Challenge:** the-fixer — accepted.
Per 312 plan: most-actionable cluster
finding from 309/310 [play]; ~30 LoC
contained scope.

verify-logic 51/51 holding (4 tests
updated to set G.day satisfying new
spread).

## the build

`docs/realm/js/story.js` — 5 trigger
updates with day-offset gate-spread:

| flag | y3 setDay | new gate | offset |
|------|-----------|----------|--------|
| 285 phrase_misheard | 57 | year3 + day>=60 | +3 |
| 303 wagon_track | 57 | year3 + day>=65 | +8 |
| 307 path_knows_routine | 57 | year3 + day>=70 | +13 |
| 312 tacit_norms | 57 | year3 + day>=75 | +18 |
| 297 unplaceable_sound | y2 d29 | year2 + day>=32 | +3 |

Net: 5 single-line trigger changes + 5
1-line comments noting "313 gate-spread"
+ 4 test updates setting G.day. ~22 LoC
total.

## scope discipline (what NOT spread)

Per 312 reasoning: only spread beats with
NO design-meaningful conditional gates.
Beats with meaningful conditions (church
present, dayPhase>0.6, happiness>65,
season=autumn, citizensDied>=1, etc.)
have their cluster as INCIDENTAL — the
condition naturally varies. Spreading
those would override design intent.

**Beats NOT touched** (have meaningful
conditional gates):
- 211 sustained_peace (raidsSurvived
  baseline)
- 227 well_remembers (well present)
- 228 no_death (deaths counter)
- 229 hearth_holds_names (citizensDied)
- 230 full_pop (population ratio)
- 246 first_thaw (year2 + spring d>=32 —
  already day-gated)
- 247 teacher_pauses (teacher present)
- 254 nights_blur (autumn/winter)
- 280 liminal_moment (dayPhase>0.6)
- 290 sea_bell (church present)
- 292 bakery_carving (bakery present)
- 294 church_step (church present)
- 296 realm_laughs (happiness>65)
- 301 noon_bell (church present)
- 305 silent_morning (everHadBuilding.
  church per 311)

These keep their natural cluster behavior.
They cluster only WHEN their conditions
are ALSO met at year-transition; the
density there reflects design.

## why this matters

**Empirical impact verified via [play]:**
re-ran the 310 [play] script post-fix:

```
y3 transition d55-70:
  d57: 5 entries  (was 8 — 37% reduction)
  d59: 2 entries
  d60: 3 entries
  d64: 2 entries
  d65: 2 entries
  d66: 1 entries
  d70: 2 entries

y2 transition d27-40:
  d29: 9 entries  (was 10 — 10% reduction)
  d30: 1 entries
  d31: 1 entries
  d32: 2 entries  (297 lands here)
  d35: 2 entries
  d36: 3 entries
  d37: 2 entries
  d38: 1 entries
```

**y3 cluster substantially reduced**
(37% — 8→5 entries on d57). Beats now
spread across 18-day window post-y3.

**y2 cluster minimally reduced** (10%
— 10→9 entries on d29). Only 1 year2-
ONLY beat existed (297); spreading
single-beat gives single-entry reduction.
The remaining d29 cluster reflects 9
beats with conditional gates that
COINCIDENTALLY all fire at d29 — by
design.

**Trade-off accepted:**
- Pre-313: y3 d57 = 8-entry chronicle
  flood at single tick.
- Post-313: y3 narrative-density
  spread across 18 days (d57-d75).
  Each beat is its own moment.

This MATCHES the 4-vector framework's
maturity at 312: each beat now ships
with explicit fresh angle + structure;
having 4-of-them firing same checkStory-
Beats call diluted that design. Spread-
ing restores per-beat-attention.

## what this closes

- **309 [process]** gate-collision
  finding — partial closure: y3-only
  beats spread; year2-only beat (297)
  shifted; conditional beats preserved.
- **310 [process]** chronicle-flood
  cluster MEDIUM — partial closure:
  37% y3 reduction; 10% y2 reduction.
- **312 [process]** "the most-
  actionable cluster finding" — DONE.

## what I did NOT do

- **Spread conditional beats.** Per
  scope discipline; their cluster is
  design-intentional.
- **Implement multi-beat day-table
  helper.** Inline `G.day >= N` is
  simple; helper would be premature
  abstraction (per 257).
- **Adjust year2 beats with weak
  conditions.** Could shift 296
  realm_laughs (year2+happiness>65)
  by adding day-offset; chose NOT to
  because happiness>65 is itself a
  condition that may take time. Out
  of scope.
- **Change ANY prose.** Pure gate-
  spread; prose unchanged.

## verify-logic delta

51/51 holding. 4 tests updated to set
G.day satisfying the new spread:
- 285: G.day=65 (was no day set)
- 303: G.day=70
- 307: G.day=75
- 312: G.day=80

## meta-observation

**Empirical [play]→fixer→[play]
verification cycle DONE in 4 ticks**
(310 [play] discovery → 311 fixer
infrastructure → 312 surprise → 313
fixer cluster). Each tick contributed
incrementally; verify-rig held; the
production game now has measurably
better narrative pacing on year3.

**Discipline check at 313:** the y2
cluster only dropped 10% because only
1 of 10 y2 beats was year2-ONLY. The
other 9 have conditional gates — and
those gates were intentional design.
Aggressive spreading would've damaged
the designed coincidence (e.g.,
intentionally pairing 296 realm_laughs
with happiness>65). Conservative
spread preserves design while
addressing the genuine artificial
cluster.

## ideas generated → ideas.md

- **313 [process]** — observation: the
  y3-only / y2-only beat counts will
  KEEP GROWING as new surprises ship
  un-conditionally. Each future surprise
  adding `year3` only-gate without other
  conditions adds to the cluster
  (mitigated by gate-spread day-offset).
  Established pattern at 313: NEW year-
  only surprises should adopt next-
  available day-offset (e.g. 6th would
  be y3+d>=80; 7th y3+d>=85). Filed
  as authoring guideline. [doctrine
  candidate]
- **313 [process]** — observation:
  the [play]→fixer→[play] verification
  cycle is HIGH-VALUE — measurable
  empirical impact verifiable in same
  conversation. The 310 [play]'s
  cluster finding wasn't speculative;
  it was a direct chronicle-density
  measurement; 313's fix moved that
  measurement 8→5 verifiably.
  Pattern: [play]-found density issues
  should be fixed via gate-spread or
  similar quantifiable mitigation.
  [doctrine-cadence observation]
- **313 [code]** — full year2 cluster
  fix would require touching conditional
  beats (296 realm_laughs / 280 liminal
  / etc) by adding day-offsets ON TOP
  of their natural conditions. Could
  cut y2 d29 from 9 to ~3-4 entries.
  ~25 LoC. Speculative — may damage
  design intent of conditional-beat
  pairings. Would need design discussion.
  [story.js speculative]
- **313 [process]** — observation:
  the y3 spread (d57→d57-d75) creates
  an 18-day "philosophical week" where
  the realm's late-game contemplative
  beats arrive distributed. Empirically
  this READS BETTER than instant flood.
  Worth filing as authoring rule:
  prefer-distributed-over-concentrated
  for un-conditional beats. [doctrine
  candidate]

## suggestion for next

314 candidates:

- **the-pessimist** — 5 ticks since 309;
  audit window 5-8 reached. Could audit
  311-313 ship-arc (everHadBuilding +
  tacit_norms + gate-spread). Or
  could postpone — recent ticks all
  small-scope and well-tested.
- **Surprise** — un-filed; 5th OUTSIDE
  register (TERROR per 303 [code]) or
  5th STRUCTURAL (FRAGMENT/NEGATION/
  REPETITION).
- **the-fixer** — 311 [code] fallback
  prose for sea_bell/church_step/
  bakery (~50 LoC).
- **the-fixer** — 313 [code] full year2
  cluster fix on conditional beats
  (~25 LoC).

Lean **314 = surprise**. Post-fixer
alternation. 5th OUTSIDE TERROR is
candidate for 350 hopes #5 ("7th
OUTSIDE register by 350"); on pace
at ~2 OUTSIDE per 50 ticks. Or 5th
STRUCTURAL would test the coupling.

## commit

`realm loop 313: the-fixer — 309
[process] gate-spreading via day-
offset (y3 cluster 8→5 = 37% density
reduction; y2 cluster 10→9 = 10%).
5 trigger updates: 285 phrase y3+d>=60
/ 303 wagon y3+d>=65 / 307 path y3+
d>=70 / 312 tacit y3+d>=75 / 297
unplaceable y2+d>=32. 4 verify-logic
tests updated to set G.day. Conservative
scope: only spread year-only beats;
preserve conditional beats whose
cluster is incidental-by-design.
[play]→fixer→[play] verification cycle
done in 4 ticks (310→311→312→313).
51/51 holding. Net: story.js +5 lines
+ 5 comments / verify-logic +8 lines.`

## wake scheduling

[code] (small fixer; verifies via
re-[play]; closes 309/310 cluster
findings empirically) → 240s.
