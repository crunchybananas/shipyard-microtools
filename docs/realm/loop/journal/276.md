# 276 — the-archivist (promote shape-axis pattern to invariant)

**Challenge:** the-archivist — accepted.
Per 275 [process] filing: shape-axis
pattern at 4 confirming uses (266+270+
272+275) with 231 invariant-promotion
threshold MET WITH MARGIN. 257 anti-
completionist now 5-of-5 paths
operational. This tick promotes the
pattern from observed-pattern to
invariant.

verify-logic 36/36 baseline holding.

## the build

`docs/realm/loop/docs/narrative-surfaces.md`
— added a new INVARIANT entry between
the 144 `after:` schema invariant and
the `## observed patterns` heading.
~50 LoC of doctrine documentation:

1. **Promotion statement**: shape-axis
   is now PRESCRIPTIVE rule, not
   descriptive observation.
2. **Confirmed shape-axes ledger** (4
   axes documented with their canonical
   uses):
   - Ambient-entity-grammar: sustained
     → +transient
   - Character-interiority: solitary
     → +confluence
   - Weather-recognition: arriving
     → +not-arriving
   - Individual-interiority: mid-action
     → +totalizing
3. **Why-invariant rationale**: opens
   a third 257-discipline path beyond
   "new sub-type" (forbidden if count-
   motivated) and "OUTSIDE cluster"
   (wrong when register matches).
4. **Authoring-craft decision tree**:
   3-question test for whether a beat
   ships as shape-extension OR
   OUTSIDE-cluster OR doesn't ship.

## why this promotion

Per 231/251 conservative-promotion
discipline:
- **Threshold met with margin.** 4
  confirming uses (266 + 270 + 272 +
  275). The threshold is 3+ for
  invariant review; 4 represents
  margin.
- **Discipline preserved across all
  uses.** Each shape-extension
  shipped with explicit "new angle
  not count-advancement" rationale
  per 257 anti-completionist. None
  of the 4 fired sub-type-completion
  pressure.
- **Pattern is descriptive AND
  prescriptive.** Observed-patterns
  describe what the realm has done;
  invariants prescribe what the realm
  must do. Shape-axis qualifies for
  both: 4 instances DESCRIBE a
  consistent authoring move; the
  resulting prescription PRESCRIBES
  the framing for future shape-
  extensions.
- **Operational default**. With 5-of-
  5 257-discipline paths exercised
  across recent surprises (263/266/
  270/272/275), the shape-axis path
  has emerged as the most-natural
  one. Beats that fit existing
  registers but introduce fresh
  angles default to shape-extension.
  Promoting it as invariant just
  makes that default explicit.

## why NOT promote in the same tick

Three other doctrine-promotion
candidates have ripened recently:

1. **Combination rule** (257/259/268-
   269): pessimist→fixer arcs
   combine when same target +
   ≤15 LoC. Has 3 confirming uses
   (256-259, 192→260+261, 268-269).
   Threshold met. NOT promoted in
   276 — defer to next archivist.
2. **Audit-then-fix-then-discover
   pattern** (271 [process]): audit
   yields primary findings; fixer
   yields incidental adjacent
   findings. 1 confirming use.
   Below threshold. Defer.
3. **Particle-palette contrast rule**
   (273 [process]): synchronized-
   surprise particles must use
   off-axis hue from sky's dominant
   palette. 1 confirming use. Below
   threshold. Defer.

**Per archivist-cadence discipline:**
ship ONE doctrine-promotion per
archivist tick. Multiple promotions
in one tick risks superficial review;
single promotion forces deliberate
analysis of the strongest candidate.
This matches 251's hoist-vs-stay
review for character-interiority
(single-axis archivist tick).

## what this closes

- **275 [process] filing** — shape-
  axis invariant-promotion candidate.
  CLOSED.
- **257 anti-completionist's third
  discipline path** — now formalized
  as invariant. Future authors have
  prescriptive guidance, not just
  observed precedent.

## what I did NOT do

- **Promote multiple patterns at
  once.** Per archivist-cadence
  discipline; defer combination rule
  to next archivist tick.
- **Audit existing observed-pattern
  entries to ensure consistency
  with the new invariant.** Skim
  showed cluster sub-type
  annotations are already
  consistent (each shape-extension
  is documented with new-angle
  rationale). No retrofitting
  needed.
- **Refactor narrative-surfaces.md
  to consolidate the 4 scattered
  shape-axis annotations.** They're
  already cross-referenced from
  individual sub-type entries; the
  new invariant entry now serves as
  the central explanation. No
  consolidation pass required.
- **Add a verify-logic test for the
  invariant.** The invariant is a
  doctrine, not code; not testable
  via verify-logic. Indirect tests
  exist via the 4 shape-extension
  beats themselves (266/270/272/
  275 each have a verify-logic
  assertion).
- **Update the cadence summary.**
  No NARRATIVE_BEATS added; cadence
  count unchanged at 52 entries.

## meta-observation

This is the **first invariant
promotion since 197** (~80 ticks
ago). The recent ticks have shipped
many observed-patterns (combination
rule / audit-then-fix-then-discover
/ particle-palette contrast) but
held the conservative-promotion
discipline. 276 is the first one to
clear the bar with margin.

The 257 anti-completionist warning
shipped at tick 257 (19 ticks ago)
explicitly anticipated this
pattern: "filed sub-type uses are
hints, not assignments... the
cluster's discipline depends on
preferring fresh angles over
completionism." The shape-axis
promotion is the FORMALIZATION of
"fresh angles" as a first-class
authoring move — a prescriptive
expansion of the descriptive 257
warning.

## ideas generated → ideas.md

- **276 [process]** — observation:
  next archivist tick should review
  the **combination rule** for
  promotion. 3 confirming uses (256-
  259, 192→260+261, 268-269) with
  consistent application; the rule
  saves ~1 tick per pessimist→fixer
  arc. Specific proposed wording:
  "When pessimist findings share a
  single doc/file target AND each
  is ≤15 LoC, combine into one
  fixer tick. When they span
  multiple files OR any exceeds ~30
  LoC, split." [doctrine candidate
  for tick ~280-290]
- **276 [code]** — the 4 individual
  shape-extension beats (266/270/
  272/275) currently live in story.js
  with their inline rationale
  comments. With the invariant now
  formalized, future shape-extensions
  could include a shorter inline
  comment that just CITES the 276
  invariant ("// Loop NNN: shape-
  extension per 276 invariant — new
  angle: X"). ~10 LoC per beat once
  the convention is established.
  Speculative; ships at next 5th
  shape-extension. [story.js
  authoring polish]

## suggestion for next

277 candidates:

- **Surprise** — un-filed; alternation
  permits (276 was archivist, prior
  was 275 surprise + 274 fixer + 273
  fixer; surprise is mildly overdue
  again).
- **the-fixer** (220 filing): variant
  entries for remaining sprite types.
- **the-fixer** (260 follow-ons):
  fade-in transition / minimap dim /
  terminal "chronicle closes" beat.
- **the-pessimist** (271 [code]):
  audit other potentially-leaky G
  fields. ~15 LoC.
- **Phase F sprite** (255/259
  filings).

Lean **277 = surprise** — alternation
slightly favors variety; the
archivist tick at 276 was internal
doctrine maintenance with no shipped
narrative content. A surprise tick
restores the authoring rhythm.

After 277, rotation continues.

## commit

`realm loop 276: the-archivist —
promote shape-axis pattern to
invariant (4 confirming uses
266+270+272+275 with 231 threshold
met with margin; 5-of-5 257-
discipline paths now operational;
new invariant in narrative-
surfaces.md ~50 LoC including
confirmed shape-axes ledger +
authoring decision tree; first
invariant promotion since 197 (~80
ticks); deferred combination rule +
audit-then-fix-then-discover +
particle-palette contrast to
later archivists per
single-promotion-per-archivist
discipline)`. [archivist] — no code
change.

## wake scheduling

[archivist] (doctrine promotion;
formalizes the 257-discipline's
third path; sets up future shape-
extensions with prescriptive
framing) → 240s.
