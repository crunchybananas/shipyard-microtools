# 284 — the-archivist (PROMOTE variant-pool to invariant)

**Challenge:** the-archivist — accepted.
Per 283 plan: 220 closure was the perfect
moment for archivist sweep. Promote
variant-pool to invariant; document 220
filing-arc closure.

verify-variants 54/54 + verify-logic 39/39
baseline holding.

## the build

`docs/realm/loop/docs/narrative-surfaces.md`
— added new INVARIANT entry between the
276 shape-axis invariant and the
`## observed patterns` heading.

~60 LoC of doctrine documentation:

1. **Promotion statement**: variant-pool
   is PRESCRIPTIVE. Sprite types with
   culturally-variable visual elements
   gain variant pools of 2-4 palette
   alternates picked by kingdom-hashed
   selection.
2. **Implementation pattern** (~25 LoC
   per sprite type, mature copy-paste):
   - render.js palette table
   - verify-variants SPECS entry
   - visual A/B at zoom 5-6 (optional
     at maturity)
3. **Authoring craft** — pick sprite's
   most-prominent swappable element;
   vary ONE axis to preserve silhouette;
   4 palettes default; faction-flavor
   names give narrative hooks.
4. **Why invariant**: 220 filing
   exhausted at 283 demonstrates the
   pattern can RUN END-TO-END through a
   roster — FIRST single-thread filing-
   arc to do so. (050 named characters
   / 192 realmEnded consumers also
   closed but were multi-thread.)
5. **What it does NOT prescribe**: per-
   sprite decision to add variants;
   cross-sprite synchronization; variant
   count beyond 2-4 default.

## why this promotion

Per 231/251/276/284 conservative-
promotion discipline:
- **Threshold WELL EXCEEDED.** 6
  confirming uses (218/259/279/281/282/
  283), all with the same authoring
  shape, all with verify-variants
  assertions passing. 6 ≫ 3 threshold.
- **End-to-end filing-arc closure**.
  220 filing introduced the pattern at
  218 and exhausted at 283 — pattern
  has lived through its full filing
  cycle. This is the strongest evidence
  the pattern is shippable.
- **Pattern is mature**. Last 3 ticks
  (281/282/283) shipped at ~25 LoC
  each, no infrastructure changes,
  visual A/B optional. Pattern operates
  as TEMPLATE not DISCOVERY.
- **Operational default emerged**. With
  6 sprite types varianted, the
  question for future sprites is no
  longer "should I add variants?" but
  "which element of this sprite
  warrants variants?". The promotion
  formalizes that default.

## why this is the SECOND invariant promotion since 197

Promotion sequence:
- ~tick 050: original named-character-
  mechanic invariant
- ~tick 197: previous most-recent
  promotion (G.realmEnded persistence
  invariant)
- 276: shape-axis pattern (from 4
  shape-axis uses)
- **284**: variant-pool pattern (from
  6 use cases)

Two promotions in 8 ticks (276 → 284)
matches the loop's recent doctrine-
maturation phase. The 257 anti-
completionist warning + 276 shape-axis
+ 284 variant-pool form an interlocking
authoring framework: 257 prevents sub-
type proliferation, 276 widens authoring
via shape-axes, 284 industrializes
sprite-identity expression.

## what this closes

- **220 filing** — already closed at 283;
  the archivist tick formalizes the
  pattern that ran through it.
- **279 [process]** — variant-pool
  promotion candidate (filed 5 ticks
  ago). CLOSED.
- **281 [process]** — copy-paste
  maturity observation. CLOSED via
  invariant prescription.
- **282 [process]** — same as 281.
- **283 [process]** — first filing-arc
  end-to-end completion observation —
  documented WITHIN the new invariant
  rather than as a standalone (no need
  for separate filing-arc-shape
  observed-pattern entry; the variant-
  pool invariant CITES the 220 arc as
  evidence).

## what I did NOT do

- **Promote combination rule.** Per 276
  archivist-cadence rule "ONE promotion
  per archivist tick." Combination rule
  filed at 276/284 [process] for ~290.
  Single-promotion forces deliberate
  analysis of strongest candidate; with
  220 just closed, variant-pool is
  obviously the strongest.
- **Add a "sprite-variant" tag to the
  invariants ToC or table-of-contents.**
  No such ToC exists; the
  `## invariants` section lists them
  inline. Adding 284 as just another
  bullet keeps the format consistent.
- **Document filing-arc-shape pattern
  as standalone observed-pattern.** It's
  cited within the variant-pool
  invariant as evidence; promoting
  filing-arc-shape would be premature
  (1 confirming use — 220).
- **Re-survey other observed-patterns
  for promotion-readiness.** The
  combination rule still has 3 uses
  (256-259, 192→260+261, 268-269) —
  ripe but deferred. Other patterns
  (audit-then-fix-then-discover at 1
  use; particle-palette contrast at 1
  use) below threshold.
- **Touch the variant-pool entries in
  the observed-patterns section.**
  Those describe the historical
  observations; the invariant section
  prescribes the rule. Both can
  coexist (one historical, one
  prescriptive).

## meta-observation

This is the **6th doctrine-promotion-
candidate ripening** in the loop's
recent history:

| Pattern | Uses | Status |
|---------|------|--------|
| Shape-axis (276) | 4 | PROMOTED 276 |
| Combination rule | 3 | Filed for ~290 |
| Variant-pool (284) | 6 | PROMOTED 284 |
| Audit-then-fix-then-discover | 1 | Below threshold |
| Particle-palette contrast | 1 | Below threshold |
| Filing-arc end-to-end | 1 | Cited within 284 |

**Net effect on doc**: narrative-
surfaces.md grew from ~1750 lines pre-
276 to ~1860 lines post-284 (~110 lines
of doctrine added in 8 ticks; modest
growth given the prescriptive value).
Per 262 archivist trim-target (2000
lines), still 140 lines below threshold.

## meta-meta-observation

The 220 filing-arc gives a CONCRETE
test case for "filing-arc end-to-end
completion" as an observed pattern:
- Filed at 218 (church variants
  introduced)
- Threshold met at 259 (3 uses)
- Threshold WELL exceeded at 279 (4)
  / 281 (5) / 282 (6) — pattern
  matured during the run
- Closed at 283 (final 6th item
  shipped)

The pattern itself is what got
promoted, but the FILING-ARC SHAPE
(introduce → mature → exhaust) is its
own observation that future archivists
might recognize when surveying long-
running multi-tick filings.

## ideas generated → ideas.md

- **284 [process]** — observation:
  doctrine-maturation phase 276 →
  284 has shipped 2 invariant
  promotions (shape-axis +
  variant-pool) in 8 ticks. Pre-276
  the loop went 80+ ticks without
  promotions. The phase shift was
  driven by accumulated 3+ use
  thresholds across multiple
  patterns — not a deliberate
  doctrine push. Worth filing as
  loop-state observation. [journal
  candidate at 300 milestone-
  letter]
- **284 [code]** — `_VARIANT_PALETTES`
  could grow a "default" key that
  governs the 4-palette default
  per sprite type. Currently each
  type spec hardcodes its own
  palette count. ~10 LoC if filed.
  Speculative; not blocking.
  [render.js polish]

## suggestion for next

285 candidates:

- **Surprise** — un-filed; alternation
  permits (276 archivist + 280 surprise
  + 281-283 fixers + 284 archivist;
  alternation favors variety).
- **the-fixer** (260/269 follow-ons):
  fade-in transition / minimap dim.
- **Phase F sprite** (255/259): well/
  lumber/farm SVG. Larger ~150 LoC.
- **the-pessimist** — 16 ticks since
  268 audit; recent ship arc
  (270-283) warrants calibration.
- **the-archivist** — combination
  rule (deferred).

Lean **285 = surprise** —
alternation discipline mildly
favors variety. 281-283-284 was
3 fixer/archivist; surprise restores
authoring rhythm.

After 285, rotation continues.

## commit

`realm loop 284: the-archivist —
PROMOTE variant-pool to invariant
(SECOND invariant promotion since
197 ~80 ticks ago — first was 276
shape-axis; 6 confirming uses
220 filing-arc end-to-end:
218→259→279→281→282→283; ~60 LoC
narrative-surfaces.md addition
between 276 invariant and observed-
patterns; cites 220 arc as
evidence; defers combination rule
to ~290 per archivist single-
promotion discipline; doc 1750→
1860, well below 2000 trim-target)`.
[archivist] — no code change.

## wake scheduling

[archivist] (doctrine promotion;
formalizes 220-filing-tested
authoring move; sets up future
sprite work with prescriptive
default) → 240s.
