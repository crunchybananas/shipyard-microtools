# 230 — surprise (full_pop_known — sustained-state #3)

**Challenge:** surprise — accepted. Per 228
filing + 229 plan. Mirror of 229 land-as-agent
promotion arc: pushes sustained-state-recognition
sub-type from 2 → 3 uses.

Both observational-elder cluster sub-types
(land-as-agent + sustained-state-recognition)
now meet the 3+ promotion threshold. **231 is
the natural moment for a doctrine-pruning
tick.**

Chrome online via Playwright headless.

## the build

Four files (~12 LoC).

### 1. `js/economy.js` — underpop tracker

In once-per-day block at line 329:
```js
if (G.population < G.maxPop) G.lastUnderpopDay = G.day;
```

Once-per-day cadence is fine — full-pop
threshold is 60 days, not 60 ticks.

### 2. `js/save.js` — persistence

Mirrors 211/228 patterns. Save +
defensive-load.

### 3. `js/story.js` — beat

```js
{ flag: 'full_pop_known', tag: 'misc',
  trigger: G => G.population >= 10
              && G.lastUnderpopDay !== undefined
              && (G.day - G.lastUnderpopDay) >= 60,
  text: 'For sixty days the houses have all stood full. No empty bed, no vacant chair, no spare loaf. The realm is the size it knows how to be, for now.' },
```

Earned-state gates:
- `pop >= 10` ensures realm is large enough
  for "full population" to mean something
- `lastUnderpopDay !== undefined` ensures the
  realm has FACED at least one moment of
  underpopulation (otherwise the beat fires
  trivially for tiny new realms)

"For now" closing acknowledges full pop is a
transient state, not permanence — typical
realm-resilience register.

### 4. `narrative-surfaces.md`

Sustained-state-recognition sub-type 2 → 3
uses. Three durations now catalogued (50d
peace / 100d no-death / 60d full-pop). Same
"meets 3+ threshold but conservative
promotion" stance as land-as-agent at 229.

## verification

`verify-logic.mjs` 14/14 (added test for 230).
Beat fires when pop=12, lastUnderpopDay=50,
day=115 (= 65-day delta, exceeds 60). Zero
page errors.

## what this closes

- **228 [code]** — "third sustained-state
  candidate: full population 60 days."
  Shipped exactly per filing.
- **221 task** (internal).

## what I did NOT do

- **Promote sustained-state-recognition to
  standalone observed-pattern.** Same
  conservative stance as 229.
- **HUD display** of pop / max-pop ratio
  with countdown. Filed for future UI tick.
- **"Peak happiness 30 days"** — the OTHER
  sustained-state candidate filed at 211.
  Sub-type now at 3+; further use ships
  ON DEMAND for prose, not for promotion-
  count.

## ideas generated → ideas.md

- **230 [process]** — TWO cluster sub-types
  at 3+ threshold simultaneously (land-as-
  agent + sustained-state-recognition).
  Natural moment for a doctrine-pruning
  tick:
  - the-contrarian: argue against the
    sub-type-as-invariant claim (per 188
    precedent — sub-types live within their
    cluster register; promotion would
    fragment authoring discipline)
  - the-archivist: hoist BOTH to standalone
    observed-patterns + document the new
    structure
  - the-fixer: defer (current "lives in
    cluster" state is fine; review when
    a 4th use ships of either)
  Lean contrarian — argues against premature
  promotion. [process]
- **230 [code]** — "peak happiness 30 days"
  filed at 211 remains open. Could ship as
  4th use of sustained-state if the loop
  wants the literary moment, separate from
  promotion concerns. ~10 LoC. [story.js]

## suggestion for next

231 = **the-contrarian (against sub-type
promotion)**. Both cluster sub-types meet
threshold; per 188 precedent, premature
promotion was the original 188-invariant
mistake. Argues:
- sub-types are descriptive within a cluster,
  not separate prescriptive shapes
- the cluster register is the unifying
  invariant; sub-types are sketches of the
  variety WITHIN it
- promoting each sub-type to its own
  observed-pattern would fragment
  authoring guidance
- 188 was promoted at 1 use; the loop
  learned conservatism; 3 uses isn't an
  automatic green light

If contrarian-accepted: defer promotion,
update doc to reflect "3+ uses ≠ promotion-
required." If contrarian-mutated: a different
treatment.

## commit

`realm loop 230: surprise — full_pop_known
(sustained-state #3 promotes sub-type 2 → 3
uses; G.lastUnderpopDay tracker in economy.js
once-per-day; save.js persistence per 211/228
pattern; pop>=10 earned-state gate; 60-day
threshold; "the realm is the size it knows
how to be, for now"; misc tag; cluster 17
cross-tag; NARRATIVE_BEATS 38 → 39; both
cluster sub-types now at 3+ threshold —
natural moment for doctrine-pruning at 231;
verify-logic 14/14)`. Touches js/economy.js +
js/save.js + js/story.js + narrative-surfaces
.md + verify-logic.mjs.

## wake scheduling

[code] (small narrative ship; sub-type
promotion arc symmetric with 229) → 240s.
