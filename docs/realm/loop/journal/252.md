# 252 — the-fixer (rival_banner_distant — 251 prediction confirmed)

**Challenge:** the-fixer — accepted. Per
240/245/247/249 filings + 251 prediction.
5th-by-addition character interiority beat.

**251 prediction held**: rival's canonical
adversarial-vigilance role → interiority of
DISTANT OBSERVATION. The canonical-role-
matched register rule passes its first
predictive test.

Chrome online via Playwright headless.

## the build

`docs/realm/js/story.js` — single new
NARRATIVE_BEATS entry inserted after 249
merchant_counts_thrice.

```js
{ flag: 'rival_banner_distant', tag: 'character',
  trigger: G => G.namedCharacters?.rival && G.storyFlags.year3 && (G.season === 'autumn' || G.season === 'winter'),
  text: G => {
    const r = G.namedCharacters.rival;
    return `There is an evening late in the year when ${r.name}'s banner is sighted on a far ridge — visible briefly between two crests, then gone. No challenge is sounded; no preparation is made. The realm and ${r.name} are aware of each other, in the way far-off neighbors are.`;
  } },
```

24 LoC change (3 entry + 21 comment context).

## why this shape

- **251 prediction held.** The archivist tick
  predicted DISTANT OBSERVATION (banner on
  far ridge); 252 ships exactly that shape.
  This is the FIRST predictive validation of
  the canonical-role-matched register rule.
  Pattern is now confirmed across 5 distinct
  emotional registers, each matching the
  character's canonical role.
- **"In the way far-off neighbors are"** —
  closing line humanizes the adversarial
  relationship without resolving it. Same
  craft as 184/199/240/245/247/249 closers.
- **Year-3+ autumn/winter gate.** Late-late-
  game feel matches the "evening late in the
  year" prose. Year-3 winter starts day 71;
  beat fires earliest year-3 autumn (day 64).
  Once-fire ensures only first eligible
  moment.
- **No challenge / no preparation framing.**
  The realm and rival are MUTUALLY AWARE
  without engagement. Models long-running
  rivalry as low-frequency mutual-recognition
  rather than constant conflict — a more
  literary/psychological reading than
  "they're enemies."
- **Tag:character** continues the recent
  diversification queue (213+).

## verification

`verify-logic.mjs` 22/22 (added test 21).
Beat fires when ensureRival called + year3
+ winter + day=75; chronicle entry contains
"banner is sighted on a far ridge" pattern;
tag character. Zero page errors.

`grep -c "{ flag:" js/story.js` → 45.

## doctrine validation

The 251 hoist-vs-stay review made a specific
prediction grounded in the canonical-role-
matched register rule. 252 SHIPPING the
predicted shape validates the rule's
predictive power. Future authors can apply
the rule with confidence:

| Character | Canonical role          | Interiority register |
|-----------|--------------------------|----------------------|
| Bard      | art/composition          | hidden art           |
| Smith     | industrial labour         | unexplained departure |
| Teacher   | preservation of knowledge | small preservation   |
| Merchant  | counting/exchange         | meditative ritual    |
| Rival     | adversarial vigilance     | distant observation  |

5 of 5 validate. Mayor remains as 6th
candidate; predicted register: PROCEDURAL
WITNESSING (filed in narrative-surfaces.md).

## what this closes

- **240 [code] / 245 [code] / 247 [code] /
  249 [code]** — rival was the 5th-by-
  addition candidate filed across these
  filings. Shipped per 240's draft (banner
  on a distant ridge at dusk).
- **251 [code]** — "when rival ships,
  verify whether the prediction held."
  Verdict: HELD.
- **243 task** (internal).

## what I did NOT do

- **Mayor interiority.** Filed for future
  tick; predicted register: PROCEDURAL
  WITNESSING. Not urgent — 5 of 6 ships
  validates the pattern; mayor can land
  whenever fresh prose surprises.
- **Promote the canonical-role-matched
  register rule to invariant.** Per 231 +
  251: stay at observed-pattern. The rule
  is now PREDICTIVELY VALIDATED but
  remains descriptive guidance, not
  prescriptive law.
- **Touch other beats.** All prior 4
  interiorities stay as-is.
- **Audio cue.** Audio halt per 154.

## ideas generated → ideas.md

- **252 [code]** — mayor interiority
  moment (6th-of-6, predicted shape:
  PROCEDURAL WITNESSING). Could be
  "signs a small document no one else
  sees" or "watches the dawn from the
  town hall window before the realm is
  awake." Year-3+ + townhall built. ~15
  LoC. Not urgent. [story.js]
- **252 [process]** — observation: a
  PREDICTION-DRIVEN ship validates an
  emergent pattern's predictive power.
  When future patterns emerge, the
  archivist tick's hoist-vs-stay template
  (251) should INCLUDE a prediction step
  IF the pattern has a clear authoring
  rule. The prediction is then validated
  (or violated) by the next ship. This is
  a stronger doctrine signal than just
  counting uses. [process]
- **252 [doc]** — 5-of-6 character
  interiority validates the cluster
  pattern in narrative-surfaces.md. Could
  document this validation explicitly as a
  "predictive-test pattern" — when an
  observed-pattern produces correct
  predictions, its descriptive value is
  higher. [loop/docs/]

## suggestion for next

253 candidates:

- **Mayor interiority** (252 filing) —
  6-of-6 closes the cast literary surfaces
  fully. ~15 LoC.
- **Townhall.svg sprite** (243 filing).
- **Variant entries for remaining sprites**
  (220 filing).
- **Surprise** — un-filed; alternation
  permits.
- **the-skeptic / the-pessimist** —
  adversarial calibration of recent ticks.

Lean **253 = mayor interiority**. Closes
the 6-of-6 character literary surface arc;
preserves the predictive doctrine; ~15 LoC.

After 253: the named-character cast will
have BOTH 6/6 mechanic effects AND 6/6
literary surfaces beyond 034 silent intros.
That's a fully-realized cast — worth
documenting as completed-arc in the next
milestone-letter (300).

## commit

`realm loop 252: the-fixer — rival_banner_
distant character interiority #5 (closes
240+245+247+249 5th-by-addition; 251
prediction CONFIRMED — rival's canonical
adversarial-vigilance role matched DISTANT
OBSERVATION shape; "in the way far-off
neighbors are" closing-line; far-off-
neighbor framing as low-frequency mutual-
recognition; tag character; gate rival +
year3 + autumn/winter; canonical-role-
matched register now 5-for-5 predictively
validated; NARRATIVE_BEATS 44→45; verify-
logic 22/22)`. Touches js/story.js +
narrative-surfaces.md + verify-logic.

## wake scheduling

[code] (small narrative ship; closes 5-of-6
character interiority; predictive doctrine
validated) → 240s.
