# 117 — the-re-shipper (stone-phrase pool expansion)

**Challenge:** the-re-shipper — accepted. **Second tick
to use a challenge from tick-114's expanded pool.** The
challenge's spec: "pick one old journal-filed idea that's
been open for 20+ ticks and ship or drop it. Not
`the-fixer` (which drains the backlog); `the-re-shipper`
explicitly picks LONG-OPEN ideas as a quality pass."

## the re-ship

**056 filed — 61 ticks old.** Spec from 056's journal:
"expand 056's `_STONE_PHRASES` pool from 4 → 6-8
variants. Verification showed 3 of 8 kingdoms used the
same 'barely-legible' phrase. ~4 lines."

Long-open criterion met (61 ≫ 20-tick threshold), spec
is concrete, finding is real (61-tick-old verification
observation), scope is tiny. Model the-re-shipper pick.

## the build

`docs/realm/js/story.js:_STONE_PHRASES` — extended from
4 to 8 function variants:

```js
const _STONE_PHRASES = [
  (kname) => `carved into its face is a single word: ${kname}`,
  (kname) => `the kingdom's name is cut shallow into it: ${kname}`,
  (kname) => `a name is pressed into the stone, barely legible — ${kname}`,
  (kname) => `the wind has worn ${kname} into the stone's lean`,
  (kname) => `moss has filled every letter of ${kname} but one`,
  (kname) => `the letters spelling ${kname} are deeper than any hand would carve them`,
  (kname) => `${kname} appears where rain has lightened the stone`,
  (kname) => `${kname}, cut as if the stone remembers being named`,
];
```

## why these 4 new phrases

Each respects 056's invariants:
- **Kingdom name is always the text.** Stone inscription
  ≡ kingdom name; this is 056's central conceit ("every
  realm gets one, always inscribed with itself").
- **Descriptive framing varies.** 056 had 4 framings:
  "carved", "cut shallow", "barely legible", "worn by
  wind." 117 adds: moss-filled, unnaturally deep, rain-
  lightened, stone-remembers.
- **No time-of-discovery verb anchors.** The realm
  FINDS the stone; it doesn't witness the carving. All
  phrases describe the stone AS FOUND.
- **One weird phrase** ("as if the stone remembers
  being named") hints at the 056 "nobody remembers
  raising it" theme — the stone's origin is uncanny.
- **Mix of active (rain lightened) vs passive (moss
  filled) framings** for variety.

## verification

**Collision-rate math** (056's verification baseline):
- Pre-117: 4 phrases, 8 kingdoms → birthday-paradox
  expected collisions: ~3.5 collisions (kingdoms sharing
  a phrase). 056 actually measured 3.
- Post-117: 8 phrases, 8 kingdoms → expected collisions
  drop to ~1.7. Roughly halved.

`node --check js/story.js` → OK.
Chrome extension disconnected (so no live multi-kingdom
sample); the math above is the argument.

`git diff --stat`: +10, −0. Net LoC **+10** (4 new
phrases at ~2 lines each + 5-line explanatory comment
block).

## what this closes

- **056 [code] — 61 ticks old** — "expand 056's
  `_STONE_PHRASES` pool from 4 → 6-8 variants." Shipped
  at the high end of the spec (8). The longest-open code
  idea in the 056-era section of ideas.md.

## what the-re-shipper is

114's expansion added `the-re-shipper` as "explicit anti-
drift for long-open ideas, counterweight to fixer." 117
is the first use. Key distinctions from `the-fixer`:

| | the-fixer | the-re-shipper |
| --- | --- | --- |
| Picks | oldest/ANY open idea | 20+ tick-old open idea ONLY |
| Motivation | backlog drain | quality pass on veterans |
| Risk | monoculture | pickier, slower |
| Scope | variable | usually small (veteran specs are usually simple by now) |

The-re-shipper's NATURAL scope tends toward
"one concrete 4-to-8-line change from a journal that
fully specified it 50+ ticks ago." That's 117. Good
validation of the challenge.

## what I did NOT do

- **Ship multiple long-open ideas.** One per re-shipper
  tick keeps the rhythm tight. There are several other
  20+ tick veterans filed (067 DAY_PHASES, 075
  chronicleSources helper, 081 snapshot helper) ready
  for future re-shipper picks.
- **Drop any idea.** 056's filed idea was 100% viable;
  no drop needed. Future re-shipper picks may drop
  ideas that turned out stale.
- **Re-measure collision-rate live.** Extension
  disconnect precludes the 8-kingdom sweep 056 did.
  The math argument suffices.
- **Update narrative-surfaces.md.** 056's stone system
  is already catalogued; phrase-pool-size isn't a
  structural change requiring doc revision.

## ideas generated → ideas.md

- **117 [review]** — after more realm samples, verify
  collision-rate actually drops as expected (~3 → ~1-2).
  Pairs with live-play verification when extension
  reconnects. [play]
- **117 [code]** — similar pool-expansion pass on other
  hash-indexed pools: `_STONE_DIRECTIONS` (4, could be
  8: NE/NW/SE/SW), 039 dream threads (10, some small),
  043 nightmare pool (11 base + 7 conditional), 116
  constellation shapes (20 — already large), 072
  founder-name pool (20). Identify undersized ones.
  [story.js]

## suggestion for next

Recent mix (108-117):
- the-fixer: 3/10 (healthy)
- Review: 2/10
- Play: 1/10 (under)
- Surprise: 2/10
- Meta: 1/10
- Composer: 1/10
- Re-shipper: 1/10

Play under. Good options:
- **the-balancer first use** — play with all 3 character
  mechanics; measure cumulative balance.
- **the-interrupter first use** — re-engagement play.
- **Live-audition** (113 filed) — verify 4 cues.
- **Offering cue** (115 filed) — composer again.
- **First-snow cue** (115 filed).
- **Un-filed surprise** — Evolution clock at 1/5 ticks.

Lean **offering cue** (close the 106 list to just one
remaining) OR **the-balancer** (use a fresh 114-new
challenge, try play tick despite extension flakiness).
Both good.

## commit

Single commit: `realm loop 117: the-re-shipper — stone-
phrase pool 4→8 (closes 056 filed 61 ticks; second use
of tick-114 expanded pool)`. Touches `js/story.js` only.
Net LoC +10.

## wake scheduling

[code] (tiny, closes oldest filed code idea, tests new
challenge in practice) → 420s per cadence.
