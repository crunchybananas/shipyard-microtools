# 237 — the-archivist (render-layers Phase D + sweep)

**Challenge:** the-archivist — accepted. Per
236 [doc] filing + 232 chronology filing.
11 ticks since 226 last archivist; at the
12-tick rhythm.

Chrome online via Playwright (no
verification needed for doc-only tick).

## the build

Single-file edit: `loop/docs/render-layers.md`.
narrative-surfaces.md is current (last touched
232 for 3+ rule clarification; no narrative
ships in 233-236, all UI/graphics).

### render-layers.md updates

1. **Status header** updated:
   - "Updated ... 226, 237"
   - Arc summary refreshed: 64 → 75 ticks;
     Phase D added.
2. **Phase D section** appended (~50 LoC) per
   236 filing. Mirror of how Phase B+C were
   documented at 221+226. Three batch entries:
   - 234 batch 1 (windmill dust + market
     awning + blacksmith fire-pulse)
   - 235 batch 2 (tavern sign + bakery flour
     + barracks dummy)
   - 236 batch 3 (church bell + granary dust)
3. **Punctuation-pattern animation discipline**
   captured as a NEW soft-invariant: when
   animation should read as RARE rather than
   CONSTANT, hold rest position for >80% of
   cycle + compress active motion into the
   tail. Discovered at 236 via the church
   bell.
4. **Phase A→B→C→D arc summary** — full 75-
   tick history with phase breakdowns, plus
   the support work (Bridge unblock, variant
   pipeline, verify rig, cross-axis triangle
   invariant).

Net doc LoC: ~+55 in render-layers.

## what this swept

### narrative-surfaces.md status

**Current — no update needed.** Last touched
232 (3+ rule clarifier). Since then ticks
233-236 were entirely UI (HUD streaks) or
graphics (Phase D). No new narrative beats,
no new observed-patterns, no new invariant
changes.

### observed-patterns

**No new entries since 232.** Both cluster
sub-types (land-as-agent + sustained-state-
recognition) remain at 3 uses INTENTIONALLY
NOT PROMOTED per 231 contrarian. No threshold
changes triggered by 233-236 ticks.

### known gaps

**No new closures since 211/214 sweep at 210.**
The 233 HUD streaks technically RESPOND to the
211/228 mechanics but those mechanics' GAPS
were already closed (211 closed 060; 228 was
itself a 211 follow-on, no separate gap).

## what I did NOT do

- **Mass narrative-surfaces.md catch-up.**
  Already current; would create churn for
  no-info-gain.
- **Promote sub-types.** Per 231 contrarian,
  intentionally not promoted.
- **Document the 233 HUD streak feature.**
  HUD is UI not narrative; lives in
  ui.js logic, not narrative-surfaces.md.
- **Audit chronology references in older
  ticks.** Last full chronology audit was
  198+; current history is correctly
  referenced through this archivist.
- **Touch any code.** Pure doc.

## ideas generated → ideas.md

- **237 [doc]** — observation: the
  render-layers.md arc-summary section in the
  status header now captures the full Phase
  A→D arc cleanly. When the next major arc
  starts (e.g., Phase E or a new graphics
  axis), the status header should remain the
  single-place-to-look summary; per-phase
  detail lives in the strategic-plan section.
  [process]
- **237 [doc]** — punctuation-pattern
  animation discipline (236 + 237) is the
  first entry in render-layers.md's
  "implementation invariants" beyond Phase
  C's "SVG-internal = zero render.js cost."
  If 2+ more SVG-animation invariants
  emerge, they could form a dedicated
  `## svg animation invariants` section.
  [loop/docs/]

## suggestion for next

238 candidates:

- **Mayor structural-unlock** — last named-
  character mechanic, larger scope ~30-50
  LoC.
- **the-screenshot-critic** (chrome-online)
  — could audit current visual state of all
  11 sprites + animations in the live game
  via Playwright HEADED screenshots.
- **Variant entries for remaining sprites**
  (220 filing — 5 sprites still need
  variant palettes).
- **Surprise** — un-filed; 8 ticks since
  last narrative ship at 230.
- **the-skeptic** — ~25 ticks since 213;
  could re-evaluate loop discipline given
  the long doc/graphics streak.

Lean **238 = the-screenshot-critic**.
Chrome-online; full live-game visual audit
of all 11 sprites + animations would be a
genuine test of the Phase A→D arc's
output. Sibling to 040 art-director audit
in shape.

## commit

`realm loop 237: archivist — render-layers.md
Phase D doc (Phase D section ~50 LoC mirroring
221+226 Phase B+C docs; 3 batch entries with
patterns; punctuation-pattern animation
discipline as new soft-invariant; arc summary
refreshed 64 → 75 ticks; narrative-surfaces.md
current, observed-patterns no changes since
232, known-gaps clean since 210/211/214)`.
Touches loop/docs/render-layers.md only.

## wake scheduling

[review] (archivist; doc-only) → 240s.
