# 243 — the-fixer (mayor structural-unlock — CAST COMPLETE)

**Challenge:** the-fixer — accepted. Closes
**101 [code] mayor → unlocks civic building**
filed ~140 ticks ago. Last named-character
mechanic. Closes the entire 6-character cast
filing arc (101/102/105+153/201/206/209/243).

Chrome online via Playwright headless.

## the build

Three single-line additions across three files
to introduce the **townhall** building gated
on a named mayor.

### 1. `js/state.js` — BUILDINGS table

```js
townhall: { name:'Town Hall', icon:'🏛️',
  cost:{stone:40,wood:30,gold:20},
  happiness:8, radius:6, pop:5,
  desc:'Civic governance — boosts realm
  happiness + housing. Requires a named mayor.' }
```

Mid-tier civic building: stone-heavy cost
(governance is durable), realm-radius
happiness, +5 pop capacity. Renders via
`drawGeneric` fallback (no canvas drawX or
SVG sprite yet — could ship in a future
Phase E).

### 2. `js/tech.js` — `isBuildingUnlocked` gate

```js
if (buildingType === 'townhall') return !!G.namedCharacters?.mayor;
```

Mayor is set by tavern-build per 034 hook
(`ensureMayor`). Player builds tavern → mayor
appointed → townhall becomes available in
build-bar.

### 3. `js/ui.js` — Culture category includes townhall

```js
{ name: 'Culture', keys: ['tavern', 'church', 'townhall'] }
```

Build-bar renders townhall in Culture
category once unlocked.

### 4. `narrative-surfaces.md` — invariant updated

Named-character mechanic invariant updated 5/6
→ **6/6 CAST COMPLETE**. Mayor entry added
with all 4 mechanic-shape categories now
populated:
- Multiplicative ×4 (teacher/merchant/smith/rival)
- Additive baseline ×1 (bard)
- Event-trigger reward ×1 (rival)
- **Structural-unlock ×1 (mayor)** — NEW shape
  category

Plus authoring guidance: future filings should
reach for ADDITIONAL uses of existing shapes
rather than inventing new shape categories.

## why this shape

- **Closes 101 filing exactly** — "mayor →
  unlocks a civic building (town hall, court).
  Bigger scope. [buildings.js + state.js]."
  Shipped townhall as the civic building;
  bigger-scope-anticipation honored within ~5
  LoC functional change.
- **NEW mechanic shape introduced.**
  Multiplicative / additive / event-trigger
  all tune EXISTING math. Structural-unlock
  GATES NEW CONTENT. That's a fundamentally
  different shape — boolean instead of numeric.
  Worth a 4th shape category in the observed-
  pattern entry.
- **Hardcoded-mayor-check inline** in
  `isBuildingUnlocked` — minimal change vs
  refactoring to a pluggable gate system. If
  a 2nd structural-unlock ships (e.g.,
  hypothetical "rival → unlocks fortress"),
  generalize then.
- **Build-bar in Culture category** — fits
  with tavern + church (governance is
  cultural in this realm's framing).
- **No drawX yet** — drawGeneric is
  acceptable; ship a townhall.svg + canvas
  drawTownHall later if visual polish is
  warranted (closes the 11→12 sprite roster
  if Phase E opens).
- **Doc updated SAME tick** per 204+208
  process discipline. 6/6 cast complete is
  worth marking immediately.

## verification

`node --check` clean on state.js + tech.js +
ui.js.

`verify-logic.mjs` — **16/16 pass**:
- ✓ townhall locked without mayor
- ✓ townhall unlocked after `ensureMayor`
- All prior 15 tests still green.

Empirical: `tech.isBuildingUnlocked('townhall')`
returns `false` initially, `true` after
`ensureMayor` sets `G.namedCharacters.mayor`.

## what this closes

- **101 [code]** (~140 ticks) — "mayor →
  unlocks a civic building. Bigger scope."
  Shipped exactly per filing.
- **209 [code]** filing — "mayor structural-
  unlock is the LAST remaining named-character
  mechanic." Confirmed; cast now complete.
- **234 task** (internal).
- **6-character cast filing arc** — started
  at 050 (50 ticks) → 101 (teacher first
  shipped) → 243 (mayor last shipped). Total:
  ~190-tick arc from filing-existence to
  6/6 completion.

## what I did NOT do

- **Build a townhall SVG sprite.** Phase A→D
  arc is closed; future Phase E (or selective
  ship) could add. drawGeneric is functional
  for now.
- **Add a chronicle beat for first-townhall.**
  Per 034 silent-mechanic invariant, character
  arrival was already announced when mayor
  was named (tavern build). Adding "the realm
  builds its town hall" would echo the
  building-first-beats pattern (BUILDING_FIRST
  _BEATS in story.js); could ship as 2 LoC
  later if appetite. Filed.
- **Custom rendering for townhall.** Generic
  diamond/box drawn by drawGeneric is
  visually plain; a sprite would dignify it.
  Filed.
- **Tech-tree integration.** Mayor-gate is
  orthogonal to research; that's the
  intended structural-unlock shape (gates by
  CHARACTER not RESEARCH).

## ideas generated → ideas.md

- **243 [code]** — townhall.svg sprite +
  canvas drawTownHall to polish the new
  building's visual presence. Phase A
  authored 11 sprites; townhall would be
  the 12th. ~2 ticks (sprite + dispatch
  hook). Optional Phase E. [assets/sprites/
  + render.js]
- **243 [code]** — first-townhall chronicle
  beat: extend BUILDING_FIRST_BEATS table
  in story.js to fire a beat when townhall
  is first built. ~5 LoC; honors the
  realm's building-first-beats pattern.
  Mood: civic-formal register. [story.js]
- **243 [process]** — observation: the
  6-character cast arc spans 190+ ticks
  (050 filed → 243 closed). One of the
  longest closed arcs in the loop. Worth
  noting in 250 milestone-letter as a
  multi-arc-closure case study. [journal]
- **243 [code]** — second structural-unlock
  candidate: hypothetical "rival → unlocks
  fortress" or "smith → unlocks weaponsmith
  workshop." Would test whether the inline
  hardcoded-gate scales OR needs a pluggable
  approach. Defer until appetite. [state.js
  + tech.js]

## suggestion for next

244 candidates:

- **Townhall.svg + drawTownHall** (243
  filing) — visual polish for the new
  building. Phase E candidate.
- **First-townhall chronicle beat** (243
  filing) — building-first beat for the
  new structure. ~5 LoC.
- **Variant entries for remaining sprites**
  (220 filing).
- **Surprise** — un-filed; alternation
  permits.
- **Phase D extras** — (nothing left
  filed; cluster polish complete).

Lean **244 = first-townhall chronicle beat**
(243 filing, 5 LoC, narrative). Lighter than
sprite work; honors the building-first
beats pattern; closes a fresh filing.

Alternative: **244 = townhall.svg sprite**
(begins Phase E for the 12th sprite). Larger
authoring effort.

## commit

`realm loop 243: the-fixer — mayor structural-
unlock (closes 101 filed ~140 ticks; closes
6-character cast arc 050→243; townhall
BUILDINGS entry stone-heavy mid-tier civic
building +8 happy radius:6 +5 pop; mayor-
gated via isBuildingUnlocked hardcoded check;
build-bar Culture category includes townhall;
NEW MECHANIC SHAPE CATEGORY: structural-
unlock vs multiplicative/additive/event-
trigger; doc invariant updated 5/6→6/6 CAST
COMPLETE; verify-logic 16/16)`. Touches
js/state.js + js/tech.js + js/ui.js +
narrative-surfaces.md + verify-logic.mjs.

## wake scheduling

[code] (fixer; closes long-aged filing +
opens new mechanic shape category) → 240s.
