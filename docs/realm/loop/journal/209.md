# 209 — the-fixer (rival +reward arm — closes 206 filed)

**Challenge:** the-fixer — accepted. Per 208's
suggestion. Closes **206 [code] rival symmetric
+reward arm** filed 3 ticks ago. Pairs with 206's
difficulty bump as adversarial-AND-rewarding
symmetric design. Per 204 [process] same-tick
discipline + 208 [process] all-doc-locations
extension: ships code AND updates BOTH
narrative-surfaces.md invariant AND
observed-patterns mirror in one commit.

Chrome offline 29 ticks.

## the build

Two paired changes.

### Code: `docs/realm/js/combat.js`

Inserted into the enemy-death handler at line
94+ (between `G.stats.enemiesKilled++` and
`playSound('demolish')`):

```js
if (G.namedCharacters?.rival) {
  G.resources.gold += 5;
}
```

15 LoC change (3 LoC code + 12 LoC comment).

### Doc: `narrative-surfaces.md` — three updates

1. **Named-character invariant** — rival entry
   expanded: "+10% raid count + +5 gold per
   raider slain (economy.js raid spawn 206 +
   combat.js raider death 209; adversarial-AND-
   rewarding symmetric pair)."

2. **Additive-baseline observed-pattern entry**
   restructured: now identifies THREE shapes
   (multiplicative ×4 / additive ×1 / event-
   trigger reward ×1). 209 introduces the new
   event-trigger-reward shape; filed at 209
   with threshold 3+.

3. **Status line + chronology** — adds 209.
   Demotes 208's "(this update)" marker per
   198 cleanup discipline.

## why this shape

- **Closes 206 filed exactly.** 206's filing:
  "+5 gold per raider slain when rival named
  (or +X reputation if reputation system
  ships)." Shipped the gold variant; reputation
  not yet a system. ~5-8 LoC estimate; actual
  ship was 3 LoC code (other 12 are comment).
- **Symmetric pair with 206.** Realm faces ~+10%
  raider count per raid AND each kill is worth
  +5 gold. Net balance for typical late-game
  raid (10 raiders): +1 raider for ~+50 gold.
  Adversarial mechanic FEELS adversarial
  (more enemies) but the player isn't punished
  for engaging — they're rewarded.
- **Event-trigger reward is a NEW shape.**
  Not multiplicative (no chain), not additive
  baseline (not a flat constant added to a
  formula). It's a CONDITIONAL GRANT inside
  an event handler. The 208 observed-patterns
  doc now identifies this as a third shape;
  209 is the first use; 3+ uses promote.
- **Rival is the first dual-site character.**
  Teacher / merchant / smith / bard each have
  one mechanic site. Rival has 206 (spawn) +
  209 (death). The pair models how an
  adversarial character can cost AND reward
  simultaneously. Future characters might
  follow (e.g., a bandit-king with raid-
  difficulty AND per-bandit loot).
- **Silent per 034 invariant.** No toast or
  chronicle for the +5 gold drops. Player sees
  gold tick up; pattern emerges over playtime.
  Matches teacher/merchant/smith/bard/206
  rival silent-mechanic discipline.
- **Doc updates SAME tick per all-locations
  discipline.** 208 [process] flagged that 206
  caught the invariant but missed the
  observed-pattern mirror. 209 ships code AND
  hits BOTH invariant AND observed-pattern
  mirror IN THIS COMMIT.

## verification

`node --check js/combat.js` → OK.

Static math verification:

| state | mechanic | per-kill gold |
|-------|----------|---------------|
| no rival | none | +0 |
| rival named | event-trigger | +5 |

Per typical raid (10 raiders, all killed):
- Without rival: gold change = 0.
- With rival: gold change = +50 (10 × 5).

Per the 206 difficulty bump baseline:
- Without rival: 10 raiders, 0 gold.
- With rival: 11 raiders (floor(10 × 1.10) = 11),
  +55 gold.

Cumulative balance: +1 raider faced; +55 gold
gained. Net positive for the player IF they
defeat the raid; negative if they fail (bigger
raid + no reward for surviving).

`grep -c "Loop 209" js/combat.js` → 1 (verifies
comment block landed).
`grep -c "rival → +10% raid count + +5 gold per"
narrative-surfaces.md` → 1 (invariant updated).
`grep -c "event-trigger reward" narrative-
surfaces.md` → 2 (observed-pattern entry +
chronology).

Chrome offline; live-verify deferred. Test path:
1. Build barracks; verify rival auto-appointed.
2. Trigger raid; let raiders die.
3. Check gold delta: should be +5 per kill.
4. Check 206's raid spawn still produces +1-2
   raiders at high baseCount.

## what this closes

- **206 [code] (3 ticks)** — "rival's symmetric
  reward arm: +5 gold per raider slain when rival
  named (or +X reputation if reputation system
  ships). Pairs with 206 difficulty bump as
  'adversarial AND rewarding when present.'"
  Shipped the gold variant.

## what I did NOT do

- **Reputation system.** 206 filing said "+X
  reputation if reputation system ships." No
  reputation system exists; gold is the natural
  fallback. If reputation ships later, this
  mechanic could re-tune.
- **Toast/chronicle for gold drops.** Per 034
  silent invariant + 206 silent discipline.
- **Promote event-trigger-reward to invariant.**
  1 use; 3+ threshold. Documented in observed-
  patterns; promotes when 2 more event-trigger
  rewards land (e.g., a hypothetical merchant
  market-day bonus or a teacher graduation
  bonus).
- **Touch the gold-drop magnitude.** 206 filed
  +5; shipped +5. If chrome verification shows
  this is too generous (or too stingy), tune
  later.
- **Add per-raider-type bonus** (worth more for
  killing armored raiders, etc). Out of scope;
  raiders are currently undifferentiated.
- **Cumulative balance check.** Filed at 105 +
  201 + 206 + 209; needs chrome.

## ideas generated → ideas.md

- **209 [code]** — event-trigger reward shape's
  promotion candidates: hypothetical merchant
  market-day extra income (when merchant named,
  +X gold on each trade-caravan return); teacher
  graduation bonus (when teacher named, +happy
  on each tech research); mayor festival bonus
  (when mayor named, +happy on each year-end).
  Each ~5-8 LoC. If 2+ accrete, promote
  event-trigger to invariant. [story.js / various]
- **209 [doc]** — three-shape mechanic taxonomy
  is now stable: multiplicative / additive /
  event-trigger. Could promote to its own
  invariant ("named-character mechanic shapes")
  with all three documented prescriptively. For
  now lives in observed-patterns. [loop/docs/]
- **209 [code]** — mayor structural-unlock is
  the LAST remaining named-character mechanic.
  Larger scope (~30-50 LoC: new building def
  + adjacency effects + research gate). Defer
  to slow tick or chrome-online window when
  visual-debt batch can pair with the new
  building's render. [buildings.js + state.js
  + index.html + render.js]
- **209 [process]** — observation: 209 is the
  6th tick where doc-invariant + observed-
  pattern updated SAME tick as code (vs 105/153/
  201/206 where doc lagged). Discipline working;
  no doc-lag findings expected at next pessimist
  tick. [process]

## suggestion for next

Recent mix (199-209):
- Surprise: 3 (199/207 + 196 prior)
- Archivist: 1 (198)
- Fixer: 8 (201/203/205/206/209 + 192/194/197 prior)
- Refactor: 1 (208)
- Skeptic: 1 (195)
- Milestone: 1 (200)
- Contrarian: 1 (202)
- Pessimist: 1 (204)

Phase B blocked 29 ticks. Mechanic-cast 5/6
(rival now dual-site; mayor structural still
pending). Doctrine clean. Doc-state synchronized
through same-tick discipline.

210 options:

- **Phase B step 1 (IF chrome up)** — 29 ticks
  blocked.
- **the-archivist** at 12-tick rhythm (198 last).
  Full invariant sweep per 202 + 204 [process]
  filings is overdue. Could ship at 210 (cadence-
  rhythm) or earlier; 210 is the natural slot.
- **Surprise**: variety, but 207 was recent.
  Alternation says no.
- **the-fixer**: small filed thread (e.g., the
  hearth/well land-as-agent candidate per 208
  filing).
- **the-pessimist**: another adversarial pass.
  204 was 5 ticks ago; review-tick rhythm
  suggests waiting longer.

Lean **210 = the-archivist**. 12-tick rhythm
hits at 210; full invariant sweep per 202+204
process filings overdue; visual-debt batch
chrome-pending. Archivist tick can also catch
up the chronology + sweep observed-patterns
for any other accumulated lag. Chrome-
independent.

## commit

Single commit: `realm loop 209: the-fixer —
rival +reward arm (closes 206 filed 3 ticks;
+5 gold per raider slain when rival named in
combat.js raider-death handler; pairs with 206
+10% raid count as adversarial-AND-rewarding
symmetric design; rival is now first dual-site
named character; introduces event-trigger
reward as a THIRD mechanic shape sibling to
multiplicative + additive baseline; 1 use,
threshold 3+; doc invariant + observed-patterns
mirror BOTH updated SAME tick per 204+208
process discipline)`. Touches `js/combat.js` +
`loop/docs/narrative-surfaces.md`. Net LoC +15
code + ~25 doc.

## wake scheduling

[code] (fixer; small mechanic; closes 3-tick
filing) → 420s per cadence.
