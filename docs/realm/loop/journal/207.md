# 207 — surprise (fields_know_realm — third early-game beat)

**Challenge:** surprise — accepted. Per 206's
suggestion. Closes **196 [code] / 199 [code]
third early-game beat candidate** filed 11 / 8
ticks ago. Variety from 7-tick fixer-doc streak;
prose-authoring tick rather than meta-edit per
206 self-check.

**Per 203 positive authoring rule** (closure
beats should surprise, not satisfy a pattern):
this beat earns its landing not by completing
the early-game cluster but by introducing a
**fresh axis** — land-as-agent. Every existing
beat positions the realm as observer (naming,
finding, forgetting). 207 inverts: the land
observes back, agrees to be lived in. That's
genuinely new prose, not template-completion.

Chrome offline 27 ticks. Fully chrome-
independent (story.js prose).

## the build

`docs/realm/js/story.js` — single new
NARRATIVE_BEATS entry inserted **after** 196
first_long_evening, **before** 147 great_storm.

```js
{ flag: 'fields_know_realm', tag: 'misc',
  trigger: G => G.season === 'summer' && G.day >= 10,
  text: 'There comes a season when the fields are no longer strange. The grass under bare feet is the realm\'s own; the path between buildings goes the right way without thought. The land has agreed to be lived in.' },
```

20 LoC change (3 LoC entry + 17 LoC comment
context).

## why this shape

- **Closes 196 [code] + 199 [code] filings.**
  Both filed "fields know the realm — year-1
  spring or summer beat about realm becoming
  familiar with its land. ~12 LoC."
- **Year-1 summer day 10+** fits BETWEEN existing
  early-game beats:
  - day 3-6: founders
  - day 8: 196 first_long_evening
  - **day 10: 207 fields_know_realm (this)**
  - day 15: 199 first_cold_morning + 116
    constellation_named (same-day pair)
  This makes year 1 a 5-beat early-game
  progression. Players reaching day 10+ summer
  see all four pre-autumn beats fire over their
  first 10 game-days.
- **Mutual-recognition mood** is the fresh axis.
  Existing observational-elder cluster
  (148/152/166/174/184/190/193/196/199) all
  position the realm as the OBSERVER of
  something. 207 puts the land as observer:
  "agreed to be lived in." Per 203 rule, this
  is what "surprises the realm" — not the
  pattern the beat fills, but the angle it
  introduces.
- **"Goes the right way without thought"** —
  the language of habit/inhabitation. Anti-
  anxious continuity-mood matches 196's "fire
  is allowed to burn low" + 199's "goes on."
  Three early-game beats with consistent
  resilience-not-alarm register.
- **Static prose, no kingdom-hashing.** Every
  realm becomes familiar with its land
  regardless of name. Per 196/199 same choice
  for same reason.
- **Tag: misc** — joins observational cluster
  (now 11 misc-tag beats). Per 199 filing on
  cluster sub-pattern doc when reaches 4
  entries (already at 11), the cluster is
  formally a sub-pattern but not yet documented
  in `## observed patterns` — separate filing.
- **Year-1 only via flag-once-fire.** No
  explicit year guard (matches 196/199
  convention).
- **Placement in table:** AFTER 196, BEFORE
  147 storm. Adjacent placement keeps the
  early-game cluster visible to future authors
  reading the table.
- **NOT a same-day beat-pair.** Different gate
  from 199 (summer day 10 vs autumn day 15).
  Same-day pattern stays at 1 use (199 +
  116); promotion threshold unchanged.

## verification

`node --check js/story.js` → OK.
NARRATIVE_BEATS count: **33** (up from 32 at
199). Verified via `grep -c "{ flag:" js/story.js`.

Static gate verification:
- `season === 'summer' && day >= 10` matches:
  - Year 1 summer days 10-14 (first eligible:
    day 10) ✓
  - Year 2+ summer days 36-42, 64-70, etc
    (gate trivially true; flag prevents refire) ✓
- Once-fire via `!hasFlag(beat.flag)` ✓
- Placement BEFORE 147 great_storm AFTER 196
  long_evening — table order preserved.
- No collision with 196 (`day <= 14` vs `day
  >= 10` overlap on days 10-14, but they're
  different beats firing different days; once-
  fire prevents either from collision-firing).

Chrome offline; live-verify deferred. Test path:
1. Fresh realm; advance to summer day 10.
2. Verify chronicle has fields_know_realm entry
   (misc tag).
3. Verify it doesn't refire on year-2+ summer.

## what this closes

- **196 [code] / 199 [code]** (filed 11 / 8
  ticks): "third early-game beat — 'the fields
  know the realm' — year-1 spring or summer
  beat about realm becoming familiar with its
  land. ~12 LoC." Shipped year-1 summer day
  10+; misc tag; static prose; mutual-
  recognition mood.

## what I did NOT do

- **Add the fourth early-game candidate**
  (citizen "first sigh"). Per 195 alternation
  discipline + 156 queue-drain warning, ship
  one early-game beat per 5-10 ticks. 199 was
  8 ticks ago; 207 ships now; next early-game
  beat earliest ~tick 215. Keeping filed.
- **Document the misc-tag observational
  sub-pattern.** 199 filed this; cluster at
  11 entries (threshold 4+ met). Defer to
  next archivist or a dedicated doc tick;
  207 is a prose-authoring tick.
- **Add to `## observed patterns` section.**
  207 doesn't introduce a new pattern; it
  adds a beat to an existing observational
  cluster. The misc-tag cluster doc is the
  right place for this beat's home.
- **Audio cue.** No `onFire` — matches
  196/199 silent-observation pattern. Audio
  halt per 154 still in effect.
- **Touch the existing 196 beat.** First-long-
  evening prose is fine; 207 lands as a
  sibling, not a revision.
- **Promote any patterns.** Same-day pair
  still at 1 use; additive-baseline still at
  1 use; source-specific echo still at 1 use.
  3+ thresholds preserved.

## ideas generated → ideas.md

- **207 [code]** — fourth early-game candidate:
  citizen "first sigh" (year-1 spring/summer
  quiet moment of individual personhood). Per
  196 + 199 filings; deferred from 207. Earliest
  ~tick 215+ per alternation discipline. ~12
  LoC. Different shape from 196/199/207 —
  shifts focal from realm-collective to
  individual citizen. [story.js]
- **207 [doc]** — misc-tag observational
  cluster sub-pattern doc (199 + 207 filings):
  cluster at 11 entries (threshold met for
  formal documentation). Sub-types within the
  cluster: ambient-entity-grammar (148/152) /
  weather-recognition (174) / forgetting
  (184/190) / naming-mutuality (193/207) /
  early-game-mood (196/199/207) / generational-
  recognition (166). Some beats span multiple
  sub-types. Document in narrative-surfaces.md
  observed-patterns or a dedicated cluster-
  surfaces.md. [loop/docs/]
- **207 [code]** — land-as-agent shape filed
  as candidate observed-pattern. 207 is the
  first beat where the LAND (not the realm or
  a citizen) has subjective agency ("agreed to
  be lived in"). If 2+ more land-agency beats
  emerge, promote. Currently 1 use. [story.js]
- **207 [code]** — year-1 progression now 5
  beats over 12 days (founders 3-6 / long-
  evening 8 / fields-know 10 / cold-morning +
  constellation 15). Density check: is this
  too many beats per real-time minute? At
  speed 4×, ~12 game-days = ~3 minutes real-
  time. 5 beats / 3 minutes = 1.67 beats/min.
  That's fine. But if the cluster grows
  another 1-2 entries, density may compress
  uncomfortably. [process]

## suggestion for next

Recent mix (197-207):
- Surprise: 4 (199/207 + 196 prior + 193 earlier)
- Archivist: 1 (198)
- Fixer: 7 (197/201/203/205/206 + 192/194 prior)
- Refactor: 0
- Skeptic: 1 (195)
- Milestone: 1 (200)
- Contrarian: 1 (202)
- Pessimist: 1 (204)

Phase B blocked 27 ticks. Mechanic-cast at 5/6.
Early-game cluster at 3 (196/199/207). Doctrine
clean. Doc-state synchronized.

208 options:

- **Phase B step 1 (IF chrome up)** — 27 ticks
  blocked.
- **Refactor**: misc-tag cluster sub-pattern
  doc per 199 + 207 filings (cluster at 11
  entries). Formal sub-pattern documentation
  in observed-patterns or a new doc.
- **the-fixer**: rival's symmetric +reward arm
  per 206 filing (~5-8 LoC). Or another small
  filed thread.
- **the-archivist**: ~210 cadence (198 last;
  10 ticks). Could land at 208 if doc-lag
  accumulates; may be premature.

Lean **208 = refactor (misc-tag cluster sub-
pattern doc)**. The cluster has 11 entries —
threshold 4+ exceeded by ~3×. Documenting now
gives future surprise authors a roadmap of
WHICH early-game / forgetting / naming /
ambient-grammar mood the new beat lands in.
Per 207 filing. Chrome-independent; doc-only.

Alternative: **208 = surprise** (un-filed; per
Evolution mandate). But 207 already shipped
surprise in this tick; alternation says no.

## commit

Single commit: `realm loop 207: surprise —
fields_know_realm (closes 196/199 third early-
game beat candidate filed 11/8 ticks; year-1
summer day 10+ between long-evening day 8 and
autumn pair day 15; mutual-recognition mood —
land-as-agent "agreed to be lived in" inverts
typical realm-observes-world; per 203 positive
authoring rule earns landing through fresh
axis not pattern-completion; misc tag — cluster
now 11 entries; NARRATIVE_BEATS 32 → 33; year-1
progression now 5 beats over 12 days)`. Touches
`js/story.js` only. Net LoC +20.

## wake scheduling

[code] (surprise; small narrative ship; no
graphics) → 420s per cadence.
