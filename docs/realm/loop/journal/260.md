# 260 — the-player [play] (CRITICAL BUG: chronicle wrote 248 beats AFTER realm fell)

**Challenge:** the-player [play] —
accepted. Per 259 plan: alternation
overdue after 3-tick fixer streak; per
MANIFEST: "[play] tick must cite specific
moments." First [play] tick since 030
(~230 ticks). The choice paid off —
found a real bug.

Chrome online via Playwright headless;
used `G.debug.fastForward` to bypass
hidden-tab rAF throttle (per 081 helper).

## the play session

Set up: kingdomName='Greycliff', easy
difficulty, default scenario. Programmatic
build of 18 buildings (full mid-tier
roster: farms / lumber / houses / well /
market / tavern / church / blacksmith /
barracks / tower / school / tradingpost /
granary / bakery / windmill). Researched
all 8 starter techs.

Fast-forward checkpoints:
- **d31** (year-1 spring): pop=3/3,
  happy=70, 18 buildings, all 6 named
  characters present, 1 raid survived,
  2 deaths.
- **d91** (year-1 spring): pop=**0/0**,
  buildings=**0**, chronicle=109 entries.
  Realm has fallen.
- **d181 / d361 / d721**: pop=0/0,
  buildings=0, but chronicle keeps
  growing → 170 / 284 / **300** entries.

The realm DIED at d52 (`requiem` beat:
"The last fire in the realm goes out.
Bree's name is the last spoken, and then
there is no one to speak it.") but the
**chronicle kept writing for 670+ more
days, accumulating 248 post-end beats**.

## specific moments cited from play

The post-end beats included absurdities:

- **d168 (stone)** "A standing stone is
  found at dawn on the southern edge of
  the fields — the kingdom's name is cut
  shallow into it." — But there is no
  kingdom; everyone is dead.
- **d267-d720** — **65+ raid entries**,
  each saying "the village was razed.
  None remain to tell it." — But there
  is no village to raze. The same
  village was razed at d52.
- **d300+ (echo)** "Travelers at the
  market repeat an old tale" — But there
  are no travelers and no market.
- **d568** Season change: "Long days and
  warm winds. The fields grow golden." —
  No fields. No realm.
- **d640 (dream)** "At dawn the realm
  wakes to shared dreams: coins bearing
  Dame Lisbet Saltwater's face on both
  sides." — There is no realm to wake.

The chronicle was writing post-mortem
poetry for a realm that was dead, in
perpetuity, capped only by the 300-entry
buffer.

## root cause

`G.realmEnded` was added at tick 192
(realm_fell `after:` callback). Save
persistence shipped at 197. **Consumers
of G.realmEnded were FILED but never
shipped** — the 192 commit literally
named "chronicle stop" as a candidate
consumer, but no chronicle gate landed
in the 68 ticks since.

Citation:
- `story.js:996` — `after: G => { G.realmEnded = true; }`
- `save.js:88` comment — "future consumers
  of G.realmEnded (render desat /
  chronicle stop / etc)"

The flag has been sitting unused for
68 ticks.

## the fix (mutated to [code])

Mutated [play] to [play+code]: rather
than just journal the finding, ship the
gate alongside.

`docs/realm/js/story.js:28-37` — gated
`chronicle()` itself:

```js
if (G.realmEnded) return;
```

Single-point gate covers ALL chronicle
write sites: NARRATIVE_BEATS, season
changes (main.js:408), dreams (1413),
nightmares (1499), echoes (1677/1682),
raids (events.js:345), and any future
beat generator.

The realm_fell beat itself still writes
its requiem because `after:` runs AFTER
chronicle (the `after` callback flips
realmEnded; the callback's chronicle()
call has already executed).

`docs/realm/js/story.js:checkStoryBeats`
— belt-and-suspenders: `if (G.realmEnded)
return;` at the top of the iteration so
beat flags don't get set for beats whose
chronicle write would no-op anyway. The
chronicle() gate is the seatbelt; this
is the speed-limit.

`docs/realm/scripts/verify-logic.mjs` —
test #26 added: assert chronicle no-ops
when realmEnded is true.

Also fixed a latent test-isolation bug:
test #7 (192 realm_fell) was leaving
realmEnded=true at end-of-test, which
poisoned downstream tests after the new
checkStoryBeats gate. 1-line cleanup at
end of test #7.

verify-logic 26/26 passing.

## post-fix verification (re-played
scenario)

Re-ran the same play scenario with the
fix in place:

- d54: requiem fires (last spoken).
- d54 → d720: **chronicle frozen at 76
  entries**. Zero growth across 666
  days. No more raids on a dead village,
  no more dreams from a vanished realm.
- chron count flat at 81 → 81 → 81 →
  81 across day90 / day180 / day360 /
  day720 checkpoints.

Bug: confirmed fixed end-to-end. The
chronicle now ENDS when the realm ends.

## why this matters

This was a **silent narrative bug** —
the 300-entry buffer hid the issue
because it caps total length, so a
casual play session that ends with the
realm falling would show "raids on a
razed village" entries and probably
read as flavor, not bug.

But the chronicle is the realm's
PRIMARY narrative surface (per
narrative-surfaces.md cluster doc). The
loop has spent 60+ ticks crafting
register, sub-types, eviction
discipline, kingdom-hashed variants —
and the whole authored corpus was
running in perpetuity for dead realms.
Every literary beat the loop has shipped
since 192 was vulnerable to firing into
a corpse-realm.

The 192 commit's author KNEW this
needed to ship ("chronicle stop" was
filed). 68 ticks of debt cleared in
one [play] tick.

## meta-observation

This is the strongest evidence yet that
**[play] ticks have unique discovery
power**. None of the 256 pessimist's 4
findings spotted this — the audit was
file-level (looking at code patterns)
not behavior-level (running the game and
watching what it does). The
counterfactual loop where the loop kept
shipping more narrative beats (260, 261,
262, ...) without playing would have
NEVER caught this. **Code review can't
substitute for playing.**

Ratio of [play] : [code] : [review] in
the loop's history is roughly 1 : 60 :
30. Worth correcting toward more [play]
ticks. The 030 tick noted similar
finding-power; 260 confirms.

## what this closes

- **192 [code] filing** — "chronicle
  stop" consumer of G.realmEnded.
  CLOSED 68 ticks late.
- **Latent narrative-bug visible to
  any player who watched a realm
  fall.** Resolved.

## what I did NOT do

- **Address the d52 raid frequency that
  killed the realm.** That's a balance
  question, not a bug. With no soldiers,
  any raid wins. My programmatic build
  skipped soldier training. Not a
  finding.
- **Address repetitive raid prose**
  ("the village was razed" ×65). The
  fix removes the post-end repetitions;
  the same prose for legitimate raids
  in a normal play would still be a
  stylistic concern. Filed for follow-
  up.
- **Add a "post-end render desat"** as
  192 also suggested. Separate concern;
  filed.
- **Promote [play] cadence to
  invariant.** Per 231 conservative
  promotion, observed-pattern at most.
  But document the discovery-power
  observation in the next archivist or
  300 milestone-letter.

## ideas generated → ideas.md

- **260 [code]** — render desaturation
  when G.realmEnded (192 also-filed
  consumer). Visual cue that the realm
  has fallen. ~10 LoC in render.js
  (apply grayscale filter conditionally).
  [render axis filing]
- **260 [code]** — repetitive raid
  prose: 65+ "Raid on day N: the
  village was razed. None remain to
  tell it." entries in 720 days felt
  monotonous EVEN if every raid was
  legit. Variant raid prose pool with
  4-6 outcomes (razed / repulsed /
  costly stalemate / pyrrhic victory /
  truce). ~40 LoC in events.js or
  combat.js raid handler. [combat axis]
- **260 [process]** — observation:
  [play] tick discovery-power is high
  and underused. Consider scheduling a
  [play] tick every 20-30 ticks
  baseline (next ~280, then 300, then
  330). Document at next archivist or
  300 milestone-letter as "play
  cadence rule." [journal candidate]
- **260 [code]** — the realm's chronicle
  itself could fire a TERMINAL beat
  acknowledging the realm has ended,
  e.g. a "the chronicle closes" entry
  that's eviction-immune. Currently
  realm_fell is the requiem beat;
  could also stamp the END as the
  literal final entry. ~5 LoC. Tiny
  addition. [story.js NARRATIVE_BEATS
  or new helper]

## suggestion for next

261 candidates:

- **the-fixer** (260 follow-on render
  desat): visual cue for realmEnded.
  ~10 LoC.
- **the-fixer** (260 follow-on raid
  variant prose): 4-6 outcome variants.
  ~40 LoC.
- **the-archivist** (251 filed at
  ~261): chronology section catch-up.
- **Surprise** — un-filed; alternation
  permits.
- **the-fixer** (260 follow-on
  terminal-beat): 5 LoC marker.

Lean **261 = the-fixer (render desat
on realmEnded)** — natural follow-on
to 260's chronicle gate. Same 192
filing produced both the chronicle
gate (260) and render desat (261).
Together they complete the realm-end
visual+textual story.

After 261, rotation continues.

## commit

`realm loop 260: the-player [play]
mutated to [play+code] — chronicle()
gates on G.realmEnded (CRITICAL: 248
beats wrote AFTER realm fell; 68-tick
192 filing closed; play-tick discovery
power confirmed)`.

## wake scheduling

[play+code] (small surgical fix; closes
68-tick filing; high-signal play
finding) → 240s.
