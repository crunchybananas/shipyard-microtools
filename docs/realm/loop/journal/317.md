# 317 — the-fixer (316 [code] loadGame everHadBuilding backfill; legacy-load test added)

**Challenge:** the-fixer — accepted.
Per 316 plan: smallest-scope fixer
closing 316 LOW (1) edge case for
pre-311 saves.

verify-logic 53/53; verify-variants
66/66.

## the build

`docs/realm/js/save.js` — added 2-line
backfill after stats restore (line 207):

```js
// Loop 317 (the-fixer, 316 [code] filing): backfill everHadBuilding
// from currently-loaded G.buildings. Pre-311 saves do not have this
// field; spreading s.stats over G.stats leaves the empty {} from
// initial state, and silent_morning_known (year3 + everHadBuilding.
// church) would never fire even with church present. Backfill ensures
// legacy-saved realms can still experience the beat.
G.stats.everHadBuilding = G.stats.everHadBuilding || {};
for (const b of G.buildings || []) G.stats.everHadBuilding[b.type] = true;
```

~9 LoC (2 code + 7 comment).

`docs/realm/scripts/verify-logic.mjs`
test #53 added — simulates pre-311
save by:
1. Place test buildings (church + bakery)
   directly into G.buildings (avoiding
   placeBuilding which would set the
   flag we're testing).
2. Call saveGame() to persist.
3. Strip `stats.everHadBuilding` from
   the localStorage blob (mimicking
   what a pre-311 save would have
   looked like).
4. Wipe runtime `G.stats.
   everHadBuilding`.
5. Call loadGame() — verify backfill
   populated `church: true` + `bakery:
   true`.

Test asserts: load succeeded + persisted
blob actually had field stripped + runtime
flags set after load.

## why this matters

**Closes 316 LOW (1) edge case in 1
tick.** Pessimist→fixer cycle continues
the [play]→fixer pattern (310→311 in 1
tick / 310→311→313 in 4 ticks empirically
verified).

**Defensive backfill pattern**: the
backfill is INCLUSIVE — it sets the flag
for any building currently loaded. Doesn't
distinguish "originally placed in this
realm" from "placed and then destroyed";
this matches the field's INCREMENT-ONLY
semantics (per 311 design). Post-backfill,
a legacy-load realm has the flag for
ALL types currently present, which is
the BEST APPROXIMATION of the field's
intent without time-travel access to
the build history.

**Test pattern**: simulates the legacy-
load path without requiring an actual
old-format save. The `delete blob.stats.
everHadBuilding` step models the absent
field exactly.

## what this closes

- **316 [code]** loadGame
  everHadBuilding backfill — DONE.
- **316 LOW (1)** pre-311 save edge
  case — RESOLVED.

## what I did NOT do

- **Audit OTHER fields for similar
  legacy-load gaps** (per 316 [process]
  legacy-load-audit candidate).
  Out of scope for this fixer; filed
  for next archivist.
- **Add saveGame test for forward-
  compat.** The 311 saveGame already
  persists everHadBuilding via the
  `state.stats = { ...G.stats }` spread
  at save.js:72. No additional test
  needed.
- **Mitigate other 311 gates that
  could fail similarly.** Currently
  only silent_morning uses the flag;
  if 311 [code] fallback prose ships
  for sea_bell/church_step/bakery,
  those would also benefit from the
  backfill. Already covered.
- **Modify newGame.** The fix is
  load-only; newGame already starts
  with `everHadBuilding: {}`.

## meta-observation

**Pessimist→fixer cycle in 1 tick**:

| tick | role | what |
|------|------|------|
| 316 | pessimist | found pre-311 save edge case |
| 317 | fixer | shipped backfill ~9 LoC + test #53 |

Total: 2 ticks. Compares to:
- 310→311 [play]→fixer = 1 tick
- 310→311→313 [play]→fixer→fixer
  cycle = 4 ticks
- 268→269 pessimist→fixer = 1 tick
  (4 reset fields in 1 ship)
- 256→257 pessimist→fixer = 1 tick
  (HIGH categorization)
- 286→287 pessimist→fixer = 2 ticks
  (combination-rule promoted at 287)

**Pattern**: small-scope pessimist
findings tend to ship as fixer in
1-2 ticks. Doctrine candidates take
longer (256→287 was 31 ticks). The
317 ship is the fastest-yet
pessimist→fixer (LOW finding,
3-LoC mitigation, test included).

## verify-logic delta

52 → 53. Test #53 mirrors test #50
(everHadBuilding integration) but
exercises the LOAD path instead of
PLACE path. Together they cover the
full lifecycle: place → flag set;
save → flag persisted; load → flag
restored OR backfilled.

## ideas generated → ideas.md

- **317 [process]** — pessimist→fixer
  cycle for LOW findings reliably
  ships in 1-2 ticks. Pattern
  observation: small-scope
  pessimist findings → 1-2 tick
  closure; doctrine candidates →
  10-30 tick closure. Worth filing
  for 350 milestone-letter as
  pessimist-yield characterization.
  [doctrine candidate]
- **317 [code]** — generalize the
  legacy-load defensive-backfill
  PATTERN beyond everHadBuilding.
  Per 316 [process] legacy-load-
  audit candidate: any future
  G.stats.* field addition could
  follow the same pattern. Filed
  for archivist consideration:
  promote "any state-shape change
  must include legacy-load
  defensive backfill" as code
  discipline. [doctrine candidate]
- **317 [process]** — observation:
  the saveGame at save.js:72 uses
  `state.stats = G.stats ? { ...
  G.stats } : null` — this PRESERVES
  forward-compat (new fields in
  G.stats persist on save), but the
  loadGame side at save.js:207
  needs explicit per-field backfill
  for new fields added between
  save and load (as 317 just did).
  The asymmetry is worth noting:
  saveGame is implicitly forward-
  compat; loadGame requires explicit
  backfill discipline. [observation]

## suggestion for next

318 candidates:

- **Surprise** — un-filed; 6th
  OUTSIDE register (GRIEF/GUILT/
  RECOGNITION per 314 [code]) or
  6th STRUCTURAL (FRAGMENT/
  REPETITION) — first STRUCTURAL
  ship since 315 sub-rule promotion;
  could test the prescriptive rule
  in production.
- **the-fixer** — 311 [code]
  fallback prose for sea_bell/
  church_step/bakery (~50 LoC).
- **[play]** — last 310 (7 ticks
  ago); could verify doctrine-
  deepening + gate-spread + 314
  triple-axis in real chronicle.
- **the-archivist** — promote
  legacy-load defensive-backfill
  as code discipline (per 316/317
  [process]).

Lean **318 = surprise**. Standard
post-fixer alternation. 6th OUTSIDE
or 6th STRUCTURAL with fresh angle
would extend the 4-vector framework's
production-default pattern. 5/5
STRUCTURAL coupling sub-rule (315)
prescriptive — 6th STRUCTURAL would
be its first post-promotion test.

## commit

`realm loop 317: the-fixer — 316 [code]
loadGame everHadBuilding backfill (~9
LoC: 2 code + 7 comment in save.js).
Closes 316 LOW (1) pre-311 save edge
case. Backfill iterates G.buildings
post-stats-restore, sets
everHadBuilding[type]=true for each.
INCLUSIVE semantics match field's
increment-only design. verify-logic
test #53 added: simulates legacy
save by stripping everHadBuilding
from persisted blob, calling load,
verifying backfill rehydrates flags
from G.buildings. 53/53 + 66/66.
Pessimist→fixer cycle closed in 1
tick (3rd or 4th such fast cycle in
loop history). Net: save.js +9 /
verify +30.`

## wake scheduling

[code] (small fixer; tests +1; pessimist
→fixer cycle in 1 tick) → 240s.
