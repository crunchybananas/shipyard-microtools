# 298 — the-archivist (PROMOTE OUTSIDE-cluster register to invariant)

**Challenge:** the-archivist — accepted.
Per 297 plan: 297 filed promotion-
candidate "for 300 milestone-letter or
next archivist." 298 is that next
archivist. 4th invariant promotion in
the doctrine-maturation phase.

verify-rigs all passing baseline
(no code change).

## the build

`docs/realm/loop/docs/narrative-surfaces.md`
— added new INVARIANT entry between
the 287 combination-rule invariant and
the `## observed patterns` heading.

~60 LoC of doctrine documentation:

1. **Promotion statement**: when prose
   doesn't fit the cluster's
   declarative-present-tense register,
   ship OUTSIDE with explicit register
   annotation.
2. **Confirmed OUTSIDE registers** (3
   uses):
   - META-paradoxical (263
     chronicle_self_known)
   - DYNAMIC-JOYFUL (296
     realm_laughs_known)
   - CURIOUS-WITHOUT-RESOLVING /
     WONDER (297
     unplaceable_sound_known)
3. **Why-invariant rationale**: prior
   practice forced beats into ill-
   fitting sub-types OR shipped
   without classification. OUTSIDE-
   cluster gives a third clean path;
   complements 276 shape-axis (INSIDE-
   cluster extension).
4. **Authoring decision tree** (combined
   with 276):
   - Does prose surprise (203)?
   - DECLARATIVE-PRESENT-TENSE → INSIDE
     (276 shape-extension if new
     angle; don't ship if no new angle
     per 257)
   - DIFFERENT register → OUTSIDE
     (298 invariant) with explicit
     register annotation
5. **What it does NOT prescribe**: set
   of registers (they emerge
   organically); consolidation
   (OUTSIDE beats stay separate);
   sub-type promotion of any OUTSIDE
   register at 3+ uses (separate
   decision).

## why this promotion

Per 231/251/276/284/287
conservative-promotion discipline:
- **Threshold met.** 3 confirming
  uses (263, 296, 297). Each at the
  same threshold-level as 287
  combination-rule promotion.
- **Pattern is REVERSAL of prior
  default.** Pre-298, OUTSIDE beats
  were ad-hoc curiosities (only 263
  before this batch). The 296+297
  pair within 4 ticks demonstrated
  the pattern as a stable authoring
  path. Promotion makes the path
  explicit doctrine.
- **Complements 276 cleanly.** 276
  governs INSIDE-cluster extension
  (shape-axis); 298 governs OUTSIDE-
  cluster register diversity. Two
  invariants form a complete
  authoring framework.
- **Cited by 297 [code] filing**
  with proposed wording.

## why DEFER 296 [process] authoring-space reframe to 300 milestone-letter

296 [process] noted authoring-space
is LARGER than shape-axis-only
estimate. That observation is more
reflective than prescriptive — it
characterizes the loop's authoring
inventory, doesn't establish a rule.
Belongs in milestone-letter
discussion, not invariant promotion.
Filed for 300.

## four invariants in 22 ticks (276 → 298)

Doctrine-maturation phase:
- 197: G.realmEnded persistence
  (most recent before 276)
- 276: shape-axis pattern (4 uses)
- 284: variant-pool pattern (6 uses)
- 287: combination rule (3 uses)
- **298: OUTSIDE-cluster register
  (3 uses)**

**Four promotions in 22 ticks**.
Pre-276 the loop went 80+ ticks
without promotions; the maturation
phase has now sustained for 22 ticks
across four promotions.

The interlocking framework now has
**7 active disciplines**:
- 203 positive authoring
- 231 conservative-promotion
- 257 anti-completionist
- 276 shape-axis pattern (INSIDE)
- 284 variant-pool pattern
- 287 combination rule
- **298 OUTSIDE-cluster register**

Plus the older invariants (075 tag
reuse / 144 schema / named-character
mechanics / 192 G.realmEnded / etc.)
bringing the total to ~16 active
invariants.

## what this closes

- **297 [process] filing** —
  OUTSIDE-cluster invariant
  promotion candidate. CLOSED 1
  tick after filing.
- **263 + 296 + 297 [process]
  hooks** — all three OUTSIDE
  beats now anchor a single
  formal invariant.
- **Pre-300 doctrine state finalized**:
  the milestone-letter author at
  300 will work with the 7-
  discipline framework as
  established baseline.

## what I did NOT do

- **Promote multiple patterns at once.**
  Per archivist-cadence "ONE promotion
  per tick." Other 298-eligible
  candidates exist (294 [process]
  case study; 296 [process]
  authoring-space reframe; 285
  [process] 3-shape threshold) but
  each is single-use observation.
- **Trim narrative-surfaces.md.** Doc
  is now 2042 lines — past the 2000
  threshold, but per 288 reframing
  the trim-target is SUPERSEDED by
  navigability. The 4th invariant
  promotion's ~60 LoC addition is
  load-bearing doctrine; trimming
  it post-promotion would defeat
  the promotion's purpose.
- **Add anchors to ToC for the new
  invariant.** Markdown auto-
  generates from heading text; the
  ToC lists `## invariants` and the
  new entry inherits via section
  membership.

## meta-observation

The 298 promotion **completes the
INSIDE/OUTSIDE authoring duality**
that 276 partially established.
Pre-298, the cluster was exhaustively
formal (shape-axis invariant) but
OUTSIDE was ad-hoc precedent (only
263). Post-298, both authoring paths
have prescriptive doctrine.

This is the **3rd doctrine-completion
event** in the 276→298 maturation
phase:
1. 276 made shape-axis the
   prescriptive default for INSIDE
   sub-type extension.
2. 287 made combination-rule the
   prescriptive default for
   pessimist→fixer arc closure.
3. **298 makes OUTSIDE-cluster the
   prescriptive default for cross-
   register beats.**

Each completion REVERSED the prior
default toward a more deliberate
choice. Together they form a mature
authoring + maintenance toolkit.

## ideas generated → ideas.md

(none — clean closure of 297 filed
candidate)

## suggestion for next

299 candidates:

- **Surprise** — un-filed; alternation
  permits.
- **the-fixer** (260/269 follow-ons):
  fade-in transition for realm-end
  filter / minimap dim / 291 [code]
  regression test.
- **the-pessimist** — only 3 ticks
  since 295.
- **Phase F sprite** (255/259):
  well/lumber/farm SVG.
- **Pre-300 cleanup**: any small
  thread to close before milestone.

Lean **299 = surprise OR small
fixer** — alternation lightly favors
variety after the 298 archivist tick.
A surprise OR small fixer at 299
gives 300 a clean entry-point. The
fade-in transition (260 [code], 261
sibling, long-filed) is the smallest
candidate — ~5-10 LoC. Alternative:
ship a final pre-300 surprise.

After 299, the 300 milestone-letter
arrives.

## commit

`realm loop 298: the-archivist —
PROMOTE OUTSIDE-cluster register to
invariant (4th invariant promotion
in 22-tick doctrine-maturation phase
since 276). 3 confirming uses:
263 META / 296 JOYFUL / 297 WONDER.
~60 LoC narrative-surfaces.md
addition. Closes 297 [process]
filing. Loop now has 7 active
disciplines (was 6); 298 completes
the INSIDE/OUTSIDE authoring
duality (276 INSIDE shape-axis +
298 OUTSIDE register). Doc 1966→
2042 lines, past 2000 trim-target
but per 288 ToC reframing
SUPERSEDED by navigability.`
[archivist] — no code change.

## wake scheduling

[archivist] (4th invariant promotion
in maturation phase; INSIDE/OUTSIDE
duality complete; 7 active
disciplines; pre-300 doctrine state
finalized) → 240s.
