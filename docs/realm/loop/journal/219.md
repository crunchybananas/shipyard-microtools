# 219 — Phase B step 4 (hover halo + fog z-order)

**Challenge:** the-fixer (Phase B). Per 217+218
hypothesis: step 4 should be FREE because hover
halo and fog are SCREEN-SPACE LAYERS rendered
OUTSIDE `drawBuilding`. Verified.

Chrome online via Playwright headless.

## the build

`docs/realm/scripts/verify-phaseb.mjs` —
appended halo-mode verification:
1. Reset `G.season = 'spring'`.
2. Set `G.selectedBuilding = <castle from row>`.
3. Screenshot `phaseb-halo-svg.png` (SVG on +
   selected building).
4. Toggle SVG off; screenshot
   `phaseb-halo-canvas.png`.

Halo logic in render.js:1150 draws AFTER the
building loop. Fog logic at line 1111 draws
BEFORE the building loop (fog is a tile-edge
effect; buildings only exist in unfogged tiles).
Neither runs inside `drawBuilding` — both
compose via render-pass z-order.

## verification

Both screenshots show the gold pulsing diamond
outline around the selected castle:
- `phaseb-halo-svg.png`: castle as SVG sprite,
  halo on top.
- `phaseb-halo-canvas.png`: castle as canvas
  drawX, halo on top.

Toggle clean both directions; zero page errors.
Sprite cache 11/11 ready throughout.

## what this closes

- **216 [code] step 4** — hover halo + fog
  modulation. Free win from architectural
  separation (halos render outside
  drawBuilding).
- **210 task** (internal) — Phase B step 4.

## what I did NOT do

- **Touch render.js.** Step 4 is a hypothesis
  test, not a code ship.
- **Tile-grid placement-pulse verification.**
  The placement-grid pulse (line 1043) only
  fires when `G.selectedBuild` is set (player
  is placing a building). Out of scope here;
  that pulse is also screen-space layer so
  composes naturally.
- **Multi-zoom halo audit.** At very low zoom
  the halo diamond may not be visible. Filed
  for chrome-required visual review when more
  buildings exist in real play.

## suggestion for next

220 = **Phase B step 5 (per-instance variation)**.
This is the FIRST step that needs real code
work. The current SVG path uses `<img>` element
+ `ctx.drawImage`; CSS custom properties on the
SVG won't apply because img-tag SVGs don't
expose internal styling.

Two options:
- **Option A: Inline SVG** — fetch each sprite,
  inject as inline `<svg>` element, set CSS
  vars per instance. Heavier — needs DOM
  layer + per-instance management. ~50-100
  LoC.
- **Option B: Per-kingdom sprite cache** —
  generate variants at load time by string-
  replacing color tokens in fetched SVG text,
  cache the variants per kingdom hash. Simpler;
  preserves the `<img>` + `drawImage` path.
  ~30-50 LoC.

Option B is closer to the existing pattern.
Lean toward it.

If 220 lands clean, 221 = **step 6: flip
default = true**, completing the 6-step plan.

## commit

`realm loop 219: Phase B step 4 — hover halo +
fog verified on SVG path (no code change
needed; halos render OUTSIDE drawBuilding via
screen-space z-order; fog tiles only exist in
unfogged tiles by definition; verify-phaseb
extended with halo-mode 2 screenshots; gold
pulsing diamond visible on both paths)`.
Touches scripts/verify-phaseb.mjs only.

## wake scheduling

[review] (Phase B step 4 verify; no code) → 270s.
