# 189 — refactor-only (type-scale phase 5)

**Challenge:** refactor-only — accepted. Per 188's
plan: chrome offline (8 ticks now); pivot to non-
graphics. Phase 5 of the type-scale migration arc
(140 tokens / 143 chronicle / 150 phase 2 / 160
phase 3 / 169 phase 4 / 189 phase 5). 20 ticks
since 169.

## the build

**14 exact-match font-size migrations** across 8
panel surfaces in `index.html`. Largest single-
tick migration in the arc (vs 8 each in
150/160/169).

| Selector              | Before   | After          | Surface |
|-----------------------|----------|----------------|---------|
| `.diff-btn`           | 0.82rem  | `var(--fs-base)` | Title screen difficulty |
| `#scenario-desc`      | 0.75rem  | `var(--fs-sm)`   | Title screen |
| `.rate`               | 0.65rem  | `var(--fs-xs)`   | HUD rate display |
| `.at-title`           | 0.65rem  | `var(--fs-xs)`   | Achievement toast |
| `.ach-sum-row`        | 0.75rem  | `var(--fs-sm)`   | Achievement panel |
| `.ach-sum-note`       | 0.65rem  | `var(--fs-xs)`   | Achievement panel |
| `.ach-item`           | 0.75rem  | `var(--fs-sm)`   | Achievement panel |
| `.tut-skip`           | 0.65rem  | `var(--fs-xs)`   | Tutorial |
| `.trade-empty`        | 0.82rem  | `var(--fs-base)` | Trade panel |
| `.rp-sublabel`        | 0.65rem  | `var(--fs-xs)`   | Research panel |
| `.tech-card`          | 0.75rem  | `var(--fs-sm)`   | Tech panel |
| `.tc-desc`            | 0.65rem  | `var(--fs-xs)`   | Tech panel |
| `.rp-pct`             | 0.65rem  | `var(--fs-xs)`   | Research panel |
| `.tech-item`          | 0.75rem  | `var(--fs-sm)`   | Tech panel |

All zero perceptual shift (source values match
token targets pixel-for-pixel: 0.65=fs-xs,
0.75=fs-sm, 0.82=fs-base).

Net LoC 0 (14+/14-).

## why this shape

- **Exhausts the standalone exact-match pool.**
  After 189, only ONE exact-match remains: the
  bundled-line 520 `.tech-name` 0.82 + `.tech-
  cost` 0.65 sharing one tag with `.tech-icon`
  / `.tech-info` / `.tech-desc 0.7rem`.
  Surgical phase 6 tick can handle that
  precisely.
- **Larger batch than prior phases.** 150 +
  160 + 169 each shipped 8 migrations. 189
  ships 14 — broader sweep because the pool
  was clearly bounded (no risk of missing a
  panel cluster) and individual edits are
  trivial pattern-matches with zero LoC delta.
- **No near-exact migrations included.** The
  envelope held: still no design-shift
  decisions inside refactor-only. Filed for
  the deliberate design-choice tick (when
  someone wants 0.72→0.75, 0.8→0.82, 0.7→0.75,
  0.78→0.82 to actually ship).
- **Achievement panel cluster fully migrated.**
  3 of 14 hit the achievement surface
  (`.at-title` toast + `.ach-sum-row` +
  `.ach-sum-note` + `.ach-item`). Plus
  `.ach-icon` etc (1.3rem display tier) which
  140 left inline.
- **Tech + Research cluster mostly migrated.**
  `.tech-card` + `.tc-desc` + `.tech-item` +
  `.rp-pct` + `.rp-sublabel` shipped. Bundled-
  line 520 still has `.tech-name` + `.tech-
  cost` exact-matches (with `.tech-desc`
  near-exact 0.7→0.75 nearby).
- **Title screen completion.** `.diff-btn` +
  `#scenario-desc` migrated. Title screen now
  fully on tokens (152 migrated
  `.title-section-label` + `.scen-btn` +
  `#title-version`).

## verification

No js touched; no `node --check` relevant.

Static verification:
- Each replaced value matches token definition
  (`:root` lines 21-27).
- Each edit confined to `font-size:` value;
  no other CSS property touched.
- Git diff: 14 insertions + 14 deletions; only
  index.html.
- Remaining `font-size:0.(65|75|82)rem` count:
  **1** — the bundled line 520 selector cluster.

## what this closes

- **130 HIGH (phase 5)** — ongoing migration.
  Pre-189: ~26 of ~155 declarations migrated
  (~17%). Post-189: ~40 of ~155 migrated
  (~26%). Phase 6 (bundled line 520) closes
  the standalone exact-match arc; remaining
  near-exact migrations require deliberate
  design-choice tick.
- **169 [code] phase 5** — "phase 5 exact-
  match migrations: .diff-btn 0.82→fs-base,
  .rate 0.65→fs-xs, .at-title 0.65→fs-xs,
  .ach-sum-row 0.75→fs-sm, .ach-sum-note
  0.65→fs-xs, .ach-item 0.75→fs-sm, .tut-skip
  0.65→fs-xs, .trade-empty 0.82→fs-base,
  .rp-sublabel 0.65→fs-xs, .tc-desc 0.65→fs-
  xs, .rp-pct 0.65→fs-xs. ~11 more exact-
  matches." Shipped at 14 (exceeded estimate
  by including `.tech-card` + `.tech-item` +
  `#scenario-desc`).

## what I did NOT do

- **Bundled-line 520 surgical migration.**
  Last exact-match cluster pending. `.tech-
  name` 0.82 + `.tech-cost` 0.65 share a line
  with `.tech-icon` 1.4rem + `.tech-info` (no
  font-size) + `.tech-desc` 0.7rem (near-
  exact). Phase 6 needs precise string-match
  strategy (anchored regex via `Edit
  replace_all`, OR full-line replacement with
  ALL 5 selectors written exactly).
- **Near-exact migrations.** Still deferred:
  `.mission-progress` 0.72→0.75 (+4.2%),
  `.scen-name` 0.8→0.82 (+2.5%), `#missions
  h3` 0.8→0.82, `#tooltip` 0.72→0.75,
  `.tech-desc` 0.7→0.75 (+7.1% — borderline),
  `.sv-stats` 0.85→0.82 (-3.5%).
- **Display-tier consolidation.** 1.4rem+ left
  inline per 140 invariant.
- **Touch js, 3D, or SVG.** Pure CSS hygiene
  tick.
- **Live-verify.** Chrome offline; static-only.
  Visual debt continues — but this tick has
  ZERO perceptual shift so visual-debt risk
  is minimal here.

## ideas generated → ideas.md

- **189 [code] PHASE 6** — surgical bundled-
  line 520 migration. Strategy: anchored
  regex via `Edit replace_all` matching the
  full line, OR write the entire 520 line
  with `.tech-name` 0.82→fs-base + `.tech-
  cost` 0.65→fs-xs and `.tech-desc` 0.7→0.7
  (UNCHANGED — near-exact stays near-exact).
  ~3 char changes within a long line. Closes
  standalone exact-match arc. [index.html]
- **189 [code]** — design-shift tick: ship
  the 5+ near-exact migrations as a
  deliberate design choice (each is < 5%
  visual shift, but together they consolidate
  to 7 unique font-sizes vs current ~10).
  ~10 LoC changes; refactor-only OR
  design-shift framing. [index.html]
- **189 [doc]** — when phase 6 ships, close
  130 HIGH in ideas.md and document the
  type-scale migration arc total in
  render-layers.md or a dedicated
  `loop/docs/type-scale-arc.md`. [loop/docs/]

## suggestion for next

Recent mix (179-189):
- Code (sprite/mesh): 8 (179/180/182/183/185/
  186/187 + earlier)
- Surprise: 2 (174/184)
- Archivist: 2 (177/188)
- Fixer: 1 (178)
- Meta: 1 (181)
- Refactor: 1 (189)

Phase B blocked. SVG/3D queue exhausted.
Refactor variety just shipped.

190 options:

- **Phase B step 1 (IF chrome up)** — 9 ticks
  blocked.
- **Surprise non-founder** — 6 ticks since
  184; Evolution mandate window.
- **Phase 6 (bundled line 520)** — surgical
  hygiene; closes standalone exact-match arc.
- **Fixer** — pick an old idea (098 cover-art
  recipes doc; 116 audio cue if 154 halt
  resolved; etc).
- **Sibling forgetting beat (188 filed)** —
  one of constellation / wanderer / great-
  storm. 4-6 ticks since 184; not
  queue-drain risk.

Lean **190 = surprise** (Evolution mandate
window + variety after 2 archivist + 1 refactor
in last 4 ticks). OR **189 phase 6** to close
the migration arc.

If chrome stays offline past 191, the loop is
seriously visual-debt-burdened. **Continued
suggestion to user**: chrome connection
re-prompt would unblock Phase B + the visual-
debt batch (now 21 items + every refactor /
narrative tick since 178 also unverified).

## commit

Single commit: `realm loop 189: refactor-only —
type-scale phase 5 (14 exact-match migrations
across title / HUD / achievement / tutorial /
trade / tech / research panels; ~26% of font-
sizes now migrated; only bundled-line 520
exact-match cluster pending for phase 6)`.
Touches `index.html` only. Net LoC 0 (14+/14-).

## wake scheduling

[code] (refactor-only; phase 5 closes the
standalone exact-match pool) → 420s per
cadence.
