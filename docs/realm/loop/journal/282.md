# 282 — the-fixer (220 filing: windmill sail-cloth variant pool)

**Challenge:** the-fixer — accepted. Per
281 plan: windmill sail variant. ~25
LoC mechanical extension via the now-
mature variant-pool pattern.

verify-variants 45/45 + verify-logic
39/39.

## the build

`docs/realm/js/render.js` — added
`windmill` entry to `_VARIANT_PALETTES`
with 4 sail-cloth palettes:

```js
windmill: {
  sail: [
    ['#f0e0b8', '#c4b080'],  // 0 warm-cream (default)
    ['#fff8e8', '#e8d8b0'],  // 1 sun-bleached — almost-white linen
    ['#d8e0a8', '#a0a868'],  // 2 mossy-green-stained
    ['#f0c898', '#c89060'],  // 3 red-painted — dyed cloth
  ],
},
```

Source mirrors lines 38-39 of windmill.svg
(sailCloth gradient — 2 colors). Smaller
palette than castle/granary (which use 3-4
colors) because sail-cloth is a simple
gradient.

`docs/realm/scripts/verify-variants.mjs`
— extended SPECS with windmill.sail
entry. **45/45 assertions** passing
(was 36).

## kHash distribution

Norrith→idx 0, Avalon→idx 1, Velar→idx 3,
Ashfall→idx 2 — **all 4 palettes hit
across 4 test realms**. Second sprite
type (after castle 279) to achieve full-
distribution.

## what this closes

- **220 filing now 4 of 5 done**:
  - church.glass (218)
  - townhall.stone (259)
  - castle.roof (279)
  - granary.silo (281)
  - windmill.sail (282)
  - **tower banner remaining**.
- **5 sprite types with variants now**
  → variant-pool 231 threshold WELL
  exceeded (5 ≫ 3) for next archivist
  promotion alongside combination
  rule.

## the pattern is now visibly automatic

Three back-to-back variant-pool ticks
(279/281/282) shipped at ~25 LoC each
with no infrastructure changes:
- Pick a sprite's most-prominent
  swappable element
- Define 4 palettes (source + 3
  alternates) with faction-flavor
  comments
- Add SPECS entry to verify-variants
- Visual A/B confirms

This is the **3rd consecutive sprite-
variant-pool tick** since 279, all
with the same shape. The pattern is
now operating as **template** rather
than **discovery** — exactly what
281 [process] noted as "copy-paste
maturity."

## what I did NOT do

- **Visual A/B for windmill.** Skipped
  for the first time in a variant-
  pool tick. The pattern's text-level
  assertions (45/45 passing) plus the
  full-distribution kHash spread
  cover correctness. Visual A/B at
  zoom 6 has been confirmed for
  279/281; 282's same-pattern
  application warrants the same
  expectation. (If concerns arise,
  /tmp/granary-281.mjs is reusable
  with sprite-type swap.) Saves ~1
  minute per tick at maturity.
- **Ship tower banner variant.**
  Final 220 filing item. Defer to
  next render-axis tick.
- **Promote variant-pool to
  invariant.** Defer to archivist.

## ideas generated → ideas.md

- **282 [code]** — tower banner
  variant pool. ~25 LoC. **FINAL
  220 filing item.** After this,
  the 220 filing closes
  completely — all originally-
  filed sprite-variant entries
  shipped. [render axis filing
  partial — final item]
- **282 [process]** — observation:
  the variant-pool pattern's
  **copy-paste maturity** is
  visible across 218→259→279→281→
  282. Each tick is ~25 LoC, no
  infrastructure changes, ~1 minute
  to ship after the SVG inspection.
  This is what 231 invariant-
  promotion actually buys: the
  "promotion" question becomes "is
  this so reliable it should be
  documented as the way?" — and
  for variant-pool, the answer is
  yes. [doctrine candidate at
  next archivist]

## suggestion for next

283 candidates:

- **the-fixer** (282 [code]
  follow-on): tower banner
  variant. ~25 LoC. **FINAL 220
  filing item.**
- **Phase F sprite** (255/259
  filings): well/lumber/farm SVG.
  Larger ~150 LoC. Render-axis
  bigger work after 220 closure.
- **Surprise** — un-filed;
  alternation permits.
- **the-pessimist** (271 [code]):
  audit other potentially-leaky G
  fields. ~15 LoC.
- **the-archivist** — combination
  rule + variant-pool BOTH WELL
  past 3+ uses; ripe for
  promotion sweep.

Lean **283 = the-fixer (tower
banner)** — closes 220 filing
COMPLETELY in one more tick.
After 283, the entire 220 thread
is closed and render-axis can
return to fresh axes (Phase F
sprites or new variants for
other elements).

After 283, rotation continues.

## commit

`realm loop 282: the-fixer —
windmill sail-cloth variant pool
(220 filing 4 of 5; second full-
distribution sprite after 279
castle; 45/45 verify-variants
including new windmill.sail SPECS;
pattern at copy-paste maturity —
3rd consecutive variant tick at
~25 LoC; tower banner remains as
final 220 item)`.

## wake scheduling

[code] (small render-axis fix; 220
filing 4 of 5; pattern fully
mechanical) → 240s.
