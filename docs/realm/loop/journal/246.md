# 246 — surprise (first_thaw_known — un-filed weather-memory beat)

**Challenge:** surprise — accepted. Un-filed
beat. Per 245 plan: break 4-tick fixer streak
(242/243/244/245); per 213 diversification:
prefer non-misc tag.

Chrome online via Playwright headless.

## the build

`docs/realm/js/story.js` — single new
NARRATIVE_BEATS entry inserted after 174
first_frost_marked (its seasonal sibling).

```js
{ flag: 'first_thaw_known', tag: 'event',
  trigger: G => G.storyFlags.year2 && G.season === 'spring' && G.day >= 31,
  text: 'The first warm day of the second year arrives. The realm goes barefoot to the river. The water is colder than memory expected; somehow this is reassuring.' },
```

19 LoC change (3 entry + 16 comment).

## why this shape

- **Sibling to 174 first-frost.** Both
  year-2+ once-per-realm seasonal-memory
  beats — but OPPOSITE seasonal boundary.
  174 fires year-2 autumn day ≥ 45; 246
  fires year-2 spring day ≥ 31. Together
  they bracket the realm's
  remembering-the-year-cycle: cold-arrives
  + warm-returns, both noticed-differently
  the second time.
- **tag:event for 213 diversification.**
  9 misc-tag surprises since 207; 246 is
  the second non-misc tag in the recent
  diversification queue (after 240 bard
  character). Matches 147 great-storm
  weather-memory pattern (also tag:event).
  Echo-eligible per 060/063.
- **"Colder than memory expected; somehow
  this is reassuring."** Small psychological
  observation: weather memory is unreliable,
  and the realm finds the unreliability
  itself reassuring (means winter wasn't
  as bad as remembered, or memory itself
  has softened, or both — left ambiguous).
- **Closing line lifts beyond literal moment**
  per 184/199/240 craft pattern.
- **"The realm goes barefoot to the river"**
  — communal action, no specific characters,
  matches the observational-elder register
  even though tag is event not misc.
- **No META-frame** per 213 finding 2.
- **Year-2+ via storyFlags.year2 explicit
  gate.** Avoids ambiguity at year-1 spring
  (the FIRST thaw isn't this beat — the
  realm doesn't have memory of a prior
  winter then).

## verification

`verify-logic.mjs` 19/19 (added test 18).
Beat fires when year2 set + spring + day=32;
chronicle entry contains "first warm day"
pattern; tag event. Zero page errors.

`grep -c "{ flag:" js/story.js` → 42 (up
from 41).

## what this closes

- **237 task** (internal). Un-filed beat
  shipped per Evolution mandate.
- **9-tick narrative gap since 240 bard**
  (240/245 narrative; 241-244 process/code/
  fixer; 246 returns to fresh narrative).

## what I did NOT do

- **Add a year-3+ "second-thaw" deepening
  beat.** Single year-2 thaw beat is
  sufficient; multiple thaw beats would
  saturate the seasonal-memory cluster.
- **Touch 174 first-frost.** Its prose +
  gate stay as-is. 246 is sibling, not
  amendment.
- **Add more weather-memory to year-3+
  cycles.** Filed possibility: year-3+
  bracketing cycle (frost-of-year-3,
  thaw-of-year-3) where the realm has
  TWO winters in memory. Defer.

## ideas generated → ideas.md

- **246 [code]** — year-3+ "second cycle"
  weather memory: when the realm has
  experienced 2+ winters, a beat about
  having multiple-winters in memory
  (the way each winter remembers
  differently). Different shape from
  first-thaw / first-frost (which mark
  the FIRST). Could pair with the
  observational-elder cluster's
  forgetting sub-type. Earliest year-3
  spring/autumn. ~12-15 LoC. [story.js]
- **246 [doc]** — observation: 174 +
  246 form a YEAR-CYCLE PAIR (frost +
  thaw) — pattern: year-2+ once-fire
  beats marking each seasonal boundary
  with memory-of-prior-cycle texture.
  If a 3rd year-cycle pair emerges
  (e.g., year-2 first-shadow at autumn
  equinox + year-2 first-light at
  spring equinox), promote to observed
  pattern. [loop/docs/]

## suggestion for next

247 candidates:

- **Townhall.svg sprite** (243 filing,
  Phase E candidate, ~2 ticks).
- **Variant entries for remaining sprites**
  (220 filing — 5 still need variants).
- **Another character interiority**
  (240/245 filing — teacher/merchant/
  rival remain). ~15-20 LoC each.
- **the-archivist** — 12 ticks since 237;
  due at ~249 by rhythm. One more tick
  before the cadence trigger.
- **the-skeptic** — ~33 ticks since 213;
  could re-evaluate loop discipline given
  the recent fixer + narrative streak.

Lean **247 = teacher interiority moment**
per 240 filing. Closes another character
moment; ~15-20 LoC; chrome-independent.
After teacher, only merchant + rival
remain (3 of 4 shipped after 247).

## commit

`realm loop 246: surprise — first_thaw_known
(un-filed un-misc-tag year-2+ weather-memory
beat; sibling to 174 first-frost at OPPOSITE
seasonal boundary; tag:event matches 147
great-storm pattern; "colder than memory
expected; somehow this is reassuring" small
psychological observation; closes 4-tick
fixer streak per 245 plan + 213 diversification
queue; NARRATIVE_BEATS 41→42; verify-logic
19/19)`. Touches js/story.js + verify-logic.

## wake scheduling

[code] (small narrative ship; closes recent
fixer streak with un-filed surprise) → 240s.
