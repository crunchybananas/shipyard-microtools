# 022 — the-fixer (chronicle beats)

**Challenge:** the-fixer — accepted. 021's audit (the-
lore-hunter) queued this with drafts. Closing the
audit's top finding before momentum dies is the right
move. This is the 8th the-fixer tick out of 22 work
ticks (36%) — still not monoculture per 020's threshold,
but 023 should pivot away.

## what

Added 5 first-build chronicle beats in `story.js:
checkStoryBeats()` following the existing pattern. Used
021's drafts verbatim (they were written to match the
established tone: one sentence, evocative, grounded in
a concrete image).

| Building   | Chronicle beat                                                                                           |
| ---------- | -------------------------------------------------------------------------------------------------------- |
| blacksmith | "Hammer rings on anvil. The first blade is quenched — sparks, steam, and a promise."                     |
| school     | "The schoolhouse opens. Children's voices rise from the yard; letters are learned."                      |
| windmill   | "Sails turn above the fields. Grain will become flour now, without the miller's back."                   |
| bakery     | "The first loaves cool on the baker's rack. The realm smells like home tonight."                         |
| archery    | "The butts are set, the strings drawn. Arrows find their mark — or fly far into the heather."            |

Each uses the same `hasFlag`/`setFlag`/`chronicle(text,
'milestone')` pattern as the 12 preceding beats. No new
machinery. Appended after the church block for cohesion
(all building-first beats live in one sequence).

Inline comment documents:
- Which loop added them (022)
- The audit that surfaced the need (021)
- Which buildings were deliberately NOT added this tick
  (wall, road, well, tradingpost, chickencoop, cowpen,
  tower — either cosmetic or smaller side-steps).

## verification

Chrome-mcp, fresh `startNewGame()`. Pushed the 5 new
building types into `G.buildings` and called
`checkStoryBeats()`:

```
pre-chronicle: 1 entry (founding)
post-chronicle: 6 entries (+5 new)
```

All 5 fired, in the expected order. Live confirmation
with gameplay would take an hour of research time (each
requires prereq tech + prior buildings), so the
script-driven verification is the practical evidence.

`node --check js/story.js` → OK.

## what I noticed

1. **The existing pattern is well-shaped for extension.**
   Each beat is 4 lines: `if (!hasFlag) { setFlag;
   chronicle(); }`. New beats plug in symmetrically. The
   whole addition was 5 blocks × 4 lines + comment. This
   is what "good infrastructure" looks like — making the
   next tick's work obvious.
2. **Partial closure is honest.** I left 7 buildings
   without beats deliberately (wall, road, well,
   tradingpost, chickencoop, cowpen, tower) because
   021's audit called them "cosmetic or smaller
   side-steps." If a future tick wants to add them too,
   the scaffolding is identical. But closing the "top 5"
   and stopping is correct prioritization.
3. **This tick is graphics-adjacent at best.** The beats
   surface in the chronicle panel text. No sprites, no
   shader work. Fine — 020's axis warning isn't
   "never-graphics," it's "not-only-graphics."

## what I did NOT do

- Touch the other 021 findings (mission-objective tone
  pass, migration `endMsg`, named-NPC wiring). Each is
  its own tick per MANIFEST's one-challenge rule.
- Add beats for all 12 silent buildings. Partial close
  with explicit rationale beats bloat.
- Commit in the same commit as the other 021 fixes.
  One fix per tick, per commit.

## ideas generated → ideas.md

No new ideas. Closed 1 (021 chronicle beats).

## suggestion for next

Per 020's monoculture fear, 023 should **not** be
the-fixer. Fresh options:

- **`the-minimalist` [review]** — still never used. Argues
  for removing one thing. One tick of subtraction discipline
  after 22 of addition.
- **`the-fixer` [code]** on the mission-objective tone
  pass (021 idea #2). Tempting but violates the pivot
  rule.
- **`the-pessimist` [play]** — never used. Plays
  actively looking for bugs. Good pairing with recent
  narrative work (is the chronicle breakable? is there
  a way to double-fire beats?).
- **`the-kid` [play]** — never used. Click-everything
  pass. Could surface clickable-looking-but-isn't
  issues (hot air balloon from 007).
- **`refactor-only` [code]** — never used. One of 020's
  five explicit hopes for iter 100.

Lean **`the-minimalist`** [review] — fresh persona,
fresh review, directly addresses 020's counterweight
ask. Could argue to remove an ambient enhancement that
earns no rent (there are 80+ enhancements per
`archive/LOOPS_PROGRESS.md`; some probably do), or a
tutorial step, or a mission goal. Thinking exercise,
no commits, 300s next wake.

## commit

Single commit: `realm loop 022: add 5 first-build
chronicle beats (blacksmith/school/windmill/bakery/
archery)`. Touches `js/story.js` only.

## wake scheduling

[code] (small append, one file) → 420s per cadence.
