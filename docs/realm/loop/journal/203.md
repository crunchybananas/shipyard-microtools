# 203 — the-fixer (ships 202's mutation — demotes 188 invariant)

**Challenge:** the-fixer — accepted. Direct closure
of 202 contrarian's recommendation. Doc-only;
chrome-independent. Maintains the loop's discipline
that contrarian critiques produce concrete follow-
ups (011→012; 154→155+156; 195→196; 202→203).

Chrome offline 23 ticks.

## the build

Six structural updates to
`loop/docs/narrative-surfaces.md`:

### 1. New `## observed patterns` section

Inserted between `## invariants` (line 514) and
`## known gaps` (line ~634). Distinguishes:
- **invariants** — prescriptive: what the realm
  must do.
- **observed patterns** — descriptive: what the
  realm has done. Promote to invariants only
  when prescriptive AND consistently applied.

Four entries seeded into the new section:
- **Object-arc closure (observation, not template)**
  — re-frames stone's 4-beat (056/079/122/184) and
  constellation's 2-beat (116/190) as DIFFERENT
  shapes called the same name. Names the prior
  188-invariant framing; cites 202 contrarian
  rejection. Replaces "earned closure" with the
  positive authoring rule: *closure beats should
  surprise the realm, not satisfy a pattern.*
  Explicitly notes wanderer / great-storm / night-
  shape / frost / long-evening / cold-morning are
  NOT obligated to close. Year-3+ + citizensBorn
  ≥ 10 retained as guidance (when closure IS being
  authored), not enforcement.
- **Same-day beat-pair pattern** (199 cold-morning
  + 116 constellation, 1 use, promote at 3+).
- **Additive-baseline vs multiplicative-bonus for
  named-character mechanics** (201 bard additive +
  101/102/105+153 multiplicative, 1 additive use,
  promote at 3+).
- **Source-specific echo branch** (194
  constellation, 1 use, promote at 3+ → registry
  refactor).

### 2. Removed 188 invariant from `## invariants`

The "Object-arc closure template (184)" passage
deleted from the invariants list (lines 612-632
in the pre-203 doc). Replaced with the new
observed-pattern entry.

### 3. Updated 190 references throughout

Three call-sites that called 190 the "second use of
188 template" now read:
- Line 113-119: 190 framed as "2-beat closure"
  with note that 202 rejected the template framing
  and 203 demoted.
- Line 252-262: same — 190 description in the
  NARRATIVE_BEATS section. "SECOND USE of 188
  object-arc closure template" → "Closes the
  constellation arc as a 2-beat cycle ... 202
  contrarian rejected that framing — stone's
  4-beat and constellation's 2-beat are different
  shapes — and 203 demoted the template to
  observed-pattern."
- Related-loops chronology entry for 190.

### 4. Updated 188 chronology entry

The 188 entry in `## related loop references` now
reads "added the object-arc closure passage
(originally promoted to 'invariant'; 203 demoted
to observed-pattern per 202 contrarian)" — a
simple historical note that maintains the chronology.

### 5. Added entries 199-203 to chronology

Five new chronology entries:
- 199 first_cold_morning (NARRATIVE_BEATS 32;
  same-day pair note)
- 200 milestone letter (4th in chain)
- 201 bard mechanic (additive baseline pattern
  filed)
- 202 contrarian (6-point case against 188)
- 203 (this update; ships the mutation; documents
  what changed)

### 6. Status line updated

`Status: ... 188, 198, 203` — adds 203 to update
roster.

Net doc LoC: +164 / -49 = **+115 lines**.

## why this shape

- **Doc edit, not story.js change.** 202's case
  was about META-claims (the doctrine label), not
  the prose. Stone arc beats remain great; 184/190
  forgetting beats remain shipped. Only the
  framing shifts.
- **New section structure preserves history.**
  Rather than nuking the 188 passage, the new
  `## observed patterns` section explicitly notes
  the demotion path (188 invariant → 202 contrarian
  → 203 demoted). Future ticks can read the
  reasoning. **A doc edit that erases its own
  history is worse than the original mistake.**
- **Positive authoring rule replaces "earned
  closure."** Per 202: "earned" was unfalsifiable.
  The replacement — *closure beats should surprise
  the realm, not satisfy a pattern* — has teeth.
  Future ticks proposing forgetting beats can be
  rejected on the grounds "this is template-
  completion, not surprise."
- **Three sibling observations seeded into
  the new section.** 199 same-day pair, 201
  additive-baseline, 194 source-specific echo
  were already filed at 1 use with 3+ promotion
  threshold. Putting them in the same section
  IMMEDIATELY makes 188 the outlier visible in
  context: 188 was promoted at 1 use; the
  others wait. Now they all sit together,
  filed identically.
- **Chronology updated through 203.** Prior
  archivist tick (198) ended at 198; 203
  chronology adds 199/200/201/202/203 entries.
  Future archivist (likely ~210) will catch up
  beyond 203.
- **NO PROSE CHANGES TO ANY NARRATIVE_BEATS
  ENTRY.** This is critical: 202 contrarian
  said the prose is fine. 203 only edits doc
  framing. js/story.js untouched.

## verification

`grep -c "## invariants" narrative-surfaces.md` →
1 (unchanged). The invariants section still
exists; only 188's passage moved out.
`grep -c "## observed patterns" narrative-surfaces.md`
→ 1 (new section).
`grep -c "188 invariant" narrative-surfaces.md` →
0 references to 188 as INVARIANT (was 5 before;
all updated to "188 originally framed... 202
contrarian rejected... 203 demoted").
`grep -c "second use of object-arc closure" narrative-surfaces.md`
→ 0 (was 2; both reframed).

Doc structure intact:
- ## why this exists ✓
- ## tag inventory ✓
- ## 20 chronicle-writing systems ✓
- ## execution order ✓
- ## invariants ✓ (now 188 absent)
- ## observed patterns ✓ (NEW; 188 + 3 siblings)
- ## known gaps ✓
- ## retired hypotheses ✓
- ## cadence summary ✓
- ## how to update ✓
- ## related loop references ✓ (extended through 203)

## what this closes

- **202 [doc] PRIORITY** — "narrative-surfaces.md
  edit per 202 contrarian: demote 188 object-arc
  closure from 'invariant' to 'observed pattern';
  relocate to new `## observed patterns` section;
  remove 'template tolerates either' patch
  language; replace 'earned closure' with positive
  authoring rule." Shipped exactly as filed.
- **202 [doc]** — "new `## observed patterns`
  doc section distinguishing observations from
  invariants." Shipped with 4 seeded entries.

## what I did NOT do

- **Touch story.js.** Per 202: prose is fine; only
  meta-claims were wrong. Zero code changes.
- **Sweep all "invariants" against the 3+ rule**
  (202 [process] filing). Out of scope for a
  fixer tick; that's an audit better done by the
  next archivist who can review structurally
  rather than focally.
- **Document the closure-pressure check
  discipline** (202 [process] filing). That's a
  process-doc, not a content-doc edit; defer.
- **Remove the "Filed: constellation 116 +
  wanderer 048/148 + great-storm 147 are
  candidates for forgetting beats at year 4-5+"
  language entirely.** The candidates remain
  filed in ideas.md; demoting that passage out of
  invariants is sufficient. The forgetting beats
  may still ship — but only when the prose
  surprises (per the new positive rule), not as
  template-completion.
- **Re-litigate 184 / 190 prose.** Both shipped;
  both fine. Doc edit only.
- **Promote any of the 3 sibling observations.**
  199 / 201 / 194 still at 1 use; threshold
  honored.

## ideas generated → ideas.md

- **203 [doc]** — when next archivist tick lands
  (~210 per 12-15-tick rhythm), sweep all current
  invariants against the 3+ promotion threshold.
  202 contrarian flagged 188 as the visible miss;
  others may exist (silent-walker grammar at 148+
  152 = 2 uses; named-character mechanic
  multiplicative pattern at 3 uses — meets
  threshold; etc). [loop/docs/]
- **203 [doc]** — when a forgetting beat is next
  proposed (wanderer / great-storm / landmark
  closure), reviewer applies the positive
  authoring rule explicitly: "does the prose
  surprise the realm, or satisfy a pattern?" If
  the only justification is "the template called
  for closure," reject. [process]
- **203 [doc]** — observation pattern: if a
  contrarian critique produces a concrete
  mutation that ships in the very next tick (202
  → 203), that's a sign of HEALTHY doctrine
  pruning. Document this as a pattern of its own
  if 3+ contrarian-then-fixer pairs accrete in
  loop/docs/. So far: 011→012 (lighting tint),
  154→155+156 (audio halt + grammar invariant
  doc), 195→196 (early-game beat), 202→203 (188
  demotion). 4 pairs already. PROMOTE candidate
  at 5+. [loop/docs/]

## suggestion for next

Recent mix (193-203):
- Surprise: 4 (193/196/199 + 190 prior)
- Archivist: 1 (198)
- Fixer: 5 (192/194/197/201/203)
- Refactor: 1 (191)
- Skeptic: 1 (195)
- Milestone: 1 (200)
- Contrarian: 1 (202)

Phase B blocked 23 ticks. Doctrine pruned (188
demoted; new observed-patterns section with 4
filed entries waiting for 3+ promotion).

204 options:

- **Phase B step 1 (IF chrome up)** — 23 ticks
  blocked.
- **the-pessimist** — 200 flagged neither
  contrarian nor pessimist had run since ~150.
  202 covered contrarian; pessimist still
  outstanding. Adversarial bug-hunt against
  recent ships (192/194/197/199/201/203 candidates).
- **Refactor** — 199 filed misc-tag cluster
  sub-pattern doc when it reaches size 4
  (currently 10; threshold met). Could be a
  sub-doc or a section in narrative-surfaces.md.
- **Surprise** — variety per Evolution mandate.
  But 195 cadence flag still applies; bias
  early-game or non-narrative.

Lean **204 = the-pessimist**. Adversarial
sibling to 202 contrarian; 200 explicitly
flagged neither had run; recent ships are
all chrome-unverified (visual-debt batch ~22
items + 5 new logical-correctness ships
192-203). Pessimist hunts for actual bugs in
those ships rather than meta-claims about
them. Pure review tick; chrome-independent.

## commit

Single commit: `realm loop 203: the-fixer —
ships 202 mutation (demotes 188 object-arc
closure from invariant to observed-pattern;
new ## observed patterns doc section with 4
seeded entries — object-arc closure / same-day
beat-pair / additive-baseline / source-specific
echo, all at 1-2 uses with consistent 3+
promotion threshold; "earned closure" replaced
with positive authoring rule; chronology
extended through 203; story.js untouched)`.
Touches `loop/docs/narrative-surfaces.md` only.
Net doc LoC +115 (164+/49-).

## wake scheduling

[code] (fixer; doc-only; closes contrarian
follow-up) → 420s per cadence.
