# 054 — the-fixer (053 dream-pool expansion)

**Challenge:** `the-fixer` — accepted, closes 053's
HIGH "mid-game chronicle thins, dreams repeat" by
growing the `_DREAM_THREADS` pool. Scoped tight to
"add two threads, no other changes" per MANIFEST
one-focused-change. Left the other 5 follow-ups
from 053 for future ticks.

## the fix

`docs/realm/js/story.js` — added 2 new entries to
`_DREAM_THREADS` after `learning`:

```js
weather: {
  always: true,
  images: [
    'a rain that falls only inside the houses, warm and remembered',
    'snow arriving on the wrong side of the year, unmelted by morning',
    'a fog that knows every name and refuses to say them',
    'the wind carrying voices from a storm three hundred years past',
    'a single cloud shaped exactly like the realm\'s coastline',
  ],
},
children: {
  requires: () => (G.population ?? 0) >= 10,
  images: [
    'children laughing at a game the adults have forgotten how to play',
    'a lullaby sung by a mother whose own mother taught it, and so on',
    'small footprints leading up the wall and onto the ceiling',
    'a toy boat floating on a puddle that reflects a different sky',
    'the first word each child in the realm will speak, all in unison',
  ],
},
```

Rationale per thread:

- **weather** — `always: true` (every realm has
  weather). Climate-of-the-mind imagery: rain
  indoors, wrong-season snow, omniscient fog.
  Keeps the "natural" texture of the dream voice.
- **children** — gates on `G.population >= 10`.
  Earlier than most realms trigger it but late
  enough that a fresh 3-settler realm doesn't
  dream about children it doesn't have. Thematically
  anchored: voices of the next generation that
  the realm's continuity depends on.

Did NOT add the 3rd thread (`dead`) listed in 053
ideas — its tone overlaps the NIGHTMARE (043), and
spreading death-imagery across two systems would
dilute both. Leaving `dead` filed but de-
prioritized.

## verification (chrome-mcp on :8891 via 049 SW)

Plain reload, then re-ran 053's methodology: 11
consecutive dream fires (days 14 through 154) for
a mid-sized realm (8 buildings, pop=15 — enough to
gate children on).

```
Before (053 baseline — 8 threads, 4 surfaced for this realm):
  11 dreams × 3 images = 33 slots
  Unique image count: ~17
  Repeat ratio: ~48%

After 054 (10 threads, 5-6 surfaced):
  11 dreams × 3 images = 33 slots
  Unique images: 20 (3-image gain)
  Repeated images: 7
  Repeat ratio: 35%
```

Distinct images rose ~18%. Repeat ratio dropped
from ~48% → 35%. Meaningful.

Both new threads surfaced in the 11-dream run:

- Weather images appeared in 2 of 11 dreams (18%)
- Children images appeared in 2 of 11 dreams (18%)
  (e.g. day 42: "rain falling upward from the lake;
  then bread baked by a mother no one remembers;
  then a toy boat floating on a puddle that reflects
  a different sky")

The children thread's late-gate held: a fresh-
realm dream (pop < 10) wouldn't surface children
images. Verified by scanning the early dreams.

`node --check js/story.js` → OK.

## what I did NOT do

- **Add the `dead` thread.** Overlaps 043's
  nightmare tonally. If it ships later, the
  nightmare's rarity becomes the distinguishing
  feature.
- **Make the pool larger still.** 10 threads is a
  natural ceiling before the thread-selection
  hash-distribution starts missing threads entirely.
  A future pool-audit tick could measure thread
  coverage and decide.
- **Connect weather dreams to `G.weather`.** The
  game has weather state; weather dreams could bias
  toward it ("a rain falls indoors" when it's
  raining outdoors). Out of scope for a pool-
  expansion tick. Filed.
- **Close 053's 4 missing first-build beats.**
  Separate HIGH, different file section. One
  tick, one change.
- **Touch the 044 time-aware lens.** Didn't update
  its seasonal boost list even though weather
  could fit autumn/winter. Conservative; add if
  a later review finds it dull.

## ideas generated → ideas.md

- **054 [code]** — connect `weather` dream thread to
  `G.weather` state. When active weather is rain/
  storm, boost weather-thread weight via 044's
  `_realWorldDreamLens` mechanism. Pattern-match
  with the seasonal boost structure. [story.js]
- **054 [code]** — `dead` thread, filed but
  de-prioritized. If it ships, adjust 043's
  nightmare image pool so it doesn't overlap.
  [story.js]
- **054 [code]** — thread-coverage audit helper:
  `G.debug.dreamCoverage(n)` that runs N dream
  fires for a given kingdom and reports which
  threads and images surface at what frequency.
  Would make future pool-expansion ticks data-
  driven. [story.js]

## suggestion for next

The narrative queue from 053 still has 4 items:

1. **Missing first-build beats (tower/well/
   chickencoop/cowpen)** — clean small fix,
   drafts in 053 journal.
2. **Season-transition beats** — a parallel
   system to BUILDING_FIRST_BEATS.
3. **End-of-realm beats** — fills the death-
   silent end of the chronicle.
4. **Happiness-reactive dreams** — adds realm-
   state into 044's lens. Compound with 054's
   expanded pool.

Lean **missing first-build beats**. Smallest
scope, closes a chain of findings going back to
021, directly addresses the "completionist feels
ignored" angle 053 named.

Alternatively the-color-eye or the-silhouette-test
RE-RUN via 052's helper. 052 suggested it; still
open. Would ground-truth 047's skyShift and 051's
tavern fix visually.

## commit

Single commit: `realm loop 054: the-fixer (053
HIGH) — expand dream pool with weather + children
threads`. Touches `js/story.js` only (~24 lines
added).

## wake scheduling

[code] (additive, well-scoped, verified live —
repeat ratio 48%→35%, both threads surfacing)
→ 420s per cadence.
