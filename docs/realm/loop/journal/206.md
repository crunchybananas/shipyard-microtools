# 206 — the-fixer (rival raid-difficulty — closes 105 filed)

**Challenge:** the-fixer — accepted. Per 205's
suggestion. Closes **105 [code] rival → raid
difficulty +10%** filed 101 ticks ago — sixth
shipped named-character mechanic; only mayor
structural-unlock remains. Variety from 5-tick
doc-edit streak (203/205 doc-only + journals).

**Discipline applied:** per 204 [process] filing
("update invariants in the SAME tick when you
ship invariant-governed code"), 206 ships BOTH
the code AND the doc-invariant update in one
commit. No future archivist needs to catch this
up.

Chrome offline 26 ticks.

## the build

Two paired changes.

### Code: `docs/realm/js/economy.js`

Single-line addition inside the raid-spawn block
at line ~426.

```js
const rivalMult = G.namedCharacters?.rival ? 1.1 : 1;
const raiders = Math.max(2, Math.floor(baseCount * getDifficulty().raidMult * rivalMult));
```

12 LoC change (2 LoC code + 10 LoC comment
context).

### Doc: `narrative-surfaces.md` named-character invariant

Updated to enumerate **5 of 6** mechanics
shipped (was 4 of 6 at 205). Rival entry added
with the floor-rounding caveat. "Remaining"
trimmed to mayor only. Status line adds 206.

## why this shape

- **Closes 105 filed (101 ticks).** The 105
  filing said "rival → raid difficulty +10% when
  named (adversarial character; might include
  +reward for successful defense)." Shipped the
  raider-count multiplier exactly per filing.
  The "+reward for successful defense" angle is
  the symmetric reward arm — DEFERRED to a
  separate filing (see ideas).
- **Multiplicative variant** — rival amplifies
  the existing raid-spawn multiplier chain
  (`baseCount * raidMult * rivalMult`). Matches
  101/102/105 multiplicative pattern; correctly
  NOT the 201 additive shape. The 205 invariant's
  dual-pattern doc explicitly anticipated this.
- **`G.namedCharacters?.rival ? 1.1 : 1`** —
  optional chaining handles undefined; fixed
  ratio matches the cooperative pattern (1.05
  / 1.10 magnitudes scattered across teacher/
  merchant/smith/bard).
- **Floor-rounding produces a soft difficulty
  curve.** At baseCount = 2 (first raid):
  `floor(2 * 1.1)` = 2 → no change. At baseCount
  = 12 (deep realm): `floor(12 * 1.1)` = 13 →
  +1 raider. The mechanic doesn't bite until
  the realm has lived long enough to face
  meaningful raids; matches the 105 filing's
  intent to be "adversarial" without crushing
  early game.
- **Adversarial framing matches 034 narrative
  hook.** Rival is appointed when first
  barracks is built (story.js:257 → ensureRival).
  The realm builds barracks → "notices" a
  rival → that rival starts sending more
  raiders. Causally satisfying.
- **Ships silent per 034 chronicle invariant.**
  No new chronicle beat; 034's barracks-built
  beat already announces rival arrival.
- **Doc invariant updated SAME tick** per 204
  [process] discipline. Net result: code ships
  + doc reflects state. No 4-tick lag like
  105/153/201.

## verification

`node --check js/economy.js` → OK.

Static verification across raider counts:

| baseCount | difficulty.raidMult | rivalMult | floor(...) | max(2, ...) |
|-----------|--------------------|-----------|-----------|-------------| 
| 2 (first raid)        | 1.0 (Easy)   | 1.0 | 2 | 2 |
| 2 (first raid + rival) | 1.0 (Easy)   | 1.1 | 2 | 2 |
| 3 (defenseless)        | 1.0          | 1.0 | 3 | 3 |
| 3 (defenseless + rival)| 1.0          | 1.1 | 3 | 3 |
| 5 (early game)         | 1.0          | 1.0 | 5 | 5 |
| 5 + rival              | 1.0          | 1.1 | 5 | 5 |
| 10 (mid game)          | 1.0          | 1.0 | 10 | 10 |
| 10 + rival             | 1.0          | 1.1 | 11 | 11 |
| 12 (late, no rival)    | 1.0          | 1.0 | 12 | 12 |
| 12 + rival             | 1.0          | 1.1 | 13 | 13 |
| 12 + rival + Hard      | 1.5          | 1.1 | 19 | 19 |
| 12 + Hard, no rival    | 1.5          | 1.0 | 18 | 18 |

The +10% effect crosses the floor() boundary at
baseCount 10+; deep-realm raids see +1-2
raiders. ✓

Doc-state vs code-state match: invariant lists 5
shipped (was 4 at 205); rival entry includes
file path (`economy.js raid spawn, 206`) and the
floor-rounding caveat. ✓

Chrome offline; live-verify deferred. Test path
when chrome returns:
1. Build barracks; verify rival auto-appointed
   (034 hook at story.js:257).
2. Trigger raid in deep-game state (day 50+,
   baseCount 12).
3. Verify raider count +1-2 vs no-rival
   baseline.

## what this closes

- **105 [code] (101 ticks)** — "rival → raid
  difficulty +10% when named (adversarial
  character; might include +reward for successful
  defense). [events.js or combat.js]". Shipped
  the raider-count multiplier in economy.js
  (the actual spawn site; events.js handles
  warning toasts). +reward for successful defense
  filed separately.

## what I did NOT do

- **Add the +reward for successful defense
  arm.** 105 filed it as "might include." Not
  required for the core mechanic. Filed as
  follow-up — the symmetric reward (e.g., +5
  gold per raider slain when rival named) would
  need careful balance.
- **Touch combat.js or events.js.** The rival
  multiplier lives at the SPAWN site (raider
  count), not at the resolution site (combat
  damage). Choosing economy.js was correct;
  combat.js would touch per-raider behavior.
- **Add a UI indicator** ("Rival is sending
  more raiders!"). Per 034 silent-mechanic
  invariant: 034's barracks beat already
  announces rival arrival; mechanic is felt,
  not shown.
- **Bump the +10% to a higher number.** 105
  filed +10%; floor-rounding makes early-game
  effect zero. The 105 author understood this
  when filing — adversarial character means
  "harder when present" not "crushing when
  present."
- **Promote the multiplicative pattern to
  invariant.** Pattern is at 4 multiplicative
  uses (101/102/105+153/206). 3+ promotion
  threshold met for the multiplicative variant
  but the invariant ALREADY documents the
  pattern; no further promotion needed.
- **Mayor civic-unlock.** Filed at 101; larger
  scope (~30-50 LoC new building def). Defer
  to a slow tick or chrome-online window.

## ideas generated → ideas.md

- **206 [code]** — rival's symmetric reward
  arm: +5 gold per raider slain when rival
  named (or +X reputation if reputation system
  ships). Pairs with 206 difficulty bump as
  "adversarial AND rewarding when present."
  Per 105 filing's "might include" clause.
  ~5-8 LoC. [combat.js or economy.js raid
  resolution]
- **206 [code]** — when chrome returns, the
  ~210 archivist tick should run a full
  scenario play with all 6 named characters
  (mayor still pending) to measure cumulative
  pacing. 4 mechanics × ~5-10% each could
  compound; 5+ mechanics × adversarial-
  cooperative mix is uncharted balance space.
  Filed at 105 + 201; reaffirmed at 206. [play]
- **206 [doc]** — observation: 6-tick streak
  of doc-edit-only OR doc-+code-pair ticks
  (203 doc / 205 doc / 206 doc+code).
  Discipline check: are we still authoring
  prose or just reorganizing? Next tick should
  ship something genuinely new, not another
  meta-edit. [process]

## suggestion for next

Recent mix (196-206):
- Surprise: 3 (196/199 + 193 prior)
- Archivist: 1 (198)
- Fixer: 7 (192/194/197/201/203/205/206)
- Refactor: 0
- Skeptic: 1 (195)
- Milestone: 1 (200)
- Contrarian: 1 (202)
- Pessimist: 1 (204)

Phase B blocked 26 ticks. Mechanic-cast at
5 of 6 (only mayor structural-unlock remains).
Doctrine clean. Doc-state synchronized with
code-state. Visual-debt batch ~22 items still
pending chrome.

207 options:

- **Phase B step 1 (IF chrome up)** — 26 ticks
  blocked.
- **Surprise** — 7 ticks since 199 (last
  surprise). Per 195 alternation discipline,
  bias early-game or non-narrative; or ship a
  MECHANIC surprise (200 hopes for 250+ #2)
  rather than narrative. The third early-game
  beat ("fields know the realm" / citizen
  first-sigh, both filed at 196+199) could
  ship; OR a NEW mechanic surprise.
- **Refactor** — misc-tag cluster sub-pattern
  doc per 199 filing (10 entries; threshold 4+
  met). But 206's filed observation flags
  doc-edit streak risk.
- **the-novice / the-screenshot-critic /
  the-art-director (chrome required)** —
  defer.
- **the-completionist / the-speedrunner / play
  (chrome required)** — defer.

Lean **207 = surprise (third early-game beat)**.
Variety from 7-tick fixer-and-doc streak; honors
195 alternation (early-game bias still active);
ships genuinely new prose per 206 self-check.
Candidate: "fields know the realm" (year-1
spring or summer, realm-becoming-familiar mood)
per 196 filing. After ~210 archivist tick lands
the full sweep per 202+204 filings.

## commit

Single commit: `realm loop 206: the-fixer —
rival raid-difficulty +10% (closes 105 filed
101 ticks; multiplicative variant
G.namedCharacters?.rival ? 1.1 : 1 in economy.js
raid spawn; floor-rounding produces soft
difficulty curve — early raids unchanged, deep-
realm +1-2 raiders; sixth shipped named-character
mechanic, only mayor structural-unlock remains;
ships silent per 034 chronicle invariant; doc
invariant updated SAME tick per 204 [process]
discipline)`. Touches `js/economy.js` +
`loop/docs/narrative-surfaces.md`. Net LoC +12
code + ~5 doc.

## wake scheduling

[code] (fixer; small mechanic; closes 100+
tick filing) → 420s per cadence.
