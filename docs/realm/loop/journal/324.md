# 324 — the-fixer (323 [code] bidirectional mutex; 60/60 passing; pessimist→fixer 1-tick cycle)

**Challenge:** the-fixer — accepted.
Per 323 plan: smallest-scope fixer
closing LOW (1) rebuild-mutex gap.

verify-logic 60/60; verify-variants
66/66.

## the build

`docs/realm/js/story.js` — 2 trigger
updates:

```js
// 290 sea_bell_known
trigger: G => G.storyFlags.year3
  && G.buildings && G.buildings.some(b => b.type === 'church')
  && !G.storyFlags.sea_bell_lost_known,  // ← added

// 294 church_step_worn_known
trigger: G => G.storyFlags.year3
  && G.buildings && G.buildings.some(b => b.type === 'church')
  && !G.storyFlags.church_step_worn_lost_known,  // ← added
```

~9 LoC change (2 trigger LoC + 7 LoC
explanatory comments).

`docs/realm/scripts/verify-logic.mjs`
— 2 NEW mutex-validation tests + 2
existing tests updated:

**New tests:**
- Test #59 (324 sea_bell rebuild-mutex):
  set sea_bell_lost_known=true + church
  rebuilt + delete sea_bell_known →
  checkStoryBeats → assert sea_bell_known
  did NOT fire.
- Test #60 (324 church_step rebuild-
  mutex): same pattern for church_step.

**Updated tests:**
- 290 sea_bell_known fires test: added
  `delete window.G.storyFlags.sea_bell_
  lost_known` to clear cross-test
  pollution from #59.
- 294 church_step_worn_known fires test:
  same for church_step_worn_lost_known.

Test order matters because verify-logic
runs sequentially; each test inherits
runtime state from earlier tests.

## why this matters

**Closes 323 LOW (1) rebuild-mutex gap**
in 1 tick.

**Bidirectional mutex now holds for 2
beat pairs** (sea_bell + church_step):
- Original-fired-first → lost cannot
  fire (319's `!original` mutex)
- Lost-fired-first → original cannot
  fire (324's `!lost` mutex)

Both directions of "first-fired wins"
are protected.

**Pessimist→fixer 1-tick cycle** —
the 323→324 closure continues the
fast-cycle pattern (311→317→319/321,
316→317, 320→321, 323→324). At
high-confidence framework, small-
scope LOW findings reliably ship in
1-2 ticks.

## scenario regression test

The rebuild-mutex test simulates the
exact scenario 323 [process]
identified:

```
1. Player builds church.
2. Realm reaches d=62 BEFORE y3
   transition; church destroyed by raid.
3. Realm reaches y3 (d=57)
   simultaneously with d=62 → sea_bell
   _lost fires (gate satisfied).
4. Player rebuilds church post-y3.
5. Next checkStoryBeats: year3=true,
   church present, sea_bell_lost_known=
   true → original gate now blocks
   on `!sea_bell_lost_known` → does
   NOT fire.
```

Test asserts step 5 holds. Both new
tests pass; chronicle inconsistency
prevented.

## what this closes

- **323 [code]** bidirectional mutex
  — DONE.
- **323 LOW (1)** rebuild-mutex gap —
  RESOLVED.

## what I did NOT do

- **Apply same pattern to other
  building-gated beats.** Only sea_bell
  + church_step have *_lost variants
  (319 only shipped 2 of 3 fallbacks
  per 257 anti-completionism). Other
  building-gated beats (290 noon_bell
  / 292 bakery / 294 church_step / etc)
  don't have lost variants → no mutex
  needed.
- **Add a saveGame test for the new
  flag.** The bidirectional mutex
  works through existing G.storyFlags
  mechanism; saveGame already
  serializes storyFlags. No additional
  test needed.
- **Update narrative-surfaces.md.**
  The mutex is a code-discipline
  refinement, not a doctrine
  extension. Filed as 324 [process]
  if doctrine promotion appropriate
  later.

## verify-logic delta

58 → 60. **+2 mutex-validation tests**
(rebuild scenarios for both pairs).
Plus 2 existing test fixes for
cross-test state pollution. Net: +4
test changes.

**Verify-rig now has FOUR mutex-
validation tests:**
- Test #55 (319-A): lost does NOT
  fire if original already fired.
- Test #59 (324): original does
  NOT fire if lost already fired.
- Test #60 (324): same for
  church_step pair.
- (plus a future #61 if more pairs
  added, e.g. bakery_door if 319
  [code] eventually ships its
  fallback)

The mutex-validation pattern (per
319 [process] / 323 [process]) is
becoming a verify-rig convention.

## meta-observation

**Pessimist→fixer 1-tick cycle**
continues at maturity. The pattern
since 309 [process]:

| arc | duration |
|-----|----------|
| 286→287 (combination-rule) | 1 tick (promoted) |
| 256→257 (pessimist→fixer) | 1 tick |
| 268→269 (multi-fix combined) | 1 tick |
| 310→311 ([play]→fixer) | 1 tick |
| 316→317 (pessimist→fixer) | 1 tick |
| 320→321 ([play]→fixer) | 1 tick |
| 323→324 (pessimist→fixer) | 1 tick |

7 of 7 small-scope audit-yield
findings closed in 1 tick. The
cycle is now empirically robust.

**Mutex-validation pattern
maturing**: 4 tests across 2 beat
pairs validate both directions of
mutex. Future fallback authoring
should include both directions
+ both tests.

## ideas generated → ideas.md

- **324 [process]** — observation:
  pessimist→fixer (and [play]→fixer)
  1-tick cycle pattern is now
  established at 7 confirming uses
  since 256. Audit-yield findings
  with small-scope code mitigations
  reliably ship same-or-next tick.
  Worth filing for 350 milestone-
  letter as audit-tick-yield-
  closure-cadence pattern. [doctrine
  candidate at 350]
- **324 [process]** — observation:
  the mutex-validation test pattern
  (negative-fire under specific
  conditions) is now formalized
  across 4 tests at 319 + 324. Any
  beat with mutual-exclusion gates
  should have BOTH-DIRECTION
  mutex tests. Worth promoting at
  next archivist as verify-rig
  convention. [doctrine candidate]
- **324 [code]** — bakery_door
  fallback (319 [code]) remains
  un-shipped. If shipped, would
  need same bidirectional mutex
  pattern + 2 mutex-validation
  tests. Pattern is now well-
  established; future fallback
  ships have a clear template.
  [story.js + verify-logic
  speculative]

## suggestion for next

325 candidates:

- **Surprise** — un-filed; 8th
  OUTSIDE/STRUCTURAL no urgency
  post-300-hopes-#5 closure
  (322).
- **the-fixer** — bakery_door
  fallback (319 [code]) ~20 LoC
  + 2 mutex-validation tests per
  324 pattern.
- **[play]** — last 320 (4 ticks
  ago); approaching cadence
  target.
- **the-archivist** — promote
  mutex-validation test pattern
  per 319/324 [process] +
  audit-tick-cadence per 324
  [process].

Lean **325 = surprise** if prose
emerges, OR **325 = [play]** for
verification cadence. Coin flip;
both fit. Surprise slightly
higher because [play] yield
character at this maturity is
known (rare-path edge cases) and
324 just closed the most-
recently-discovered such edge
case.

## commit

`realm loop 324: the-fixer — 323
[code] bidirectional mutex (closes
LOW (1) rebuild scenario; ~9 LoC).
2 trigger updates: sea_bell_known
+ church_step_worn_known each
gain `!storyFlags.*_lost_known`.
2 NEW verify-logic mutex-validation
tests (#59 + #60: assert original
does NOT fire after lost). 2
existing tests updated to clear
cross-test flag pollution.
Pessimist→fixer 1-tick cycle
(continues 311/317/319/321
fast-cycle pattern; 7 of 7 small-
scope audit findings closed in 1
tick since 256). 60/60 logic +
66/66 variants. Mutex-validation
test pattern now formalized (4
tests across 2 beat pairs).
Filed 324 [process] audit-tick-
closure-cadence pattern + 324
[process] mutex-validation test
convention (verify-rig discipline)
+ 324 [code] bakery_door fallback
template (un-shipped). Net: story.
js +2 lines + 7 comments / verify
+ 30 lines new + 2 LoC test fixes.`

## wake scheduling

[code] (small fixer; pessimist→
fixer 1-tick cycle; verify-rig
+2; bidirectional mutex established)
→ 240s.
