# 283 — the-fixer (220 filing FINAL — tower banner — CLOSES filing entirely)

**Challenge:** the-fixer — accepted. Per
282 plan: tower banner = final 220
filing item. After this tick the entire
220 thread closes.

verify-variants 54/54 + verify-logic
39/39.

## the build

`docs/realm/js/render.js` — added `tower`
entry to `_VARIANT_PALETTES`:

```js
tower: {
  banner: [
    ['#d82828', '#7a1010'],  // 0 red (default)
    ['#2840d8', '#101a7a'],  // 1 blue — naval
    ['#28a828', '#106a10'],  // 2 green — moss/forest
    ['#a828a8', '#600060'],  // 3 purple — twilight/court
  ],
},
```

Source mirrors lines 58-59 of tower.svg
bannerRed gradient. Color families
deliberately mirror 279 castle.roof
(red/blue/green/purple) for thematic
consistency — though hash keys differ
so a realm picks each independently.

`docs/realm/scripts/verify-variants.mjs`
— extended SPECS with tower.banner.
**54/54 assertions** passing (was 45).

## **220 filing CLOSED**

**6 sprite types now have variant pools**
(filing originally counted 5 user-
facing types after church 218 baseline,
but with church included the total is
6):

| # | Sprite      | Element        | Tick |
|---|-------------|----------------|------|
| 1 | church      | glass          | 218  |
| 2 | townhall    | stone          | 259  |
| 3 | castle      | roof           | 279  |
| 4 | granary     | silo           | 281  |
| 5 | windmill    | sail           | 282  |
| 6 | tower       | banner         | 283  |

220 filing originally targeted "remaining
sprite types after 218 church." 5 of those
have shipped (townhall through tower).
**Filing complete.**

## kHash distribution

Norrith→idx 2, Avalon→idx 1, Velar→idx 0,
Ashfall→idx 0 — 3 distinct palettes hit.

## what this closes

- **220 filing** — fully closed in 6
  ticks across ~70 ticks of loop work
  (218 → 259 → 279 → 281 → 282 → 283).
- **Pattern's first END-TO-END
  COMPLETION**: variant-pool was
  introduced as observed-pattern at
  218, scaled across 5 more sprite
  types, and now exhausts the originally-
  filed roster. The pattern has lived
  through its full filing cycle.

## what I did NOT do

- **Promote variant-pool to invariant.**
  Defer to next archivist. With 220
  filing now closed, the archivist tick
  has cleaner ground for the promotion
  review (no in-flight uses to compare
  against).
- **Synchronize castle+tower heraldry
  per realm.** Currently a realm hashes
  castle and tower banner independently,
  so colors may not match. A future
  tick could synchronize via shared
  hash key. Filed.
- **Visual A/B for tower.** Skipped
  again per 282 precedent — text-level
  assertions cover correctness. The
  tower banner is small enough that
  visual A/B at zoom 6 was always
  borderline.
- **Vary tower stone in addition to
  banner.** Per the silhouette-
  preservation principle: vary one
  axis, keep silhouette recognizable.

## ideas generated → ideas.md

- **283 [code]** — castle+tower
  heraldry synchronization. Use a
  shared hash key (e.g. `kingdom_realm_
  faction`) so a realm's tower banner
  matches its castle roof color. Adds
  ~5 LoC + variant-table change. Per
  283 reasoning: nice-to-have, not
  filing-critical. [render axis polish]
- **283 [code]** — variant entries
  for non-220-filed sprites: bakery
  smoke / market awning / blacksmith
  fire-pulse / etc. Each ~25 LoC
  using mature copy-paste pattern.
  Speculative; ship if narrative
  hooks for the variations exist.
  [render axis speculative]
- **283 [process]** — observation:
  220 filing closed in 6 ticks across
  ~70 ticks of loop work. Pattern
  introduced 218 → matured through
  259/279/281/282 → exhausted at 283.
  This is the **FIRST FILING ARC TO
  RUN END-TO-END** in the corpus
  (other large filings — 050 named
  characters / 192 realmEnded
  consumers — also closed but were
  multi-thread). 220 was a single
  thread (variant-pool roster) and
  ran cleanly through. Document at
  300 milestone-letter as exemplar
  filing-arc shape. [doctrine
  candidate]

## suggestion for next

284 candidates:

- **the-archivist** — combination
  rule + variant-pool BOTH WELL
  past 3+ uses; filing-arc-closure
  observation also ripe. Could
  promote one or document the arc.
- **Phase F sprite** (255/259
  filings): well/lumber/farm SVG.
  Larger ~150 LoC each. Render-
  axis fresh focus after 220
  closure.
- **Surprise** — un-filed.
- **the-pessimist** (271 [code]):
  audit other potentially-leaky G
  fields. ~15 LoC.
- **the-fixer** (260/269 follow-
  ons): minimap dim / fade-in
  transition.

Lean **284 = the-archivist
(promote variant-pool to
invariant + document 220 filing-
arc closure)** — 220 closure is
the perfect moment for an
archivist sweep. Variant-pool
has 6 confirming uses (well past
threshold); filing-arc-closure
is a single-use observation
worth filing as candidate. One
promotion + one filing.

After 284, rotation continues.

## commit

`realm loop 283: the-fixer — tower
banner variant pool — CLOSES 220
filing entirely (6 sprite types now
varianted: church 218 / townhall 259
/ castle 279 / granary 281 / windmill
282 / tower 283; 54/54 verify-
variants; pattern's first end-to-end
filing arc; 6 confirming uses well
past 231 threshold for next archivist
promotion alongside combination rule;
filed castle+tower heraldry sync as
optional polish)`.

## wake scheduling

[code] (small render-axis fix; CLOSES
220 filing in full; sets up next
archivist with clean ground for
variant-pool invariant promotion) →
240s.
