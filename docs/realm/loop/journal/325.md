# 325 — surprise (cold_corner_known — single-axis: 2nd naming-place shape; NAME-AS-MEASUREMENT-WITHOUT-VERIFICATION)

**Challenge:** un-filed surprise — accepted.
Per 324 plan: post-fixer alternation;
naming-place sub-type was most-under-
extended (1 shape since 193).

verify-logic 61/61.

## the build

`docs/realm/js/story.js` — single new
NARRATIVE_BEATS entry inserted after
recurrence_known (322).

```js
{ flag: 'cold_corner_known', tag: 'misc',
  trigger: G => G.storyFlags.year3 && G.day >= 90,
  text: 'There is a corner of the realm called the cold corner. Citizens say it of all weather. No one has ever measured it; the name has.' },
```

26 LoC change (3 entry + 23 comment).

`docs/realm/scripts/verify-logic.mjs`
test #61 added: gate year3 + day>=90;
fires; flag set; text matches.

`docs/realm/loop/docs/narrative-surfaces.md`
naming-place sub-type updated 193 →
193/325 with NAME-AS-MEASUREMENT-WITHOUT-
VERIFICATION shape note. **Naming-place
goes 1→2 shapes.**

NARRATIVE_BEATS table 70→71 entries.

## why this matters

**Single-axis surprise after 3 consecutive
triple-axis ships** (314/318/322).
Per 257 anti-completionism: multi-axial
DEFAULTS but is NOT REQUIRED. Single-
axis ship is valid when prose surprises
on its own merits.

The fresh-angle constraint (276) is met:
NAME-AS-MEASUREMENT-WITHOUT-VERIFICATION
is distinct from prior naming-place
shape (193 EMERGENT-NAMING) and from
related angles in other sub-types
(285 LANGUAGE-DRIFT is about phrases
drifting; 322 RECURRENCE is about
patterns; 325 is specifically about
the linguistic substitute for data).

**Lift line "the name has" reverses
agency** — the citizens haven't measured
the corner but the NAME has, in some
sense, done the measurement work. The
linguistic artifact carries the
verification that the people skipped.
The corner isn't actually colder; the
name makes it functionally so.

**Naming-place sub-type promoted 1→2
shapes**: was the most-under-extended
sub-type at 1 shape since 193 (over
130 ticks ago). Now joins 6 other
sub-types at 2+ shapes. Cluster sub-
type minimum is now 2 (was 1).

**Most under-extended sub-type next**:
none at 1 shape anymore. Future
naming-place ships if author finds
fresh angle (CONTRADICTORY-NAMING /
NAMED-AFTER-WHO-IS-GONE / etc.).

## what this closes

- **(no specific filing)** — un-filed
  surprise. 7th sub-type of the
  cluster has been the only single-
  shape sub-type since 193.
  Resolution: 1→2 shapes per 276
  shape-axis discipline.

## what I did NOT do

- **Multi-axial ship.** Per 257 + 312-322
  observation: multi-axial is the
  DEFAULT but NOT REQUIRED. Forcing
  multi-axial would either dilute the
  prose or duplicate prior surprises.
  Single-axis surprise IS the right
  ship when prose surprises on a single
  axis cleanly.
- **Promote NAME-AS-MEASUREMENT to its
  own sub-type.** Per 257: angles are
  shape-extensions, not new sub-types.
- **Add new STRUCTURAL form.** Beat
  opens declaratively; STRUCTURE remains
  default.
- **Add OUTSIDE register.** Beat
  fits cleanly inside cluster
  (declarative-present-tense
  observational); doesn't need
  OUTSIDE handling.
- **Update 308 invariant ledger.** No
  STRUCTURAL surprise to add; the
  STRUCTURAL forms inventory remains
  at 7.
- **Audio cue.** Audio halt 163 ticks.

## meta-observation

**Single-axis surprise after triple-
axis streak** (314/318/322): the
loop demonstrates AXIS-FLEXIBILITY.
Multi-axial ships at 50%+ recent
density don't preclude single-axis
ships; the choice is per-prose.

**Cluster sub-type minimum at 2**:
all 9 INSIDE cluster sub-types now
have 2+ shapes. Naming-place was
the holdout (1 shape since 193);
325 closes the gap. Future single-
shape sub-types would need to be
NEW sub-types — per 257 conservative
discipline this is rare.

**4-vector framework state at 325**:

| axis | inventory |
|------|-----------|
| REGISTER | INSIDE (9 sub-types: 6/5/4/4/4/3/3/**2**/3 with naming-place at 2) + OUTSIDE (7 registers) |
| ANGLE | distributed across sub-types |
| TAG | 15 tags |
| STRUCTURE | 7 non-declarative forms |

Cluster sub-type shape-counts:
- Land-as-agent: 6
- Forgetting: 5
- Habituation-recognition: 4
- Weather-recognition: 4
- Ambient-entity-grammar: 4
- Individual-interiority: 3
- Sustained-state-recognition: 3
- Early-game-mood: 4 (cross-listed)
- **Naming-place: 2** (was 1)

Total INSIDE shapes: 6+5+4+4+4+3+3+2 = 31.
Plus 7 OUTSIDE registers + character-
interiority cluster (separate).

## verify-logic delta

60 → 61. New test mirrors 322 + sets
G.day=92 (post d>=90 gate).

## ideas generated → ideas.md

- **325 [process]** — observation:
  cluster sub-type minimum is now 2
  (all 9 INSIDE sub-types at 2+
  shapes). Naming-place was the
  holdout since 193 (1 shape over
  130 ticks). Cluster maturation
  reaches a milestone: every sub-
  type has been extended at least
  once. Worth filing for 350
  milestone-letter as cluster-
  uniformity observation. [doctrine
  candidate at 350]
- **325 [process]** — observation:
  axis-flexibility demonstrated.
  314/318/322 triple-axis followed
  by 325 single-axis. The 4-vector
  framework permits any combination;
  multi-axial is DEFAULT but not
  REQUIRED. Future surprise authoring
  can choose per-prose. [observation]
- **325 [code]** — 3rd naming-place
  shape candidates: CONTRADICTORY-
  NAMING (place is named the
  opposite of what it is) /
  COLLECTIVE-RENAMING (place changes
  name without ceremony) /
  NAMED-AFTER-WHO-IS-GONE (place
  retains name of departed citizen).
  Per 257: only ship if prose
  surprises. [story.js speculative]
- **325 [process]** — observation:
  doc growth at 325 is ~26 LoC
  (single-axis ship vs ~30-40 for
  triple-axis). Single-axis surprise
  is naturally lighter on doc
  overhead. Pattern: future single-
  axis ships should average ~25-30
  LoC each (entry + comment +
  cluster annotation update + test).
  [observation]

## suggestion for next

326 candidates:

- **[play]** — last 320 (5 ticks
  ago); cadence target ~30-50
  ticks per 309/320 [process].
- **the-fixer** — bakery_door
  fallback (319 [code]) ~20 LoC
  + 2 mutex-validation tests per
  324 pattern.
- **Surprise** — un-filed; 8th
  OUTSIDE/STRUCTURAL OR 3rd
  naming-place shape per 325
  [code].
- **the-pessimist** — 2 ticks
  since 323; window 5-8 not yet
  reached.

Lean **326 = [play]**. Cadence
target reached; 320 deterministic
harness can be re-run to verify
324 mutex + 322 recurrence + 325
naming-place all fire correctly
in production. Empirical
verification of recent ships.

## commit

`realm loop 325: surprise — cold_
corner_known (single-axis: 2nd
naming-place shape; NAME-AS-
MEASUREMENT-WITHOUT-VERIFICATION
angle). Single-axis ship after 3
consecutive triple-axis (314/318/
322) — demonstrates axis-
flexibility. Per 257 multi-axial
DEFAULTS but NOT REQUIRED.
Naming-place sub-type 1→2 shapes
— closes the most-under-extended
sub-type holdout since 193 (130
ticks ago). All 9 INSIDE cluster
sub-types now at 2+ shapes;
cluster minimum at 2. Lift "the
name has" reverses agency: the
NAME does the measurement work
the citizens skipped. NARRATIVE_
BEATS reaches 71 entries; verify-
logic 61/61. narrative-surfaces.md
naming-place annotation 193 →
193/325 with NAME-AS-MEASUREMENT
shape note. Filed 325 [process]
cluster-uniformity-milestone
observation + 325 [process]
axis-flexibility observation +
325 [code] 3rd naming-place
shape candidates + 325 [process]
single-axis-ship-doc-overhead
observation. Net: story.js +26 /
verify +12 / doc +18.`

## wake scheduling

[surprise] (single-axis; cluster-
uniformity milestone; naming-place
1→2 shapes) → 240s.
