declare const ENV: {
  modulePrefix: string;
  environment: string;
  rootURL: string;
  locationType: string;
  EmberENV: Record<string, unknown>;
  APP: Record<string, unknown>;
};

export default ENV;
