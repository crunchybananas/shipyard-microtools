import type { TOC } from "@ember/component/template-only";
import Base64ToolsApp from "base64-tools/components/base64-tools/app";

const IndexRoute: TOC<object> = <template><Base64ToolsApp /></template>;

export default IndexRoute;
